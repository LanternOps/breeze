---
// Standalone registration is not supported — funnel to partner registration,
// which performs the runtime registration-enabled gate (#1308).
return Astro.redirect('/register-partner', 301);
---
