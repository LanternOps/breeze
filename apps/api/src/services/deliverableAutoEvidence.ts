import type { SweepDeliverable } from './serviceDeliverableService';

export async function generateAutoEvidenceForDeliverable(_d: SweepDeliverable, _today: string): Promise<number> {
  throw new Error('not implemented (W02)');
}
