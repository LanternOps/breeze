import { Hono } from 'hono';

// TODO(Phase 5): replace this stub with the real PDF download routes
// (GET /:id/pdf streaming the rendered artifact). Phase 4 only needs the
// router to exist so the invoices hub can mount it without breaking the build.
export const invoicePdfRoutes = new Hono();
