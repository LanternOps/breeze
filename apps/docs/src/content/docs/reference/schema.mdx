NO_CHANGES_NEEDED

The code changes in the PR are bug fixes to the `aggregateMetricsByInterval` function in `apps/api/src/routes/devices/metrics.ts`. These changes:

1. Ensure proper Date object conversion before calling `toISOString()`
2. Add `Number()` type coercion for numeric aggregations to prevent string concatenation issues

These are internal implementation fixes that don't affect the documented database schema, table structures, column definitions, relationships, or API contracts described in the documentation. The changes are purely about improving data type handling and timestamp conversion reliability—they don't introduce new tables, modify column definitions, or alter any documented functionality that users or developers need to know about.