NO_CHANGES_NEEDED

The code changes in this PR focus on:
1. Fixing remote desktop quality issues by improving session detection logic (preferring active sessions, falling back to connected sessions)
2. Adding infrastructure for SCM session event handling and helper lifecycle management on Windows services
3. Adding desktop peer disconnect notifications to allow viewer reconnection

None of these changes affect the upgrade process, versioning strategy, auto-update mechanism, or any user-facing upgrade procedures documented in `deploy/upgrades.mdx`. The changes are internal implementation details related to remote desktop session management and do not require documentation updates to the upgrade guide.