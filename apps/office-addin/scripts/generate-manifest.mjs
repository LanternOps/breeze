#!/usr/bin/env node
/**
 * Stub — Task 2 (manifest generation) lands this script fully.
 *
 * Running it now is a no-op: it prints a notice and exits cleanly so that
 * `pnpm dev`, `pnpm build`, and `pnpm manifest` are not broken while the
 * full implementation is pending.  The real implementation will read
 * ADDIN_BASE_URL / ADDIN_MANIFEST_ID / ADDIN_SUPPORT_URL from the environment
 * and emit a signed manifest.xml for `validate-manifest` to check.
 */
console.log('[generate-manifest] stub — full implementation arrives in Task 2 (manifest generation).');
