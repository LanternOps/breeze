{
	auto_https off
}

:80 {
	encode zstd gzip

	@api path /api/* /s/* /health /ready /metrics /metrics/* /i/* /activate/setup-intent /activate/complete/webhook
	handle @api {
		reverse_proxy host.docker.internal:3001
	}

	handle {
		reverse_proxy host.docker.internal:4321
	}
}
