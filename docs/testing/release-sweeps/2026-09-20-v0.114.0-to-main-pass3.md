# Pre-release sweep — v0.114.0 → main (2026-09-20, pass 3)

| | |
|---|---|
| Range | `v0.114.0` (`59a94162ca`, 2026-09-17 16:02Z) → `origin/main` `58f76b854` (2026-09-20 23:05Z) — 221 commits. **This pass inventories pass-2 base `b4f589a6b3` → `58f76b854` (69 PRs).** |
| Gate | Merge queue drained to 0 before the sweep base was fixed. Merged during the gate: #6469, #6418, #6420, #6421, #6424, #6425, #6427, #6428, #6429, #6430, #6431, #6432, #6433, #6434, #6426, #6439(no — see below), #6440, #6442, #6448, #6450, #6451, #6453, #6454, #6455, #6458, #6459, #6423, #6481. Waiting (rebased during the gate, CI pending, NOT in the sweep base): #6439, #6468 → follow-up rows. |
| Stack | `pnpm wt-stack up` on `qa/sweep-v0.114.0-pass3` @ `58f76b854` |
| Flags enabled | `MCP_BOOTSTRAP_ENABLED` `MCP_OAUTH_ENABLED` `BREEZE_AI_AGENTS_ENABLED` `LLM_PROVIDER_CATALOG_ENABLED` `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED` `BREEZE_AI_SCRIPT_AUTHORING_ENABLED` `BREEZE_AI_AGENTS_SWEEP_ACT_ENABLED` `TOOL_SOURCES_ENABLED` = true; `BREEZE_WORKSPACE_ENABLED=false`; `WEBAUTHN_RP_ID=localhost`; `MFA_FORCE_FOR_PARTNER_ADMIN=false`. Stack `breeze-wt-qa-sweep-v0-114-0-pass3`, baseUrl `http://localhost:33032`, admin@breeze.local (platform admin), 3 orgs / 2 devices seeded. |
| Driver | skill `pre-release-sweep` |

Prior passes in this release window: `2026-09-19-v0.114.0-to-main.md` (pass 1),
`2026-09-19-v0.114.0-to-main-pass2.md` (pass 2). This pass covers the ~120
commits that landed after pass 2, plus the 09-20 issue-fixer fan-out.

## Phase 0 findings (before the stack)

### Portal Dev E2E (hydration) — standing FAILURE on main, not flake

The job is `continue-on-error` on `pull_request` **and** was failing on the
`merge_group` runs of #6442, #6434, #6426 and #6451 — i.e. every entry that
landed, landed with this job red. It is only green where it is `skipped`
(#6481, docs-only). So it is a real break on `main` that the non-blocking flag
has been absorbing, not per-PR noise.

Two specs, both timing out at 30 s:

1. `tests/partner-sending-domains.spec.ts:48` — "The tab is offered and opens
   on its own hash". `page.getByTestId(rootTestId).waitFor()` never resolves;
   the run then reports `Target page, context or browser has been closed`.
   Partner sending domains is feature #6180, whose waves are specced but
   reportedly not implemented — so this spec may be asserting a surface that
   does not exist yet, which would make the spec the defect rather than the app.
2. `tests/multi-currency.spec.ts:97` — "org currency selection, manual invoice,
   and quote acceptance in a EUR org". Fails on
   `expect(an authenticated /api/v1 request from the app).toBeTruthy()` — the
   portal never issues an authenticated API call within 30 s, which reads as a
   hydration or session failure rather than a currency bug.

**RESOLVED — both are E2E test defects, not application defects.** Reproduced
locally against the sweep stack, then diagnosed to root cause.

**Finding 1 — `partner-sending-domains.spec.ts` asserted a testid that does not
exist.** `PartnerSendingDomainsPage.navTab()` looked for
`settings-nav-tab-sending-domains`. That string appears **nowhere** in
`apps/web/src` — the only occurrence in the repo is the page object itself.
`SettingsSectionNav` emits `<testIdPrefix>-tab-<key>`, and `PartnerSettingsPage`
passes `testIdPrefix="partner-settings"` with the camelCase `TabKey`, so the
real id is `partner-settings-tab-sendingDomains`. Verified in a live browser:
15 `partner-settings-tab-*` ids render and the sending-domains one is present
and visible. Step 1 could never have passed. **Fixed** (page object).

**Finding 2 — the same spec waited for a native `confirm()` that the app never
shows.** Step 7 registered `authedPage.once('dialog', …)`, but the tab confirms
removal through its own `ConfirmDialog` (`confirmTestId=
"sending-domains-remove-confirm"`). No native dialog event ever fires, the modal
stays open, and **no DELETE is ever issued** — confirmed against the API log,
which shows create `201`, check `202`, identity `200`, test-send `202` and no
DELETE at all. This is the repo's own documented trap (CLAUDE.md: modals are
`fixed inset-0` divs, assert on testid, not a dialog container). **Fixed**
(click the confirm testid instead of handling a dialog).

Net: steps 2–6 of this spec were already exercising the feature correctly
against a real API. The app is fine; the spec's first and last steps were wrong.

**Still open on this spec:** not yet proven green end to end. Each failed run
leaves a `partner_sending_domains` row behind, and the recommendation card only
renders in the empty state, so a re-run then fails at step 1 for a *third*,
unrelated reason. The spec is not idempotent against its own leftovers — worth
a follow-up issue. A raw `DELETE` is correctly refused by
`breeze_sending_domain_release_guard()` (provider-managed domains must be
released first), so cleanup needs the release path, not SQL.

**Finding 3 — `multi-currency.spec.ts` was a false alarm from my own session.**
Its `readAccessToken()` failures (landing on `/login`, no Bearer request) were
caused by my concurrently signed-in browser session and a tripped login rate
limiter, not by the app. Verified separately that the app does send
`authorization: Bearer …` on every `/api/v1/` call. **Re-measure this one on a
clean stack before drawing any conclusion.**

**Operational note for future sweeps:** the login limiter keys on the *caddy*
IP (`login:ip:::ffff:172.31.2.10`, `login:socket:…:<email>`), so every browser
login on the stack shares one bucket — an agent logging in repeatedly will
rate-limit `globalSetup`. Clear with
`redis-cli --scan --pattern 'login:*' | xargs redis-cli del` using
`REDIS_PASSWORD` from `.env`.

## Change inventory

Status ∈ `TODO | PASS | PARTIAL | FAIL | BLOCKED | N/A`. Every row must leave `TODO`.

| PR | Merged | Surface | Title | Route / click-path | Expected visible outcome | Prereq | Status |
|---|---|---|---|---|---|---|---|
| #6481 | Y | docs-chore | W09 — token-mode recovery follows cross-snapshot object references | N/A — plan doc only | No UI change | none | TODO |
| #6451 | Y | api-only | close the DR reconcile and bare-metal in-flight races | `POST /dr/plans/:id/execute` and BMR recovery start race | Concurrent execute/recover calls rejected cleanly; `bareMetalRecoveryInFlightIndex` unique index | live agent for a full BMR run — BLOCKED-candidate | TODO |
| #6426 | Y | api-only | enqueue the SNMP immediate poll after the request transaction commits | Devices → Network → SNMP asset "Poll now" (`POST /monitoring/assets/:id/poll`) | Immediate poll no longer races the DB commit | live SNMP device — BLOCKED-candidate | TODO |
| #6434 | Y | api-only | give whole-machine restores a 24h ceiling instead of a 60-minute reap | stale command reaper on a whole-machine restore command | Restore in progress past 60 min is not reaped; 24h ceiling | live agent BMR restore — BLOCKED-candidate | TODO |
| #6442 | Y | web | Disk Cleanup v2 W05 — system_cleanup AI/MCP tool, docs parity, release notes | `/devices/[id]` → AI chat → ask assistant to clean disk; `/ai-risk` Approval History Feed | Chat shows a "System Cleanup" tool-call card with a labeled outcome; feed lists system_cleanup | AI chat enabled | TODO |
| #6423 | Y | web | preview the inherited tax rate on draft invoices | `/billing/invoices/[id]` (draft) → summary panel | Muted hint "Tax rate inherited: X% — $Y tax, $Z total" (`invoice-tax-inherited-hint`) when a taxable line exists and no explicit rate | partner Billing tax rate configured | PASS |
| #6459 | Y | web | page site and org pickers to exhaustion instead of one server page | `/billing/invoices` org filter, `/billing/quotes`, Move to Organization org picker, New Ticket org/site select | Org/site dropdowns list all orgs/sites beyond the first server page | >1 page of orgs/sites | PASS |
| #6458 | Y | web | atomic budget reservations for /client-ai and warm technician sessions | AI chat sidebar; rapid messages at the AI budget cap | Budget-exceeded toast consistent, no double-spend | concurrent requests — BLOCKED-candidate | TODO |
| #6455 | Y | agent-only | refuse tier-2 fleet users-FK writes for agent principals | AI agent tool call to aiToolsFleet write actions by an agent principal | Rejected with guardrail error; visible in `/ai-risk` as denied | tier-2 fleet automation running — BLOCKED-candidate | TODO |
| #6454 | Y | web | surface reclaimable size and scan confirmation in disk cleanup | `/devices/[id]` → Filesystem tab → Analyze Now / Cleanup Preview → confirm (`cleanup-confirm-dialog`) | Confirm dialog lists target paths; "Reclaimed: X" banner after cleanup | live agent for real bytes — BLOCKED-candidate; UI renders regardless | TODO |
| #6453 | Y | web | mount the file Restore Wizard so restores are not API-only | `/backup` → snapshot row → "Restore" | Multi-step Restore Wizard modal opens (picker → destination → confirm) | live agent + snapshot — BLOCKED-candidate for full restore; wizard renders without | TODO |
| #6450 | Y | docs-chore | keep noeviction, correct the allkeys-lru claim, document recovery | Docs `/deploy/environment`, `/reference/troubleshooting` | Docs state Redis uses `noeviction` with recovery procedure | none | TODO |
| #6448 | Y | agent-only | anchor fully-literal cleanup rules above the target | Agent Disk Cleanup rule engine | Literal-path rule anchors correctly | live agent — BLOCKED-candidate | TODO |
| #6440 | Y | web | name the missing prerequisite on execute; stop half-applying plan saves | `/dr` → plan card → Execute → rehearsal/failover/failback | Execute error names the missing prerequisite; invalid step save does not partially persist | DR plan missing a prerequisite | TODO |
| #6433 | Y | api-only | hold an active chain's full snapshot against retention | backup retention job; `/devices/[id]` → Backup tab snapshot list | Full snapshot anchoring an active chain is retained | retention run on active chain — BLOCKED-candidate | TODO |
| #6432 | Y | api-only | give the bundled coturn a certificate so TURNS on 5349 works | remote session over TURN; compose/.env.example TURN TLS vars | Remote session connects via `turns:` 5349 | real network topology + cert — BLOCKED-candidate | TODO |
| #6431 | Y | web | persist a terminal BMR failure reason; filter template/oneshot units in the service probe | `/devices/[id]` → Backup tab → Bare-Metal Recovery bootstrap section | Failed BMR bootstrap shows persistent specific reason after refresh | live agent BMR failure — BLOCKED-candidate | TODO |
| #6430 | Y | agent-only | preserve and restore Windows Hidden/System/Sparse file attributes | agent backup/restore engine | Attributes preserved | Windows agent — BLOCKED-candidate | TODO |
| #6429 | Y | agent-only | skip the doomed HTTP result submission for WS-direct backup_run | agent heartbeat result reporting | No spurious HTTP POST | live agent — BLOCKED-candidate | TODO |
| #6428 | Y | web | schema-validate `maintenance` inlineSettings on the feature-link route | `/configuration-policies/[id]` → Maintenance tab → save invalid inline settings | Clear 400 validation error instead of silent persist | admin editing a policy | PASS |
| #6427 | Y | agent-only | 16 MiB+ uploads to MinIO over HTTPS, and verification of an incomplete snapshot | agent S3 provider | Large upload succeeds; incomplete snapshot flagged | MinIO HTTPS + live agent — BLOCKED-candidate | TODO |
| #6425 | Y | agent-only | stop duplicating the system-state failure reason in errorLog | agent backup errorLog | Reason appears once | live agent — BLOCKED-candidate | TODO |
| #6424 | Y | api-only | fail a backup job loudly when the strict queue schema rejects its result | agent WS submits malformed backup_run result | Job marked failed with clear error on Backup tab | live agent — BLOCKED-candidate | TODO |
| #6421 | Y | web | stop the MonitorEditor owner-scope hydration mismatch | `/alerts/monitors/[id]` — hard refresh | No hydration-mismatch console warning / flash of wrong owner-scope UI | none | PASS |
| #6420 | Y | api-only | flag pure returned tool errors as isError and ledger them as failures | `POST /mcp` — tool returning an error payload | `isError: true`; ledgered as failure in `/ai-risk` | MCP client session | TODO |
| #6418 | Y | agent-only | resolve the trash directory without depending on $HOME | agent delete-to-trash | Works without $HOME | service-account agent — BLOCKED-candidate | TODO |
| #6469 | Y | api-only | refuse recovery for snapshots that reference older snapshots' objects | `POST /backup/bmr/:id/recover` on a referencing snapshot | Clear rejection | crafted storage state — BLOCKED-candidate | TODO |
| #6422 | Y | web | Billing Profiles W03: minimums and rounding land on time entries | `/tickets/[id]` → Time & Billing (log time), `/timesheet` | Entries round/apply configured billable minimum | org Billing Profile with minimums/rounding | PASS |
| #6032 | Y | agent-only | derive status path from session config and add agent fallback | Breeze Helper desktop app | Tray status correct | Helper installed — not browser | TODO |
| #6416 | Y | api-only | the converter: preview, equivalence, ledger lifecycle, routes, onboarding | `POST /monitor-definitions/:id/conversion/preview` and `/apply`; no web UI | Equivalence report; ledger entry on apply | API only this wave | TODO |
| #5841 | Y | docs-chore | bump @sentry/react-native 7 to 8 | apps/mobile | none web | mobile build | TODO |
| #5847 | Y | web | bump @simplewebauthn/server + /browser 13 → 14 | `/settings/profile` → passkey registration; login passkey step | Passkey register/use succeeds | platform authenticator (CDP virtual) | TODO |
| #6114 | Y | web | show device names instead of {{device}} in alert titles | `/alerts` list, `/alerts/[id]`, dashboard AlertsFeed, Org Overview | Real device name in titles, no literal `{{device}}` | an active alert | PASS |
| #6419 | Y | docs-chore | bump the marketing version to 1.0.1 | apps/mobile | none | none | TODO |
| #6404 | Y | agent-only | W05 lab-proof defects — mounted-image convert, unsized VHDX target, refused status | agent bare-metal rebuild | Conversion completes; refused status accurate | live agent VHDX — BLOCKED-candidate | TODO |
| #6417 | Y | docs-chore | link the iOS App Store listing | Docs `/features/mobile` | Working App Store link | none | TODO |
| #6414 | Y | agent-only | A-W06 agent-first coverage burn-down — 13 Tier-1 reads + MCP_COVERAGE contract | AI chat / MCP client Tier-1 read tools | Tier-1 read questions succeed via dedicated tool | AI chat enabled | TODO |
| #5844 | Y | docs-chore | bump @sentry/cli 2.58.4 → 3.7.0 (apps/mobile) | apps/mobile | none | none | TODO |
| #6399 | Y | web | dedicated ai_sessions:use permission so org roles can open AI chat | Header AI chat entry; Settings → Roles → Permissions | Custom org role with `ai_sessions:use` opens AI chat; without it entry disabled/hidden, no confusing 403 | admin creating a custom role | TODO |
| #6406 | Y | api-only | B-W01 additive spec catch-up — version negotiation, title/annotations/_meta, structuredContent | `POST /mcp` initialize | Protocol version negotiated; titles/annotations returned | external MCP client | TODO |
| #6393 | Y | web | W02 — billing profiles cut-over: Rates screen, resolver, stamping, conversion, override gate | `/settings/billing` → Rates tab (`billing-settings-tab-rates`) | New Rates tab with billing profiles, editable and saved | partner admin | PASS |
| #6411 | Y | web | move-org site picker asks for the route's full page of sites | `/devices/[id]` → Move to Organization → destination site dropdown | Full page of sites listed for the target org | org with many sites | PASS |
| #6405 | Y | api-only | tool capture/eval harness sends each surface's production system prompt | internal dev scripts | n/a | none | TODO |
| #6410 | Y | api-only | network_check raises one alert per check on its alert device, online or not | alert worker for network_check; `/alerts` list | Exactly one alert per failing check | live network fault — BLOCKED-candidate | TODO |
| #6409 | Y | api-only | network_check compiler writes expectedStatus, the key the agent reads | monitor compiler | Agent evaluates status correctly | live agent — BLOCKED-candidate | TODO |
| #6119 | Y | e2e-only | run the production-browser gate when topology or web build files change | CI workflow | Added check on PRs | CI only | TODO |
| #6386 | Y | web | Disk Cleanup v2 W04: OS-native cleaners | `/devices/[id]` → Filesystem tab → Cleanup Preview | OS-native cleaner categories with sizes; reclaimed bytes after run | live agent — BLOCKED-candidate for numbers; UI renders | TODO |
| #6129 | Y | api-only | purge a deleted device's collection evidence once its withdrawal is published | topology retention worker after device delete | Evidence purged | device with topology history | TODO |
| #6400 | Y | web | Move to Organization dialog with server-driven step-up | `/devices/[id]` → actions → Move to Organization | Dialog with org/site pickers; step-up prompt when configured before move commits | admin; step-up needs MFA/passkey | PASS |
| #6402 | Y | docs-chore | alerting consolidation D28–D29 + CLAUDE.md org-merge registry row | docs | none | none | TODO |
| #6115 | Y | web | logical maps, shared templates, layout and explicit diagnostics | `/devices/network/[id]` → Topology section | Topology map with layout and diagnostics panel | network device with topology data; `TOPOLOGY_DISABLED` unset | TODO |
| #6395 | Y | api-only | conversion ledger tables, retirement columns, retired rows excluded everywhere | `/configuration-policies/[id]` Monitors tab | Retired legacy rule not listed | converted org | PASS |
| #6394 | Y | web | composite kind, restart parameters, consecutiveFailures 100, monitors-link inheritance, resolver honours replace | `/alerts/monitors/new` or `/alerts/monitors/[id]` → kind "Composite"; restart_service params; consecutiveFailures | Composite kind with sub-conditions; restart_service parameter inputs; consecutiveFailures up to 100 | admin | PARTIAL |
| #6390 | Y | web | delivery preview, Notify shows the resolved answer, manage_delivery AI tool, docs | `/alerts/delivery` preview; MonitorEditor Notify step | Live preview of resolved channel/escalation; Notify step shows resolved answer, not raw IDs | admin | PASS |
| #6392 | Y | api-only | routing API rejection pinned, legacy docs notices, 09-08 spec superseded | `POST/PUT /alerts/routing` site-scoped payload | Pinned rejection; docs legacy notices | admin via API | TODO |
| #6387 | Y | web | Delivery page (Channels · Routing · Escalation policies), old URLs redirect | `/alerts/delivery`; old `/alerts/channels`, `/alerts/routing-rules` | Old URLs redirect; three sections render | admin | PASS |
| #6385 | Y | web | one delivery resolver, explicit "Everything else" routing rows, all-channels fallback removed | `/alerts/delivery` → Routing | Explicit "Everything else" catch-all row per severity | admin | PASS |
| #6389 | Y | e2e-only | MonitorsListPage waits for a fetched row, not the page shell (flake) | test only | n/a | CI | TODO |
| #6388 | Y | docs-chore | alerting consolidation D22–D27 | docs | none | none | TODO |
| #6381 | Y | web | freeze legacy alert authoring, Create monitor + Recommended on the policy Monitors tab | `/configuration-policies/[id]` → Monitors tab → Create monitor, Recommended; `/alerts/rules/[id]` | Legacy editor shows frozen notice and blocks edits; Create monitor works; Recommended list | admin | PASS |
| #6366 | Y | web | Disk Cleanup v2 W03: tab completion and consolidation | `/devices/[id]` → Filesystem tab | Consolidated cleanup UI: preview, run history, snapshot panels | live agent for data — BLOCKED-candidate; UI renders | TODO |
| #6380 | Y | api-only | restart_service response survives normalization and drives the agent watch | automation runtime | Result flows to watch/alert state | live agent — BLOCKED-candidate | TODO |
| #6378 | Y | api-only | monitors of kind offline fire through the offline-transition path | offlineAlertEffects | Offline monitor fires alert on `/alerts` | device going offline — BLOCKED-candidate | TODO |
| #6377 | Y | api-only | resolveMonitorsForDevice honours assignment role/OS filters | monitorResolver | Role/OS-scoped monitor not applied to other devices | filtered assignment + mixed devices | PARTIAL |
| #6384 | Y | docs-chore | alerting consolidation D21 | docs | none | none | TODO |
| #6383 | Y | docs-chore | pre-release sweep pass 2 tracking doc | docs | none | none | TODO |
| #6028 | Y | api-only | add canonical graph foundation | `GET /topology/graphs` | Canonical graph data | API only this wave | TODO |
| #6374 | Y | docs-chore | alerting consolidation — spec, diagrams and six wave plans | docs | none | none | TODO |
| #6324 | Y | api-only | stop tool-output redactor wiping non-secret keys | AI chat tool-call output | Non-secret fields retained next to real secrets | AI tool with secret-like key names | TODO |

### ENV VARS (from diffs)

- `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED` — #6366; ON in sweep .env.
- `BREEZE_AI_GUARDRAILS_CORE` — #6414; core guardrails gate for Tier-1 tools (not a boolean flag in .env; default path).
- `MCP_OAUTH_ENABLED` — #6406; ON.
- `TOPOLOGY_DISABLED` — #6028 kill switch; unset (topology enabled).
- `BREEZE_MCP_TOOL_NAMES`, `BREEZE_VERSION`, `BREEZE_APP_DB_PASSWORD`, `TOOL_SOURCES_ENABLED` — context-only, not new gates.

### GROUPS

- **G1 Alerting & Monitors** — #6387, #6385, #6390, #6394, #6381, #6421, #6114, #6428, #6377, #6395 (web-visible part)
- **G2 Billing / Ticketing / Pickers** — #6393, #6422, #6423, #6459, #6411, #6400
- **G3 Disk Cleanup + Backup & DR (web)** — #6366, #6386, #6454, #6453, #6440, #6431
- **G4 AI / MCP / Auth / Topology** — #6399, #6442, #6414, #6324, #5847, #6115, #6028, #6406, #6420
- **Non-browser** — all agent-only, api-only backend, docs-chore, e2e-only, mobile rows: verified by CI unit/integration suite on the merge SHA; marked N/A (docs/CI/mobile) or BLOCKED (live agent/provider) in the summary.

## Sweep log (append as you go)

### G1 Alerting & Monitors

Stack `http://localhost:33032`, admin@breeze.local (platform admin / partner scope).
Prerequisite data created during the sweep: notification channel "QA Sweep Email
Channel", routing rule "QA Sweep High Routing", escalation policy "QA Sweep
Escalation", config policy "QA Sweep Policy" (org-scoped, Default Organization),
monitor "QA Sweep Monitor" (created → edited → deleted), two `{{device}}`-titled
alerts and two legacy `alert_rules` rows (one live, one `retired_at` set) inserted
by SQL.

| PR | Status | Evidence |
|---|---|---|
| #6387 | PASS | `/alerts/delivery` renders all three sections (Channels · Routing · Escalation policies). `/alerts/channels` and `/alerts/routing-rules` both land on `/alerts/delivery` (opaqueredirect on a `redirect:'manual'` fetch; browser navigation ends on the Delivery URL). |
| #6385 | PASS | Routing shows one explicit catch-all row — "Everything else / All alerts / Send to: Inbox only / Catches every alert no row above matches. Empty its channels for inbox only." With no rule matching, the preview answers "Critical → Do not notify / No delivery row is configured" — i.e. no implicit all-channels fallback. **Inventory expected-outcome is wrong**: the source models ONE default row (`isDefault`, priority 1000000 — `RoutingSection.test.tsx`, `RoutingRuleDrawer.tsx`), not one per severity. Source wins. |
| #6390 | PASS | "Test this rule set" on `/alerts/delivery` (testids `delivery-preview-severity/kind/site`) resolved "High → QA Sweep Email Channel / Organization rule: QA Sweep High Routing" after the rule was created. MonitorEditor Notify step shows the same resolved sentence (`delivery-preview-result`), not raw IDs. |
| #6394 | PARTIAL | Composite kind present in the picker on `/alerts/monitors/new` ✓. `consecutiveFailures` on `network_check` renders `min=1 max=100` and accepts 100 ✓. **restart_service parameter inputs do not exist in the MonitorEditor** — the "Execute Command" response offers only a free-text Command field, and `grep -rn restart_service apps/web/src` finds nothing in monitoring. Reading the PR body, that is by design: #6394's restart work is API-side (`automationRuntime.restartService.test.ts`, `maxAttempts`/`cooldownSeconds` on the `execute_command` response), "no UI change". Composite likewise has no sub-condition editor — `monitorKindFields.ts:238-243` says "W05c1: the composite kind exists in the shared enum … W05c2 supplies the children editor". Inventory's expected outcome overstates both; the web-visible half passes. |
| #6381 | PASS | `/configuration-policies/{id}` → Monitors tab has `monitors-tab-create` ("Create monitor") and `monitors-tab-recommended` ("Recommended monitors" + "Attach all built-in monitors") ✓. Legacy authoring frozen: `/settings/alert-templates` shows the banner "New alert templates are created as monitors — Existing templates stay editable here and convert in the next release / Open Monitors". `/alerts/rules/{id}` does not render a frozen notice — `apps/web/src/pages/alerts/rules/[id].astro` is a bare `Astro.redirect('/configuration-policies', 301)`, verified in browser. Stronger than the inventory's description, so still PASS. |
| #6421 | PASS | Hard load of `/alerts/monitors/605bf267…` (partner-wide built-in) and of an org-scoped monitor: 0 console warnings/errors, and 20 samples over the first 3 s show one stable owner-scope render ("Partner-wide / Built-in") — no flash to the org variant. |
| #6114 | PASS | Inserted `{{device}} CPU above threshold` (high) and `{{device}} disk critical` (critical) via SQL. `/alerts` list, `/alerts/{id}`, dashboard AlertsFeed and Org Overview "Open critical alerts" all render "E2E Windows Test Device …" / "E2E macOS Test Device …"; `innerText.includes('{{device}}')` is false on all four surfaces, message bodies substituted too. |
| #6428 | PASS | API: `POST /api/v1/configuration-policies/{id}/features` with `inlineSettings:{recurrence:'sometimes',durationHours:-5,startTime:'99:99'}` → **400** `{"error":"Invalid maintenance settings","details":{"formErrors":["Unrecognized key: \"startTime\""],"fieldErrors":{"recurrence":["Invalid option: expected one of \"once\"…"],"durationHours":["Too small: expected number to be >=1"]}}}`; `inlineSettings:"not-an-object"` → 400 "expected record, received string"; unknown key → 400 `unrecognized_keys`. UI: duration 999 h → PATCH 400 and a visible toast "Invalid maintenance settings: durationHours: Too big: expected number to be <=72 / Check the highlighted fields" plus an inline field error. No silent persist. |
| #6377 | PARTIAL | API-only (`git show 03ee57c40` touches only `monitorResolver.ts` + its two test files; PR body says "no UI change"). Web-visible half verified: the filter controls live on the policy **Assignments** tab (not the Monitors tab as the inventory says) — Role Filter (12 roles) + OS Filter (Windows/macOS/Linux). Saved Server+Windows → POST 201, toast "Assignment added", FILTERS column shows `Server / Windows`, and `config_policy_assignments` holds `role_filter={server} os_filter={windows}`. Resolver behaviour itself is not observable in the UI (monitors list shows "1 policy", no device count), so it rests on its integration test. |
| #6395 | PASS | Schema confirmed via psql: `alert_rules.retired_at / retired_reason / converted_to_monitor_id`, plus tables `monitor_conversions` and `monitor_conversion_outputs`. Inserted two legacy rules in Default Organization; `/alerts/rules` lists only "QA Legacy Rule Live" and excludes "QA Legacy Rule RETIRED" (`retired_at = now()`). |

**Everyday-workflow pass** — `/alerts`, `/alerts/correlations`, `/alerts/monitors`,
`/alerts/monitors/new`, `/alerts/monitors/[id]`, `/alerts/rules`, `/alerts/delivery`,
`/configuration-policies`, `/configuration-policies/new`, `/configuration-policies/[id]`
(Overview · Monitors · Maintenance · Assignments), `/settings/alert-templates`. No
console errors other than the ones my own deliberate 401/400 probes produced; no broken
layouts, empty states present everywhere.

End-to-end CRUD, all confirmed by toast + status code:
- Monitor: create (201, "Monitor saved") → edit (PATCH 200, "Monitor saved") → delete (DELETE 204, "Monitor deleted", back to list).
- Delivery channel: create (email, "Channel Created", list 1 of 1).
- Routing rule: create (201, "Routing rule created"), row shows "Priority: 10 / Severities: high / Send to: QA Sweep Email Channel".
- Escalation policy: create ("Escalation policy created", "1 step · 15m").
- Config policy: create → Monitors attach (POST 201, "Saved") → Assignment with filters (201, "Assignment added") → Maintenance save (201, "Saved").
- Alert list actions: Ack → POST 200, "Alert Acknowledged", row flips to Acknowledged.

### G2 Billing / Ticketing / Pickers / Move-org

Stack `http://localhost:33032`, admin@breeze.local (platform admin / partner scope).
Prerequisite data created during the sweep (left in place): partner default tax rate
set to **7.5%**; billing profile **"QA Sweep Rates"** (created) and the default
**"Standard rates"** profile edited to `$120.00/h · 30 min minimum · round to 15 min`;
ticket **T-2026-0001** "QA Sweep G2 billing ticket" on Default Organization with two
time entries (10 min and 40 min); one assembled draft invoice on Default Organization
with line 1 marked taxable; **120 `QA Site NNN` rows** on org *E2E Multi-Currency
1789953310781* and **120 `QA Org NNN`** organizations (SQL) for the picker-exhaustion
rows; user **qa-tech@breeze.local** (Partner Technician role, admin's password hash)
created to prove the `time_entries:manage_billing` deny path over curl. Device
`e2e-macos.local` was moved to another org and **moved back** — both seeded devices are
in their original org/site. TOTP was enrolled on admin@breeze.local to pass the
move-org step-up and **disabled again afterwards** (`users.mfa_enabled = f` verified).

| PR | Status | Evidence |
|---|---|---|
| #6393 | PASS | Rates tab (`billing-settings-tab-rates`) lists the four partner billing profiles with currency + base-rule summary. **Create**: `billing-profile-create` → drawer → name/rounding/rate/minimum → toast "Billing profile saved.", row appears. **Edit**: `billing-profile-edit-<id>` on "Standard rates" → rounding 15 / rate 120 / minimum 30 → same toast, row re-renders "USD · Round to 15 min / Billable at $120.00/h · 30 min minimum". **Override gate** (PR body = the `time_entries:manage_billing` gate binding every writer): as `qa-tech@breeze.local` (Partner Technician: `time_entries:write`, no `manage_billing`) `POST /api/v1/time-entries` with `hourlyRate` → **403** `{"error":"Changing billing terms requires manage billing permission","code":"MANAGE_BILLING_REQUIRED"}`; same 403 with `minimumMinutes`. Allowed side exercised in the browser as Partner Admin (`*:*`), where `TicketTimeBilling.tsx:85` shows the rate/billable controls. |
| #6422 | PASS | With `$120/h · 30 min minimum · 15 min rounding` on the default card: a **10 min** entry on `/tickets/841ec804…` → card "Total time 10m / Billable 30m / Time amount $60.00", row "0.17 h worked · 0.50 h billed"; a **40 min** entry → rounds to 45 ("0.67 h worked · 0.75 h billed"), card totals "50m / 1h 15m / $150.00". `/timesheet` shows both rows with the same worked-vs-billed strings, the terms chip "$120.00/h · 30 min minimum", and week money `$150.00` (= 75 billed minutes). Invoice assembly agrees: the assembled draft has lines $60.00 + $90.00 = $150.00. See paper cut G2-1 for the day/week *minutes* aggregate. |
| #6423 | PASS | Partner default tax rate saved as 7.5% on the Defaults tab (toast "Billing settings saved"). Draft invoice assembled for Default Organization, line 1 flipped taxable → after autosave the summary renders `invoice-tax-inherited-hint`: *"Tax isn't on this draft yet — the inherited rate of 7.50% applies when you issue it, adding $4.50 for a total of $154.50."* Subtotal/Tax/Total still show the committed draft numbers ($150.00 / $0.00 / $150.00), exactly as the PR describes. Arithmetic checks: only the $60.00 line is taxable, 7.5% → $4.50. |
| #6459 | PASS | With **123 organizations** and **120 sites** on one org (both past the route's 100-row clamp): `/billing/invoices` → New invoice → `invoices-assemble-org` has **124 options** (123 orgs + placeholder), `invoices-assemble-site` **121** (120 + "All sites"); every expected `QA Org NNN` / `QA Site NNN` present, **0 missing, 0 duplicates**. Same for the move-org dialog (below). Walker is `apps/web/src/lib/fetchAllPages.ts`. See paper cut G2-2 on ordering. |
| #6411 | PASS | `/devices/42fc7de0…` → Move to Organization → target org *E2E Multi-Currency 1789953310781* → `move-org-target-site` lists **121 options = all 120 QA sites + placeholder** (none missing). Superseded in practice by #6459's exhaustion walker — the picker is no longer capped at 100 either. `move-org-target-org` shows 123 options (122 other orgs + placeholder; the device's current org is excluded). |
| #6400 | PASS | **Without a second factor** (starting state): submit → `POST /devices/:id/move-org` **403**, the dialog replaces the form with *"Add an authenticator app or passkey to your account before moving devices between organizations."* — its own copy, no factor step revealed, matching the PR's `403 MFA_REQUIRED` branch. **With step-up**: after enrolling TOTP, the same submit revealed "Confirm it's you — Moving a device between organizations requires a second factor" + `move-org-stepup-code`; a valid code → toast "e2e-macos.local moved to E2E Multi-Currency 1789953310781" and `devices.org_id/site_id` updated in psql. Moved back the same way (toast "…moved to Default Organization", DB restored). |

**Everyday-workflow pass** — `/settings/billing` (Defaults · Documents · Rates ·
Connections), `/billing/invoices` (empty state → list → `/billing/invoices/[id]`
editor + summary), `/billing/quotes`, `/contracts`, `/settings/catalog`, `/tickets`
(empty state → `/tickets/new` → `/tickets/[id]` workbench), `/timesheet`. Empty states
are present and well-written on invoices, quotes, contracts and tickets. No console
errors originating from any billing/ticketing page (the errors in the shared console
log come from other groups' tabs and from my own deliberate 403 probes).

Confirmed by toast + effect: billing settings save, billing profile create/edit, ticket
create ("Ticket T-2026-0001 created"), time log ×2 ("Time logged"), invoice assemble
("Draft invoice assembled"), invoice line taxable autosave, device move ×2.


## UI/UX paper cuts

| # | Where | Observation | Severity | Disposition (fixed <sha> / issue # / noted) |
|---|---|---|---|---|
| G1-1 | `/configuration-policies/[id]` → Monitors tab | Deleting a monitor that is attached to a policy leaves the attachment orphaned: the row renders the bare UUID `fca50f0a-3f09-4112-a8e1-7cd9ce7f2bd4` as its title with no "monitor deleted" state, and `config_policy_feature_links.inline_settings` still holds `{"items":[{"monitorId":"fca50f0a-…"}]}`. The delete confirm gives no warning even though the monitors list had just shown "1 policy" for that monitor. | med | noted |
| G1-2 | `/alerts/monitors/new` → Kind = Composite | The kind's only field renders the raw i18n key **`fields.match`** as its label. `monitorKindFields.ts:242` references `monitoring:fields.match`, which does not exist in `apps/web/src/locales/en/monitoring.json` (nor any other locale). | med | noted |
| G1-3 | `/alerts/delivery` | The "Test this rule set" preview does not re-resolve after a routing rule is created or edited — it kept answering "High → Do not notify / No delivery row is configured" until a preview control was changed by hand. Stale answer right at the moment the user wants confirmation their new rule works. | low | noted |
| G1-4 | `/configuration-policies/new` | With the default scope ("A specific organization", pre-selected over "Partner library") and no org chosen, **Create Policy** is disabled with no inline reason — the form looks complete and the button just does nothing. | low | noted |
| G1-5 | `/configuration-policies/new` | Creating a policy navigates to the detail page but fires no success toast, unlike every other create in this area (channel, routing rule, escalation policy, monitor all toast). | low | noted |
| G2-1 | `/timesheet` | The day header and week footer label worked minutes as **billable**: with a 10 min and a 40 min entry the day reads "50m · 50m billable" and the footer "Total: 50m / Billable: 50m", while the two rows directly beneath say "0.50 h billed" / "0.75 h billed" and the money on the same line is **$150.00** — i.e. 75 billed minutes at $120/h. `timeEntryService.ts:1382-1383` sums `durationMinutes` on purpose (comment at :1395 — "that figure is utilization"), so this is a copy bug, not arithmetic: the aggregate needs to say *worked* / *utilization*, or show billed minutes next to the billed money. | med | noted |
| G2-2 | every org/site `<select>` (invoice assemble, move-org, New Ticket) | After #6459 pages the lists to exhaustion the options arrive in `created_at, id` order (`routes/orgs.ts`, the sites handler's `.orderBy(sites.createdAt, sites.id)`) and nothing re-sorts client-side, so a 123-entry org dropdown reads "QA Org 078, QA Org 010, QA Org 089, QA Org 014, …". Correct for pagination, unusable as a picker — type-ahead in a native select only matches the first letter, so finding one org means scrolling an unordered list of 123. Sorting by name after the walk (or the typeahead the PR defers) would finish the job. | med | noted |
| G2-3 | `/settings/billing` → Connections tab | The tab contains no editable settings at all — three "jump to Integrations / Product Catalog / Distributors" links — yet still renders the shared **"Save billing settings"** button underneath. A save button with nothing to save. | low | noted |
| G2-4 | `POST /api/v1/time-entries` | Omitting `startedAt`/`endedAt` returns `400 "startedAt: Invalid input: expected date, received Date; endedAt: Invalid input: expected date, received Date"` — the server defaults those fields to `Date` objects and then fails its own validator, and the message is self-contradictory to the reader. Not reachable from the web UI (which always sends ISO strings), but it is the message an integrator sees. | low | noted |

## Fixes applied

| Commit | PR/area | What | Test |
|---|---|---|---|

## Issues filed

| Issue | Title | From row |
|---|---|---|

## Summary

| Group | PASS | PARTIAL | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|

**Top findings:**

**Oldest PR reached:** #…

**Before the release cut:** flags to enable, BLOCKED rows needing a live agent, open issues.
