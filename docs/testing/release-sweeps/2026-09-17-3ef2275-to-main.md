# Pre-release sweep — 3ef2275 → main 026c268 (2026-09-17)

| | |
|---|---|
| Range | previous sweep end `3ef22758c` (2026-09-16 04:47Z) → `origin/main` `026c268d4` (2026-09-17, 38 commits). Continues `2026-09-16-c07c0a3-to-main.md`; rows verified there are not repeated. |
| Gate | merged before base: #6031 (sweep fixes), #6084 (rustls / Cargo Audit). In the queue at base time, follow-up rows if they land mid-sweep: #6090 #6091 #6092 #6093 #6095. |
| Stack | `pnpm wt-stack up` on `qa/sweep-post-v0.113.0-e` @ `026c268d4`, baseUrl `http://localhost:32918`, project `breeze-wt-qa-sweep-post-v0-113-0-e`, seed admin promoted to platform admin |
| Flags enabled | `BREEZE_AI_AGENTS_ENABLED`, `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED`, `BREEZE_AI_AGENTS_SWEEP_ACT_ENABLED`, `TOOL_SOURCES_ENABLED`, `WEBAUTHN_RP_ID=localhost` |
| Driver | skill `pre-release-sweep`; inventory: sonnet Explore; browser groups: opus, sequential |

## Change inventory

Status ∈ `TODO | PASS | PARTIAL | FAIL | BLOCKED | N/A`. Every row must leave `TODO`.

| PR | Merged | Surface | Title | Route / click-path | Expected visible outcome | Prereq | Status |
|---|---|---|---|---|---|---|---|
| #6002 | 09-16 | web+api+agent | Network device page truth W01 — sourced reachability, probe, SNMP collection health, metrics history | `/devices/network/[id]` header reachability badge (source: network_check/probe/scan/unifi/snmp); "Probe now"; `#monitoring` metrics history | reachability shows a source; probe updates state; history chart populates | network device with ≥1 reachability source; live poll BLOCKED (no agent) | TODO |
| #6017 | 09-16 | agent-only (API-verifiable) | W02 — oidSpecs, bounded table walks, per-OID outcomes | agent SNMP poll; `GET` monitoring asset metrics after a cycle | per-OID outcomes recorded, not all-or-nothing | live agent + SNMP device | TODO |
| #6016 | 09-16 | web+api | W03 — sysObjectID template suggestion, Xerox template, vendor/model identity | Discovery → SNMP asset → settings (SnmpConfigForm) "Suggested template"; Identity card | suggestion by sysObjectID; Xerox template selectable; vendor/model shown | SNMP-discovered asset (seed via SQL) | TODO |
| #6015 | 09-16 | web | W04 — one asset settings modal, single writer, launchers | `/devices/network/[id]` gear → `NetworkAssetSettingsModal` (Identity/Link/Monitoring/Danger); same modal from Discovery list | one modal from every entry point; edits persist | discovered network asset | TODO |
| #6035 | 09-16 | web+api | W05 — reachability header, Health + Reachability cards, Monitoring tab OID table + charts | `/devices/network/[id]` `#overview` header/cards; `#monitoring` OID table + charts | header state/source; both cards populate; OID rows + charts render | network device with SNMP data | TODO |
| #6066 | 09-16 | web+api | persist and surface failed `snmp_poll` results | `/devices/network/[id]` header / Monitoring tab | failing poll shows last-error state instead of vanishing | SNMP device with `last_error` (seed via SQL) | TODO |
| #6060 | 09-16 | web+api | editable HTTP check target + certificate block on monitor drawer | Monitoring → monitor → drawer → HTTP check → Target field; `data-testid="monitor-check-certificate"` | Target editable + saves; cert card shows Issuer/Expires/Observed host/State | HTTP-check monitor on a TLS endpoint | TODO |
| #6034 | 09-16 | web+api+portal | `identity_access_review` evidence report end-to-end | `/reports` → New → "Identity & Access Review" → options → Generate; Portal → Reports | run completes, PDF export, listed in web + portal | M365 sync data (empty-data run still must complete) | TODO |
| #6027 | 09-16 | web+api+portal | `vulnerability_management` evidence report | `/reports` → New → "Vulnerability Management" → Generate | run completes, PDF, portal list | vuln data optional | TODO |
| #6007 | 09-16 | web+api+portal | `endpoint_management_review` evidence report over Intune sync | `/reports` → New → "Endpoint Management Review" → Generate | run completes, PDF, portal list | Intune sync optional | TODO |
| #6006 | 09-16 | web+api+portal | `threat_detection_review` evidence report end-to-end | `/reports` → New → "Threat Detection Review" → Generate | run completes, PDF, portal list | — | TODO |
| #6052 | 09-16 | portal | drop `threat_detection_review` from GENERATING_COPY (TS2353) | Portal → Reports → `ReportRunList` while a threat_detection_review run is generating | "Generating…" copy renders, no crash | in-progress run | TODO |
| #6047 | 09-16 | web+api | approved intent's terminal outcome read back into chat; terminal intents in `/approvals` (fixes #6022) | AI chat → Tier-3 action → approve in `/approvals` → back to chat; `/approvals` list | chat reflects succeeded/failed (never "approved and running" after an error); `/approvals` shows terminal intents | AI chat + approver | TODO |
| #6043 | 09-16 | api-only | org-owned tool source reachable under partner session (fixes #6023) | `/settings/tool-sources/[id]` Test drawer with `?orgId=` under a partner session | org-owned source resolves; previously 404 | `TOOL_SOURCES_ENABLED` | TODO |
| #6042 | 09-16 | api-only | tool-source audit-before-enqueue, 202 on enqueue failure, http under private egress (fixes #6025) | `POST /tool-sources`, `POST /tool-sources/:id/discover` | audit row before enqueue; 202 on enqueue failure; plain-http rejected unless `TOOL_SOURCES_ALLOW_PRIVATE_EGRESS` | `TOOL_SOURCES_ENABLED` | TODO |
| #6053 | 09-16 | web | AI run patch item shows linked intent's live status | `/ai-agents/runs/[id]` → ledger entry linked to an intent | badge tracks the intent's live/terminal status | AI run with an intent | TODO |
| #6014 | 09-16 | web | org AI budget editor moved to org settings AI tab | Org Settings → AI tab (`#ai`) → `OrgAiBudgetSettings` | editor under Settings → AI; old location gone; nav updated | org admin | TODO |
| #6045 | 09-16 | web+api | Organizations board works for system-scope platform admin (fixes #6029) | `/organizations` as a platform admin with NO membership rows | board renders readiness data, not dead | platform admin w/ zero memberships (SQL) | TODO |
| #6064 | 09-16 | web+api | "All Organizations" variables view lists only partner-wide vars; rows show owning org | `/settings/variables` → org switcher → All Organizations | only partner-scoped vars; Org column present | partner admin; org + partner vars | TODO |
| #6065 | 09-16 | web+portal | Portal Devices page is a visibility toggle; "Enable all visibility" includes it | Org settings → Portal tab (`#portal`) → Devices toggle / "Enable all visibility" | Devices off hides `/devices` in portal nav; Enable all flips it on | portal contact login | TODO |
| #6054 | 09-16 | portal+api | portal dashboard Support tile counts only the login's own tickets | Portal home → Support tile | count = tickets owned by the contact, not all org tickets | portal contact + foreign tickets | TODO |
| #6041 | 09-16 | web | paper cuts (fixes #6030): branding save outcome, Built-in provenance, monitor owner badge, escalation picker | (a) Org settings `#branding` save; (b) Scripts → Built-in filter; (c) monitor drawer owner badge; (d) escalation picker | (a) real toast incl. failure; (b) filter works; (c) badge; (d) picker populated | a Built-in script; a monitor with owner | TODO |
| #6039 | 09-16 | web | armed auto-install badge, dry-run 0-device hint, queued-check toast (fixes #6026) | `/software` Compliance Dashboard; Policy form → Enforce + autoinstall | "Armed" badge; dry-run copy at 0 devices; readable Check-compliance toast | enforce+allowlist policy | TODO |
| #6055 | 09-16 | web+api | `default_time_entry_minutes` in the ticket category editor | Settings → Ticket categories → edit → "Default time entry (minutes)" | saves + round-trips; 1–1440 validated | ticketing | TODO |
| #6061 | 09-16 | web | device cards abort metrics fetches on unmount; cap in-flight sparklines | `/devices` → fast scroll / navigate away | aborted, not orphaned, sparkline requests; no console errors | several devices | TODO |
| #5900 | 09-16 | web | Windows one-liner downloads the agent into a temp dir, not the shell cwd | `/devices` → Add device → Windows one-liner text | script text uses a temp dir | Windows host BLOCKED; inspect text | PASS |
| #6056 | 09-16 | api-only | bound the RLS GUC prologue; detect/reclaim wedged ClientRead backends (#6048) | API logs / `pg_stat_activity` during sweep | no wedged backends accumulate; reclaimer logs | — | PASS |
| #6068 | 09-16 | api-only | Fleet Designer apply steps run on the savepoint tx (#5707) | Fleet Designer apply with one failing step | all-or-nothing, no partial writes | fleet design | PASS |
| #6059 | 09-16 | api-only | partner-wide event-log forwarding destination delivered to child orgs (#5424) | partner-scoped destination; agent config delivery for a child-org device | child-org device config includes the destination | device token | PASS |
| #6057 | 09-16 | api-only | deliverable sweep closes retired deliverables' occurrences; pinned-ticket asserts get org predicate (#5609) | retire a deliverable with open occurrences; run sweep | occurrences closed | worker run | PASS |
| #6040 | 09-16 | api-only | offlineDetector matches `last_seen_at` at ms precision (fixes #6024) | SQL-seeded device with sub-ms `last_seen_at` past threshold; wait a detector cycle | device flips offline | — | PASS |
| #5907 | 09-16 | api-only | savepoint the tunnel allowlist insert; repeat proxy Connect no longer 500s | repeat network proxy Connect to the same target | second Connect not 500 | live agent tunnel | PARTIAL |
| #6063 | 09-16 | ci | contract test: every router mounted in index.ts 401s when mounted bare (#5963) | unit suite | green in CI | — | N/A (CI) |
| #5901 | 09-16 | agent-only | hosted first install stages the watchdog from its control plane (#5899) | fresh hosted install | watchdog staged | lab device | BLOCKED |
| #6058 | 09-16 | docs-chore | user-risk docs: drop stale site-scoping warning | — | — | — | N/A |
| #5967 | 09-16 | docs-chore | 2026-09-15 sweep doc + fixes (verified in that sweep) | — | — | — | N/A |
| #6031 | 09-17 | docs-chore | 2026-09-16 sweep doc + fixes (verified in that sweep) | — | — | — | N/A |
| #6084 | 09-17 | ci | rustls bump clears Cargo Audit | Cargo Audit on main `026c268d4` | green (verified) | — | N/A |

### Env vars in range
All introduced by #6031 and already mapped in `docker-compose.yml`: `DB_ACCESS_CONTEXT_PROLOGUE_TIMEOUT_MS`, `DB_WEDGED_BACKEND_{SCAN_DISABLED,SCAN_INTERVAL_MS,MIN_AGE_MS,RECLAIM_DISABLED,RECLAIM_MIN_INTERVAL_MS,RECLAIM_MAX_PER_PASS,RECLAIM_TIMEOUT_MS,CONFIRM_DELAY_MS}` (#6056), `TOOL_SOURCES_ENABLED`, `TOOL_SOURCES_ALLOW_PRIVATE_EGRESS`, `WEBAUTHN_RP_ID/ORIGIN/RP_NAME`, `BREEZE_AI_AGENTS_SWEEP_ACT_ENABLED`. No new unmapped `process.env` reads in the other 36 PRs.

### Migrations in range (10, all additive)
`2026-10-17-110000-discovered-assets-status-provenance-and-probe`, `-110100-snmp-metrics-instances-errors-index`, `-110200-snmp-devices-poll-seq` (#6002); `-110300-report-type-threat-detection-review` (#6006); `-110300-snmp-templates-prefixes-modes-xerox` (#6016); `-110400-report-type-endpoint-management-review` (#6007); `-110600-report-type-vulnerability-management` (#6027); `-110700-report-type-identity-access-review` (#6034); `-120100-snmp-devices-last-error` (#6066); `-120300-portal-branding-enable-devices` (#6065).

### Groups
- **G1 Network device page** — #6002 #6017 #6016 #6015 #6035 #6066 #6060
- **G2 Reports / evidence** — #6034 #6027 #6007 #6006 #6052
- **G3 AI / approvals / tool sources** — #6047 #6043 #6042 #6053 #6014
- **G4 Organizations / variables / portal** — #6045 #6064 #6065 #6054 #6041
- **G5 Software / tickets / devices misc** — #6039 #6055 #6061 #5900
- **G6 API-only (curl + SQL, no browser)** — #6056 #6068 #6059 #6057 #6040 #5907

## Sweep log (append as you go)

### [Group — area] — agent, timestamp

#### [#PR / area] — PASS | PARTIAL | FAIL | BLOCKED
- ✅ what was checked, concretely
- ❌ BUG: symptom → API actual (status + body) vs UI actual
- ⚠️ UI/UX: paper cut (also copy to the list below)
- BLOCKED: prerequisite

### G6 API-only (curl + SQL + psql, no browser) — opus, 2026-09-17 ~03:0xZ

Method: bearer token from `POST /auth/login` (428 then 200 — known noise), psql via `docker exec …-postgres-1`, BullMQ jobs enqueued from inside `…-api-1`, API logs via `docker logs`. Two rows whose live path needs a real agent tunnel / an AI design run were additionally verified by their PRs' own real-Postgres integration suites on a **throwaway** `pnpm test-stack` DB (port 32919, torn down afterwards) — never against the sweep's live `breeze` DB.

Seeded + cleaned up afterwards: a `brz_` agent token hash on `e2e-windows.local`, a partner-wide `partners.settings.eventLogs` destination, one `service_deliverables` row + 2 occurrences. All reverted at the end (verified by re-select).

#### #6040 offlineDetector ms precision (fixes #6024) — PASS
- ✅ Original bug condition reproduced exactly: `update devices set status='online', last_seen_at = date_trunc('milliseconds', now()) - interval '20 minutes' + interval '0.4 ms'` on `e2e-macos.local` → stored `2026-09-17 02:23:48.6604` (sub-millisecond component, which the old `eq(devices.lastSeenAt, <ISO ms>)` could never match).
- ✅ One 30 s detector cycle later: API log `[OfflineDetector] Detected 1 stale devices` → `Detection completed: 1 devices marked offline` → `[EventBus] Published device.offline for org 0d383527-…`.
- ✅ `GET /api/v1/devices/42fc7de0-48f5-48f2-846b-6dd95924baf9` → 200 `{"hostname":"e2e-macos.local","status":"offline","lastSeenAt":"2026-09-17T02:23:48.660Z"}`. Both seeded devices sit `offline` on this stack (the #6024 symptom was that they never did).

#### #6059 partner-wide event-log forwarding destination (fixes #5424) — PASS
- ✅ Control first: org `0d383527-…` has no `settings.logForwarding` and the partner no `settings.eventLogs`; `PUT /api/v1/agents/e2e-windows-agent/eventlogs` (device bearer token) → 200 `{"success":true,"count":1,"filtered":0}` and **zero** forwarding lines in the API log.
- ✅ Then partner-wide only: `partners.settings.eventLogs = {"enabled":true,"elasticsearchUrl":"http://g6-sink.invalid:9200","indexPrefix":"g6-partner-wide"}` on partner `8eeec1f4-…`, org untouched. Same PUT → 200, and the child org's device now enqueues + attempts delivery **to the partner URL**: `[logForwardingWorker] job fwd-e65460f3-…-1789613143701 failed: Error: getaddrinfo ENOTFOUND g6-sink.invalid`. So the partner destination reaches a child-org device; before #6059 `getOrgForwardingConfig` returned null and nothing was enqueued (matches the control run above).
- ⚠️ Pre-existing, NOT in this range: the same request logs the #1105 tripwire — `bullmq.add(log-forwarding) ran inside a held withDbAccessContext transaction — it pins a pooled connection idle-in-transaction across slow work (#1105)`. The enqueue in `routes/agents/eventlogs.ts` needs `runOutsideDbContext()`. Logged as a paper cut.

#### #6057 deliverable sweep closes retired deliverables' occurrences (fixes #5609) — PASS
- ✅ Seeded an org-owned deliverable (`grace_days=14`) with two occurrences past grace: one `scheduled` (due 2026-08-17) and one `open` (due 2026-08-18). Set `active=false` (retired) — the exact #5609 condition: the main sweep's `eq(active,true)` predicate can never visit it again.
- ✅ Enqueued `deliverable-sweep` onto `deliverable-jobs` from inside the api container. Worker log: `[DeliverableWorker] closing sweep complete {"today":"2026-09-17","deliverables":1,"missed":2}` followed by `sweep complete {"deliverables":0,…,"missed":2,…}` — note `deliverables:0` for the main pass (proving the retired row is invisible to it) and 2 closed by the new closing pass.
- ✅ Both occurrences now `missed` in SQL, including the `scheduled` one that only closing mode retires.

#### #6056 bound the RLS GUC prologue + wedged-backend detector (#6048) — PASS
- ✅ Detector is live with the documented defaults: `[db-wedged-backend] Detector started (interval 300000ms, threshold 300000ms)` (= `DB_WEDGED_BACKEND_SCAN_INTERVAL_MS` / `DB_WEDGED_BACKEND_MIN_AGE_MS`, both 5 min). All nine vars are mapped in `docker-compose.yml`.
- ✅ `application_name` is now set: every pooled backend reports `breeze-api` in `pg_stat_activity` (the incident's diagnosis problem — every backend looked alike).
- ✅ Ran the exact production predicate against the live stack repeatedly while every other G6 row was exercised: **0** backends matching `state='active' AND wait_event_type='Client' AND wait_event='ClientRead' AND query like 'select set_config(%'`. Pool steady at 30 `idle` backends at the end of the session — no slot accumulation.
- ✅ No `prologue`/`wedged` timeout or reclamation events in the whole API log (only the startup line above; the two `withPrologueDeadline` stack frames in the log belong to an unrelated `[backupVerification] Integrity check hook failed` error passing through the wrapper).
- ✅ The PR's own `wedgedBackendPredicate.integration.test.ts` green against real Postgres on a throwaway DB (2/2: "parses and executes against pg_stat_activity", "does not match a healthy open transaction holding a pooled connection").
- ⚠️ Doc-only nit: the "Env vars in range" list above says `DB_WEDGED_BACKEND_CONFIRM_DELAY_MS`; the real name in code, `.env.example` and `docker-compose.yml` is `DB_WEDGED_BACKEND_CONFIRM_DELAY_MS` (no `RECLAIM_`).
- Not reproduced (by design): a genuinely wedged backend. Manufacturing one means abandoning a connection mid extended-protocol exchange and would leak a backend for the session; the PR itself documents this as unit/injected-terminator covered.

#### #6068 Fleet Designer apply steps run on the savepoint tx (#5707) — PASS
- ✅ Constructing a failing apply over HTTP needs a `design`-profile AI report run + a locked approval + a step that fails on real data — not reachable by seeding alone, so verified through the PR's own live-Postgres suite `fleetDesignApply.integration.test.ts` (14/14 green as `breeze_app` on the throwaway DB). The two cases that ARE this fix: *"8. partial apply: an invalid rule shape fails step 3; steps 1-2 stay applied; rollback restores them"* and *"14. a failure after script import rolls back every step-4 write and preserves completed steps"* — i.e. the failing step's writes are gone, earlier steps survive, and the ambient transaction is still usable afterwards (the suite keeps asserting on it).
- ✅ Also green in the same run: idempotent re-apply, cross-org RLS forge 42501, rollback, and org-erasure ledger cascade.

#### #5907 savepoint the tunnel allowlist insert — PASS (live-agent path BLOCKED)
- BLOCKED for the live stack: `POST /tunnels/proxy-connect` needs an online agent to own the tunnel — this stack has no live agent (`[MonitorWorker] No online agent for org 0d383527-…`), so the route cannot reach the allowlist insert at all.
- ✅ Instead ran the PR's own real-Postgres suite `tunnel-allowlist-duplicate-savepoint.integration.test.ts` on the throwaway DB — 2/2 green, driving the **real router** as `breeze_app`: *"POST /tunnels/proxy-connect: a second Connect to the same asset:port reuses the rule and returns 201 (prod 2026-09-15 repro)"* and *"POST /tunnels/allowlist: a duplicate rule returns 409, not 500 (caught 23505 must not abort the request transaction)"*. That is the exact prod 25P02 repro; it fails with 500 on the pre-fix code per the PR.

#### #5900 Windows one-liner stages into a temp dir — PASS
- ✅ The one-liner is generated client-side by `apps/web/src/lib/installCommands.ts` (there is no API-side generator; `apps/api/src/services/installerBuilder.ts` contains no `-OutFile`). Read the generator text: it stages with `$d=Join-Path $env:TEMP 'breeze-install'; New-Item -ItemType Directory -Force -Path $d | Out-Null; $exe=Join-Path $d 'breeze-agent.exe'`, downloads with `Invoke-WebRequest … -OutFile $exe`, and runs `& $exe service install` / `enroll` / `service start` against that absolute path. No relative `-OutFile` and no reliance on the shell cwd anywhere in the Windows branch.
- ✅ The MZ check also reads `$exe` (absolute), so nothing falls back to the working directory.
- Not verified on a live Windows host with the ASR rule armed (no lab device this session) — text inspection only, which is what this row asks for.

## UI/UX paper cuts

| # | Where | Observation | Severity | Disposition (fixed <sha> / issue # / noted) |
|---|---|---|---|---|
| G6-1 | API `routes/agents/eventlogs.ts` (log forwarding) | `bullmq.add(log-forwarding)` runs inside the held `withDbAccessContext` transaction — the #1105 tripwire fires on every event-log submit that has a forwarding destination. Pre-existing, not in this range; needs `runOutsideDbContext()`. | medium | issue #6097 |
| G6-2 | This doc, "Env vars in range" | Lists `DB_WEDGED_BACKEND_CONFIRM_DELAY_MS`; the real var is `DB_WEDGED_BACKEND_CONFIRM_DELAY_MS` (code, `.env.example`, `docker-compose.yml`). | low | noted |

## Fixes applied

| Commit | PR/area | What | Test |
|---|---|---|---|

## Issues filed

| Issue | Title | From row |
|---|---|---|
| #6097 | eventlogs: log-forwarding enqueue runs inside the held request DB transaction (#1105 tripwire) | G6-1 |
| #6098 | startup: syncBinaries runs GitHub fetches inside runWithSystemDbAccess (2.7 s held connection at boot) | G6 log review |

## Summary

| Group | PASS | PARTIAL | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|

**Top findings:**

**Oldest PR reached:** #…

**Before the release cut:** flags to enable, BLOCKED rows needing a live agent, open issues.
