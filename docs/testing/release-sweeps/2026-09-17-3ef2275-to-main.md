# Pre-release sweep — 3ef2275 → main 026c268 (2026-09-17)

| | |
|---|---|
| Range | previous sweep end `3ef22758c` (2026-09-16 04:47Z) → `origin/main` `026c268d4` (2026-09-17, 38 commits). Continues `2026-09-16-c07c0a3-to-main.md`; rows verified there are not repeated. |
| Gate | merged before base: #6031 (sweep fixes), #6084 (rustls / Cargo Audit). In the queue at base time, follow-up rows if they land mid-sweep: #6090 #6091 #6092 #6093 #6095. |
| Stack | `pnpm wt-stack up` on `qa/sweep-post-v0.113.0-e` @ `026c268d4`, baseUrl `http://localhost:32918`, project `breeze-wt-qa-sweep-post-v0-113-0-e`, seed admin promoted to platform admin |
| Flags enabled | `BREEZE_AI_AGENTS_ENABLED`, `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED`, `BREEZE_AI_AGENTS_SWEEP_ACT_ENABLED`, `TOOL_SOURCES_ENABLED`, `WEBAUTHN_RP_ID=localhost` |
| Driver | skill `pre-release-sweep`; inventory: sonnet Explore; browser groups: opus, sequential |

## Change inventory

Status ∈ `TODO | PASS | PARTIAL | FAIL | BLOCKED | N/A`. Every row must leave `TODO`.

| PR | Merged | Surface | Title | Route / click-path | Expected visible outcome | Prereq | Status |
|---|---|---|---|---|---|---|---|
| #6002 | 09-16 | web+api+agent | Network device page truth W01 — sourced reachability, probe, SNMP collection health, metrics history | `/devices/network/[id]` header reachability badge (source: network_check/probe/scan/unifi/snmp); "Probe now"; `#monitoring` metrics history | reachability shows a source; probe updates state; history chart populates | network device with ≥1 reachability source; live poll BLOCKED (no agent) | PASS |
| #6017 | 09-16 | agent-only (API-verifiable) | W02 — oidSpecs, bounded table walks, per-OID outcomes | agent SNMP poll; `GET` monitoring asset metrics after a cycle | per-OID outcomes recorded, not all-or-nothing | live agent + SNMP device | PARTIAL |
| #6016 | 09-16 | web+api | W03 — sysObjectID template suggestion, Xerox template, vendor/model identity | Discovery → SNMP asset → settings (SnmpConfigForm) "Suggested template"; Identity card | suggestion by sysObjectID; Xerox template selectable; vendor/model shown | SNMP-discovered asset (seed via SQL) | FAIL |
| #6015 | 09-16 | web | W04 — one asset settings modal, single writer, launchers | `/devices/network/[id]` gear → `NetworkAssetSettingsModal` (Identity/Link/Monitoring/Danger); same modal from Discovery list | one modal from every entry point; edits persist | discovered network asset | PASS |
| #6035 | 09-16 | web+api | W05 — reachability header, Health + Reachability cards, Monitoring tab OID table + charts | `/devices/network/[id]` `#overview` header/cards; `#monitoring` OID table + charts | header state/source; both cards populate; OID rows + charts render | network device with SNMP data | PASS |
| #6066 | 09-16 | web+api | persist and surface failed `snmp_poll` results | `/devices/network/[id]` header / Monitoring tab | failing poll shows last-error state instead of vanishing | SNMP device with `last_error` (seed via SQL) | PASS |
| #6060 | 09-16 | web+api | editable HTTP check target + certificate block on monitor drawer | Monitoring → monitor → drawer → HTTP check → Target field; `data-testid="monitor-check-certificate"` | Target editable + saves; cert card shows Issuer/Expires/Observed host/State | HTTP-check monitor on a TLS endpoint | PASS |
| #6034 | 09-16 | web+api+portal | `identity_access_review` evidence report end-to-end | `/reports` → New → "Identity & Access Review" → options → Generate; Portal → Reports | run completes, PDF export, listed in web + portal | M365 sync data (empty-data run still must complete) | PARTIAL |
| #6027 | 09-16 | web+api+portal | `vulnerability_management` evidence report | `/reports` → New → "Vulnerability Management" → Generate | run completes, PDF, portal list | vuln data optional | PASS |
| #6007 | 09-16 | web+api+portal | `endpoint_management_review` evidence report over Intune sync | `/reports` → New → "Endpoint Management Review" → Generate | run completes, PDF, portal list | Intune sync optional | PASS |
| #6006 | 09-16 | web+api+portal | `threat_detection_review` evidence report end-to-end | `/reports` → New → "Threat Detection Review" → Generate | run completes, PDF, portal list | — | PASS |
| #6052 | 09-16 | portal | drop `threat_detection_review` from GENERATING_COPY (TS2353) | Portal → Reports → `ReportRunList` while a threat_detection_review run is generating | "Generating…" copy renders, no crash | in-progress run | PASS |
| #6047 | 09-16 | web+api | approved intent's terminal outcome read back into chat; terminal intents in `/approvals` (fixes #6022) | AI chat → Tier-3 action → approve in `/approvals` → back to chat; `/approvals` list | chat reflects succeeded/failed (never "approved and running" after an error); `/approvals` shows terminal intents | AI chat + approver | PASS |
| #6043 | 09-16 | api-only | org-owned tool source reachable under partner session (fixes #6023) | `/settings/tool-sources/[id]` Test drawer with `?orgId=` under a partner session | org-owned source resolves; previously 404 | `TOOL_SOURCES_ENABLED` | PASS |
| #6042 | 09-16 | api-only | tool-source audit-before-enqueue, 202 on enqueue failure, http under private egress (fixes #6025) | `POST /tool-sources`, `POST /tool-sources/:id/discover` | audit row before enqueue; 202 on enqueue failure; plain-http rejected unless `TOOL_SOURCES_ALLOW_PRIVATE_EGRESS` | `TOOL_SOURCES_ENABLED` | PASS |
| #6053 | 09-16 | web | AI run patch item shows linked intent's live status | `/ai-agents/runs/[id]` → ledger entry linked to an intent | badge tracks the intent's live/terminal status | AI run with an intent | PASS |
| #6014 | 09-16 | web | org AI budget editor moved to org settings AI tab | Org Settings → AI tab (`#ai`) → `OrgAiBudgetSettings` | editor under Settings → AI; old location gone; nav updated | org admin | PASS |
| #6045 | 09-16 | web+api | Organizations board works for system-scope platform admin (fixes #6029) | `/organizations` as a platform admin with NO membership rows | board renders readiness data, not dead | platform admin w/ zero memberships (SQL) | PASS |
| #6064 | 09-16 | web+api | "All Organizations" variables view lists only partner-wide vars; rows show owning org | `/settings/variables` → org switcher → All Organizations | only partner-scoped vars; Org column present | partner admin; org + partner vars | PASS |
| #6065 | 09-16 | web+portal | Portal Devices page is a visibility toggle; "Enable all visibility" includes it | Org settings → Portal tab (`#portal`) → Devices toggle / "Enable all visibility" | Devices off hides `/devices` in portal nav; Enable all flips it on | portal contact login | PASS |
| #6054 | 09-16 | portal+api | portal dashboard Support tile counts only the login's own tickets | Portal home → Support tile | count = tickets owned by the contact, not all org tickets | portal contact + foreign tickets | PASS |
| #6041 | 09-16 | web | paper cuts (fixes #6030): branding save outcome, Built-in provenance, monitor owner badge, escalation picker | (a) Org settings `#branding` save; (b) Scripts → Built-in filter; (c) monitor drawer owner badge; (d) escalation picker | (a) real toast incl. failure; (b) filter works; (c) badge; (d) picker populated | a Built-in script; a monitor with owner | PASS |
| #6039 | 09-16 | web | armed auto-install badge, dry-run 0-device hint, queued-check toast (fixes #6026) | `/software` Compliance Dashboard; Policy form → Enforce + autoinstall | "Armed" badge; dry-run copy at 0 devices; readable Check-compliance toast | enforce+allowlist policy | TODO |
| #6055 | 09-16 | web+api | `default_time_entry_minutes` in the ticket category editor | Settings → Ticket categories → edit → "Default time entry (minutes)" | saves + round-trips; 1–1440 validated | ticketing | TODO |
| #6061 | 09-16 | web | device cards abort metrics fetches on unmount; cap in-flight sparklines | `/devices` → fast scroll / navigate away | aborted, not orphaned, sparkline requests; no console errors | several devices | TODO |
| #5900 | 09-16 | web | Windows one-liner downloads the agent into a temp dir, not the shell cwd | `/devices` → Add device → Windows one-liner text | script text uses a temp dir | Windows host BLOCKED; inspect text | PASS |
| #6056 | 09-16 | api-only | bound the RLS GUC prologue; detect/reclaim wedged ClientRead backends (#6048) | API logs / `pg_stat_activity` during sweep | no wedged backends accumulate; reclaimer logs | — | PASS |
| #6068 | 09-16 | api-only | Fleet Designer apply steps run on the savepoint tx (#5707) | Fleet Designer apply with one failing step | all-or-nothing, no partial writes | fleet design | PASS |
| #6059 | 09-16 | api-only | partner-wide event-log forwarding destination delivered to child orgs (#5424) | partner-scoped destination; agent config delivery for a child-org device | child-org device config includes the destination | device token | PASS |
| #6057 | 09-16 | api-only | deliverable sweep closes retired deliverables' occurrences; pinned-ticket asserts get org predicate (#5609) | retire a deliverable with open occurrences; run sweep | occurrences closed | worker run | PASS |
| #6040 | 09-16 | api-only | offlineDetector matches `last_seen_at` at ms precision (fixes #6024) | SQL-seeded device with sub-ms `last_seen_at` past threshold; wait a detector cycle | device flips offline | — | PASS |
| #5907 | 09-16 | api-only | savepoint the tunnel allowlist insert; repeat proxy Connect no longer 500s | repeat network proxy Connect to the same target | second Connect not 500 | live agent tunnel | PARTIAL |
| #6063 | 09-16 | ci | contract test: every router mounted in index.ts 401s when mounted bare (#5963) | unit suite | green in CI | — | N/A (CI) |
| #5901 | 09-16 | agent-only | hosted first install stages the watchdog from its control plane (#5899) | fresh hosted install | watchdog staged | lab device | BLOCKED |
| #6058 | 09-16 | docs-chore | user-risk docs: drop stale site-scoping warning | — | — | — | N/A |
| #5967 | 09-16 | docs-chore | 2026-09-15 sweep doc + fixes (verified in that sweep) | — | — | — | N/A |
| #6031 | 09-17 | docs-chore | 2026-09-16 sweep doc + fixes (verified in that sweep) | — | — | — | N/A |
| #6084 | 09-17 | ci | rustls bump clears Cargo Audit | Cargo Audit on main `026c268d4` | green (verified) | — | N/A |

### Env vars in range
All introduced by #6031 and already mapped in `docker-compose.yml`: `DB_ACCESS_CONTEXT_PROLOGUE_TIMEOUT_MS`, `DB_WEDGED_BACKEND_{SCAN_DISABLED,SCAN_INTERVAL_MS,MIN_AGE_MS,RECLAIM_DISABLED,RECLAIM_MIN_INTERVAL_MS,RECLAIM_MAX_PER_PASS,RECLAIM_TIMEOUT_MS,CONFIRM_DELAY_MS}` (#6056), `TOOL_SOURCES_ENABLED`, `TOOL_SOURCES_ALLOW_PRIVATE_EGRESS`, `WEBAUTHN_RP_ID/ORIGIN/RP_NAME`, `BREEZE_AI_AGENTS_SWEEP_ACT_ENABLED`. No new unmapped `process.env` reads in the other 36 PRs.

### Migrations in range (10, all additive)
`2026-10-17-110000-discovered-assets-status-provenance-and-probe`, `-110100-snmp-metrics-instances-errors-index`, `-110200-snmp-devices-poll-seq` (#6002); `-110300-report-type-threat-detection-review` (#6006); `-110300-snmp-templates-prefixes-modes-xerox` (#6016); `-110400-report-type-endpoint-management-review` (#6007); `-110600-report-type-vulnerability-management` (#6027); `-110700-report-type-identity-access-review` (#6034); `-120100-snmp-devices-last-error` (#6066); `-120300-portal-branding-enable-devices` (#6065).

### Groups
- **G1 Network device page** — #6002 #6017 #6016 #6015 #6035 #6066 #6060
- **G2 Reports / evidence** — #6034 #6027 #6007 #6006 #6052
- **G3 AI / approvals / tool sources** — #6047 #6043 #6042 #6053 #6014
- **G4 Organizations / variables / portal** — #6045 #6064 #6065 #6054 #6041
- **G5 Software / tickets / devices misc** — #6039 #6055 #6061 #5900
- **G6 API-only (curl + SQL, no browser)** — #6056 #6068 #6059 #6057 #6040 #5907

## Sweep log (append as you go)

### [Group — area] — agent, timestamp

#### [#PR / area] — PASS | PARTIAL | FAIL | BLOCKED
- ✅ what was checked, concretely
- ❌ BUG: symptom → API actual (status + body) vs UI actual
- ⚠️ UI/UX: paper cut (also copy to the list below)
- BLOCKED: prerequisite

### G6 API-only (curl + SQL + psql, no browser) — opus, 2026-09-17 ~03:0xZ

Method: bearer token from `POST /auth/login` (428 then 200 — known noise), psql via `docker exec …-postgres-1`, BullMQ jobs enqueued from inside `…-api-1`, API logs via `docker logs`. Two rows whose live path needs a real agent tunnel / an AI design run were additionally verified by their PRs' own real-Postgres integration suites on a **throwaway** `pnpm test-stack` DB (port 32919, torn down afterwards) — never against the sweep's live `breeze` DB.

Seeded + cleaned up afterwards: a `brz_` agent token hash on `e2e-windows.local`, a partner-wide `partners.settings.eventLogs` destination, one `service_deliverables` row + 2 occurrences. All reverted at the end (verified by re-select).

#### #6040 offlineDetector ms precision (fixes #6024) — PASS
- ✅ Original bug condition reproduced exactly: `update devices set status='online', last_seen_at = date_trunc('milliseconds', now()) - interval '20 minutes' + interval '0.4 ms'` on `e2e-macos.local` → stored `2026-09-17 02:23:48.6604` (sub-millisecond component, which the old `eq(devices.lastSeenAt, <ISO ms>)` could never match).
- ✅ One 30 s detector cycle later: API log `[OfflineDetector] Detected 1 stale devices` → `Detection completed: 1 devices marked offline` → `[EventBus] Published device.offline for org 0d383527-…`.
- ✅ `GET /api/v1/devices/42fc7de0-48f5-48f2-846b-6dd95924baf9` → 200 `{"hostname":"e2e-macos.local","status":"offline","lastSeenAt":"2026-09-17T02:23:48.660Z"}`. Both seeded devices sit `offline` on this stack (the #6024 symptom was that they never did).

#### #6059 partner-wide event-log forwarding destination (fixes #5424) — PASS
- ✅ Control first: org `0d383527-…` has no `settings.logForwarding` and the partner no `settings.eventLogs`; `PUT /api/v1/agents/e2e-windows-agent/eventlogs` (device bearer token) → 200 `{"success":true,"count":1,"filtered":0}` and **zero** forwarding lines in the API log.
- ✅ Then partner-wide only: `partners.settings.eventLogs = {"enabled":true,"elasticsearchUrl":"http://g6-sink.invalid:9200","indexPrefix":"g6-partner-wide"}` on partner `8eeec1f4-…`, org untouched. Same PUT → 200, and the child org's device now enqueues + attempts delivery **to the partner URL**: `[logForwardingWorker] job fwd-e65460f3-…-1789613143701 failed: Error: getaddrinfo ENOTFOUND g6-sink.invalid`. So the partner destination reaches a child-org device; before #6059 `getOrgForwardingConfig` returned null and nothing was enqueued (matches the control run above).
- ⚠️ Pre-existing, NOT in this range: the same request logs the #1105 tripwire — `bullmq.add(log-forwarding) ran inside a held withDbAccessContext transaction — it pins a pooled connection idle-in-transaction across slow work (#1105)`. The enqueue in `routes/agents/eventlogs.ts` needs `runOutsideDbContext()`. Logged as a paper cut.

#### #6057 deliverable sweep closes retired deliverables' occurrences (fixes #5609) — PASS
- ✅ Seeded an org-owned deliverable (`grace_days=14`) with two occurrences past grace: one `scheduled` (due 2026-08-17) and one `open` (due 2026-08-18). Set `active=false` (retired) — the exact #5609 condition: the main sweep's `eq(active,true)` predicate can never visit it again.
- ✅ Enqueued `deliverable-sweep` onto `deliverable-jobs` from inside the api container. Worker log: `[DeliverableWorker] closing sweep complete {"today":"2026-09-17","deliverables":1,"missed":2}` followed by `sweep complete {"deliverables":0,…,"missed":2,…}` — note `deliverables:0` for the main pass (proving the retired row is invisible to it) and 2 closed by the new closing pass.
- ✅ Both occurrences now `missed` in SQL, including the `scheduled` one that only closing mode retires.

#### #6056 bound the RLS GUC prologue + wedged-backend detector (#6048) — PASS
- ✅ Detector is live with the documented defaults: `[db-wedged-backend] Detector started (interval 300000ms, threshold 300000ms)` (= `DB_WEDGED_BACKEND_SCAN_INTERVAL_MS` / `DB_WEDGED_BACKEND_MIN_AGE_MS`, both 5 min). All nine vars are mapped in `docker-compose.yml`.
- ✅ `application_name` is now set: every pooled backend reports `breeze-api` in `pg_stat_activity` (the incident's diagnosis problem — every backend looked alike).
- ✅ Ran the exact production predicate against the live stack repeatedly while every other G6 row was exercised: **0** backends matching `state='active' AND wait_event_type='Client' AND wait_event='ClientRead' AND query like 'select set_config(%'`. Pool steady at 30 `idle` backends at the end of the session — no slot accumulation.
- ✅ No `prologue`/`wedged` timeout or reclamation events in the whole API log (only the startup line above; the two `withPrologueDeadline` stack frames in the log belong to an unrelated `[backupVerification] Integrity check hook failed` error passing through the wrapper).
- ✅ The PR's own `wedgedBackendPredicate.integration.test.ts` green against real Postgres on a throwaway DB (2/2: "parses and executes against pg_stat_activity", "does not match a healthy open transaction holding a pooled connection").
- ⚠️ Doc-only nit: the "Env vars in range" list above says `DB_WEDGED_BACKEND_CONFIRM_DELAY_MS`; the real name in code, `.env.example` and `docker-compose.yml` is `DB_WEDGED_BACKEND_CONFIRM_DELAY_MS` (no `RECLAIM_`).
- Not reproduced (by design): a genuinely wedged backend. Manufacturing one means abandoning a connection mid extended-protocol exchange and would leak a backend for the session; the PR itself documents this as unit/injected-terminator covered.

#### #6068 Fleet Designer apply steps run on the savepoint tx (#5707) — PASS
- ✅ Constructing a failing apply over HTTP needs a `design`-profile AI report run + a locked approval + a step that fails on real data — not reachable by seeding alone, so verified through the PR's own live-Postgres suite `fleetDesignApply.integration.test.ts` (14/14 green as `breeze_app` on the throwaway DB). The two cases that ARE this fix: *"8. partial apply: an invalid rule shape fails step 3; steps 1-2 stay applied; rollback restores them"* and *"14. a failure after script import rolls back every step-4 write and preserves completed steps"* — i.e. the failing step's writes are gone, earlier steps survive, and the ambient transaction is still usable afterwards (the suite keeps asserting on it).
- ✅ Also green in the same run: idempotent re-apply, cross-org RLS forge 42501, rollback, and org-erasure ledger cascade.

#### #5907 savepoint the tunnel allowlist insert — PASS (live-agent path BLOCKED)
- BLOCKED for the live stack: `POST /tunnels/proxy-connect` needs an online agent to own the tunnel — this stack has no live agent (`[MonitorWorker] No online agent for org 0d383527-…`), so the route cannot reach the allowlist insert at all.
- ✅ Instead ran the PR's own real-Postgres suite `tunnel-allowlist-duplicate-savepoint.integration.test.ts` on the throwaway DB — 2/2 green, driving the **real router** as `breeze_app`: *"POST /tunnels/proxy-connect: a second Connect to the same asset:port reuses the rule and returns 201 (prod 2026-09-15 repro)"* and *"POST /tunnels/allowlist: a duplicate rule returns 409, not 500 (caught 23505 must not abort the request transaction)"*. That is the exact prod 25P02 repro; it fails with 500 on the pre-fix code per the PR.

#### #5900 Windows one-liner stages into a temp dir — PASS
- ✅ The one-liner is generated client-side by `apps/web/src/lib/installCommands.ts` (there is no API-side generator; `apps/api/src/services/installerBuilder.ts` contains no `-OutFile`). Read the generator text: it stages with `$d=Join-Path $env:TEMP 'breeze-install'; New-Item -ItemType Directory -Force -Path $d | Out-Null; $exe=Join-Path $d 'breeze-agent.exe'`, downloads with `Invoke-WebRequest … -OutFile $exe`, and runs `& $exe service install` / `enroll` / `service start` against that absolute path. No relative `-OutFile` and no reliance on the shell cwd anywhere in the Windows branch.
- ✅ The MZ check also reads `$exe` (absolute), so nothing falls back to the working directory.
- Not verified on a live Windows host with the ASR rule armed (no lab device this session) — text inspection only, which is what this row asks for.

### G1 — Network device page (#6002 #6017 #6016 #6015 #6035 #6066 #6060) — opus, 2026-09-17 02:44–03:05Z

**Seed (all SQL as `breeze` superuser on `breeze-wt-qa-sweep-post-v0-113-0-e-postgres-1`, org `0d383527-…780e`, site `b6ca4459-…7dcfb1`; `SELECT set_config('breeze.scope','system',true)` first in every script):**
- `discovered_assets` `11111111-1111-4111-8111-111111111111` — Xerox VersaLink C405 printer, `192.168.50.41`, `approval_status='approved'`, `is_online=true`, `source='scan'`, `status_source='scan'`, `status_observed_at=now()`, 4 open ports, `snmp_data = {"sysDescr":"Xerox VersaLink C405 v1.2.3","sysObjectId":"1.3.6.1.4.1.253.8.62.1.2.1","sysName":"XRX-VL-C405"}` (Xerox enterprise arc `1.3.6.1.4.1.253` from `2026-10-17-110300-snmp-templates-prefixes-modes-xerox.sql`).
- `snmp_devices` `22222222-…2222` — linked to that asset, `snmp_version='v2c'`, port 161, interval 120 s, `template_id` initially NULL (later set to the Xerox built-in through the UI), `last_error='snmp: request timeout (after 3 retries) on 192.168.50.41:161'` + `last_error_at=now()`, `last_status='warning'`, `consecutive_failures=3`.
- `snmp_metrics` — 1 703 rows for that device: `prtMarkerSuppliesLevel` (base `…43.11.1.1.9`, instances `1.1`–`1.4`, hourly ×14 d = 1 344), `prtMarkerLifeCount` (`…43.10.2.1.4`, `counter32`, hourly ×14 d = 336), supply max-capacity / description / colorant (4 each), `hrPrinterStatus=3`, `hrDeviceStatus=3`, `hrPrinterDetectedErrorState='0x2000'` (lowToner bit), `sysDescr`, `sysName`, and one protocol-2 **error row** (`prtGeneralSerialNumber`, `value_type='error'`, `error='noSuchObject'`).
- `network_monitors` — `33333333-…3333` icmp_ping on the asset (`last_status='online'`, 3.1 ms) and `44444444-…4444` http_check `https://example.com` with `tls_state='observed'`, `tls_issuer='DigiCert Global G2 TLS RSA SHA256 2020 CA1'`, `tls_not_after=now()+68 d`, `tls_observed_host='example.com'`. (`network_monitors_tls_state_chk` only accepts `observed|handshake_failed|not_tls` — `'valid'` is rejected 23514.)
- Control asset `55555555-…5555` + `snmp_devices` `66666666-…6666` — switch with a deliberately unmatched `sysObjectId` `1.3.6.1.4.1.99999.1.1`, used to prove the suggestion-block defect below.
- Temporarily set `devices.status='online'` on `e2e-macos.local` to try to make the SNMP worker dispatch a poll; reverted to `offline` / original `last_seen_at` afterwards. Nothing else was mutated.

#### #6002 — W01 sourced reachability, probe, collection health, metrics — PASS
- ✅ Header and both cards render `Responding · Network check now` — a **source**, never a bare "Online". Ranking is right: with the icmp check 8 min stale (window `max(2×60 s, 5 min)`) the header fell back to `Responding · Scan`; refreshing `last_checked` flipped it back to `Network check`, i.e. host-class evidence outranks scan.
- ✅ Reachability card shows per-source detail lines (`Network check Online · 3.1 ms · now`, `SNMP Polling · 2 min. ago`, `Scan Seen · now`) plus the SNMP caveat copy ("a failure can mean the bridging agent … counted when the poll is dispatched").
- ✅ F2 visible: with no template the SNMP line read `No template` and the Health card said "SNMP is on, but no template is assigned — so nothing is being polled" with a *Choose a template* affordance.
- ✅ `collection` on `GET /monitoring/assets/:id`: summary `8 collecting · 1 unsupported · 8 stale`; the seeded `error='noSuchObject'` row renders as **Unsupported** with the row explanation "The device answered noSuchObject — this OID is not implemented on it." Per-OID, not all-or-nothing.
- ✅ `GET /monitoring/assets/:id/metrics` works against real Postgres (the two SQL bugs the PR describes are fixed): `…/metrics?oid=1.3.6.1.2.1.43.11.1.1.9&from=…&to=…&bucket=1d` → 200, base OID fanned out to four per-instance series, `truncatedSeries:false`; `…oid=…43.10.2.1.4.1.1&bucket=1d&delta=1` → 200 reset-aware counter deltas.
- ✅ `nicVendor` renders (`XEROX CORPORATION`) and the raw sysObjectID is masked out of Model (Model = `VersaLink C405`, the raw OID lives under "All scan details").
- ✅ `POST /discovery/assets/:id/probe` from *Check now* → **409** `{"error":"No online agent in this asset’s site","code":"NO_AGENT_IN_SITE"}`, surfaced inline as "No online agent at this site can reach the device. Bring an agent online here, then try again."
- BLOCKED: the probe **success** path (202 pending → command correlation → `last_probe_*` stamped) needs a live agent in the site.

#### #6017 — W02 oidSpecs / bounded walks / per-OID outcomes — PARTIAL (agent half not provable here)
- ✅ Proved server-side: protocol-2 row shape (`base_oid` / `instance` / `error`) is ingested and served. Per-OID outcomes survive end-to-end — one OID `Unsupported` while 8 others keep `Collecting`, and instance fan-out is preserved (`prtMarkerSuppliesLevel (4)`, expandable to `1.1`–`1.4`).
- ✅ Proved server-side: `GET /monitoring/assets/:id/metrics` caps and reports series per base OID (`truncatedSeries:false` on a 4-series response).
- ❌ NOT provable on this stack: the dispatched `snmp_poll` payload (`oidSpecs`, `limits`, `poll_seq` advance, `slow` cadence every 12th poll) and the whole Go side (`WalkBounded` bounds, `noSuchObject`/`truncated`/`timeout` rows). `jobs/snmpWorker.ts` gates dispatch on `isAgentConnectedAnywhere(agentId)` (a live WS presence), so forcing `devices.status='online'` by SQL is not enough — the worker logged `[SnmpWorker] No online agent for org …` and `device_commands` never received an `snmp_poll` row. Needs a real agent + SNMP device.

#### #6016 — W03 sysObjectID template suggestion / Xerox template / identity — FAIL
- ✅ API is correct. `GET /monitoring/templates/suggest?assetId=1111…` → **200** `{"sysObjectId":"1.3.6.1.4.1.253.8.62.1.2.1","assetType":"printer","suggestion":{"templateId":"cc249fb4-8663-4a48-bf51-a9a37c1918da","templateName":"Xerox Printer","reason":"Detected Xerox printer, using Xerox Printer"}}`. The Xerox Printer built-in is seeded with `sys_object_id_prefixes = {1.3.6.1.4.1.253}`, is selectable, and polls correctly once applied.
- ❌ **BUG 1 — the suggestion never reaches the UI.** `apps/web/src/components/devices/networkDevice/settings/MonitoringSection.tsx:119` does `setSuggestion(suggestResult.value ?? null)` — it stores the whole **envelope** instead of `suggestResult.value.suggestion`. UI actual: the Monitoring section renders `Suggested: — Use suggestion` (i18n `Suggested: {{template}} — {{reason}}` with both interpolations `undefined`), and clicking **Use suggestion** leaves the template select on "No template" (`suggestion.templateId` is `undefined`). The unit test is green because `MonitoringSection.test.tsx:134` mocks the **bare** body `{ templateId, templateName, reason }`, which the route has never returned.
- ❌ **BUG 2 — the block renders even when there is no suggestion.** The envelope is truthy whatever `suggestion` holds, so the control asset `5555…` (`sysObjectId 1.3.6.1.4.1.99999.1.1`, API returns `"suggestion":null`) *also* shows `Suggested: — Use suggestion` with a live apply button.
- ❌ **BUG 3 — the server-side auto-apply is unreachable from the product UI.** `PUT/PATCH /monitoring/assets/:id/snmp` applies the suggestion only when the `templateId` key is **absent** (`monitoring.ts:581` `hasOwnProperty`), but `SnmpConfigForm` always sends it: observed request body `{"snmpVersion":"v2c","templateId":null,"pollingInterval":120,"port":161}` → 200 with `templateId` still null, no `templateSuggestion` echo. Net effect: W03's headline feature is 100 % dead in the browser, by three independent paths.
- Note: picking **Xerox Printer** by hand saves fine (`templateId` persisted, toast "SNMP settings saved") and the whole printer surface then lights up — so this is a wiring defect, not a data defect.

#### #6015 — W04 one settings modal, single writer, launchers — PASS
- ✅ Header **Settings** opens `NetworkAssetSettingsModal` at `#overview/settings/identity`; Identity / Monitoring / Link / Danger each save independently. Hash is deep-linkable — a full reload of `…#overview/settings/monitoring` restores the modal on the right section.
- ✅ Same modal from **both** other entry points: Monitoring → Assets row gear (`monitoring-asset-settings-<id>`) and Discovery → Assets row gear (`discovered-asset-settings-<id>`) both navigate to `/devices/network/<id>#overview/settings/monitoring` and open it there.
- ✅ Discovery `AssetDetailModal` is now a read-only peek — "Manage this asset — Identity, monitoring, linking and removal all live on the device page" with *Open device page* / *Settings…*; no inline monitoring form.
- ✅ Edits persist through the single writer with `runAction` feedback: Identity → `PATCH /discovery/assets/:id {"label":"Front desk printer (QA)"}` 200, toast "Identity saved", header title updated live. SNMP → `PATCH /monitoring/assets/:id/snmp` 200, toast "SNMP settings saved".
- ✅ `/monitoring#assets` rows carry the new **Reachability** and **Collection** columns (`Responding · network check 3m ago` / `Partial — Last polled 5m ago`) alongside pause/disable.
- ✅ Identity type select offers exactly the 12 accepted types (no `website`/`service`).

#### #6035 — W05 header, Health + Reachability cards, Monitoring tab — PASS
- ✅ Header: `Responding · Network check now`, `·` separators, no Approved badge on an approved asset.
- ✅ Stat strip: Reachability + **Check now**, `Last poll — Failing · 4 min. ago`, type-specific slot `Lowest supply — Yellow Toner Cartridge 31%`, `Open ports 4`.
- ✅ `PrinterHealth`: `Idle · Warning`, decoded bitmask `Toner low` (from `0x2000`), four supply meters with descriptions and per-reading freshness (correctly **Stale** — my hourly series' newest point was ~58 min old against a `max(2×120 s, 10 min)` window), `Lifetime pages 186,345` with `91 since yesterday · 1099 in the last week` from `/metrics?delta=1`.
- ✅ Overview row 2: condensed Identity with copy buttons, `nicVendor` line, "All scan details" disclosure holding the raw sysObjectID.
- ✅ Monitoring tab: poll-config summary (`Template Xerox Printer`, `every 2 min`, `v2c · 161`, Edit → the W04 modal's Monitoring section), 17-row OID table with mode / state / latest / age and expandable instance counts, network checks with latest results, and the SNMP-threshold empty state ("Nothing on this device will raise an alert from a polled value yet").
- ✅ History charts: selecting `prtMarkerSuppliesLevel` + `prtMarkerLifeCount` renders two `ChartWidget` SVGs — the gauge fanned into 4 instance series on a 0–100 axis, the counter as per-bucket deltas; 24h/7d/30d switch re-requests with `bucket=5m|1h|1d`.
- ⚠️ UI/UX: History opens pre-selected on `sysDescr` — a string scalar that can never plot — so the first thing the section shows is "No data available" (see paper cuts).

#### #6066 — persist and surface failed `snmp_poll` results — PASS
- ✅ With `last_error` / `last_error_at` set, the header renders `data-testid="network-device-last-error"` → "Last error: snmp: request timeout (after 3 retries) on 192.168.50.41:161", and the stat strip reads `Last poll — Failing · 4 min. ago` instead of the reading vanishing.
- ✅ The two fields are on the asset projection (`GET /monitoring/assets/:id/snmp` response carried `"lastError"`/`"lastErrorAt"`), and reachability correctly stays `Responding · Network check` — a protocol-class failure is not host evidence.
- Not checked: the agent-side write path (`commandResultHandlers` / `agentWs` on a real `success:false` poll) and the clear-on-next-success — both need a live agent.

#### #6060 — editable HTTP check target + certificate block — PASS
- ✅ `/monitoring#checks` → *Example HTTPS* drawer renders `data-testid="monitor-check-certificate"`: Issuer `DigiCert Global G2 TLS RSA SHA256 2020 CA1`, Expires `Nov 23, 2026`, Observed host `example.com`, State `Observed`.
- ✅ Edit → `monitor-check-target` present with the note "Changing the URL resets the TLS observation."; changing it to `https://example.org/health` sent `PATCH /monitors/:id {"name":…,"pollingInterval":300,"timeout":10,"isActive":true,"target":"https://example.org/health"}` → 200, toast **"Monitor updated"** (runAction), list row updated.
- ✅ The note is truthful: after the save `network_monitors` had `tls_state`, `tls_issuer`, `tls_not_after`, `tls_observed_host`, `tls_observed_at` all NULL.


### G2 — Reports / evidence (#6034 #6027 #6007 #6006 #6052) — opus, 2026-09-17 03:07–03:20Z

Method: browser as `admin@breeze.local` (`/reports` → Templates → options form → Create report → Generate now), run rows read back from `report_runs.result` in psql, PDFs captured by patching `URL.createObjectURL` + `HTMLAnchorElement.prototype.click` before clicking Download (no real file download), and the server render path fetched with the portal session cookie. Portal checked as `portal@breeze.local`.

Stack has **no M365/Intune/Huntress/vuln-feed data** — so every run here exercises the empty-data path, which is the path all four PRs claim to have designed for.

Seeded (SQL, Default Organization): after generating, the four report definitions were flipped to `portal_self_service = true` and renamed with the `Service evidence — ` prefix, i.e. the state `resolveManagedEvidenceDefinition` provisions, so the portal list was exercised for real; plus one `service_deliverables` + `service_deliverable_occurrences` + `service_deliverable_evidence` chain over the threat run to test the OD-12 delivery gate, and one `report_runs` row with `status='running'`.

#### [#6006 threat_detection_review] — PASS
- ✅ Template picker shows **Threat Detection Review** with the honest description; options form renders `threat-detection-top-incidents` (100) + `threat-detection-include-carried-in`, and the "prints the window it actually covers" caveat. Create → `POST /reports` **201**, toast *Created "Threat Detection Review". Generate it from the Reports list.*
- ✅ Generate now → `POST /reports/:id/generate` **200** `{"status":"completed"}`, toast *Report "Threat Detection Review" generated.* **Empty data completes, does not error.**
- ✅ Stored result is honest: `incidents.opened/resolved/bySeverity/byStatus` all **`null`** (never `0`), `coverage.sourceStatus: "not_connected"`, `dataGaps: ["Huntress is not connected for this partner…"]`, and `agentCoverage.breezeDevices: 2` — the Breeze-side count is still reported.
- ✅ PDF, both paths, renders the **designed arm**, not `renderGenericReport`: staff/browser blob 9,050 B `application/pdf` → title block *"Generated review evidence for 2026-08-18 to 2026-09-17"*, sections `What this covers` / `Endpoint coverage` / `Detections in this period` / `Detections` / `Limits of this report`; portal/server `GET /portal/reports/runs/:id/pdf` **200** `application/pdf` 11,805 B, same arm. Body reads *"This is an absence of measurement, not an absence of threats."*
- ✅ OD-10: `POST /portal/reports/generate {"type":"threat_detection_review"}` (with CSRF) → **400** `type: Invalid option: expected one of "security_compliance_posture"|"executive_summary"|"hardware_lifecycle"`. Label-only in the portal, no generate button rendered.
- ✅ OD-12 gate proven **in both directions** on the same URL: with an undelivered occurrence the row vanishes from the portal list (5 → 4 reports) and `/portal/reports/runs/:id/pdf` returns **404** `{"error":"Report run not found"}`; `status='delivered'` → row returns and the same URL is **200 application/pdf**.

#### [#6007 endpoint_management_review] — PASS
- ✅ Template **Intune Endpoint Management Review**; options form has `endpoint-management-stale-enrolment-days`, `endpoint-management-trend-days`, `endpoint-management-include-licences`, with the "judged against the Intune sync cadence, not the reporting period" copy. Create **201**, toast fired.
- ✅ Generate → **200 completed** with `M365_TENANT_SYNC_ENABLED` off. `intuneDevices/licences/compliance.byState/staleEnrolments.count` all **`null`**, `freshness.{skus,intune_devices}.note: "This domain has never completed a full snapshot."`, `stale: true`, `dataGaps: ["Microsoft 365 tenant sync is not enabled…"]`, and `breezeDevices: 2` still measured — exactly the PR's claim.
- ✅ `historyCaveat` is present in the stored summary and prints.
- ✅ PDF designed arm both paths (staff 8,963 B / portal 11,700 B): `Enrolment coverage`, `Compliance`, `Stale enrolments`, `Licence seats`, `What we could not measure`, `About this report`, and the org name `Default Organization` on the cover.
- ✅ Listed in the portal, download 200, no generate button.

#### [#6027 vulnerability_management] — PASS
- ✅ Template **Vulnerability Management Report**; options form `vulnerability-severity-floor` (critical/high/medium/low), `vulnerability-top-n`, `vulnerability-include-accepted`, and the "never hides an actively exploited (KEV) or high-EPSS finding" help line. Create **201**.
- ✅ Generate → **200 completed**, `rowCount: 1` (the seeded `CVE-2025-E2E-0001`). `open.{critical,high,medium,low,knownExploited,highEpss}` are all **`null`** with `dataGaps: ["No vulnerability feed source is configured, so open findings are unmeasured — not zero."]` — unmeasured ≠ zero holds even while a finding row exists.
- ✅ The ad-hoc-run disclosure the PR promises is present verbatim: *"Ad-hoc run: no service deliverable occurrence supplied the reporting window, so a one-month window ending today was used. The \"expiring next period\" section is therefore one month ahead, not this deliverable's cadence."* (`period.nextEnd: 2026-10-17`).
- ✅ PDF designed arm both paths (staff 14,710 B / portal 20,227 B): `Vulnerability sources`, `Open findings` (`N/A - not measured` per severity), `Remediate first - a patch is available` table with the KEV row rendered `KEV - yes`, **`Accepted risks and exceptions`** (the section the generic fall-through would drop), `Closed this period`, `What this report could not measure`.
- ✅ Listed in the portal, download 200, no generate button.

#### [#6034 identity_access_review] — PARTIAL
- ✅ Template **Identity & Access Review**; options form has `identity-access-dormant-days` (45), `identity-access-home-countries`, `identity-access-admin-detail`, and **no site selector** — instead the `identity-access-org-wide-note`: *"…Microsoft 365 identity data has no site dimension, so it cannot be narrowed to particular sites — and an account limited to certain sites cannot run it at all."* (OD-8 = A as specified). Create **201**.
- ✅ Generate → **200 completed** on empty data. `identity.*`, `signins.*`, `dormant`, `remoteAccess`, `conditionalAccess.*` all **`null`**; `dataGaps: ["Microsoft 365 tenant sync is not enabled for this deployment… This is a configuration gap, not a statement about sign-in activity."]`.
- ✅ PDF designed arm both paths (staff 11,697 B / portal 14,452 B). Title block reads **"Interactive sign-ins for 2026-08-18 to 2026-09-17"**; the cover states *"Non-interactive, service-principal and managed-identity sign-ins are not collected."*; the phrase **"VPN policy review" is absent**; the remote-access section is titled *Remote-access client presence*. (The literal `all sign-ins` does occur, but only inside the honest sentence *"This is not a finding that all sign-ins were local."* — not a scope claim.)
- ✅ OD-10 + OD-12 as for #6006: no portal generate button, listed and downloadable once delivered.
- ❌ BUG (issue **#6100**): the data-gap PDF prints *"Administrator sign-in detail **was switched off for this report**, so no individual sign-ins are listed. **That is a setting, not a finding.**"* — but the report was created with `adminDetail: true` (`select config->>'adminDetail'` = `true`). `emptySummary()` (`services/identityAccessReport.ts:161`) hardcodes `adminSignins: null` on **both** early returns (L257 restricted-authority refusal, L270 sync-off), and `reportPdf/identityAccessPdf.ts:291` reads `null` as "switched off". The renderer's next branch already carries the correct wording (*"absence of measurement, not an absence of activity"*). On the restricted-scope path it is worse: the real reason is a deliberate scope refusal and the artifact blames a setting. This is the exact class of copy-level dishonesty the wave was built to prevent.
- ❌ BUG (same issue): the same `emptySummary` sets `orgName: null` and both gap returns happen **before** the `organizations` lookup (L285), so the identity PDF carries **no customer name** while all three sibling evidence PDFs print `Default Organization` on their equivalent gap covers. On a PII-bearing artifact, unnamed is a distribution hazard.

#### [#6052 portal GENERATING_COPY] — PASS
- ✅ Portal → Reports renders with `threat_detection_review` / `vulnerability_management` / `endpoint_management_review` / `identity_access_review` rows in the same list — no crash, no blank island, `report-ledger-foot` counts correctly. This is the TS2353 surface: the wider display union lists them, the narrower `GeneratableReportType` keys `GENERATING_COPY`.
- ✅ `GENERATING_COPY` still fires for a generatable type: clicking `portal-reports-generate-posture` walked `portal-reports-status` through **"Generating your security summary…" → "Your report is ready."** (captured with a MutationObserver on the live region), `aria-busy` toggled, new row appeared.
- ⚠️ Measurement, not a defect: the list's `Still generating` cell copy is **unreachable** from a page load — `portalRunListPredicate` filters `status = 'completed'`, so a seeded `running` run of an evidence type does not appear at all (verified: row absent, count unchanged, no error). It can only be hit by the optimistic prepend when the post-generate refresh itself fails. Nothing to fix for this PR; noted so a later reader doesn't take "it renders" on faith.

### G3 — AI / approvals / tool sources (#6047 #6043 #6042 #6053 #6014) — opus, 2026-09-17 03:18–03:30Z

Method: one browser session (admin@breeze.local, partner scope) plus one bearer token from `POST /auth/login` (428 → 200, known noise). The AI chat path is **live on this stack** — `ANTHROPIC_API_KEY` is set on the API container and real model turns ran ($0.15 spent), so #6047 was reproduced end-to-end rather than simulated.

Seeded, and **deleted again at the end** (re-selected to confirm `tool_sources`, `ai_agent_runs`, `ai_agents` are all back to 0 rows): one org-owned tool source `g3local` + one `tool_source_tools` row (created via the API for #6042/#6043), and, by SQL, one `ai_agents` + one `ai_agent_runs` + three `action_intents` rows for #6053. The org AI monthly budget was set to $42 while testing #6014 and reverted to NULL (inherited) afterwards. The AI chat session and the two real `action_intents` it minted are left in place — they are genuine product artifacts and are the evidence for #6047.

#### #6047 — approved intent's terminal outcome read back into chat + terminal intents in `/approvals` (fixes #6022) — PASS

- ✅ **The #6022 repro, reproduced exactly and now truthful.** `/workspace` → New Conversation → "Create a software policy named 'G3 Sweep Policy' … with `remediationOptions.autoInstall` ON". The model called `manage_software_policies:create` (Tier 3) → approval card in chat → approved **out-of-band** via `POST /api/v1/approvals/02453586-…/approve` (200). The `#5934` guardrail refused it; `action_intents` shows `status=failed`, `error_code=tool_execution_failed`, `result={"error":"Arming autoInstall requires a human operator with devices.execute and MFA; the AI agent cannot arm software installation."}`. The chat then rendered **"❌ Action Rejected by Server — Arming `autoInstall: true` … cannot be performed by an AI agent"** and offered to create the policy without autoInstall. This is the exact inverse of #6022, where the assistant reported the change as live.
- ✅ A second, earlier intent in the same turn failed on `Invalid input: name and mode are required for create action` — and the model's next line was *"Need to include the mode parameter"*, i.e. it observed the failure and self-corrected rather than narrating success.
- ✅ **`GET /approvals/pending?view=recent`** → 200, both terminal rows, each carrying `intentOutcome: {status:"failed", errorCode:"tool_execution_failed", reason:"<the verbatim guardrail string>", executedAt}`. `GET /approvals/pending` (no param) is unchanged — it returned only the one live `pending` row, with `intentOutcome: null`.
- ✅ **`/approvals` UI**: `data-testid="approvals-recent"` renders collapsed ("Recent · Already decided or finished") and issues **zero** `view=recent` requests until opened; one click → exactly one fetch → two rows, each "Failed · Default Organization · Just now" with the refusal string underneath.
- Not exercised: the `approved_failed` **handoff** branch (`data-testid="ai-tool-approved-failed"`). Both intents here were decided with `decided_via=session_tap`, i.e. the chat session won the `approved → executing` CAS and executed inline, so the tool error reached the model through the ordinary error path, not through `waitForIntentTerminalOutcome`. Forcing the worker to win the CAS needs a race this stack can't stage on demand. The user-visible contract (#6022's actual symptom) is nevertheless verified.

#### #6043 — org-owned tool source reachable under a partner session (fixes #6023) — PASS

- ✅ Created an **org-owned** source under the partner-scoped admin token: `POST /tool-sources {ownerScope:"organization", orgId:"0d383527-…"}` → 201, row has `orgId` set / `partnerId` null. Seeded one enabled Tier-1 tool `g3local__ping` on it.
- ✅ `POST /tool-sources/:id/tools/:toolId/test` under that same partner-scoped token → **200** `{"success":false,"data":{"result":"{\"error\":\"MCP transport error: URL points to blocked address: 127.0.0.1\"}","isError":true,"durationMs":102}}`. Pre-fix this was `404 {"error":"Tool is not currently available"}` — the resolver dropped org-owned rows for a partner session. It now resolves and reaches dispatch; the only remaining failure is the endpoint itself (loopback, correctly blocked by the SSRF guard), which is this stack's limit, not the route's.
- ✅ `GET /tool-sources/:id?orgId=<Default Organization>` → 200 with the org-owned row; `GET /tool-sources/:id/tools` → 200, the tool listed.
- ⚠️ First attempt returned the pre-fix-looking 404 for an unrelated reason — the resolver also requires `tool_sources.status = 'active'` and the source was `error` after its first discovery attempt. See paper cuts.

#### #6042 — tool-source audit-before-enqueue, 202 on enqueue failure, http under private egress (fixes #6025) — PASS

- ✅ **Audit before enqueue**: `POST /tool-sources` → 201 and `audit_logs` carries `tool_source.created` at `03:19:06.999` with the right `org_id`/`resource_id`; `POST /:id/discover` → 202 and `tool_source.discover` at `03:20:46.946`. No `runOutsideDbContext` / held-DB-context (#1105) warning in the API log for either route; all three routes are in `SELF_MANAGED_DB_CONTEXT_ROUTES`.
- ✅ **The old `discover:<id>` jobId bug is gone**: Redis holds `bull:tool-source-discovery:discover-6d7c3771-…` and the job ran to completion (the source's `last_error` was written by the worker), i.e. BullMQ accepted the id instead of throwing `Custom Id cannot contain :`.
- ✅ **202 on enqueue failure, exercised for real**: temporarily replaced the `bull:tool-source-discovery:meta` hash with a string so `Queue.add` would throw, then `POST /:id/discover` → **202** `{"success":true,"source":{…},"data":{"queued":false},"warning":"discovery_not_queued"}` — not a 500, and the row/audit were already written. Restored the meta hash immediately (`version=bullmq:5.81.2`) and re-verified a normal `202 {"data":{"queued":true}}`.
- ✅ **http accepted only under private egress**: `TOOL_SOURCES_ALLOW_PRIVATE_EGRESS=true` on this stack, and `http://127.0.0.1:39999/mcp` was accepted at create (201). `ftp://…` was rejected `400 "endpointUrl: must be an http or https URL"` — the *WithHttp* schema's own message, proving the private-egress variant is the one in force rather than the https-only default.
- Not checked: the https-only rejection with the flag **off** (the flag is process-level on this stack and other groups share it). Covered by the PR's unit tests.

#### #6053 — AI run patch item shows the linked intent's live status — PASS

- ✅ Seeded one completed patch run with three `intent_created` items linked to three intents (`pending_approval`, `approved`, `failed`). `/ai-agents/runs/<id>` renders, by `data-testid="ai-agent-run-patch-item-<i>-intent"`:
  - item 0 → `<a href="/approvals#intent-cccc…001">` "Awaiting approval — open card" (still a link, as intended);
  - item 1 → `<p>` "Approved";
  - item 2 → `<p>` "Failed".
- ✅ Decided items are plain text with no dead link to a card that can no longer be decided — the #5961 symptom (every linked item permanently reading "Awaiting approval") is gone.

#### #6014 — org AI budget editor moved to the org settings AI tab — PASS

- ✅ `/settings/organizations/<org>#ai` deep-links to a new **AI** entry ("AI assistant budget, rate limits and approval mode") sitting directly after **Approval Security**, hosting `data-testid="org-ai-budget-settings"` with all eight fields.
- ✅ Inherited values render as placeholders, not pre-filled: `Default (50)` / `Default (20)` / `Default (200)`, and the two selects read `Default (Enabled)` / `Default (Per Step (default))`.
- ✅ **Only touched fields are sent**: typed `42` into Monthly Budget, clicked Save → `PUT /api/v1/ai/budget?orgId=0d383527-…` body exactly `{"monthlyBudgetCents":4200}` → 200, toast **"Saved successfully"** via `runAction`. (An earlier attempt during the API container restart got a 502 and produced a **"Failed to save budget"** toast — the failure path surfaces too, not a silent no-op.)
- ✅ `/settings/ai-usage` is usage-only: no form, no `ai-budget-save`, one input (the flagged-only filter). In its place `data-testid="ai-effective-budget"` lists all eight fields with source chips — after the save, Monthly Budget read `$42.00` / **Set by organization** linking to `/settings/organizations/<org>#ai`, every other field **Default** with no link.
- ✅ Relabels landed: sidebar entry is "AI Usage", page subtitle "Monitor AI assistant usage".
- ✅ Reverted afterwards: `PUT /ai/budget {"monthlyBudgetCents":null}` → 200, `ai_budgets.monthly_budget_cents` back to NULL.


### G4 Organizations / variables / portal — opus browser agent, 2026-09-17 ~03:40Z

Method: Playwright MCP, two tabs (web admin + customer portal), one login per app plus the one extra web login #6045 requires. Every mutation was clicked with a `MutationObserver` on `[data-testid="toast"]` and a `window.fetch` wrapper installed in the **same** `browser_evaluate`, so toast text and the request's status/body are captured together; `browser_network_requests` used where the fetch happens before script injection (page load).

Seeded by SQL (all left in place, listed at the end): a membership-less platform admin, a second `portal_users` contact, two `tickets`, two `tenant_variables`, two `escalation_policies`. Created through the UI: a second organization, one imported script, one invited user.

#### #6045 — Organizations board for a system-scope platform admin (fixes #6029) — PASS

- Setup (SQL, no UI path): `g4-sysadmin@breeze.local`, `is_platform_admin=true`, `password_hash` copied from the seed admin, `org_id NULL`, **zero** `partner_users` and **zero** `organization_users` rows (`users.partner_id` is NOT NULL so it keeps the partner id — that is the #5978 system-scope shape, not a membership).
- ✅ Control first, as the ordinary partner admin: `/organizations` renders the full board — ACCOUNTS 1, SETUP INCOMPLETE 1, ACCOUNT DATA MISSING 1, OPEN TICKETS 1, and per-row readiness chips. Unchanged by this PR.
- ✅ Logged in as the membership-less platform admin (web login #2). `/organizations` renders **the same board with real data**: ACCOUNTS 2 · SETUP INCOMPLETE 2 · ACCOUNT DATA MISSING 2 · OPEN TICKETS 1, both org rows carrying their own chips (`No site`, `No devices enrolled`, `No policy assigned`, `No backup configured`, `Primary contact` / `Billing contact` / `Billing address`, `No active contract`). **Zero occurrences of "Unavailable"** anywhere on the page — the #6029 symptom was every Setup/Account cell reading exactly that.
- ✅ The fix is visible on the wire, which is the precise #6029 claim ("the client never sends `partnerId`"):
  - `GET /api/v1/orgs/account-readiness?partnerId=8eeec1f4-1bae-484f-b963-c01ed2a1200e&orgIds=051ed777-…%2C0d383527-…&orgId=0d383527-…` → **200** (was `400 {"error":"partnerId is required for system scope"}`)
  - `POST /api/v1/events/ws-ticket?partnerId=8eeec1f4-1bae-484f-b963-c01ed2a1200e` → **200** (the second 400 named in the issue; note the ws-ticket call now carries `partnerId` **instead of** `orgId`, while account-readiness carries both)
- ⚠️ Two residual system-scope rough edges on that page, neither of them the #6029 defect — logged as paper cuts G4-1 and G4-2.

#### #6064 — "All Organizations" variables view (partner-wide only + owning-org column) — PASS

- Setup: `tenant_variables` seeded with one org-owned row (`g4_org_var`, `org_id=0d383527-…`) and one partner-wide row (`g4_partner_var`, `partner_id=8eeec1f4-…`); a second organization **G4 Second Org** created through `/organizations` → Add organization (`POST /orgs/organizations` → **201**, toast *"G4 Second Org added. Its row shows what to set up next."*) so "All Organizations" has something to mean.
- ✅ In a concrete org context `/settings/variables` lists **both** rows and the table now carries an **APPLIES TO** column: `g4_org_var` → `Default Organization`, `g4_partner_var` → `All organizations` (`data-testid="tenant-variable-all-orgs-badge"`). That is the "rows carry the owning org" half.
- ✅ Switching the header org switcher to **All organizations** (`Fleet view · 2 organizations`) fires `GET /api/v1/tenant-variables?scope=partner` → 200 and the table drops to **exactly one row**, `tenant-variable-row-g4_partner_var`; the org-owned row is gone and the scope-filter toolbar is replaced by the explanatory line *"Organization-owned variables are listed under each organization."* No org-scoped variable leaks into the fleet view.
- ⚠️ The org switch still fires **three** `tenant-variables` GETs (`?scope=partner`, bare, `?scope=partner`) — the duplicate-fetch half of #6030, which #6041 explicitly deferred. Paper cut G4-5.

#### #6065 — Portal Devices is a visibility toggle; "Enable all visibility" includes it — PASS

Verified against both the `portal_branding` row and the live portal session (portal@breeze.local, second tab) — the admin change and the portal reload are independent observations, not the same client state.

- ✅ Control: `enable_devices=f`, `enable_self_service=f` on this seed (the new column ships `NOT NULL DEFAULT false`, `2026-10-17-120300-portal-branding-enable-devices.sql`). Portal nav = Dashboard · Proposals · Invoices · Support · Reports · Profile — **no Devices**.
- ✅ Org settings → **Customer Portal** (`#portal`) has a dedicated **Visibility** section with `org-portal-toggle-enableDevices` under the copy *"Show the Devices page. Self-service also grants access to this page."* Toggling it on and saving → `PATCH /orgs/organizations/<org>/portal-settings` **200**, body carries `"enableDevices":true`, toast **"Portal settings saved"**; `portal_branding.enable_devices` → `t`.
- ✅ Portal reload: **Devices** appears in the nav and `/portal/devices` renders the real list (E2E Windows / E2E macOS Test Device, `0 OF 2 ONLINE`).
- ✅ Toggling it back off + save → 200, `"enableDevices":false`, `enable_devices=f`. Portal: nav item **disappears** *and* the route is gated server-side — `/portal/devices` redirects to `/portal/quotes`.
- ✅ **"Enable all visibility"** (`org-portal-enable-all-visibility`) flips Devices back on together with the other eight visibility flags (`enableDashboard/Security/Backups/Reports/SupportUsage/Service/Documents/Lifecycle` all → true) and correctly leaves the four *feature* toggles alone (`enableTickets`, `enableAssetCheckout`, `enableSelfService`, `enablePasswordReset` unchanged). Saved 200; SQL confirms nine `t` and `enable_self_service`/`enable_asset_checkout` still `f`. Portal `/portal/devices` renders again.
- ⚠️ The disabled-route redirect lands on **Proposals**, not the dashboard, with no explanation — paper cut G4-3.

#### #6054 — portal Support tile counts only the login's own tickets — PASS

- Setup: two open tickets in Default Organization — `G4-0001` `submitted_by = eeaf417a-…` (portal@breeze.local's `portal_users` row) and `G4-0002` `submitted_by = bbbb0001-…` (a second `portal_users` contact seeded for this row). Org total = **2** open tickets (the admin Organizations board independently shows `2 open` for the org, so the "all org tickets" number really is 2).
- ✅ Portal home → `data-testid="portal-dashboard-tile-support"` reads **`SUPPORT / 1 / open request`** — the contact's own ticket only. Singular copy is correct too.
- ✅ Bonus, same predicate one level down: `/portal/tickets` lists **only** `G4 own ticket (portal@breeze.local)` `#G4-0001` and footers `1 OPEN REQUEST`; the foreign contact's `G4-0002` is absent.
- Note on the schema: ownership is `tickets.submitted_by` (FK → `portal_users.id`) OR `tickets.requester_contact_id` (FK → `contacts`) — there is **no** `tickets.portal_user_id`; that column exists only on `ticket_comments`.

#### #6041 — #6030 paper cuts (branding outcome, Built-in provenance, monitor owner badge, escalation picker) — PASS (all four)

**(a) Branding save reports one real outcome.** `data-testid="branding-custom-css"` now exists on the textarea.
- ✅ Failure path (the actual #6030 item 1): Custom CSS `@import url("https://evil.example/x.css");` → `PATCH /orgs/organizations/<org>/portal-settings` → **400** `{"error":"customCss: Custom CSS contains a disallowed pattern: @import","details":{…}}`, and the page shows **exactly one toast**, the error: *"customCss: Custom CSS contains a disallowed pattern: @import"*. The second write (`PATCH /orgs/organizations/<org>`) **never fired** — the short-circuit is on the wire, not just in the toast host. No "Settings saved".
- ✅ Success path: `.portal-header{outline:2px solid #00ff00}` → both PATCHes **200**, exactly one toast **"Branding settings saved."**
**(b) Scripts → Provenance → "Built-in".**
- ✅ Behaves as the PR describes (routes to the library, does not filter to nothing): selecting **Built-in** opens `data-testid="scripts-library-dialog"` ("System Script Library"), the select snaps back to `all`, and the tenant list keeps its row — no more "No scripts found". (Needed a tenant script first; the toolbar only renders at ≥1 script, so one library script was imported through **Import from Library**.)
- ⚠️ It is still an *action* wearing a *filter's* clothes — paper cut G4-4.
**(c) Monitor owner badge.** ✅ `/alerts/monitors/161c2f97-…` (a partner-wide built-in monitor) header renders `data-testid="scope-badge"` = **"Partner-wide"** alongside `monitor-built-in-badge` = "Built-in". The #6030 item 3 symptom (badge only in the list) is gone.
**(d) Escalation picker.** Seeded one partner-wide policy (`G4 partner escalation`) and one org-owned policy (`G4 org escalation`) so the filter has something to discriminate.
- ✅ `monitor-editor-escalation-policy` on the partner-wide monitor lists **only** `— ` and `G4 partner escalation`; the org-owned policy is **not offered**. Picker is populated (the old complaint was both an incompatible list and an empty-looking control).
- ✅ Selecting it and saving → `PATCH /monitor-definitions/161c2f97-…` **200**, toast **"Monitor saved"** — no `INVALID_MONITOR:` prefix, no server 400. The pre-#6041 negative case cannot be constructed from the UI any more, which is the point.

#### Everyday workflows (extras) — PASS with two paper cuts

- ✅ **Create org**: `/organizations` → Add organization → `POST /orgs/organizations` **201**, toast *"G4 Second Org added. Its row shows what to set up next."*, row appears on the board.
- ✅ **Rename**: org settings `#general` → `org-name-input` → Save → `PATCH /orgs/organizations/<id>` **200**, toast **"Organization name saved"**; the board row re-reads `G4 Second Org (renamed)`.
- ✅ **Archive**: row actions → `org-board-menu-archive` → a genuinely good confirm modal (what happens, retention selector defaulting to *90 days (recommended)*) → `POST /orgs/organizations/<id>/archive` → **202** `{"status":"offboarding","purgeAt":"2026-12-16T03:37:58.166Z"}`. The row leaves the active list and appears under the **Archived** filter as `Archiving… · Purges in 90 days`. ⚠️ **no toast at all** — paper cut G4-6.
- ✅ **Restore**: Archived → row actions → `org-board-menu-restore` → `POST /orgs/organizations/<id>/restore` **200** `{"status":"active","recreateRequired":["Agents that completed the archive uninstall must be re-enrolled."],"aborted":true,"uninstallsCancelled":0}`, toast **"G4 Second Org (renamed) restored — status: Active. Before it's fully usable again: Agents that completed the archive uninstall must be re-enrolled."** — the best outcome message seen this sweep, and exactly the asymmetry that makes archive's silence stand out.
- ✅ **Invite a user**: `/settings/users` → Invite user → `POST /users/invite` **201** `{"id":"4f8c95ac-…","email":"g4-invite@breeze.local","status":"invited","roleId":"ec1d5a00-…","inviteEmailSent":true}`. No toast, but an **inline** confirmation *"Invitation sent to g4-invite@breeze.local"* plus the new `Invited` row with `Resend invite` — a visible outcome, so not a silent mutation.
- ✅ **Invite token**: there is no DB table for it — `generateInviteToken` (`routes/users.ts:200`) stores it in Redis. Verified both keys exist for the new user: `invite:8c00b3aa…` and `invite-user:4f8c95ac-aa63-431c-90e4-6ffe2461f4b9`. `inviteEmailSent:true` means the mail really went out via Resend (this stack inherits a live `RESEND_API_KEY` from the worktree `.env`; the key is in no tracked file — `.gitignore:11` — so this is a QA-hygiene note, not a repo leak).

#### State left behind by G4 (all intentionally left in place)

| Object | Id / key | How |
|---|---|---|
| `users` `g4-sysadmin@breeze.local` (platform admin, 0 memberships) | `eba4db40-f765-4d13-b3db-fb91e56bf894` | SQL |
| `users` `g4-invite@breeze.local` (invited, Partner Technician) | `4f8c95ac-aa63-431c-90e4-6ffe2461f4b9` | UI |
| `organizations` **G4 Second Org (renamed)** (created → renamed → archived → restored, now `active`) | `051ed777-20db-4d1a-bdef-5942730864e0` | UI |
| `portal_users` `g4-other@breeze.local` | `bbbb0001-0000-4000-8000-000000000001` | SQL |
| `tickets` `G4-0001` (own) / `G4-0002` (foreign contact), both `open` | `87c218f6-…` / `83bc45ab-…` | SQL |
| `tenant_variables` `g4_org_var` (org) / `g4_partner_var` (partner-wide) | — | SQL |
| `escalation_policies` `G4 partner escalation` / `G4 org escalation` | `bbf74934-…` / `4f771df3-…` | SQL |
| `monitor_definitions` `High CPU usage` now points at `G4 partner escalation` | `161c2f97-…` | UI |
| `scripts` one org copy of **Clear Package Cache** (`origin=human`) | — | UI |
| `portal_branding` Default Organization — **all nine visibility flags now `true`** (was: devices/security/backups/support-usage/service/documents `f`), `custom_css = .portal-header{outline:2px solid #00ff00}` | — | UI |


## UI/UX paper cuts

| # | Where | Observation | Severity | Disposition (fixed <sha> / issue # / noted) |
|---|---|---|---|---|
| G1-1 | Network device page → Monitoring tab → History | The chart picker opens pre-checked on the first chartable OID, which on every template is a string scalar (`sysDescr`), so the History section's first paint is always "No data available". Default to the first numeric/counter OID instead. | low | noted |
| G1-2 | Network asset settings → Monitoring → Suggested template | Renders `Suggested: — Use suggestion` (empty name/reason) on every SNMP asset, including ones with no matching template, and the apply button is a no-op. | high | see #6016 FAIL above — needs an issue |
| G6-1 | API `routes/agents/eventlogs.ts` (log forwarding) | `bullmq.add(log-forwarding)` runs inside the held `withDbAccessContext` transaction — the #1105 tripwire fires on every event-log submit that has a forwarding destination. Pre-existing, not in this range; needs `runOutsideDbContext()`. | medium | issue #6097 |
| G6-2 | This doc, "Env vars in range" | Lists `DB_WEDGED_BACKEND_CONFIRM_DELAY_MS`; the real var is `DB_WEDGED_BACKEND_CONFIRM_DELAY_MS` (code, `.env.example`, `docker-compose.yml`). | low | noted |
| G2-1 | Portal → Reports list | Managed-evidence definitions are provisioned as `Service evidence — <name>`, but `reportDisplayName()` only trims the `Customer portal — ` prefix, so the customer sees the MSP-internal prefix (and "Intune Endpoint Management Review"). | low | issue #6101 |
| G2-2 | Reports list → Saved Reports | The row action buttons (Generate/Edit/Delete, `ReportsList.tsx` ~L584-612) are icon-only and take their accessible name from `title` alone — no `aria-label`, so the ACTIONS column reads as empty in a text extraction and the name is tooltip-derived. Pre-existing pattern, not introduced by these PRs. | low | noted |
| G3-1 | Tool sources → Test drawer / AI chat tool resolution | The resolver requires `tool_sources.status = 'active'`, so **every** tool of a source whose last discovery failed disappears with `404 {"error":"Tool is not currently available"}` — byte-for-byte the #6023 symptom. A transient endpoint outage is therefore indistinguishable from a real access/resolution bug, and nothing on the tool row says why. Distinguish "source unhealthy" from "tool not available". | medium | issue #6102 |
| G3-2 | `POST /tool-sources` with `TOOL_SOURCES_ALLOW_PRIVATE_EGRESS=true` | `http://127.0.0.1:…` is accepted at create (201) but the SSRF guard always blocks loopback (`isAlwaysBlockedIp`), so the source flips to `status=error` on its first discovery with "URL points to blocked address: 127.0.0.1". The private-egress opt-in widens the *scheme*, never loopback — so the form happily accepts a URL that can never work. Reject loopback/metadata literals at validation time. | low | noted |
| G4-1 | `/organizations` as a **system-scope** platform admin | `GET /api/v1/partner/me?orgId=…` → **404**, logged as a red console error on every page load. The system-scope session has no partner membership, so the client should not ask for a partner profile (or should tolerate the 404 quietly). Cosmetic sibling of #6029 — the board itself is fine. Observed only under the system-scope session; not compared against the partner-scope session. | low | noted |
| G4-2 | `/organizations` as a system-scope platform admin | React **hydration mismatch** error ("the server rendered HTML didn't match the client… this tree will be regenerated") on first paint. Board renders correctly afterwards; a dev-build warning, but it fires on the system-scope path specifically. | low | noted |
| G4-3 | Portal, Devices visibility off | `/portal/devices` redirects to **`/portal/quotes`** (Proposals) with no message. A customer who bookmarked Devices silently lands on an unrelated page; the dashboard (or a "this page is not enabled for your account" state) would be the honest destination. | low | noted |
| G4-4 | Scripts → Provenance filter → "Built-in" | Post-#6041 the option is an **action**, not a filter: choosing it opens the System Script Library dialog and snaps the select back to "All origins". Correct behaviour, wrong affordance — it sits in a `<select>` of origins where every other option filters. Move it to a button ("Browse built-in library") or keep it as an option that filters to the org's `origin='system'` rows (currently always zero — importing a library script deliberately stamps `origin='human'`). | low | noted |
| G4-5 | `/settings/variables`, org switch | Switching the org context fires **three** `tenant-variables` GETs (`?scope=partner`, bare, `?scope=partner`). This is #6030 item 5, which #6041 explicitly deferred — but #6041 says `Closes #6030`, so merging it closes the issue with this and the baseline "Next run" item unfixed and unrecorded anywhere else. Re-file the two deferred items before #6030 closes. | medium | needs a follow-up issue |
| G4-6 | `/organizations` → row actions → Archive organization | The archive **succeeds silently**: `POST …/archive` → 202 `{"status":"offboarding","purgeAt":"2026-12-16T…"}` and the only feedback is the row leaving the list. No toast, and the purge date the API just returned is never shown. Restore, the inverse action, toasts a full sentence including what still needs doing — the asymmetry is the tell. For an action that uninstalls agents and stops billing, the confirmation should be at least as loud as the undo. | medium | noted |

## Fixes applied

| Commit | PR/area | What | Test |
|---|---|---|---|
| `d8f5362ff` | #6034 / #6100 api | `identityAccessReport` keeps `adminDetail` (`adminSignins: []` = unmeasured, not "switched off") and resolves `orgName` before both empty-data early returns | `identityAccessReport.test.ts` — 4 new assertions red → 30/30 green |
| `76f10b4a0` | #6016 web | `MonitoringSection` unwraps the `/monitoring/templates/suggest` envelope (`.suggestion`) so the suggestion block renders the template name and "Use suggestion" sets the select; hidden when `suggestion` is null | `MonitoringSection.test.tsx` — mock now uses the real envelope; red (2) → green (24/24) |

## Issues filed

| Issue | Title | From row |
|---|---|---|
| #6097 | eventlogs: log-forwarding enqueue runs inside the held request DB transaction (#1105 tripwire) | G6-1 |
| #6099 | network device: SNMP template auto-apply on save unreachable (form always sends templateId:null) | #6016 / G1-2 |
| #6102 | tool sources: source in `error` hides every tool behind 404 "Tool is not currently available" (+ G3-2 loopback note) | G3-1 |
| #6098 | startup: syncBinaries runs GitHub fetches inside runWithSystemDbAccess (2.7 s held connection at boot) | G6 log review |
| #6100 | identity_access_review data-gap PDF says admin detail was "switched off" when it was on, and omits the org name | #6034 |
| #6101 | Portal report list shows the internal "Service evidence — " prefix to customers | G2-1 |

## Summary

| Group | PASS | PARTIAL | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|

**Top findings:**

**Oldest PR reached:** #…

**Before the release cut:** flags to enable, BLOCKED rows needing a live agent, open issues.
