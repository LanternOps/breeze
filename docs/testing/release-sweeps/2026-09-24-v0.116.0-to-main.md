# Pre-release sweep — v0.116.0 → main (2026-09-24)

| | |
|---|---|
| Range | `v0.116.0` (`92d1ec0417`, 2026-09-23) → `origin/main` `f2b28d742c` (107 PRs) |
| Gate | Merged before sweep: #6740 #6847 #6920 #6921 #6928. #6929 (agent-only verify fix) in queue — no UI, covered by a separate lab run. Held drafts: #6904 #6917 (alerting W05, after v0.117 cut). |
| Stack | `pnpm wt-stack up` on `qa/sweep-post-v0.116.0` @ `f2b28d742c`, baseUrl `http://localhost:32955` |
| Flags enabled | admin promoted to platform admin; `WEBAUTHN_RP_ID=localhost`; anomaly detection toggled in-sweep (Settings ▸ Partner ▸ AI Features) |
| Driver | skill `pre-release-sweep`; inventory agent (sonnet); browser sweep agents (opus), one group at a time |

## Change inventory

Status ∈ `TODO | PASS | PARTIAL | FAIL | BLOCKED | N/A`. Only UI-affecting rows are listed; the rest are summarised under "No UI surface".

| PR | Title | Surface | Route / click-path | Expected visible outcome | Prereq | Group | Status |
|---|---|---|---|---|---|---|---|
| #6744 #6826 #6833 | System page: Connections + Deprecations tabs | web | `/admin/system` (Connections default) and `?tab=deprecations` | Connection status cards; Deprecations tab lists usages, no 500 | platform admin | A Admin/System | PASS |
| #6753 #6821 | Webhook edit keeps redacted secrets; enable toggle + legacy headers survive edit | web | Settings ▸ Webhooks ▸ edit, save without touching secret; toggle Enabled | Secret retained; toggle persists; headers kept | none | A Admin/System | PASS |
| #6791 | "Security settings" search shortcut → MFA page | web | Global search "security settings" | Lands on Settings ▸ Security | none | A Admin/System | PASS |
| #6815 | Sample CSV + import-guide link on org bulk import | web | Organizations ▸ bulk import dialog | "Download sample CSV" + guide link present, CSV downloads | partner admin | A Admin/System | PASS |
| #6892 | Destructive confirmations surface failures via runAction | web | Any destructive confirm (e.g. delete) with a forced backend failure | Error toast instead of silent close | force a failure | A Admin/System | PASS |
| #6891 | Effects don't re-run on locale change | web | Settings ▸ Profile ▸ switch language, revisit data pages | No refetch loop / flicker; strings switch | none | A Admin/System | PASS |
| #6847 | Partner API `alerts:read` scope | web | Settings ▸ API keys / partner API credentials ▸ scopes | `alerts:read` selectable | partner admin | A Admin/System | PASS |
| #6813 #6850 | Business reports W03 templates/options; read-only history for inactive orgs | web/portal | `/analytics` reports; portal `/reports` | Template picker + options form, run/export; inactive org history read-only | report perms; inactive org (SQL) | B Reports | PARTIAL |
| #6814 | Quote email uses proposal title + customer name | web | Billing ▸ Quotes ▸ quote ▸ Send | Email subject/body (check dev mail log) | email transport | C Billing/Settings | PARTIAL |
| #6718 | Work-type pickers + billables export dimensions | web | Timesheet work-type picker; billables export | Picker present; export has new columns | none | C Billing/Settings | PASS |
| #6845 | Partner identity falls back to company details | web | Settings ▸ Partner / billing identity, invoice preview | Blank identity fields show company details as inherited | partner admin | C Billing/Settings | PASS |
| #6865 | Org-level payment terms override | web | Settings ▸ Organizations ▸ [org] ▸ billing | Override settable; shows inherited value when blank | partner admin | C Billing/Settings | PASS |
| #6863 #6846 | Legacy labour-pricing columns dropped; invoice theme frozen at issue | api (regression) | Billing ▸ contracts/rates, issue an invoice | No errors on pricing/rates pages; invoice PDF renders | none | C Billing/Settings | PASS |
| #6740 | Gmail / Google Workspace inbound mailbox connector | web | Ticketing mailbox settings; Integrations ▸ Google Workspace DWD scope list | Google option present; scope list includes gmail.readonly/openid/userinfo.email; OAuth flow BLOCKED locally | Google creds | C Billing/Settings | PARTIAL |
| #6816 | Devices opens on Agent segment, remembers choice | web | `/devices` segmented control | Default Agent; remembers last choice | none | D Devices | PASS |
| #6824 | Installed helper version shown | web | Device ▸ overview | Helper version field (or empty state) | helper installed | D Devices | PASS |
| #6724 #6644 | Monitors W05c2: device Monitoring tab, Alert Templates removed, library Recommended, Needs-conversion view, Convert everything, admin sweep | web | Device ▸ `#monitoring`; `/alerts/monitors` views; `/admin/monitor-conversion` | Tab + controls render; Alert Templates nav gone; Recommended facet; conversion view + Undo | partner-wide rights | D Devices | PASS |
| #6725 | Fleet Designer writes monitors | web | `/ai-agents/fleet-design` | Page renders; monitor write path | AI provider | D Devices | PASS |
| #6893 #6894 #6898 #6882 | Hardware health: Storage & RAID section, device-list column/filter, BMC card | web | Device detail Storage & RAID; `/devices` hardware column/filter | Empty state without data; with SQL-seeded rows cards render, Unknown size | hardware data (SQL seed) | D Devices | PASS |
| #6766 | Network devices can change site | web | `/discovery` ▸ network device ▸ Change site | Site actually changes | discovery data (SQL seed) | D Devices | PASS |
| #6610 | Backup providers: Integrations Backup tab, health overview, device card | web | `/integrations#backup` (old `settings/integrations/backup` redirects) | Tab + health overview + Test connection; device card | provider creds (BLOCKED for live test) | D Devices | PARTIAL |
| #6683 #6760 | Remote-session error codes localized; self-signed install failure names cert fix | web | Remote session failure toast; install error | Code-backed messages | live agent | D Devices | PARTIAL |
| #6825 #6538 | Anomaly episodes panel; partner/org anomaly toggle | web | Settings ▸ Partner `#ai-features`; Org `#ai`; Device `#anomalies` | Toggles persist, org shows inherited value; device tab shows episodes or empty state | anomalies on | E Alerts/AI | PASS |
| #6829 | Interactive AI approval timeout 5–60 min | web | Partner settings + org `#ai` | Field clamps 5–60, org override | partner admin | E Alerts/AI | PASS |
| #6852 | Per-tool AI/MCP rate-limit multiplier | web | `/settings/ai-usage` | Field visible, clamped | partner admin | E Alerts/AI | PASS |
| #6887 | Hardware alerting kind | web | `/alerts/rules` rule builder | hardware_health condition offered | none | E Alerts/AI | PASS |
| #6838 #6763 | Script exit-code severity mapping raises alerts; custom-field write-back outcome in test runner | web | Scripts ▸ script edit ▸ exit-code mapping; Test run | Mapping UI saves; test runner shows write-back result | live agent for run | E Alerts/AI | PARTIAL |
| #6914 | AI notifications deep-link to exact card | web | Notification bell ▸ AI notification | Opens the specific run/approval | AI run (SQL seed) | E Alerts/AI | PASS |

**No UI surface (N/A, CI only):** api-only 46, agent-only 12 (incl. #6921 #6928 #6906 #6775 #6764 #6752 #6817 #6871 #6883 #6879 #6880 #6915), docs/deps/tooling 15. Full per-commit classification from the inventory agent is in the PR description.

## Sweep log (append as you go)

### Group A — Admin/System — 2026-09-24 21:25Z

#### #6744 #6826 #6833 — PASS
- ✅ `/admin/system` loads Connections by default: version banner, 17 Enabled / 37 Disabled / 2 Misconfigured summary, per-service cards with secret values masked ("Secret value, never shown" + set/not set). The 2 Misconfigured cards (billing service, AI usage metering) give specific reasons, which match the stack env.
- ✅ Clicking the Deprecations tab sets `?tab=deprecations`, and a direct load of `?tab=deprecations` selects the tab. The report renders (running/last-recorded version, "900 applied, 0 pending" migrations, the ticket-labour-pricing retirement row, version history with first-seen timestamp). No 500 (#6833).
- Note: "Running version 0.82.0" is the wt-stack env (`BREEZE_VERSION=0.82.0` in the api container), not a product bug. That is why the 0.116.0 removal row reads "Upcoming".

#### #6753 #6821 — PASS
- ✅ Created "Sweep A hook" in the UI (Integrations ▸ Webhooks; `/settings/webhooks` redirects to `/integrations#webhooks`) with an HMAC secret and an `X-Api-Key` header. Then used psql to rewrite `headers` to the **legacy record shape** `{"X-Api-Key":"enc:v2:…"}`.
- ✅ Edit form showed the secret as `********` and the legacy header as key `X-Api-Key` / value `********` (before #6821 the GET returned `headers: []`). Save Changes without touching anything: in the DB `secret` is byte-identical, and the header kept the same ciphertext, now in array shape `[{"key":"X-Api-Key","value":"enc:v2:…5fNab1yk…"}]`.
- ✅ Clicking the row's Active pill sent PATCH 200. The DB `status` changed to `disabled`, and the row still read Disabled after a reload. The edit form switch read Disabled for the paused row. Clicking again resumed it (DB `active`).
- ⚠️ Create, Save and toggle show no success toast; the list updating is the only confirmation. (runAction calls at WebhooksPage.tsx:185 toggle, :276 create/save have no `successMessage`.)

#### #6892 — PASS
- ✅ Webhooks: opened the Delete confirm, deleted the row via psql, then confirmed. `DELETE /api/v1/webhooks/38f8…` returned **404**, the error toast read "Webhook not found", and the modal stayed open.
- ✅ API keys (`/settings/api-keys`): created `sweepA-key` in the UI, opened Revoke, deleted the row via psql, then confirmed Revoke Key. `DELETE /api/v1/api-keys/4927…` returned **404**, the toast read "API key not found", and the modal stayed open.
- ⚠️ After a 404 ("already gone"), the modal stays open and the stale row stays in the list. The only way out is Cancel plus a manual refresh. A 404 on delete could close the dialog and refetch.

#### #6791 — PASS
- ✅ Cmd+K ▸ "security settings" returns one SETTINGS result, "Security settings / Manage MFA and account security". Enter goes to `/settings/profile`, which has the Multi-factor authentication and Passkeys sections.
- ✅ `curl /settings/security` returns `301 → /settings/profile` (old bookmark redirect).

#### #6815 — PASS
- ✅ `/organizations` ▸ Bulk import shows "Download sample CSV" and "Import guide". The guide opens `https://docs.breezermm.com/migration/toolkit/#recipe-1--bootstrap-the-tenancy-tree-from-csv` in a new tab (`target=_blank`); the live page returns 200 and has that anchor id.
- ✅ The download saved `breeze-organization-import-sample.csv`: header `organization,site,externalId,externalSystem,timezone` plus 3 rows. Uploading it back into the importer auto-mapped all 5 columns and showed "Preview 3 rows". Nothing was imported.

#### #6891 — PASS
- ✅ Settings ▸ Profile ▸ Theming ▸ `locale-option-de-DE` switches the UI right away (h1 became "Profileinstellungen") and saves `users.preferences.locale = de-DE`.
- ✅ With de-DE saved, fresh loads of `/admin/system`, `?tab=deprecations`, `/alerts`, `/integrations#webhooks` and `/organizations` sent each data fetch the **same number of times as in English** (connections 1, deprecations 1, webhooks 1, alerts 2, organizations list 2). No languageChanged-driven refetch. Headings render in German (`<html lang="de-DE">`). Switched back to en afterwards.
- ⚠️ Present in both locales, so not a #6891 regression: `/alerts` sends `GET /alerts?status=…` twice at the same ms, and `/organizations` sends `GET /orgs/organizations?page=1&limit=100` twice.
- ⚠️ de-DE: the `/alerts` tab title is "Warnmeldungen" but the page h1 is "Warnungen" (inconsistent term).

#### #6847 — PASS
- ✅ Settings ▸ Service principals (`/settings/partner-service-principals`) ▸ Create offers `alerts:read` **unchecked by default** (the other 8 read scopes are pre-checked). Created `sweepA-sp` with `alerts:read`: the "Service principal created" toast showed and the DB `scopes` include `alerts:read`. Issue key showed a `brz_sp_…` key once.
- ✅ `GET /api/v1/partner-api/alerts?limit=2` with `X-API-Key` returned **200** `{"schemaVersion":"1","mode":"full","data":[…2 alerts with changeVersion/revision…]}`. After Edit ▸ uncheck `alerts:read` ▸ Save, the same call returned **403** `{"error":"Partner API scope required","code":"partner_api_scope_required"}` while `/partner-api/organizations` still returned 200.
- ⚠️ The principal row shows no scopes. You have to open Edit to see what a principal can read.

Group A DB leftovers: service principal `sweepA-sp` (id 3db66201-…, active, one key `sweepA-k`, without alerts:read). The webhook and API key created for testing were deleted via psql as part of the #6892 forced-404 test. The admin locale was reset to `en`.

### Group B — Reports — 2026-09-24 21:45Z

#### #6813 — PASS
- ✅ `/reports/templates` lists a **Business** group first (Ticket SLA attainment, Technician time & billability, AR aging) with their own cadence/format/default-range tiles ("Last full month", "As of run date").
- ✅ Each "Use template" opens the options modal: Report covers (All organizations / One organization), plus type options: SLA (period presets + custom, group-by incl. Automatic, no-SLA toggle), Technician time (period, group-by, weekly capacity), AR (as-of date, group-by org/currency, paid-MTD note).
- ✅ AR aging, One organization: POST `/reports` 201 with `ownerScope:"organization"` + `orgId`; Generate now → 200, toast `Report "AR aging" generated.`; Recent Runs row Completed; Download saved `ar_aging-report-2026-09-24.pdf` (1 page, "AR AGING / As of 2026-09-24 (UTC)", basis notes, "No open invoices" empty states, "Prepared by Default Partner").
- ✅ Technician time, All organizations: POST body `ownerScope:"partner"` with **no orgId**; the row shows the "Partner-wide" badge while an org is focused; generate → 200 `{"status":"completed"}`.
- ✅ Edit AR aging (`/reports/:id/edit`): business options form renders with stored values; changed group-by to Currency → PUT 200, body config is exactly `{groupBy, includePaidInPeriod, schedule, emailRecipients}` (no builder keys), toast `Report "AR aging" updated.`
- ⚠️ The business edit page still renders the generic builder below the options: "Report type" (Devices/Alerts/Patches/Compliance/Activity), "Data source" (Policy set, scoring…), "Columns and fields", chart type. None of it is saved for a business type (PUT drops it), so the controls are dead and misleading.
- ⚠️ Console on `/reports/templates`: `[i18n] missing key: reports:reports.reportTemplates.templates.<uuid>.name/.description` for server-synced templates (fallback text renders; noise only).
- Portal (`/portal/reports`, portal@breeze.local): with the org's AR aging definition flipped to `portal_self_service=true` via psql, `GET /api/v1/portal/reports/runs` **returned the `ar_aging` run**, and the portal list correctly hid it (only the Executive summary I generated showed, "1 REPORT AVAILABLE"). Fail-closed UI filter works; note the API itself has no type filter (by design per the PR, but the run is still reachable by id). Flag reverted.
- `/analytics` is the fleet analytics dashboard (Operations/Capacity/SLA tabs) and renders fine; business reports live under `/reports`, not `/analytics`.

#### #6850 — PARTIAL (FAIL for suspended/churned in the UI)
- Setup: psql-created sibling org "Sweep B Org" (`21b474ca-…`) + site, created and generated an AR aging report for it in the UI, then set `status` via psql.
- ✅ API, org **suspended**: `GET /reports?orgId=` 200 (definition), `GET /reports/runs?orgId=` 200 (metadata, no `result`), `GET /reports/:id` 200; `GET /reports/:id/recipients` 404 and `POST /reports/:id/generate` 404 `{"error":"Report not found"}` — read-only as designed.
- ✅ UI, org **archived**: `/organizations/21b4…` shows the archived banner + "Report history — Read-only: this organization is Archived. Reports can't be generated, scheduled or downloaded." with the definition and the completed run (0 rows); no generate/download controls.
- ❌ BUG, org **suspended**: `/organizations/21b4…` shows "Organization not found" and no history. `GET /orgs/organizations/21b4…` → 404 `{"error":"Organization not found"}` (expected), and the lifecycle fallback in `apps/web/src/components/organizations/record/OrganizationRecordPage.tsx:229-249` only renders `OrgReportHistory` when `useOrgStore().organizations` holds the org with status suspended/churned. That store is filled from `GET /orgs/organizations`, whose partner branch is limited to `accessibleOrgIds` (active|trial only, `apps/api/src/routes/orgs.ts` ~1590-1616) — the list returned `["Default Organization:active"]` after reload. So a suspended/churned org is never in the store and the history panel is unreachable; offboarding is not even in that condition. The API reach works, only the UI entry point is dead. Also no path to the suspended org from the Organizations list (readiness shows "0 suspended").
- ⚠️ Archived record overview shows error panels ("Summary counts are unavailable right now", "Couldn't check for open critical alerts", "Couldn't load recent activity", key dates "Not found") — `/summary` 404s for archived orgs.
- Org restored to `active` afterwards.

### Group C — Billing/Settings — 2026-09-24 22:00Z

#### #6814 — PARTIAL
- The stack has a **live** `RESEND_API_KEY` (`EMAIL_PROVIDER=auto`), so I did not send a quote (it would go out as real mail). Rendered the real server builder instead: `buildQuoteTemplate` from `apps/api/src/services/quoteEmail.ts`, run with tsx inside the api container.
  - Title "Office Wi-Fi Upgrade", customer "Default Organization": subject `Office Wi-Fi Upgrade — proposal from Default Partner`; body "Thank you for the opportunity to work with Default Organization. We've prepared Office Wi-Fi Upgrade (proposal Q-2026-0001) … $450.00 … valid until …".
  - Blank title and blank customer: subject `Proposal Q-2026-0001 from Default Partner`; body "…work with you. We've prepared proposal Q-2026-0001…". No leftover `{{…}}`.
- ✅ Settings ▸ Partner ▸ Email templates ▸ Quote / proposal: the default subject is `{{quote_title}} — proposal from {{partner_name}}` and the heading is `{{quote_title}}`. "Proposal title" and "Organization name" are in the Insert list, and the preview renders the new warmer copy.
- ✅ Created quote Q-2026-0001 "Office Wi-Fi Upgrade" in the UI (pricing table, 1 line at $450). The Send dialog opens. Cancelled without sending.
- ⚠️ Send dialog subject **placeholder** still reads `Proposal Q-2026-0001 from your company` (`billing.json` `quotes.actions.sendConfirm.subjectPlaceholder`, QuoteActions.tsx:1345). A blank subject actually sends `Office Wi-Fi Upgrade — proposal from Default Partner`, so the placeholder misstates the default.
- BLOCKED: real delivery / dev mail sink. There is no sink, and the transport is live.

#### #6718 — PASS (web surfaces; mobile + Outlook add-in not covered)
- ✅ Settings ▸ Billing ▸ Rates: added work type "Sweep Remote Support" (POST 201, toast "Work type saved."). Set Standard rates to $120/h (PUT `/billing-profiles/:id/save` 200, toast "Billing profile saved."). The matrix shows the work-type column "Uses All other work".
- ✅ Ticket T-2026-0001 ▸ Time & billing: the Work type picker lists the new type. Log time with it: POST `/time-entries` 201 with `workTypeId`, toast "Time logged", 1h 30m = $180.00.
- ✅ Invoices ▸ Export billables shows the new note "Includes the work type and the contract-included minutes for each time entry." The CSV header ends `…,"approved","work_type","included_minutes"`, and the row carries `"Sweep Remote Support","0"`.
- ⚠️ With the card's default range (To = today) the CSV had **no rows**. Today's entry only appears with `to=2026-09-25`. `listBillables` uses `lte(startedAt, to)` with `to` at midnight (`apps/api/src/services/timeEntryService.ts:1756`), and the card defaults To to `today()`. Invoice assemble with the same To date did include the entry. Likely pre-existing.

#### #6845 — PASS
- Set company details on Settings ▸ Partner ▸ Company (address, phone, website). Save returned 200 with the "Partner settings saved" toast. The billing letterhead fields were left blank.
- ✅ Settings ▸ Billing ▸ Documents: Phone and Website show the company values as placeholders with "Inherits from Company details". The address fields use the company address as placeholders with "Same as company details".
- ✅ The issued invoice (UI and PDF) FROM block shows `Default Partner / 100 Sweep Street / Springfield, IL, 62701 / US / +1 555 0100 / https://sweep.example.com`.
- ⚠️ The Company name field is blank with no inherited placeholder. The document still prints `partners.name` ("Default Partner", via `sellerSnapshot.ts:83` fallback). This is the only identity field that doesn't show its inherited value (settings rule 4).

#### #6865 — PASS
- ✅ `/settings/organizations/<Default Org>#billing`: when blank, the Payment terms (days) field shows placeholder `30` and "Inherits from Partner default". I set 14, which sent PATCH `/orgs/:id/billing-settings` 200 with `invoiceTermsDays:14` and the toast "Organization billing settings saved". After reload the field reads "Overrides Partner default (30)". Entering 400 shows "Enter a whole number of days from 0 to 365."
- ✅ The invoice issued while the override was 14 is due Oct 08 (issued Sep 24), on screen and in the PDF.
- ✅ Clearing the field saves `invoiceTermsDays:null` and goes back to "Inherits from Partner default" with placeholder 30. Left inheriting.

#### #6863 #6846 — PASS
- ✅ No 4xx/5xx on these pages: Billing ▸ Rates, all 7 Ticketing settings tabs, `/contracts`, `/contracts/new`. Saves work: rates (above), Ticketing ▸ Time capture merge gap (200, toast "Time tracking settings saved"), and a new contract "Sweep C managed services" (200, toast "Contract created").
- ✅ Assembled a draft from unbilled work (200, "Draft invoice assembled"), then Issue: POST `/invoices/:id/issue` 200 → INV-2026-0001 Issued. Download PDF returned 200 `application/pdf`, and the PDF renders the letterhead, bill-to, dates, line and totals. The DB shows `document_theme=classic`, `document_page_size=letter` frozen on the issued row.
- The API log for the session has no column/labour-pricing errors.
- ⚠️ The invoice line and PDF say `Ticket #QIMK3YNBYR: Sweep C billable ticket` (the internal `ticketNumber`), but the ticket UI calls it `T-2026-0001`.
- Env note, not a product bug: the PDF's "Pay online" link is `https://2breeze.app/portal/invoice/…` because the stack's api has `PUBLIC_APP_URL=https://2breeze.app`.

#### #6740 — PARTIAL
- ✅ Settings ▸ Ticketing ▸ Email shows a new "Google Workspace support mailbox" card (DWD copy, org picker, address, display name, Connect). Connecting with Default Organization returns POST `/tickets/mailbox/connect/gmail` 400 `{"error":"This organization has no active Google Workspace connection to read the mailbox with."}`, and that message shows as an error toast.
- ✅ Read-only credential-org name: I seeded a `provider='gmail'`, `status='connected'` row for Sweep B Org via psql. `GET /tickets/mailbox/connections` returns `orgName:"Sweep B Org"`. A **Partner Viewer** user (`sweepc-viewer@breeze.local`, created via psql; has `ticket_mailbox:read` but not admin) sees "sweepc-help@example.com / Connected / Credential: Sweep B Org's Google Workspace connection", with no Disconnect button and no connect form. I deleted the seeded row afterwards.
- BLOCKED: `/integrations#identity` ▸ Google Workspace only shows "integration is not enabled on this instance" because `GOOGLE_WORKSPACE_ENABLED` is unset on the stack, so the DWD scope list does not render. Checked in code instead: the page renders `GOOGLE_DWD_SCOPES_CSV` from `packages/shared/src/constants/googleDwdScopes.ts`, which includes `gmail.readonly` (l.32), `openid` (l.39) and `userinfo.email` (l.40).
- BLOCKED: the live DWD connect needs Google credentials.

Group B/C DB leftovers:
- Org "Sweep B Org" (`21b474ca-…`, active, 1 site) and its AR aging report + run.
- Reports "AR aging" (Default Org, group-by currency) and "Technician time & billability" (partner-wide), each with one run, plus a portal Executive summary run.
- User `sweepc-viewer@breeze.local` (Partner Viewer, same password as admin).
- Partner company details (address, phone, website).
- Work type "Sweep Remote Support", with Standard rates at $120/h.
- Ticket T-2026-0001 with a 90 min entry, now billed on **issued** invoice INV-2026-0001.
- Draft quote Q-2026-0001 and draft contract "Sweep C managed services".
- Ticketing time-capture merge gap changed 10 → 12.
- Default Org payment terms are back to inherit.

### Group D — Devices — 2026-09-24 22:55Z

#### #6816 — PASS
- ✅ With nothing saved (`localStorage` empty, no hash), `/devices` opens with **Agent** pressed. The segment shows All 2 / Agent 2 / Network 0 / Manual 0.
- ✅ Clicking All writes `#deviceClass=all` and saves `breeze.devices.deviceClass=all`. A fresh load of `/devices` with no hash reopens on All.
- ✅ The deep link `#deviceClass=network` selects Network and does not overwrite the saved `all`. The next plain load opens on All again. The saved value was reset to `agent` afterwards.

#### #6824 — PASS
- ✅ Device ▸ Details (`#details`) on E2E Windows shows a "Helper Version" row under Watchdog Version. With `helper_version` NULL it reads `—`.
- ✅ Seeded via psql: `helper_version='0.12.3'` on E2E Windows and `'0.9.0'` on E2E macOS. After reload the Details row reads `0.12.3`.
- ✅ `/devices` ▸ Columns has an opt-in "Helper Version" column that shows both values. Sorting is version-aware: ascending gives 0.9.0 then 0.12.3, and descending reverses it. A plain string sort would put 0.12.3 first.
- Seeded values left in place.
- Note: every device detail load logs a console 404 for `GET /api/v1/reliability/<id>` on these agentless seed devices (noise).

#### #6724 #6644 — PASS
- Setup via psql: org policy "Sweep D legacy policy" (`d0000000-…0001`, Default Org) with an `alert_rule` feature link holding two inline rules. "Sweep D high CPU" is cpu>85. "Sweep D CPU or offline" is cpu>95 plus offline 10 min. The policy is assigned to E2E Windows at device level.
- ✅ `/alerts/monitors` shows the banner "2 legacy rules across 1 policies have not been converted" with Review and Convert everything…. The Needs conversion tab (`#needs-conversion`) lists "Policies with legacy rules ▸ Sweep D legacy policy", which links to `/configuration-policies/<id>#monitors`. There the Needs conversion panel reads "1 device checked for identical behavior", shows both rules as Convertible with the proposed cpu and composite monitors, and offers "Convert all 2 convertible".
- ✅ Convert everything… opens an inline confirm. Confirming sent POST `/monitor-definitions/conversion/partner/convert-all` 200 `{"policies":1,"converted":2,"unconvertible":0}` with the toast "Converted 2 rows across 1 policies; 0 could not be converted." The DB rows are retired with `converted_to_monitor_id` set.
- ✅ Undo on "Sweep D high CPU" in Conversion history sent POST `/conversion/<id>/revert` 200 `{"success":true}` with the toast "Reverted 1 conversion." In the DB the monitor is gone, the rule is un-retired and `reverted_at` is set. After a reload that ledger row's Undo is disabled.
- ✅ `/admin/monitor-conversion` ("Legacy alert conversion") lists Default Partner with 1 pending row and 1 pending policy. Preview sent POST `…/preview` 200 (1 convertible). "Convert reviewed scope" sent POST `…/convert` 200 `{"converted":1}` with the toast "Conversion complete". The row then read 0/0 and "Converted 1 · 0 unconvertible".
- ✅ Device ▸ Monitoring (`#monitoring`) is an "Effective monitors" table (monitor, kind, source policy, state, open episode, escalation). With no policy it reads "No monitors apply to this device." After conversion it lists both monitors from the Sweep D policy. With an escalated, responses-paused `monitor_device_state` row seeded via psql, a Reset escalation button appears. Clicking it opens a confirm, then POST `…/reset` 200 with the toast "Escalation reset". The DB shows `escalated_at` NULL, `responses_paused` false and `reset_at` set.
- ✅ Monitor editor: a composite monitor shows "Child conditions", All/Any, per-child kind/operator/value/duration, Remove and Add condition. With an Execute Command response it shows "Restart service on the device", Maximum restart attempts (3) and Restart cooldown (300). Saving sent PATCH 200 with the toast "Monitor saved". I reset the response to `[]` via psql afterwards.
- ✅ Alert Templates: there is no nav entry. `/settings/alert-templates` and `/settings/alert-templates/<id>` both return **301 → `/alerts/monitors`**. No web source still links there.
- ✅ Recommended: the library shows a "Recommended" strip ("Attach the built-in monitors to a policy."). Choosing Sweep D legacy policy ▸ Attach sent PATCH to the monitors feature link 200 with the toast "Monitor attached". It kept the existing items and added all 8 built-ins, which now read "1 policy". The strip then disappears.
- ⚠️ The restart-service toggle is offered for any monitor kind, and the API accepts it on a composite cpu/offline monitor with an empty command. At runtime `executeCommandAction` (`automationRuntime.ts:1615`) records "succeeded" and does nothing, because only a `service` monitor's agent watch performs the restart. The result is a silent no-op response.
- ⚠️ Conversion history is unstyled raw markup. It shows a raw ISO timestamp, the converter's **user UUID** instead of a name, and output monitors as bare UUID links. A reverted row has no "Reverted" label: its Undo is only `disabled`, with no styling (`ConversionLedger.tsx`). The admin-sweep conversion is attributed to "System", not the admin who ran it.
- ⚠️ After Convert everything or Undo, the "Policies with legacy rules" list and the banner are not refreshed. The list still named the converted policy until reload, and after Undo it said "No policy carries…" until reload.
- ⚠️ In the Needs conversion view, the standalone-rules table has no heading, so "No legacy rules." sits directly under "Policies with legacy rules: Sweep D legacy policy" and reads as a contradiction. The banner also says "1 policies" (no plural handling).
- ⚠️ `/admin/monitor-conversion` is not in the Administration nav.
- ⚠️ The device Monitoring reset confirm passes the monitor name as `{{device}}` ("Reset the escalation for Sweep D high CPU?", `DeviceMonitoringTab.tsx:80`).
- ⚠️ The Recommended attach picker's opener button is labelled "Configuration policy". It reads as a field label, not an action.

#### #6766 — PASS
- Setup via psql: a second Default Org site "Sweep D Branch" and an approved switch `sweepd-switch` (192.168.50.2) in Default Site.
- ✅ Manual move: `/discovery` ▸ asset row opens the detail drawer ▸ Settings… (→ `/devices/network/<id>#overview/settings/identity`). Identity shows Site "Default Site" and **Change site…**. The modal lists "Default Site (current)" and "Sweep D Branch". Move device sent PATCH `/discovery/assets/<id>` `{"siteId":"…a1"}` 200 with the toast "Moved to Sweep D Branch.". DB `discovered_assets.site_id` = Sweep D Branch. After a reload the network device header and the `/devices#deviceClass=network` row both read Sweep D Branch.
- ✅ Profile move: seeded profile "Sweep D profile" (Default Site) with a completed job that discovered 2 assets. Edit profile ▸ Site → Sweep D Branch shows "This profile will scan from the new site from now on…" and an **unchecked** "Also move the 2 network devices this profile discovered to the new site". With it checked, Update Profile sent PATCH `/discovery/profiles/<id>` 200 with `assetMove {"candidates":2,"moved":2,…}` and the toast "Profile moved to Sweep D Branch. 2 network devices moved." In the DB the profile and both assets are in Sweep D Branch.

#### #6725 — PASS
- The stack has a live Anthropic key (`ANTHROPIC_API_KEY=sk-ant-…`), so the write path was not blocked. One design run cost 43¢.
- ✅ `/ai-agents/fleet-design` (sidebar AI ▸ Fleet Design) renders the org/site picker and the empty state "No Fleet Designer agent exists yet." Enable Fleet Designer showed the toast "Fleet Designer enabled." and created the partner-wide "Fleet Designer" agent.
- ✅ Start a Fleet Design sent POST `/ai/fleet-design/runs` 202 with the toast "Fleet Design run started.". The run completed in about 5 min (10 turns). The report lists functions, watches and rules. It proposes offline/CPU/memory/patch-compliance **monitor** rules and lists the two converted Sweep D monitors under "What is not carried forward".
- ✅ I selected the Shared workstation function and the "High CPU Usage" rule, then Review & Apply. The drawer read "CREATES … 0 watches, 1 rules". Confirm sent POST `…/apply` 200 `{"applied":["functions:shared_workstation","policy:shared_workstation","monitoring:shared_workstation:rule:1"],"rollbackAvailable":true}`, and the drawer read "Applied 3 item(s).". DB: new org `monitor_definitions` "High CPU Usage" (cpu > 90 for 15 min), and policy "Fleet Design: Shared workstation" with a **`monitors`** feature link `{"items":[{monitorId…}],"inheritance":"cumulative"}`, assigned to a device group. It wrote no `alert_rule` or `monitoring` link.
- ⚠️ POST `/ai/fleet-design/designer/enable` returned 200 `{"data":{"status":"missing","agentId":null,"canEnable":true}}` even though it had just created the agent. `designerSetup.ts:160-161` re-reads right after `createAgent` and gets a stale result. The UI refetches, so it looks fine, but the audit row records `status: missing` with `resourceId` = the orgId, not the agent.
- ⚠️ The apply drawer says "Shared workstation — 2 devices · **100%** confidence" while the report says 62% confidence. It also says "1 rules".
- Note: a mid-test `/auth/refresh` 401 burst logged the browser out (the console shows "[vite] server connection lost" first). Logging back in recovered the session. It looks environmental, not caused by this row.

#### #6893 #6894 #6898 #6882 — PASS
- ✅ Device ▸ Hardware (`#hardware`) with no data: the "Storage & RAID" section reads "No hardware health report received yet. Installation guidance". GET `/devices/<id>/hardware-health` returns 404 `{"error":"no_hardware_health"}`, which is by design but logs a console error.
- Seeded via psql on E2E Windows, using the W01 tables `device_hardware_health`, `device_hardware_components` and `device_hardware_events`: a perccli controller "PERC H740P Mini" (Optimal); VD0 RAID1 **Degraded**; Disk 32:0 Online and Disk 32:1 **Failed**/predictive; a smartctl OS disk `/dev/nvme0` with **NULL size_bytes**; an ipmi **BMC** "iDRAC9" (Dell, 10.20.30.40); the rollup `critical`; 3 ok sources; 2 events.
- ✅ With data, the section shows a Critical pill and "Collected now". The controller card has model/serial/firmware. There is a Virtual disks table (VD0 RAID1 893.8 GiB, Degraded pill), a Physical disks table (Online / Failed, 31 °C), "OS-visible disks" with `/dev/nvme0` size **"—"** (unknown, as #6898 intends, not `0 GiB`), a **Management controller** card (Vendor Dell, Firmware 6.10.30.00, IP, MAC, "Last reported …; collected daily."), a Sources footer (perccli/smartctl/ipmi Healthy with versions) and "Hardware events (2)". No horizontal overflow at 1280px.
- Note: the brief said "Unknown". The shipped behaviour and #6898's own test use `—` for an unknown size.
- ✅ `/devices`: the "Hardware" column is opt-in under Columns. E2E Windows shows a Critical pill, and macOS shows `—`. The quick filter select "All hardware health / Warning / Critical / Unknown" sends `hardwareHealth=critical|warning|unknown` to `/devices`. Critical shows only E2E Windows, and Warning and Unknown show no rows.
- Seeded hardware rows left in place.

#### #6610 — PARTIAL (live provider BLOCKED)
- ✅ `curl /settings/integrations/backup` returns **301 → `/integrations#backup`**, and the browser lands on the Backup tab: "Backup providers … Cove Data Protection is supported today", Add connection, and "No backup provider is connected yet."
- ✅ Add connection with bogus Cove creds (partner `sweepd-bogus-partner`) ▸ Save sent POST `/backup/providers/connections` **422** `{"success":false,"error":"Cove login was rejected: Unknown partner/username or bad password","reauth":true}`. That error shows as the toast, and no row is created. Save validates live against Cove, so this made one real call to `api.backup.management`.
- Because a connection cannot be created without valid creds, I seeded one via psql: `backup_provider_connections` "Sweep D Cove (seeded)" with the same bogus creds, encrypted with the api's own `encryptProviderCredentials` via tsx in the container. I also seeded a customer mapped to Default Org and 2 `backup_provider_devices`: `e2e-windows` failed with 4 errors, linked to E2E Windows, and `orphan-laptop` completed and unlinked.
- ✅ The connection card shows Last sync, Customers 1 / Devices 2 / Linked 1 / Unmapped 1 / Ambiguous 0, the portal-name toggle, Test connection / Sync now / Save / Delete, and a Customer mapping table (the org select pre-selects Default Organization).
- ✅ **Test connection** sent POST `…/connections/<id>/test` 200 `{"success":false,"error":"Cove login was rejected…","reauth":true}`. That surfaced as an **error toast**, because runAction treats a 200 `success:false` as a failure. The DB `status` is now `reauth_required`. After a reload the card shows "Credentials rejected · Re-enter credentials · Last sync failed · <error>".
- ✅ `/backup` ▸ Backup health overview: "1 of 2 endpoints have a recent backup · 1 provider-only endpoints · 0 Microsoft 365 accounts", status and last-success buckets, health/source filters ("Breeze", "Cove Data Protection"), and a device table mixing Breeze and Cove rows with selected/used sizes and errors. Attention Needed lists "e2e-windows: external backup needs attention".
- ✅ Device ▸ Backup (`#backup`) on E2E Windows shows the **External backup** card: e2e-windows, failed, last success/session, provider customer, Selected 112 GB, Used 74.5 GB, Errors 4, data sources, and Unlink.
- BLOCKED: a live Cove sync needs real Cove credentials.
- ⚠️ After Test connection fails, the card still reads "Active" with no reauth banner until a page reload. The "Last sync failed" line then shows the test error although `last_sync_status` is `success`.
- ⚠️ Raw enum values are shown to users: status `failed`/`completed` and data sources `files_folders, system_state` on the device card and in the health table.
- ⚠️ A device linked to a Cove device appears twice in the health table (the Breeze row and the Cove row) and is counted in both status buckets (4 rows for 3 endpoints).
- ⚠️ The Add-connection form's password field was pre-filled with the Breeze login password (`BreezeAdmin123!`, most likely browser autofill). None of the fields set `autocomplete` (`new-password`/`off`), so a saved Breeze login can end up in the Cove connection. The "Connection name" placeholder is a real MSP name, "OliveTech Cove".
- Seeded connection, customer and devices left in place.

#### #6683 #6760 — PARTIAL (live agent BLOCKED)
- ✅ #6683: POST `/remote/sessions` for a non-existent device returns 404 `{"error":"Device not found or access denied","code":"NOT_FOUND"}`, so the code is now present. The offline E2E Windows returns 400 `{"error":"Device is not online","deviceStatus":"offline"}` with no code, which the PR deliberately left out. In the UI, Connect Desktop is disabled on an offline device ("Device is offline"), so no session error surface is reachable without an agent. `errors.json` in all 8 locales has `AGENT_EXECUTION_FAILED` and `DEVICE_UNREACHABLE`. The 6 non-en/pt-BR locales carry English placeholders, as the PR states.
- ⚠️ Because runAction swaps in `errors:<code>` when a translation exists (`runAction.ts:121`), coded remote errors now show the generic catalog text instead of the more specific server prose: "Device not found or access denied" becomes "Not found", and "Organization context required" or "Access to this site denied" becomes "Access denied". `NOT_FOUND` is also returned with status **403** at two sites (`remote/sessions.ts`, per the PR diff).
- ✅ #6760: `/devices` ▸ Add ▸ Install agent… ▸ CLI Commands ▸ Windows. The one-liner wraps the download in `try{Invoke-WebRequest …}catch{if("$($_.Exception)" -match 'remote certificate is invalid|establish trust relationship for the SSL/TLS'){throw "Breeze: this machine rejected the TLS certificate of <server> (…). If the server uses a self-signed or private-CA certificate, import its root CA into Cert:\LocalMachine\Root; … See https://docs.breezermm.com/deploy/tls/#trusting-the-internal-ca-on-agents"}; throw}`. The docs page returns 200 and has `id="trusting-the-internal-ca-on-agents"`.
- BLOCKED: an actual self-signed install failure (agent/MSI/PowerShell against an untrusted cert) and a live remote-session failure both need a Windows endpoint and a live agent.
- ⚠️ At 1280×900 the Add New Device modal with the Windows CLI command is taller than the viewport, and its **Done button sits at y≈951–991 where it cannot be reached** (no scroll container). `Dialog` without `alignTop` uses `items-center` with no `overflow-y-auto` (`shared/Dialog.tsx:252-253`, `AddDeviceModal.tsx:703`). Esc still closes it.
- Note: opening the CLI Commands tab mints an onboarding token right away (POST `/devices/onboarding-token`), so each view of the tab leaves a 50-use enrollment token behind.

Group D DB leftovers:
- `devices.helper_version` is 0.12.3 on E2E Windows and 0.9.0 on E2E macOS.
- Hardware health rows on E2E Windows: `device_hardware_health`, 6 components and 2 events.
- Config policy "Sweep D legacy policy" (`d0000000-…0001`) assigned to E2E Windows. It holds 2 inline alert rules, both converted, and a `monitors` link with both converted monitors plus the 8 built-ins attached through Recommended. Its `monitor_device_state` row is reset.
- Fleet Designer agent (partner-wide, enabled), one design run and report, and an applied design: monitor "High CPU Usage" plus policy "Fleet Design: Shared workstation" on a device group, with rollback available.
- Site "Sweep D Branch" in Default Org. Discovery profile "Sweep D profile" with one job, and 3 network assets (`sweepd-switch`, `sweepd-printer`, `sweepd-ap`), all now in Sweep D Branch.
- Cove connection "Sweep D Cove (seeded)" (status `reauth_required`), 1 mapped customer and 2 provider devices, one linked to E2E Windows.
- One onboarding token from the CLI tab.
- Browser `localStorage`: `breeze.devices.deviceClass=agent`; Helper Version and Hardware columns enabled in `breeze.devices.columns`.

### Group E — Alerts/AI — 2026-09-24 22:35Z

#### #6825 #6538 #6828 — PASS
- ✅ Settings ▸ Partner `#ai-features` ("AI Features ▸ Anomaly detection defaults") has two self-saving checkboxes, "Detect anomalies" and "Create alerts from anomalies". Each click sent PATCH `/orgs/partners/me` 200 with the toast "Saved". Both read checked after a reload. DB `partners.settings.ml` = `{"anomalies":{"enabled":true,"create_alerts":true}}`.
- ✅ Org `#ai` (`/settings/organizations/<id>#ai`) has an Inherit/On/Off select per switch. The inherit option reads "**Inherit (on, partner setting)**", so it names both the value and its source. Setting Off saved with the toast "Saved" and wrote `ml.anomalies.enabled=false`. After a reload the select still reads Off. Going back to Inherit **deletes the key** (`{"anomalies":{}}`) rather than copying the partner value.
- ✅ With the org override Off, Device ▸ Anomalies reads "Anomaly detection disabled".
- Seeded via psql on E2E Windows, using the W01 `metric_anomaly_episodes` table: 3 episodes (`e0000000-…0001` cpu spike open, `…0002` memory_growth open, `…0003` disk spike resolved/cleared). I also seeded one member `metric_anomalies` row each for 0001 and 0002.
- ✅ With the org back on Inherit, Device ▸ Anomalies (`#anomalies`, tab badge 2) renders 2 episode cards. For example: "spike · CPU has been 97.5% for 45 m, normally 22.0%. · Ongoing since … · 9 detections". Each card has Dismiss for 7 days, Resolve and Promote to alert.
- ✅ Resolve showed the toast "Anomaly resolved". Dismiss showed the toast "Dismissed for 7 days". The list then reads "No open anomalies … Show recently closed". In the DB, 0001 is `resolved` and 0002 is `dismissed` with snoozed_until set, both with close_reason `user` and resolved_by set. The member rows cascaded to resolved/dismissed.
- ✅ #6828 feedback: `ml_feedback_events` has exactly one `anomaly_episode.resolved` row and one `anomaly_episode.dismissed` row (`source_id` = episode id, `memberCount:1`), plus the per-member `anomaly.resolved` and `anomaly.dismissed` rows.
- ✅ The Recently closed filter lists all 3 episodes with their close reason ("resolved", "dismissed", "cleared"). The dismissed card shows "Dismissed · snoozed until Oct 1" and a Stop snoozing button.
- ⚠️ When the org has anomaly detection Off, the device tab badge still reads "Anomalies 2" but the panel only says "Anomaly detection disabled". It does not say where detection was turned off or link to the setting.
- Note: the growth sentence reads "Memory grew from 91.0% to 91.0%" because my seed has only one member row for a 30-bucket episode. This is a seed artifact, not a bug.

#### #6829 — PASS
- ✅ Partner Settings ▸ "AI Approval Timeout" (`#ai-approvals`) is a select: "Product default (5 minutes)", then 5, 10, 15, 20, 30, 45 and 60 minutes, with the hint "Choose 5-60 minutes." The tab also has an org-override note and a "not for unattended work" note. Choosing 30 and clicking Save Settings showed the toast "Partner settings saved" and wrote `partners.settings.aiApprovals = {"interactiveTimeoutMinutes":30}`. The `ml` block was preserved.
- ✅ API clamp: PATCH `/orgs/partners/me` with 90 returned 400 `settings.aiApprovals.interactiveTimeoutMinutes: Too big: expected number to be <=60`. With 2 it returned 400 `…Too small: expected number to be >=5`.
- ✅ Org `#ai` ▸ AI Approval Timeout card: the inherit option reads "**Inherit (30 minutes, partner default)**". Choosing 10 and clicking Save showed the toast "Settings saved" and wrote org `aiApprovals.interactiveTimeoutMinutes=10`. After a reload it reads 10, and the ML override set just before was not clobbered.
- Not exercised: an actual approval wait with the new timeout in a live chat turn. That would spend Anthropic tokens and hold a turn open for 10 min.

#### #6852 — PASS
- Where it lives: `/settings/ai-usage` shows the **read-only** "Effective budget" view. The editable field is on Org Settings ▸ AI (`OrgAiBudgetSettings`). The partner-level field (`#ai-budgets`) only appears after "Enable AI budget enforcement" is turned on. I did not turn it on, so the org fields stay unlocked.
- ✅ Before any change, `/settings/ai-usage` lists "Per-tool rate limit multiplier · 1 · Default".
- ✅ The org AI tab field "Per-tool rate limit multiplier" is a number input (min 1, max 10, placeholder "Default (1)") with the help text "Raises every per-tool AI and MCP rate limit by this factor (1–10)…". Entering 15 and clicking Save sent PUT `/ai/budget` **400**, with the error toast "toolRateLimitMultiplier: Too big: expected number to be <=10 / Check the highlighted fields" and the same line inline. Entering 3 and saving showed the toast "Saved successfully", and the value stayed after refetch.
- ✅ `/settings/ai-usage` now reads "3 · Set by organization". GET `/ai/tool-rate-limits?orgId=…` returns 200 `multiplier:3` (execute_command 10 → 30, run_script 5 → 15). `/ai-risk` ▸ Rate Limits says "Limits include a per-tool rate limit multiplier of 3×…" and shows "30 req (default 10)" per tool.
- ⚠️ The 400 message shows the raw API key `toolRateLimitMultiplier:` rather than the field label. "Check the highlighted fields" is also misleading, because the input gets no invalid styling (no `aria-invalid`, no destructive border).
- ⚠️ `/ai-risk` ▸ Rate Limits puts 35 of 79 tools in "Other" with tier "—" and permission "—".
- The org multiplier is left at 3.

#### #6887 — PASS
- There is no separate "rule builder" any more. Conditions are authored as monitor kinds on `/alerts/monitors`.
- ✅ `/alerts/monitors` lists the 4 new built-ins (built-ins v3), all of kind "Hardware health": RAID array degraded or failed (Critical), Physical disk failed or predicted to fail (High), Controller cache battery problem (Medium) and Hardware monitoring tool failing (Low).
- ✅ New monitor ▸ Kind offers **Hardware health** (`hardware_health`). It renders a Components multiselect (Controllers, Virtual disks, Physical disks, Cache batteries, Enclosures, Monitoring tools; defaults are virtual and physical disks), Minimum health (Warning/Critical, default Critical), "Include predictive failure" (on) and Consecutive snapshots (2, 1–10). I saved "Sweep E RAID health" with Controllers added. The toast read "Monitor saved" and the DB `condition` = `{"minHealth":"critical","componentTypes":["controller","virtual_disk","physical_disk"],"consecutiveSnapshots":2,"includePredictiveFailure":true}`.
- ✅ End-to-end alert: this reuses the Group D hardware rows on E2E Windows, which already carries all 8 built-ins through "Sweep D legacy policy". The seeded components had 0 streaks and a `last_seen_at` more than 30 min old, so nothing could breach. Via psql I set Disk 32:1's `critical_streak`/`predictive_streak` to 2, refreshed `last_seen_at`, and marked the device online with a fresh `last_seen_at` for one evaluate-all cycle. Within 60 s the sweep raised the High alert "Physical disk Disk 32:1 (MZ7LH960HAJR S45NNA0SWEEP2) Failed on E2E Windows Test Device" with **`subject_key = perccli:c0:e32:s1`**. It appears on `/alerts`. Its detail page shows the message "…is Failed (Drive failed)", Alert Rule, an Open monitor link, and component context (componentType, controller, predictiveFailure). The device is back to offline.
- ❌ **Pre-existing, not from this range:** the same sweep also raised the built-in "Low patch compliance" alert, and its message is **raw template text**: "Low patch compliance: patch compliance {{actualValue}}% ({{operator}} {{threshold}}%)". This shows on `/alerts` and is stored in `alerts.message`. `evaluateConditions` (`services/alertConditions/index.ts:~232-238`) only fills `actualValue`/`threshold`/`operator` into the context for *threshold* conditions. The patch_compliance handler's `actualValue` (`handlers/patchCompliance.ts:32`) never reaches `templateContext`, but `services/monitors/kinds/patchCompliance.ts:15` uses all three. This is unchanged since v0.116.0 (the `git diff v0.116.0` of alertConditions shows no change there), but every patch-compliance alert is affected. The alert's `conditionsMet` does hold the real text: "Patch compliance score < 80% (actual: 50%)".

#### #6838 #6763 — PARTIAL (live agent BLOCKED)
- ✅ #6838 mapping UI: `/scripts/new` ▸ Exit Code Severity. The help text now reads "…When an unattended run (scheduled, automation, policy or alert-triggered) returns one of these codes, an alert is raised…; manual runs never raise one. A later run that exits cleanly resolves it." The severity options are Critical, High, Medium, Low, Info and Suppress alert.
  - I created the partner-wide script "Sweep E exit-code test" (`78a201a7-…`, Windows) with the mapping 2 → High. The toast read "Script created" and DB `exit_code_severity_mapping` = `{"2":"high"}`.
  - On re-open the row reloads with the badge "1". I added 3 → Suppress alert and clicked Save Changes. The toast read "Script saved", DB = `{"2":"high","3":null}`, and after a reload 3 shows Suppress alert.
- ✅ #6763 test runner, everything up to dispatch: Test run lists "e2e-windows.local (offline)". Clicking Test Run showed **Queued** with Run context System, View on device and View history. It created the `script_executions` row `a0722056-…` (manual, queued).
- ✅ Completion, with the agent simulated: via psql I set that execution to `completed`, `exit_code 2`, stdout NULL, `custom_field_result = {"applied":["asset_tag"],"rejected":[{"key":"ram_slot_tpe","reason":"unknown_field"},{"key":"owner","reason":"not_script_writable"}]}`. The running poll picked it up. The panel shows "Completed · exit 2" and a **Custom field writes** section above stdout. It reads "Applied: asset_tag". Under "Rejected": "ram_slot_tpe — No custom field with this key — check the spelling…" and "owner — Field is not script-writable — turn on "Allow scripts to write this field" in Settings → Custom Fields". Stdout shows "(empty)".
- BLOCKED: a real agent run needs a live agent. That covers the marker parsing, the ingest-side stdout=null fallback, and the actual alert from an unattended (scheduled/automation) run exiting 2, including its dedupe and auto-resolve.
- ⚠️ After the run completes, the test runner still shows "The selected device is offline — the run will wait until it reconnects." Stdout reads "(empty)" and "No output", the same thing twice.
- Note: Playwright could not click Add exit code or the Monaco editor directly because a neighbouring element intercepted the pointer. An `elementFromPoint` hit test with the button scrolled to centre lands on the button, so a mouse user is not affected.

#### #6914 — PASS
- Seeded via psql, reusing the Group D Fleet Designer run `a92fdc0e-…`:
  - a pending four_eyes `action_intents` row `e2000000-…0001` (source `ai_agent`, manage_alerts resolve, target e2e-macos.local) and its `approval_requests` row for admin. The inbox reads `approval_requests`, not intents, so an intent with no request row shows "That approval is no longer pending".
  - four `user_notifications`, each using the link the new producers write: approval `/approvals#intent-<id>`; ai `/ai-agents/runs/<runId>`; ai `/ai-agents/runs#agent=<agentId>`; and one approval with a **NULL link** and `metadata.intentId` to exercise the `buildHref` fallback.
- ✅ From the bell ("Notifications, 6 unread"):
  - "approval requested" opens `/approvals#intent-e2000000-…`, and **that card is ringed** (`ring-2 ring-primary`, `id="intent-…"`), "Resolve alert "E2E fixture: high CPU" · Proposed by Fleet Designer (Sweep E) (AI agent)".
  - "agent action outcome" opens `/ai-agents/runs/a92fdc0e-…`, the Fleet Designer run page. That page's "Review in Approvals" link is now `/approvals#intent-<id>` rather than bare `/approvals`.
  - "circuit breaker opened" opens `/ai-agents/runs#agent=5dca37d9-…`, with the Agent filter preselected to Fleet Designer.
  - The null-link notification falls back to `/approvals#intent-<id>` with the same card ringed.
- ✅ Real producer: Deny ▸ Confirm denial on that card showed the toast "Action denied" and set the intent to `rejected`. The release worker then wrote the **real** notification "Agent proposal denied", link **`/ai-agents/runs/a92fdc0e-…`**, which is the agent-originated outcome path from the PR's table.

#### Everyday pass (Group E pages)
- `/alerts`: 4 active (1 Critical, 1 High, 2 Medium), with row actions and grouped-incident chips. No layout overflow at 1280px. The patch-compliance message shows raw `{{…}}` placeholders (see #6887).
- `/ai-agents` is **404**, but nothing links to it: the sidebar uses `/settings/ai-agents`, `/ai-agents/runs`, `/ai-agents/impact` and `/ai-agents/fleet-design`. `/settings/ai-agents` lists Fleet Designer (Act · Running · Completed). `/ai-agents/runs` works.
- AI chat panel: one message, "How many active alerts are there right now?", cost about 22¢ ($0.43 → $0.65 this month) and correctly answered "4 active alerts".
- Console: only self-inflicted errors (400s from the clamp probes, one 422 from Group D's Cove test) and known Group D noise (`/reliability` 404).
- ⚠️ While streaming, the chat panel did not render the **user's own message**. The transcript started with an empty assistant avatar, and both tool rows were labelled "**Updated alerts**" for a read-only lookup. After reopening the conversation from history, it shows the user message and "Checked alerts / Updated alerts". This was seen once (sent with Cmd+Enter). The DB has the user row.
- ⚠️ `/settings/ai-agents` reads "Partner-wide: applies to every organization…" and then "No partner-wide baseline exists yet for this kind…", directly above a partner-wide ("All orgs") Fleet Designer agent. That reads as a contradiction.
- ⚠️ `/settings/ai-usage` shows "80% monthly alert sent 9/24/2026" while the effective Monthly Budget is "No limit · Default".

Group E DB leftovers:
- Partner `settings.ml.anomalies` = `{enabled:true, create_alerts:true}` and partner `aiApprovals.interactiveTimeoutMinutes=30`.
- Default Org: `aiApprovals.interactiveTimeoutMinutes=10`, `ml.anomalies={}` (inherit), and AI budget `toolRateLimitMultiplier=3`.
- 3 `metric_anomaly_episodes` (`e0000000-…0001` resolved, `…0002` dismissed/snoozed to Oct 1, `…0003` resolved), 2 member `metric_anomalies`, and 4 `ml_feedback_events` on E2E Windows.
- Monitor "Sweep E RAID health" (org, hardware_health, not attached to any policy).
- Group D hardware component Disk 32:1 now has streaks of 2, and all 6 components have a fresh `last_seen_at`. There are 2 new active alerts on E2E Windows ("Physical disk Disk 32:1 … Failed" and "Patch Compliance…"). The device is back to `offline`.
- Partner-wide script "Sweep E exit-code test" (`78a201a7-…`, mapping `{"2":"high","3":null}`) and one simulated completed manual execution `a0722056-…`.
- Action intent `e2000000-…0001` (now `rejected`) and its approval request; 4 seeded "Sweep E:" notifications plus the real "Agent proposal denied" notification.
- One AI chat session, about 22¢.

## UI/UX paper cuts

| # | Where | Observation | Severity | Disposition (fixed <sha> / issue # / noted) |
|---|---|---|---|---|
| A1 | Integrations ▸ Webhooks | Create / Save Changes / Active-Disabled toggle give no success toast (runAction without successMessage, WebhooksPage.tsx:185 toggle, :276 create/save); the list update is the only signal | low | noted |
| A2 | Webhooks delete, API key revoke | On a 404 (row already gone) the confirm modal stays open and the stale row stays listed; the user must Cancel and refresh. A 404 on delete could close and refetch | low | noted |
| A3 | `/alerts`, `/organizations` | List endpoint fetched twice per page load (same URL, same ms), in every locale | low | noted |
| A4 | `/alerts` (de-DE) | Tab title "Warnmeldungen" vs h1 "Warnungen" | low | noted |
| A5 | `/settings/partner-service-principals` | Principal row doesn't list granted scopes (e.g. whether `alerts:read` is on) | low | noted |
| B1 | Report edit (business types) | Generic builder sections (Report type, Data source, Columns, chart) render under the business options but are never saved | medium | noted |
| B2 | `/reports/templates` console | i18n missing-key warnings for server-synced template uuids | low | noted |
| B3 | Archived org record | Overview shows several error panels (summary 404, alerts, activity, key dates) instead of a read-only empty state | low | noted |
| C1 | Quote ▸ Send dialog | Subject placeholder "Proposal Q-… from your company" no longer matches the real default subject "<title> — proposal from <partner>" (QuoteActions.tsx:1345, `quotes.actions.sendConfirm.subjectPlaceholder`) | low | noted |
| C2 | Invoices ▸ Export billables | Default To = today drops today's entries (`lte(startedAt, to@00:00)`, timeEntryService.ts:1756); assemble with the same date includes them | medium | noted |
| C3 | Settings ▸ Billing ▸ Documents | Company name shows no inherited value although documents fall back to the partner name | low | noted |
| C4 | Invoice line / PDF | Line reads "Ticket #QIMK3YNBYR" (internal ticketNumber) while the ticket UI shows T-2026-0001 | low | noted |
| D1 | Monitor editor ▸ Execute Command | "Restart service on the device" is offered and saved on any monitor kind (e.g. composite cpu/offline) with an empty command; runtime records it "succeeded" and does nothing (`automationRuntime.ts:1615`) | medium | fixed 783b4f653e |
| D2 | `/alerts/monitors` Conversion history | Unstyled markup, raw ISO timestamp, converter shown as a user UUID, outputs as bare monitor-UUID links; a reverted row just has a disabled, unstyled Undo with no "Reverted" label; the admin sweep is attributed to "System" (`ConversionLedger.tsx`) | medium | noted |
| D3 | `/alerts/monitors` Needs conversion | After Convert everything or Undo, the "Policies with legacy rules" list and the banner are stale until reload; the headingless standalone table's "No legacy rules." reads as contradicting the policy list; "1 policies" | low | noted |
| D4 | `/admin/monitor-conversion` | Not in the Administration nav | low | noted |
| D5 | Device ▸ Monitoring reset confirm | `{{device}}` is filled with the monitor name ("Reset the escalation for Sweep D high CPU?") (`DeviceMonitoringTab.tsx:80`) | low | noted |
| D6 | Monitors ▸ Recommended | The opener button for the attach picker is labelled "Configuration policy" | low | noted |
| D7 | Fleet Design enable | POST `/designer/enable` returns `{"status":"missing","agentId":null}` after creating the agent, and the audit row records `status: missing` against the orgId (`designerSetup.ts:160-161`) | low | noted |
| D8 | Fleet Design apply drawer | Shows 100% confidence for a function the report rates at 62%; "1 rules" | low | noted |
| D9 | Integrations ▸ Backup connection card | After a failed Test connection, the card keeps "Active" until reload; then "Last sync failed" shows the test error while `last_sync_status=success` | low | noted |
| D10 | Backup device card / health table | Raw enum values shown (`failed`, `completed`, `files_folders, system_state`) | low | noted |
| D11 | `/backup` health table | A Breeze device linked to a Cove device is listed and counted twice (Breeze row and provider row) | low | noted |
| D12 | Backup ▸ Add connection | No `autocomplete` on the Cove username/password fields, and the password field was pre-filled with the Breeze login password (likely browser autofill); the name placeholder is a real MSP name, "OliveTech Cove" | low | fixed 01d61f49ab (add form; edit-credentials card still open) |
| D13 | Remote session errors (runAction) | Coded errors replace specific prose with generic catalog text ("Device not found or access denied" becomes "Not found"); `NOT_FOUND` is sent with status 403 at two sites | low | noted |
| D14 | Add New Device modal (CLI ▸ Windows) | At 1280×900 the Done button is off-screen and cannot be scrolled to; `Dialog` has no overflow when not `alignTop` (`shared/Dialog.tsx:252`) | medium | fixed 411d416032 |
| D15 | Device detail (agentless) | Console 404s for `/reliability/<id>` and `/hardware-health` on devices without data; these are expected empty states but log as errors | low | noted |
| E1 | Device ▸ Anomalies (org detection Off) | Tab badge still counts open episodes ("Anomalies 2") while the panel only says "Anomaly detection disabled", with no pointer to where it was turned off | low | noted |
| E2 | Org ▸ AI ▸ AI budget, tool multiplier | Out-of-range 400 shown as raw `toolRateLimitMultiplier: Too big…` plus "Check the highlighted fields", but the input gets no invalid styling | low | noted |
| E3 | `/ai-risk` ▸ Rate Limits | 35 of 79 tools sit in "Other" with tier and permission "—" | low | noted |
| E4 | Alerts (built-in Low patch compliance) | Pre-existing: message stored and shown as raw `patch compliance {{actualValue}}% ({{operator}} {{threshold}}%)`, because only threshold conditions populate actualValue/operator/threshold in the template context (`services/alertConditions/index.ts:~232`, template `services/monitors/kinds/patchCompliance.ts:15`) | medium | issue #6932 |
| E5 | Script ▸ Test run | After completion it still shows "The selected device is offline — the run will wait until it reconnects."; stdout shows "(empty)" and "No output" | low | noted |
| E6 | AI chat panel (live stream) | User's message not rendered during the live turn; a read-only alert lookup is labelled "Updated alerts" twice; history reload shows it correctly ("Checked alerts") | low | noted |
| E7 | `/settings/ai-agents` | "No partner-wide baseline exists yet for this kind" shown directly above a partner-wide ("All orgs") Fleet Designer | low | noted |
| E8 | `/settings/ai-usage` | "80% monthly alert sent" badge while the effective monthly budget is "No limit" | low | noted |
| F1 | Report edit (business types), Live preview | The right-hand Live preview is still the generic builder preview ("Compliance · Weekly · Monday", "Data source: Policy set: CIS Benchmarks", a device table), and a `one_time` report shows no selected schedule (`ReportBuilder.tsx:~2405`, not gated on `businessType`) | low | noted (follow-up pass) |
| F2 | Monitor editor, restart-service block | Save is blocked, but the only message is the generic "Some response settings are missing or out of range"; the specific "Restart service requires a service name" (MonitorEditor.tsx:149, hard-coded English) is never shown | low | noted (follow-up pass) |
| F3 | `/backup` health table | TYPE column prints the raw `osType` enum (`workstation`, `unknown`) (`BackupHealthDeviceTable.tsx:90`) | low | noted (follow-up pass) |
| F4 | Invoices ▸ Export billables | A date-only To is the end of the UTC day, while the card's "today" is the browser's local date, so a US partner's evening entries from today are still left out | low | noted (follow-up pass) |
| F5 | `/alerts/monitors` conversion banner | "1 legacy rules across 1 policies" (no plural forms for `conversion.banner.text` / `confirmBody`) (the D3 remainder) | low | noted (follow-up pass) |
| F6 | Org record, suspended / archived | Suspended org header shows the raw org UUID as its name; the archived record still issues `GET …/summary` → 404 (`OrganizationRecordPage.tsx:142`) | low | noted (follow-up pass) |
| F7 | Backup ▸ Add connection | Name placeholder is still the real MSP name "OliveTech Cove" (the D12 remainder) | low | noted (follow-up pass) |
| F8 | Org AI budget, multiplier 400 | Toast still leads with the raw key `toolRateLimitMultiplier:`; the inline text is zod's default wording (the E2 remainder) | low | noted (follow-up pass) |
| F9 | Invoice detail (issued) | The default Detail view shows no ticket reference; the frozen `Ticket #T-…` label appears only under Preview/PDF/portal | low | noted (follow-up pass) |

## Fixes applied

| Commit | PR/area | What | Test |
|---|---|---|---|
| e868b98c98 | #6850 | Suspended/churned org record page probes `GET /reports?limit=1` when the record 404s and the store has no row, so read-only report history is reachable on a fresh load (was only reachable if the org list had cached it; partner list excludes suspended/churned) | `OrganizationRecordPage.test.tsx` 27/27, new case red→green |
| 783b4f653e | D1 | Monitor editor blocks Save on a Restart-service response when the monitor has no service name | `MonitorEditor.test.tsx` 33/33, red→green |
| 01d61f49ab | D12 | Cove add-connection form: `autoComplete` off / new-password | `BackupProvidersIntegration.test.tsx` 11/11, red→green |
| 411d416032 | D14 | Add New Device modal uses Dialog `alignTop` (scrollable) so Done is reachable at 1280×900 | `AddDeviceModal.test.tsx` 32/32, red→green |

## Issues filed

| Issue | Title | From row |
|---|---|---|
| #6932 | Patch-compliance alert messages show raw placeholders (pre-existing) | E4 |

## Summary

| Group | PASS | PARTIAL | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|
| A Admin/System | 7 | 0 | 0 | 0 | 0 |
| B Reports | 0 | 1 | 0 | 0 | 0 |
| C Billing/Settings | 4 | 2 | 0 | 0 | 0 |
| D Devices | 6 | 2 | 0 | 0 | 0 |
| E Alerts/AI | 5 | 1 | 0 | 0 | 0 |
| **Total (28 rows)** | **22** | **6** | **0** | **0** | 73 non-UI PRs |

**Top findings:**
- One real defect in range: #6850's suspended/churned-org report history was unreachable on a fresh load (its test seeded a store the real API never fills). Fixed e868b98c98. Lesson: tests that seed client state must mirror what the API actually returns.
- No FAIL elsewhere; every PARTIAL is an external dependency (live agent, Google OAuth, Cove creds, real mail send) or an unexercised client (mobile/Outlook add-in).
- Pre-existing medium: patch-compliance alert text shows raw placeholders (#6932). Pre-existing medium paper cuts noted: B1 business-report edit shows unsaved builder sections; C2 billables export default To=today drops today's entries.

**Oldest PR reached:** #6738 (first merge after the tag); all 28 UI rows covered.

**Before the release cut:** merge this PR. Agent release needed for #6921 #6928 #6929 (lab proof for #6929 run separately). BLOCKED-by-environment items to spot-check on a hosted stack: Gmail connect (#6740), live Cove sync (#6610), remote-session/install failures on a real agent (#6683 #6760), script exit-code alert from an unattended run (#6838).

## Follow-up verification (main @ f9939e8450, 2026-09-25)

Fresh wt-stack at `f9939e8450` (baseUrl `http://localhost:32987`), browser at 1280×900, admin = platform + partner admin. Prerequisite rows were re-seeded via psql where noted; the earlier sweep's seed does not exist on this DB.

| ID/PR | Check | Result | Evidence |
|---|---|---|---|
| #6933 / #6850 | Suspended org record: fresh load of `/organizations/<id>` shows read-only report history | FIXED | Seeded "Sweep F Org" (`f0000000-…b001`) + AR aging report/run, set `suspended` via psql. `GET /orgs/organizations/<id>` 404 → probe `GET /reports?limit=1` 200 → page shows "This organization is inactive… report history can still be read below", "Read-only: this organization is inactive…", the definition and the completed run. ⚠️ header shows the raw org UUID in place of a name (the record 404s, so no name is known). |
| #6939 / B3 | Archived org overview shows one read-only notice | FIXED | Same org set `archived`: the Overview tab renders only "Overview data isn't shown for archived organizations." (`org-overview-archived-notice` ×1); no error panels. ⚠️ the page still issues `GET /orgs/organizations/<id>/summary` → 404 on load (`OrganizationRecordPage.tsx:142`), console noise only. |
| #6939 / B1 | Business report edit shows no generic builder sections | FIXED (residual ⚠️) | `/reports/<AR aging id>/edit` renders AR options, Report details, Schedule and delivery, recipients; Report type / Data source / Columns / chart controls are gone. ⚠️ **F1**: the right-hand "Live preview" is still the generic builder preview: it reads "Compliance · Weekly · Monday", "Data source: Policy set: CIS Benchmarks, Scoring method…" and a device table (`ReportBuilder.tsx:~2405`, not gated on `businessType`). The stored schedule is `one_time`, yet no schedule button is selected and the preview says Weekly. |
| #6939 / B2 | No i18n missing-key noise on `/reports/templates` | FIXED | Fresh load with `GET /reports/templates` returning server rows with uuid ids: 4 console messages, none `[i18n] missing key` (ReportTemplates.tsx:848 only translates ids in `defaultTemplateIds`). |
| #6933 / D1 | Restart service with no service name is blocked | FIXED (⚠️ copy) | New monitor, kind CPU, Execute Command ▸ "Restart service on the device" ▸ Save monitor: no POST sent, stays on `/alerts/monitors/new`. ⚠️ **F2**: the only message is the generic "Some response settings are missing or out of range. Nothing was saved."; the specific reason ("Restart service requires a service name", MonitorEditor.tsx:149, hard-coded English) is never shown, so the user is not told what to fix. |
| #6933 / D12 | Cove add-connection form autocomplete | FIXED (⚠️ placeholder) | Seeded connection "Sweep F Cove (seeded)" via psql (creds encrypted with the api's `encryptProviderCredentials`), 1 mapped customer, 3 provider devices. Add connection: username `autocomplete="off"`, password `autocomplete="new-password"`, both empty. ⚠️ the Connection name placeholder is still the real MSP name "OliveTech Cove" (`BackupProvidersIntegration.tsx`, `backup-add-name`). |
| #6944 / Cove edit card | Re-enter credentials fields autocomplete | FIXED | Re-enter credentials opens partner/username/password: username `autocomplete="off"`, password `autocomplete="new-password"`, all empty. |
| #6945 / D9 | Failed Test connection: card updates at once, no false "Last sync failed" | FIXED | Test connection → POST `…/test` 200 `{"success":false,"error":"Cove login was rejected…","reauth":true}`, error toast with that text; the card shows "Credentials rejected · Re-enter credentials" immediately, no reload. After reload the same, and no "Last sync failed" line (DB `status=reauth_required`, `last_sync_status=success`, `last_sync_error` NULL). This made one real call to Cove. |
| #6945 / D10 | Backup status / data-source labels translated | FIXED (⚠️ Type column) | Device ▸ Backup External backup card: status "Failed", Data sources "Files & folders, System state" (after switching my seed from `files_folders` to the real Cove value `files`, `cove/columns.ts:178`; an unknown value falls back to the raw string by design). `/backup` health table status column reads Completed/Failed. ⚠️ **F3**: the health table TYPE column still prints the raw `osType` enum (`workstation`, `unknown`) (`BackupHealthDeviceTable.tsx:90`). |
| #6949 / D11 | Linked device with no Breeze jobs = provider row only; with jobs = two rows, one endpoint | FIXED | Seeded Breeze device `sweepf-nojobs` (no backup jobs) linked to Cove device `sweepf-nojobs`; E2E Windows/macOS (seeded Breeze jobs) linked to Cove `e2e-windows`/`e2e-macos`. `GET /backup/health/devices?withBackup=true` 200 → `/backup` table: sweepf-nojobs appears once (Cove row only); Windows and macOS appear twice (Breeze + Cove); headline "2 of 3 endpoints have a recent backup". Status buckets still sum rows (5), as the dual-source contract intends. ⚠️ "1 devices sit under provider customers…" (plural; count comes from my seeded `last_sync_unmapped_devices=1`). |
| #6933 / D14 | Add New Device modal Done reachable at 1280×900 | FIXED | `/devices` ▸ Add ▸ Install agent… ▸ CLI Commands ▸ Windows: Done starts at y=1117 (below the fold), scrolls within the dialog to y=785, `elementFromPoint` hits the button, and clicking it closes the modal. |
| #6944 / A1 | Webhook create / save / toggle toasts | FIXED | Create "Sweep F hook" → POST 201, toast "Webhook created" (a first try with an unresolvable host gave 400 + toast "Invalid webhook URL: …could not be resolved"). Active pill → PATCH 200 `{"status":"paused"}`, toast "Webhook disabled", row reads Disabled; again → "Webhook enabled". Edit ▸ Save Changes → PATCH 200, toast "Webhook updated". |
| #6944 / A2 | 404 on delete closes the modal and refetches | FIXED | Webhooks: opened Delete, removed the row via psql, confirmed → DELETE 404 `{"error":"Webhook not found"}`, error toast, modal closed, one `GET /webhooks` refetch, list "0 of 0 webhooks". API keys: created `sweepF-key` in the UI, opened Revoke, deleted via psql, Revoke Key → DELETE 404 `{"error":"API key not found"}`, toast, modal closed, 1 refetch, row gone. |
| #6944 / A3 | `/alerts` fetches the list once; `/organizations` still twice | FIXED | Fresh load of `/alerts`: `GET /alerts?status=…` ×1 (and `/orgs/organizations` ×1 from the org switcher). `/organizations`: `GET /orgs/organizations?page=1&limit=100` ×2, as the PR documents (board + OrgSwitcher). |
| #6944 / A4 | de-DE `/alerts` tab title matches the h1 | **NOT FIXED** | With de-DE saved, the SSR HTML has `<title>Warnungen \| Breeze RMM</title>` (the PR changed `de-DE/pages.json` `titles.alerts`), but after hydration the tab reads **"Warnmeldungen \| Breeze RMM"** while the h1 is "Warnungen" (`page.title()` before/after 4 s: `Warnungen` → `Warnmeldungen`). Cause: `syncDocumentLocaleMetadata` (`apps/web/src/lib/i18n/index.ts:48,69-72`) rewrites the title for `/alerts` from `common:nav.alerts`, which is still "Warnmeldungen" (`locales/de-DE/common.json:18`, also the sidebar label). The PR's `terminologyQuality` test only checks pages.json. |
| #6944 / A5 | Service principal row lists granted scopes | FIXED | Created `sweepF-sp` with `alerts:read` (POST 201, toast "Service principal created"). The row reads "Scopes:" followed by all 9 granted scopes including `alerts:read`, without opening Edit. Locale reset to `en` afterwards. |
| #6946 / D2 | Conversion history formatted, names, Reverted label | FIXED | Seeded 2 org policies "Sweep F legacy policy" (2 inline rules, assigned to E2E Windows) and "…two" (1 rule, assigned to E2E macOS). After Convert everything the ledger rows read "Sweep F high CPU · 9/24/2026, 11:34:13 PM · Breeze Admin" with the output monitor by name ("Sweep F high CPU · primary"). After Undo the row shows a `ledger-reverted-…` "Reverted" label next to the disabled Undo. |
| #6946 / D3 | Lists and banner refresh after Convert/Undo; headings; plurals | FIXED (⚠️ plural not fixed) | Convert everything → POST `…/partner/convert-all` 200 `{"policies":2,"converted":3,"unconvertible":0}`, toast "Converted 3 rows across 2 policies…"; with no reload the banner is gone and the list reads "No policy carries inline alert rules…". Undo → toast "Reverted 1 conversion.", and the banner and "Policies with legacy rules ▸ Sweep F legacy policy" reappear at once. The standalone table now has its own "Legacy alert rules" heading. ⚠️ the banner still reads "**1 legacy rules across 1 policies** have not been converted" (`locales/en/monitoring.json:414` `conversion.banner.text` has no `_one` forms; `confirmBody` l.418 too). |
| #6946 / D4 | Admin nav has Monitor conversion | FIXED | Sidebar Administration lists "Monitor Conversion" → `/admin/monitor-conversion`. |
| #6946 / D5 | Reset confirm names the device | FIXED | Seeded an escalated, paused `monitor_device_state` row for "Sweep F CPU or offline" on E2E Windows. Device ▸ Monitoring ▸ Reset escalation opens the confirm "Reset the escalation for **E2E Windows Test Device**? …"; accepting cleared `escalated_at`, `responses_paused=false`, `reset_at` set. |
| #6946 / D6 | Recommended opener label | FIXED | The Recommended strip button reads "Attach to policy…". |
| #6946 / D15 | No console 404s for reliability / hardware-health on an agentless device | FIXED | Seeded agentless device `sweepf-nojobs`: overview and `#hardware` loads send `GET /reliability/<id>` **200** and `GET /devices/<id>/hardware-health` **200**; zero 4xx, zero console errors; the Storage & RAID empty state still reads "No hardware health report received yet". |
| #6946 / D7 | Designer enable returns the real status | FIXED | `/ai-agents/fleet-design` ▸ Enable Fleet Designer → POST `/ai/fleet-design/designer/enable` 200 `{"data":{"status":"ready","agentId":"74e50623-…","canEnable":false}}`, toast "Fleet Designer enabled."; audit row `ai_fleet_design.designer.enable` resource `ai_agent` / `74e50623-…` details `{"status":"ready"}`. |
| #6946 / D8 | Apply drawer shows the real confidence and pluralises | BLOCKED | Needs a completed Fleet Design run (a live Anthropic run, ~43¢ last time, over this pass's spend cap); the report cannot be seeded cheaply. Code only: the drawer now takes per-function confidence from the report and uses `watchCount`/`ruleCount` plural keys. |
| #6945 / D13 | Remote-session errors show specific text | FIXED | `sweepf-nojobs` set online via psql. Connect Desktop → POST `/remote/sessions` 503 `agent_upgrade_required` → toast "Remote desktop needs an agent update on this device… Terminal and file transfer are unaffected." With the request's `deviceId` rewritten in-browser to a non-existent id: 404 `{"error":"Device not found or access denied","code":"NOT_FOUND"}` → toast **"Device not found or access denied"** (previously the generic "Not found"). The 403 ACCESS_DENIED path was not reachable as platform admin. Device set back offline. |
| #6940 / C2 | Billables export with To = today includes today | FIXED (⚠️ UTC day) | Via API: ticket T-2026-0001 with two billable entries, 18:00Z 9/24 and 04:30Z 9/25 (= 22:30 on 9/24 in the browser's America/Denver). Billing ▸ Invoices ▸ Export billables defaults From 2026-09-01 / To **2026-09-24** → `GET /tickets/export/billables.csv?…&to=2026-09-24` 200 now includes the 18:00Z entry (previously empty). ⚠️ **F4**: the 22:30-local entry is still missing until To = 2026-09-25, because a date-only `to` is end of the **UTC** day, while the card's "today" is the browser's local date (`BillablesExportCard.tsx:16`). The PR states UTC on purpose (it matches assembly), but for US partners the evening hours of "today" are still dropped. The CSV ticket column now reads `T-2026-0001`. |
| #6940 / C1 | Quote Send subject placeholder | FIXED | Quote Q-2026-0001 "Office Wi-Fi Upgrade" (API-created, 1 manual line) ▸ Send: `quote-send-subject` placeholder = "Office Wi-Fi Upgrade — proposal from Default Partner". Dialog closed without sending. |
| #6940 / C3 | Billing ▸ Documents Company name shows the inherited value | FIXED | `/settings/billing#documents`: `partner-billing-company-name` is blank with placeholder "Default Partner" and "Inherits from Company details". |
| #6940 / C4 | Draft line shows T-number; issued label frozen across web, portal, public page | FIXED (⚠️ Detail view) | `POST /tickets/<id>/invoice` built a draft; the draft editor shows `T-2026-0001` on both lines. Issue (web) → INV-2026-0001, DB `invoice_lines.ticket_label = T-2026-0001`. Renumbered the ticket via psql to `internal_number = T-2026-0777`. After that: web invoice Preview "Ticket #T-2026-0001: Sweep F billable ticket"; portal `/portal/invoices/<id>` "Ticket #T-2026-0001"; public `/portal/invoice/<token>` "Ticket #T-2026-0001"; public PDF (`pdftotext`) "Ticket #T-2026-0001: …". ⚠️ the issued invoice's default **Detail** view shows no ticket reference at all (just the entry descriptions); it appears only under Preview. The org has no contacts, so issuing sent no mail. |
| #6948 / E1 | Anomaly badge hidden when detection is off | FIXED | Seeded 3 `metric_anomaly_episodes` (2 open) on E2E Windows. With detection off (fresh-DB default) the device tab reads plain "Anomalies" (no count) and the panel says "Anomaly detection disabled — Turn it on in organization settings → AI." Control: after PATCH `/orgs/partners/me` `ml.anomalies.enabled=true` the tab reads "Anomalies 2" and 2 episode cards render. |
| #6948 / E2 | Out-of-range multiplier highlights the field | FIXED (⚠️ toast) | Org `#ai` ▸ AI budget, multiplier 15 ▸ Save → PUT `/ai/budget` 400 `{"error":"toolRateLimitMultiplier: Too big: expected number to be <=10",…}`. The input now has `aria-invalid="true"` and a destructive border, with "Too big: expected number to be <=10" under it and "Check the highlighted fields" on the card. ⚠️ the toast still starts with the raw API key: "toolRateLimitMultiplier: Too big: expected number to be <=10", and the inline text is zod's default wording, not "Enter 1–10". |
| #6948 / E3 | AI Risk Rate Limits "Other" group | FIXED | `/ai-risk` ▸ Rate Limits: "Other" holds 3 tools (Export Dataset, Manage Tags, Manage Saved Filters), each with a tier and a permission; no "—" cells anywhere in the table. |
| #6948 / E5 | Script test-run messages after completion | FIXED | Script "Sweep F test script" (API) ▸ Test Run on e2e-windows (offline) → POST 201 `delivery:"queued_offline"`, panel "Queued". Completed the execution via psql (exit 0, stdout NULL). The poll shows "Completed · exit 0"; no "…will wait until it reconnects" notice; stdout shows "No output" once (stderr "(empty)" is the separate stderr section). |
| #6948 / E7 | `/settings/ai-agents` baseline hint | FIXED | With the partner-wide Fleet Designer enabled, the page reads "Partner-wide: applies to every organization…" above the agent; the contradictory "No partner-wide baseline exists yet…" line is gone. |
| #6948 / E8 | No 80% badge when there is no limit | FIXED | The fixture DB already has a monthly 80% `ai_budget_alert_events` row for Default Org; `GET /ai/usage` still returns it in `alerts.fired`. `/settings/ai-usage` shows Monthly/Daily Budget "No limit · Default" and **no** "80% monthly alert sent" badge. |
| #6948 / E6 | Live chat tool labels match history; user message rendered live | **NOT FIXED** (labels FIXED) | One message, "How many active alerts are there right now?", in a **new** session, sent with Cmd+Enter from `/alerts`. Cost $0.22 (the panel reads "$0.22 this month"). ✅ the live transcript labels both tool rows "Checked alerts" (was "Updated alerts"). ❌ the user's own message is still missing from the live transcript: the sidebar island's `textContent` does not contain the question, yet the header says "1 msg" and `ai_messages` has the `user` row (05:47:51Z). The PR says this "did not reproduce"; it has now reproduced twice, both times on the first message of a new session. Suspected cause (inferred, not verified): `sendMessage` awaits `createSession()` and then appends the optimistic user row (`stores/aiStore.ts:311-331`). The new `sessionId` with `messages.length === 0` meets the restore effect in `components/ai/AiChatSidebar.tsx:94-110`, which calls `loadSession()`, and that overwrites `messages` with server history fetched before the user row was persisted. |
| #6948 / #6932 | Patch-compliance alert message has no `{{` | FIXED | Attached the built-ins to "Sweep F legacy policy" through Recommended ▸ Attach to policy… (PATCH 200, toast "Monitor attached"). Seeded a `security_posture_snapshots` row with `patch_compliance_score=50` for E2E Windows (the handler reads that table, not `device_patches`, so with no snapshot nothing fires). Kept the device online via psql for one evaluate-all cycle. The real sweep raised "Patch Compliance on E2E Windows Test Device" with message **"Low patch compliance: patch compliance 50% (< 80%)"**. `/alerts` shows it with no `{{`. Device back to offline. |
| #6938 | "Suggested fixes" toggle on partner and org AI Features cards persists and shows inheritance | NEW PASS | Partner `#ai-features` ▸ "Generate suggested fixes" (`partner-ml-remediation-suggestions-enabled`) → PATCH `/orgs/partners/me` 200 `{"settings":{"ml":{"remediation_suggestions":{"enabled":true}}}}`, toast "Saved", still checked after reload; DB `ml` = `{"anomalies":{"enabled":true},"remediation_suggestions":{"enabled":true}}` (sibling key kept). Org `#ai` select reads "Inherit (on, partner setting)". Off → PATCH 200 `{…"remediation_suggestions":{"enabled":false}}`, toast "Saved", reads Off after reload. Inherit → removes the key. With anomalies set to Off, toggling suggestions On then Inherit keeps `anomalies.enabled=false` in each body. Org left on Inherit for both. |
| #6942 | Mailbox callback failure returns to the M365 card with a message | NEW PASS | Browser GET `/api/v1/tickets/mailbox/callback?state=bogus&error=access_denied&error_description=User+declined` → **302** `/settings/ticketing?ticketMailbox=error&reason=binding_mismatch#email` → the page lands on the Email tab, strips the query (URL `…/settings/ticketing#email`) and shows the error toast "Microsoft 365 connection failed: the browser came back without its sign-in cookie. Open Breeze at the same address as the redirect URI (scheme and host must match), allow cookies, and try again." |
| #6937 | M365 card PowerShell snippet shows runtime app id or placeholder | NEW PASS | `TICKET_MAILBOX_M365_CLIENT_ID` is empty on this stack, and `GET /tickets/mailbox/connections` returns `appId: null`. With a seeded `m365` connection in `error` status, "Scope this mailbox (Application Access Policy)" expands to the snippet with `New-ApplicationAccessPolicy -AppId <Breeze Ticketing app id>` (placeholder, as expected here) and the mailbox address filled in. Seeded row deleted afterwards. ⚠️ the card fetches `/tickets/mailbox/connections` twice per load. The live-app-id branch was not exercised (needs env). |
| #6931 / #6951 | Apply template with Replace while an Operator task waits on a step shows the apply-specific message | NEW PASS | Ticket T-2026-0777 with 2 unticked items (API) and org template "Sweep F template" (2 steps, API). Seeded `ai_operator_tasks` (state `waiting`) plus an `ai_operator_task_steps` row (`human_work`, `waiting`, `checklist_item_id` = "Sweep F: replace the NIC"). Apply template ▸ Replace the unticked steps ▸ Apply → POST `…/checklist/apply-template` **409** `{"code":"CHECKLIST_OPERATOR_STEP_WAITING",…}`. The toast reads "An Operator task is waiting on one of this ticket's unticked steps, so the template can't replace them. Choose “Add these steps to the existing checklist” instead, tick the step when the work is done, or stop the task first." The checklist is unchanged (both original items, no template steps). |
| #6950 | Portal settings editor has the Network alerts toggle, off by default | NEW PASS | Org Settings `#portal`: "Network alerts — Show active alert counts and linked open tickets on network assets in the customer portal." (`org-portal-toggle-enableNetworkAlerts`) is unchecked on a fresh DB and separate from "Network visibility". Save sends `enableNetworkAlerts` → 200, toast "Portal settings saved", and it stays checked after reload. |
| #6950 | Flag on: portal network assets carry alert/ticket counts (0 when none); flag off: fields absent | NEW PASS (API) / BLOCKED (UI rows) | Seeded 2 approved `discovered_assets` (`sweepf-switch`, `sweepf-printer`), an active `network_monitors` row on the switch, an active high alert with `context {source:"network_monitor", monitorId}`, and a `ticket_alert_links` row to open ticket T-2026-0777. Logged in to the portal as portal@breeze.local (`PortalTest123!`). Visibility on, alerts **off**: `GET /api/v1/portal/network/assets` 200, rows have **no** `activeAlertCount`/`highestAlertSeverity`/`openTicketCount` keys. Alerts **on**: switch `{activeAlertCount:1, highestAlertSeverity:"high", openTicketCount:1}`, printer `{0, null, 0}`. The portal `/network` page only renders the summary tiles (Total 2, Online 2, …) and has **no asset table**, so the counts cannot be seen in the UI yet (the table is #6641, waiting on #6630). |

**Follow-up totals (43 rows):** 34 FIXED (8 with a residual ⚠️, listed as F1–F9 in the paper-cut table), **2 NOT FIXED**, 0 REGRESSED, 6 NEW PASS (#6950's UI rows BLOCKED, no portal asset table yet), 1 BLOCKED (D8: needs a paid Fleet Design run).

**NOT FIXED:**
- **A4** (de-DE `/alerts` title): after hydration, `syncDocumentLocaleMetadata` (`apps/web/src/lib/i18n/index.ts:48,69-72`) rewrites the title from `common:nav.alerts` = "Warnmeldungen" (`locales/de-DE/common.json:18`). The PR only changed `pages.json`.
- **E6** (user message missing from the live chat stream): reproduced on the first message of a new session. The inferred cause is a race between the `sendMessage` optimistic append after `createSession()` (`stores/aiStore.ts:311-331`) and the sidebar's restore effect calling `loadSession()` (`components/ai/AiChatSidebar.tsx:94-110`).

**Follow-up DB leftovers (fresh stack `breeze-wt-qa-sweep-v0116-followup`):**
- **Org:** "Sweep F Org" `f0000000-…b001` (active) with site "Sweep F Site", and report "Sweep F AR aging" with 1 run. Default Org has report "Sweep F AR default".
- **Cove:** connection "Sweep F Cove (seeded)" (`reauth_required`), 1 mapped customer, 3 provider devices linked to E2E Windows, E2E macOS and `sweepf-nojobs`.
- **Devices:** new agentless device `sweepf-nojobs` `f0000000-…e1` (offline).
- **Monitors:**
  - policies "Sweep F legacy policy" (E2E Windows, now carrying all 8 built-ins through Recommended) and "…two" (E2E macOS);
  - 2 of 3 rules converted, "Sweep F high CPU" reverted;
  - one reset `monitor_device_state` row.
- **Fleet Designer:** agent enabled, no runs.
- **Service principal:** `sweepF-sp` (with `alerts:read`, no keys). The webhook and API key were deleted as part of the 404 tests.
- **Billing:**
  - ticket T-2026-0001 renumbered via psql to `internal_number = T-2026-0777`, with 2 billable entries, 2 checklist items and 1 linked network alert;
  - issued invoice INV-2026-0001 (lines frozen at T-2026-0001);
  - draft quote Q-2026-0001 with 1 line;
  - checklist template "Sweep F template".
- **Operator:** task `f0000000-…c9` (waiting) with a waiting `human_work` step on "Sweep F: replace the NIC".
- **Anomalies:** 3 anomaly episodes, 2 member anomalies. Partner `ml` = `{anomalies:{enabled:true}, remediation_suggestions:{enabled:true}}`; Default Org `ml` = `{}` (inherit).
- **Patch compliance:** `security_posture_snapshots` row (patch 50%) on E2E Windows, plus the active alert "Patch Compliance on E2E Windows Test Device".
- **Portal:** Default Org portal `enable_network_visibility=true` and `enable_network_alerts=true`. Network assets `sweepf-switch`/`sweepf-printer`, monitor `sweepf-switch ping`, and active alert "Sweep F switch unreachable" (on E2E Windows).
- **Scripts:** "Sweep F test script" with 1 simulated completed test run.
- **AI chat:** 1 session, $0.22.
- **Locale:** admin locale back to `en`. All seeded devices are left offline.
