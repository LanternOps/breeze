# Pre-release sweep — v0.108.0 → main (2026-08-28)

| | |
|---|---|
| Range | `v0.108.0` (`fe6b01cb9`, 2026-08-26) → `origin/main` `54be3ee4a` |
| Gate | PRs waited on: #4163 (What's New v0.109.0) ; merged: #4163 @ `54be3ee4a` (CI Success green on head b06570bae) ; known-noise dismissed: Trivy `CVE-2026-14456` libcrypto3 base-image (confirmed in run 33183694170 log) |
| Stack | `pnpm wt-stack up` on `qa/sweep-post-v0.108.0` @ `54be3ee4a`, baseUrl `http://localhost:<port>` |
| Flags enabled | `BREEZE_AI_AGENTS_ENABLED`, `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED`, `LLM_PROVIDER_CATALOG_ENABLED`, `BREEZE_WORKSPACE_ENABLED`, (all `true`; `MCP_OAUTH_ENABLED` already on). #4097 flags left at shipping default `false` — `AUTH_BROWSER_TERMINAL_PREPARATION_ENABLED=true` alone is refused at boot with a clear validate.ts message (`requires AUTH_BROWSER_TRANSITIONS_ENFORCED=true`) ✅ |
| Driver | skill `pre-release-sweep`; sweep agents: … |

## Change inventory

Status ∈ `TODO | PASS | PARTIAL | FAIL | BLOCKED | N/A`. Every row must leave `TODO`.

| PR | Merged | Surface | Title | Route / click-path | Expected visible outcome | Prereq | Status |
|---|---|---|---|---|---|---|---|
| 4066 | 08-26 14:47 | docs/ci/chore | chore(deps): bump github-actions group | — (`.github/workflows/*` only) | n/a | n/a | TODO |
| 4091 | 08-26 19:05 | api-only (+agent) | fix(agent,api): self-host→hosted update transition + edition-aware offer gating | No UI surface. `agent/internal/updater/edition.go` + heartbeat `agentEdition` reporting only. | Not user-visible; a self-host agent in the 0.105–0.106 band should now accept a hosted-edition update instead of wedging at `status='updating'`. | BLOCKED on a plain stack — requires a real self-host-edition agent binary in the affected version band; can't be triggered from the web UI. | TODO |
| 4071 | 08-26 19:21 | web | feat(api,web): lockout preflight + confirm-through before Enforce SSO | Settings → **SSO** (`/settings/sso`) → edit/create a provider → toggle **Enforce SSO** on → Save | If any active user lacks a link to the effective login provider, a **"Users will be locked out"** modal appears listing unlinked users, requiring the checkbox "I understand these users will be unable to sign in until they connect SSO" before **Enable enforcement** is clickable. | Needs an SSO provider already configured (no live IdP needed — preflight only reads membership/linkage) and at least one active unlinked user (the seeded admin, if never SSO-linked, qualifies and would show `enforceLockoutSelfWarning`). | TODO |
| 4083 | 08-26 19:21 | api-only | fix(api): delivery idempotency and atomic winners (AI agents wave 3.5a) | No standalone UI; makes `resolveAlert()` compare-and-swap. Verify via the #4099 click-path below (resolve races). | Two simultaneous resolutions of the same alert should produce exactly one `alert.resolved` event / one escalation-cancel, not two. | Hard to demonstrate via a single browser session; needs two concurrent resolve calls (e.g. two tabs) on the same active alert. | TODO |
| 4070 | 08-26 19:22 | docs/ci/chore | docs(release-skill): pre-tag What's New entry step | `.claude/skills/release/SKILL.md` only | n/a | n/a | TODO |
| 4080 | 08-26 19:56 | web | [Auth][SSO] Link-on-first-SSO-login ceremony | Login page → click org's SSO button → IdP redirects back with an email match to a password-holding, unlinked account → app routes to `/auth/connect-sso` ("Connect your sign-in") instead of `?error=sso_link_required` | User is prompted to re-enter their password (+ MFA if enrolled); on success `user_sso_identities` link is created and login completes normally. | BLOCKED without a working test IdP (SAML/OIDC) wired to a seeded SSO provider — the ceremony only triggers off a real SSO callback with an email match. | TODO |
| 4099 | 08-26 23:23 | web | fix(api): route every alert-resolve path through the compare-and-swap | `/alerts` (list) or `/alerts/:id` (detail) → click **Resolve** on an active alert | Alert transitions to Resolved once; no duplicate `alertsPage.alertResolved` toast / no double escalation-cancel. Regression check: resolving via `AlertsPage` bulk action, `AlertDetailPage`, and alert-correlations group-resolve should all be identical CAS-guarded behavior. | Needs at least one active alert in the seed. Race behavior (the actual bug) needs two near-simultaneous resolves to observe; a single resolve just confirms no regression. | TODO |
| 4098 | 08-26 23:23 | api-only | fix(api): recover webhook deliveries recorded but never enqueued | Indirectly: Settings → **Webhooks** (`/settings/webhooks`) → delivery history for a configured webhook | A delivery stuck in "recorded but not enqueued" should recover and show delivered instead of stuck. | BLOCKED for a simple click-path — requires contriving a recorded-but-unenqueued row (e.g. Redis restart mid-enqueue); not reproducible by clicking. | TODO |
| 4117 | 08-27 06:13 | api-only | feat(api): wave 3.5c — durable event dispatch (BullMQ ingress + per-subscriber delivery receipts) | No direct UI; backend event-bus rework. Regression-smoke via any alert/automation/webhook flow. | Alerts, automations, and webhooks should keep firing exactly as before (event delivery now durable/BullMQ-backed with per-subscriber receipts). | Needs Redis (already required); no new env flags. Not independently clickable — verify as a smoke pass over Alerts/Automations/Webhooks. | TODO |
| 4087 | 08-27 06:33 | docs/ci/chore | docs: v0.108.0 sweep — AI agents docs page | `apps/docs/**` content only | n/a | n/a | TODO |
| 4096 | 08-27 06:33 | docs/ci/chore | docs: land 2026-08-24 UI QA sweep + docs-citation rule | `.claude/skills/**`, `docs/**` only | n/a | n/a | TODO |
| 4110 | 08-27 06:33 | docs/ci/chore | docs(plans): BYO-LLM phase 2 + workspace enrichment BYOK plans | `docs/superpowers/plans/**` only | n/a | n/a | TODO |
| 4112 | 08-27 06:33 | web (nominal) | feat: workspace enrichment honors partner BYOK | Settings → **Partner** → **AI Provider** tab (`/settings/partner#ai-provider`, pre-existing tab) — the web diff here is a code comment only, no new copy. Real change is `ee/workspace` enrichment routing + Settings → **AI Usage & Budget** (`/settings/ai-usage`) billing-source attribution. | A workspace content-enrichment run should bill to the org's configured BYOK key (`billing_source='partner_key'`) instead of always the platform key; a broken BYOK config should surface a visible enrichment failure. | BLOCKED without `BREEZE_WORKSPACE_ENABLED=true` (default off, commented out in `.env.example`) and a running workspace/catalog ingest pipeline — no simple UI trigger. | TODO |
| 4113 | 08-27 06:33 | web | feat: llm provider catalog foundations (#3922 W01) | Log in as **platform admin** → Sidebar → Settings section → **LLM Provider Catalog** (`/admin/llm-provider-catalog`) → **Add provider** → fill form → **Create** revision → **Verify** → **Activate** | New provider entry appears in the catalog list; verify performs a live round-trip tool call against the declared base URL before allowing activation; activated revision becomes selectable downstream (#4118). | `platformAdminOnly` nav item — requires platform-admin role, not just org/partner admin. **Verify** step calls a real external base URL — BLOCKED for full pass without a reachable OpenAI-compatible/gateway endpoint (a local echo/stub endpoint would substitute). | TODO |
| 4115 | 08-27 06:53 | api-only | feat: llm egress enforcement layer (#3922 W02) | No UI; egress-proxy + cost-tracking backend for catalog traffic. | AI sessions routed through a catalog endpoint should be metered/guarded (SSRF-safe egress, correct pricing). | BLOCKED for standalone QA — only observable through #4118's endpoint-selection UI plus a live AI chat/agent session. | TODO |
| 4126 | 08-27 07:13 | web | feat(enrollment): default installer keys to 30 days / 50 devices | Devices → **Add Device** (`/devices`, click "Add Device") — default expiry picker and device-count field | Expiry defaults to **30 days** (was 24h) and device count defaults to **50** (was 1). Same defaults apply on Settings → **Enrollment Keys** (`/settings/enrollment-keys`) → new key. | None — visible immediately on a fresh seed. | TODO |
| 4133 | 08-27 10:20 | api-only | feat(api): wave 3.5b — socket-affinity command relay | No UI; agent-WS command relay/fenced presence for multi-instance API. | Agent command dispatch should keep working identically in single-instance dev; only observable under a scaled/worker-split deployment. | BLOCKED for single-instance local stack — needs multiple API replicas to exercise the affinity/fencing logic. | TODO |
| 4138 | 08-27 11:56 | api-only | fix(api): ordered graceful shutdown (3.5d part A) | No UI; server shutdown-sequencing fix. | Not observable via browser; verify via `docker compose stop`/SIGTERM behavior in logs. | Not a UI QA item. | TODO |
| 4082 | 08-27 17:11 | api-only | feat(api): org lifecycle wave 1 — status values, columns, deferrable FKs | No UI itself; schema/status foundation consumed by #4111/#4114/#4128/#4132. | n/a standalone | Foundational only — test via the wave 3/5 UI PRs below. | TODO |
| 4129 | 08-27 17:10 | api-only | fix(api,workspace): review round 2 — permanent-vs-transient AI errors, billing/stamp telemetry | No UI; `ee/workspace` error/telemetry hardening. | n/a | Same BLOCKED reasoning as #4112 (workspace pipeline). | TODO |
| 4130 | 08-27 17:11 | web | fix(api,web): review round 2 — self-managed revisions route, egress-blocked verdicts, listing-gate tests | Same page as #4113: `/admin/llm-provider-catalog` | Self-managed provider revisions list correctly scoped; egress-blocked verdicts surfaced. Fold into the #4113 catalog click-path as a bug-fix check. | Same platform-admin requirement as #4113. | TODO |
| 4131 | 08-27 17:44 | api-only | fix(api): review round 2 — proxy audit completeness, recorder observability, shutdown drain | No UI; LLM egress proxy hardening. | n/a | Same as #4115 (egress backend, no standalone UI). | TODO |
| 4116 | 08-27 17:45 | api-only | feat: partner catalog endpoint selection (#3922 W03) | No UI of its own — surfaces entirely through #4118's Endpoint card. | n/a standalone | Fold into #4118 click-path. | TODO |
| 4118 | 08-27 18:07 | web | feat: byo-llm partner endpoint UI + docs (#3922 W04) | Settings → **Partner** → **AI Provider** tab (`/settings/partner#ai-provider`) | **Endpoint** card (only rendered when the catalog is non-empty) shows a radio list: "Anthropic (direct)" + one row per active catalog entry with verified models. Picking a non-direct entry with a data-handling note opens a confirm dialog quoting the note behind a consent checkbox before saving. Settings → **AI Usage & Budget** (`/settings/ai-usage`) then names the catalog endpoint in its "billed to your key" note. | BLOCKED unless `LLM_PROVIDER_CATALOG_ENABLED=true` **and** a platform admin has already created+verified+activated at least one catalog entry via #4113's page — on a fresh seed the catalog is empty and the Endpoint card does not render. | TODO |
| 4111 | 08-27 18:25 | web (+api) | feat(api,web): org lifecycle wave 2 — merge engine | Backend + `OrganizationList.tsx`/`orgStore.ts` wiring only, no new buttons yet. Real UI ships in #4114. | n/a standalone | Fold into #4114 click-path. | TODO |
| 4114 | 08-27 19:26 | web | feat(web): org lifecycle wave 3 — merge UI | Settings → **Organizations** (`/settings/organizations`) → select an org → **"Merge into another organization"** button (only shown to partner-scope admins) → pick survivor org → preview (per-table row counts, warnings) → type org name to confirm → **Merge organization** | Live progress ("Merge in progress") then a persistent **"Merge complete"** summary (`N row(s) moved, M row(s) dropped`); merged org disappears from the active list. | Requires a **partner-scope** login (org-scope admins never see the button — `canMergeOrgs` checks `jwt.claims.scope === 'partner'`) **and MFA enabled** on the account (`organizationsPage.merge.errors.mfaRequired`), plus a second eligible org to merge into. | TODO |
| 4140 | 08-27 19:31 | api-only | feat(api): wave 3.5d — BREEZE_ROLE split (worker entrypoint, compose worker-split profile) | No UI; introduces `BREEZE_ROLE` / `BREEZE_API_ROLE` and the worker-split docker-compose profile. | n/a | BLOCKED on the default local compose profile (`BREEZE_API_ROLE=all`); only observable if the stack is booted with the worker-split profile. | TODO |
| 4128 | 08-27 19:44 | api-only | feat(api): org lifecycle wave 4 — archive backend | Backend only; UI ships in #4132. | n/a standalone | Fold into #4132 click-path. | TODO |
| 4145 | 08-27 19:57 | api-only | feat(api): relay lazy agentWs import — monitor/snmp/backup/discovery to worker container | No UI; part of the worker-split cluster with #4133/#4138/#4140. | n/a | Same BLOCKED reasoning as #4140. | TODO |
| 4132 | 08-27 20:01 | web | feat(web): org lifecycle wave 5 — archive UI | Settings → **Organizations** (`/settings/organizations`) → select an org → **"Archive organization"** (red button) → pick retention (**30/90/365 days / Never / Custom 1–3650**) → read consequences copy → **Archive organization** confirm | Done screen ("Archiving started" or "Organization archived" with purge date). Collapsed **Archived** section below the list now shows the org with a purge countdown and searchable/paginated read-only detail; **Restore** button returns it to its pre-archive status. | **MFA required** on the acting account (`organizationsPage.archive.errors.mfaRequired`) — same prereq as merge. | TODO |
| 4146 | 08-27 22:31 | api-only | feat(api): wave 4 part A — act-mode foundations (execution ledger, agent attribution, notify, caps v2, device pinning) | Backend only; config UI ships in #4148. | n/a standalone | Fold into #4148 click-path. | TODO |
| 4148 | 08-28 02:59 | web | feat(api,web): wave 4 part B — act mode UI | Settings → **AI Agents** (`/settings/ai-agents`) → create/edit an agent → **Mode** dropdown → select **"Act — execute matched operations unattended, with verification"** | An **"Act mode executes without a human clicking approve"** warning box appears requiring the acknowledgement checkbox ("I understand act mode executes unattended, verification can fail, and there is no rollback.") before Save succeeds; form blocks save with "Add at least one notification recipient before enabling act mode." / "Allow at least one act-eligible tool..." if prerequisites aren't met. | Config/save flow works regardless of flags, but actual unattended execution requires `BREEZE_AI_AGENTS_ENABLED=true` (default `false`) plus a live/online device — BLOCKED for real act execution on the offline-fixture stack. | TODO |
| 4154 | 08-28 05:26 | api-only | feat(api): wave 5 part A — policy-decide foundations (decision-state lifecycle, exposure ledger, kill state) | Backend only; UI ships in #4155. | n/a standalone | Fold into #4155 click-path. | TODO |
| 4155 | 08-28 08:45 | web | feat(api,web): wave 5 part B — policy-decide lane (dark) | Settings → **AI Agents** → edit an agent with **Mode = Act** → new **"Unattended policy authorization"** section appears with a checklist of policy-decidable actions ("Operations this agent may execute WITHOUT a human clicking approve...") | Section renders and lets you select/deselect action keys; saving persists `actAssets.supervisedActionKeys`. | Feature is **dark by default**: `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED=false` — the form section itself is visible, but actual policy-approved unattended execution is BLOCKED without the flag on and a live device/queued run. | TODO |
| 4157 | 08-28 13:11 | web | feat(api,web): wave 6.1 — supervision: execution-trace runs UI | Sidebar → Settings section → **"Agent Runs"** (`/ai-agents/runs`) → click a run row → run detail (`/ai-agents/runs/:id`) | Runs list (filterable by agent/status, keyset-paginated) with columns Agent/Status/Trigger/Verdict/Queued/Finished/Cost; detail page shows an execution-trace outcome timeline (executed actions, proposals, denials) and an exposure-budget readout. Legacy per-agent runs route no longer leaks raw tool args. | BLOCKED to see populated data on a fresh seed — list shows **"No runs yet."** unless AI agent runs have actually executed (needs `BREEZE_AI_AGENTS_ENABLED=true` + a live device + a triggered run, e.g. from an alert). | TODO |
| 4102 | 08-28 13:20 | api-only (UI-reachable) | feat(api): edition-migration script in the shared system script library | Scripts (`/scripts`) → **"Import from Library"** button → find **"Migrate Agent Edition (Windows)"** → import | Script clones into the org's script library (content + parameter definitions) via `POST /scripts/import/:id`; visible in the org's script list afterward. | Running the script itself requires a live Windows agent mid-edition-transition — import/visibility is fully testable locally, execution is not. | TODO |
| 4158 | 08-28 14:14 | api-only | fix(api): realign act-mode integration assertion (main red) | Test-only fix (`__tests__/integration/aiAgents.routes.integration.test.ts`, shared types) — no runtime behavior change. | n/a | Not a QA item — CI-only fix. | TODO |
| 4161 | 08-28 14:33 | web | fix(web): RemoteTerminal double-init/dead-terminal race + connectedBang locale | Device detail → **Remote Tools** → **Terminal** tab (`/remote/terminal/[deviceId]`) — connect, switch to **File Browser** tab, switch back to **Terminal** | Terminal reconnects cleanly instead of leaving a dead/disposed instance (previously reproduced issue #4152's "Terminal won't connect until visiting File Browser and back"); on connect the terminal prints a correctly localized **"Connected!"** (`remoteTerminal.connectedBang`) instead of a raw/English-only string in translated locales. | BLOCKED on fully offline fixture devices — Remote Terminal requires a live WebSocket agent connection to open a session at all. | TODO |
| 4097 | 08-28 14:55 | api-only | feat(auth): add Phase 1 browser transition foundation (#3852) | No UI surface added. Login/MFA/passkey/invite/email-verification/Cloudflare Access/SSO issuance paths + terminal logout all re-plumbed underneath, legacy issuer kept live. | Not independently visible; a full login/MFA/logout regression smoke should behave identically to before. | Both new flags default **off**: `AUTH_BROWSER_TRANSITIONS_ENFORCED=false`, `AUTH_BROWSER_TERMINAL_PREPARATION_ENABLED=false` (PR body also references `AUTH_BROWSER_TRANSITIONS_ENABLED` conceptually, but only the two flags above exist in `config/env.ts`/`.env.example`). Phase 1 explicitly does not activate anything — BLOCKED for any new-behavior QA on a stock stack; only a "nothing broke" smoke test applies. | TODO |
| 4163 | 2026-08-28 | web | chore(web): What's New entry for v0.109.0 | Log in → the What's New splash/menu entry (`apps/web/src/lib/whatsNew.ts`) — open the What's New panel from the header/help menu | v0.109.0 entry renders with its bullets; upgrade splash fires once for a user who last saw 0.108.0 | none | TODO |

### Feature-flag env vars referenced by these PRs

| Env var | Default | Introduced by | Gates |
|---|---|---|---|
| `AUTH_BROWSER_TRANSITIONS_ENFORCED` | `false` | #4097 | Enforcement of the new guarded auth-issuance path over the legacy issuer (Phase 1 foundation; not activated by this merge). |
| `AUTH_BROWSER_TERMINAL_PREPARATION_ENABLED` | `false` | #4097 | Terminal-logout preparation behavior tied to the browser-transition work. |
| `LLM_PROVIDER_CATALOG_ENABLED` | `false` | #4116 (validated in `apps/api/src/config/validate.ts`; consumed by `llmConfigResolver`) | Whether a partner's catalog-endpoint selection (#4118 UI) actually resolves/routes traffic through the catalog instead of raw BYOK credentials. Not in `.env.example` — set directly. |
| `BREEZE_ROLE` / `BREEZE_API_ROLE` | `all` | #4140 | Worker-split deployment profile (API vs. worker container role separation); only meaningful under the worker-split docker-compose profile. |
| `BREEZE_AI_AGENTS_ENABLED` | `false` | pre-existing (referenced by #4146/#4148/#4154/#4155/#4157 as the umbrella flag) | Whether AI-agent runs (shadow/act) actually execute; without it the Settings → AI Agents config UI is editable but nothing runs. |
| `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED` | `false` | #4155 | Sub-flag of `BREEZE_AI_AGENTS_ENABLED`; gates the policy-decide unattended Tier-3 approval lane. Flag off = byte-identical to Part A (#4154), per PR body. |
| `BREEZE_WORKSPACE_ENABLED` | `false` (commented out) | pre-existing, consumed by #4112/#4129 | Whether the `ee/workspace` content-enrichment pipeline (now BYOK-aware) runs at all. |
| `ENROLLMENT_KEY_DEFAULT_TTL_MINUTES` / `CHILD_ENROLLMENT_KEY_TTL_MINUTES` / `INSTALLER_BOOTSTRAP_TOKEN_TTL_MINUTES` | `43200` (30 days, was 60/1440/1440) | #4126 | Env-fallback defaults for enrollment key / installer bootstrap TTLs (self-host deployments that already set these keep their own values). |


### Groups for sweep dispatch

1. **Settings/Partner + SSO** — #4071 (SSO enforce lockout preflight), #4080 (link-on-first-SSO-login), #4112 (workspace BYOK, nominal UI), #4113 + #4130 (LLM Provider Catalog admin page), #4116 + #4118 (BYO-LLM partner endpoint selection UI + AI Usage note). Needs: platform-admin login for the catalog page, partner-scope login for the AI Provider tab, an SSO provider fixture, `LLM_PROVIDER_CATALOG_ENABLED=true`.
2. **Organizations lifecycle** — #4082, #4111 (foundations, no standalone UI), #4114 (merge UI), #4128, #4132 (archive UI). Needs: partner-scope login + MFA enrolled on the account, a second org to merge into.
3. **AI agents / workspace** — #4146+#4148 (act mode config UI), #4154+#4155 (policy-decide unattended lane UI), #4157 (execution-trace Agent Runs UI), #4102 (edition-migration script in library, adjacent). Needs: `BREEZE_AI_AGENTS_ENABLED` (and `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED`) to see anything execute; runs list will be empty without fixture/live runs.
4. **Devices/remote/scripts** — #4126 (enrollment key defaults), #4161 (RemoteTerminal race + locale), #4102 (Scripts → Import from Library). Needs: a live/online agent for #4161's actual reconnection behavior (BLOCKED on pure offline fixtures — everything else in this group is testable offline).
5. **Alerts/webhooks/eventing (mostly backend, thin UI)** — #4083, #4098, #4099 (alert resolve CAS — the one with a real click-path), #4117. Needs: at least one active alert fixture; race conditions need two concurrent tabs/requests to actually exercise.
6. **Infra/no-UI (skip or ops-verify only)** — #4066, #4070, #4087, #4091, #4096, #4097 (flags off), #4110, #4115, #4129, #4131, #4133, #4138, #4140, #4145, #4158. No browser click-path; smoke-test that login, alerts, webhooks, and AI sessions still work end-to-end after these land, and confirm the two new flags in #4097 default off.

## Sweep log (append as you go)

### [Env — stack bring-up] — main session, 2026-08-28 09:20–09:45

#### [#4113/#4116 — LLM_PROVIDER_CATALOG_ENABLED] — FAIL (fixed)
- ❌ BUG: flag is accepted by `apps/api/src/config/validate.ts:804` but never mapped in the `api` `environment:` block of `docker-compose.yml` nor documented in `.env.example`. Compose only interpolates listed vars → a self-hoster's `.env` value is silently ignored; the BYO-LLM catalog UI (#4116/#4118) can never be reached on a compose deploy. `envComposeParity.test.ts` did not catch it. → fix on this branch (see Fixes applied).

#### [dev stack — ee/workspace vs extension-sdk mounts] — FAIL (fixed)
- ❌ BUG: `pnpm wt-stack up` (no `--rebuild`) on current main crashed the API at boot: `SyntaxError: The requested module '@breeze/extension-sdk' does not provide an export named 'ExtensionAiError'` (`ee/workspace/src/hostTypes.ts:31`). `docker-compose.override.yml.dev` host-mounts `apps/api/src`, `packages/shared/src` and `ee/workspace/src` but **not** `packages/extension-sdk/src`, so the mounted workspace code ran against the SDK baked into an older image. Any SDK change (here #4129's `ExtensionAiError`) silently requires `--rebuild` while sibling packages hot-reload. → mount added (see Fixes applied).
- ⚠️ UI/UX (dev tooling): wt-stack reports only `dependency failed to start: container … api-1 is unhealthy`; the real error is three `docker logs` calls away.

### [Group — area] — agent, timestamp

#### [#PR / area] — PASS | PARTIAL | FAIL | BLOCKED
- ✅ what was checked, concretely
- ❌ BUG: symptom → API actual (status + body) vs UI actual
- ⚠️ UI/UX: paper cut (also copy to the list below)
- BLOCKED: prerequisite

## UI/UX paper cuts

| # | Where | Observation | Severity | Disposition (fixed <sha> / issue # / noted) |
|---|---|---|---|---|

## Fixes applied

| Commit | PR/area | What | Test |
|---|---|---|---|
| `80f43722a` | #4113/#4116 compose parity | Map `LLM_PROVIDER_CATALOG_ENABLED` in `docker-compose.yml` + `deploy/docker-compose.prod.yml`, document in `.env.example` | `envComposeParity.test.ts` +3 (RED with mapping removed, GREEN 14/14). Note: parity test is one-directional (`.env.example` → compose); ~24 validated-but-undocumented keys remain (hosted-only `STRIPE_*`, `QBO_*`, …) — separate initiative. |

## Issues filed

| Issue | Title | From row |
|---|---|---|

## Summary

| Group | PASS | PARTIAL | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|

**Top findings:**

**Oldest PR reached:** #…

**Before the release cut:** flags to enable, BLOCKED rows needing a live agent, open issues.
