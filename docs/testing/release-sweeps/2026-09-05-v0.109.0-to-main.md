# Pre-release sweep — v0.109.0 → origin/main (2026-09-05)

| | |
|---|---|
| Range | `v0.109.0` (`2d35e5f5dd`, 2026-09-01) → `origin/main` `78eae279f` |
| Gate | PRs waited on: none (open non-draft #4902 #4903 #4904 are other-session, #4624 held); main CI green on 78eae279f; known-noise: none dismissed |
| Stack | `pnpm wt-stack up` on `qa/sweep-post-v0.109.0` @ `78eae279f`, baseUrl `http://localhost:33028` (project `breeze-wt-qa-sweep-post-v0-109-0`, 673 migrations applied, admin promoted to platform admin) |
| Flags enabled | `BREEZE_WORKSPACE_ENABLED=true`, `BREEZE_AI_AGENTS_ENABLED=true`, `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED=true`, `LLM_PROVIDER_CATALOG_ENABLED=true` (api recreated after inventory) |
| Driver | skill `pre-release-sweep`; sweep agents: opus (browser), sonnet (inventory, fixes) |

## Pre-sweep findings (before any browser work)

- **RLS-blind migrations in range (3):** `2026-10-06-100101-authenticator-attestation-state.sql`, `2026-10-08-100600-audit-retention-manage-permission.sql`, `2026-10-08-100700-audit-retention-policies-org-unique.sql` run DML on FORCE-RLS tables with no `set_config('breeze.scope','system',true)`. Silent 0-row on managed PG (doadmin). Same class as #4483. Fix-forward replay migration `2026-10-09-000600-rls-scoped-replay-v0110.sql` + `REPLAYED_BY` assertion in the existing contract test (#4866): **PR #4926**. **Premise downgrade:** `apps/api/scripts/check-migrations-nonsuperuser.ts` models `doadmin` WITH `BYPASSRLS` (measured: without it the set aborts at `2026-05-22-snmp-multi-vendor-templates.sql` 42501), and 122 unscoped-INSERT migrations have never aborted hosted prod — so on hosted the replay will print all-zero counts. It still protects self-hosters with a NOBYPASSRLS migrator. Not a release blocker; merge on green for the evidence trail.
- **Possible v0.109.0 regression, same class:** `2026-09-25-b-cross-site-restore-permission.sql` and `2026-09-27-technician-ticket-write-permissions.sql` use unscoped `INSERT ... SELECT FROM roles`. Read-only prod check script at `~/Documents/breeze-release-checks/prod-rls-grant-check.sql` (reports `rolbypassrls` + missing grants); replayed by #4926 regardless.

## Change inventory

Status ∈ `TODO | PASS | PARTIAL | FAIL | BLOCKED | N/A`. Every row must leave `TODO`.

### New / changed environment variables

Sourced from `git diff v0.109.0 origin/main -- apps/api/src/config .env.example docker-compose.yml`:

| Variable | Default | Notes |
|---|---|---|
| `BREEZE_AUTHENTICATOR_ATTESTATION_ENFORCED` | unset (off) | Optional boolean (`true/false`, `1/0`, `yes/no`, `on/off`). L4 platform-bound-passkey attestation gate (#1374). Malformed value throws at startup. |
| `CADDY_LOCAL_CERTS` | unset (Let's Encrypt) | Set to literal `local_certs` when `BREEZE_DOMAIN` is unreachable by Let's Encrypt; Caddy issues from its internal CA instead. |
| `SMTP_TIMEOUT_MS` | `30000` | Inactivity timeout for the SMTP transport (#3905). Range 1000–600000ms. |
| `MAILGUN_TIMEOUT_MS` | `120000` | Whole-request deadline for the Mailgun API call (includes attachment upload). |
| `PARTNER_API_ENROLLMENT_KEY_MAX_TTL_MINUTES` | unset (365-day ceiling) | Partner API enrollment-key mint TTL cap; may only narrow, never widen. |
| `PARTNER_API_ENROLLMENT_KEY_WRITE_RATE_LIMIT` | `10`/hr | Per-service-principal mint rate limit. |
| `PARTNER_API_ENROLLMENT_KEY_WRITE_PARTNER_RATE_LIMIT` | `100`/hr | Partner-wide mint rate limit (charged alongside the per-principal bucket). |
| `AGENT_LOG_RETENTION_DAYS` | `7` (clamped 1–365) | `agent_logs` row pruning window (#3109 redaction hardening). |
| `AGENT_LOG_RETENTION_BATCH_SIZE` | `10000` | Prune batch size. |
| `AGENT_LOG_RETENTION_MAX_BATCHES` | `200` | Prune batches per run. |
| `TICKET_OUTBOX_RETENTION_DAYS` | `14` | Outbox retention (#4210). |
| `TICKET_OUTBOX_RETENTION_BATCH_SIZE` | `5000` | |
| `TICKET_OUTBOX_RETENTION_MAX_BATCHES` | `50` | |
| `INTENT_OUTBOX_RETENTION_DAYS` | `14` | |
| `INTENT_OUTBOX_RETENTION_BATCH_SIZE` | `5000` | |
| `INTENT_OUTBOX_RETENTION_MAX_BATCHES` | `50` | |
| `METRIC_ANOMALY_INCIDENT_RETENTION_DAYS` | `14` | |
| `METRIC_ANOMALY_INCIDENT_RETENTION_BATCH_SIZE` | `5000` | |
| `METRIC_ANOMALY_INCIDENT_RETENTION_MAX_BATCHES` | `50` | |
| `QBO_CLIENT_ID` / `QBO_CLIENT_SECRET` | unset (dark) | QuickBooks Online OAuth app credentials (Phase C/D). |
| `QBO_REDIRECT_URI` | unset | Must match the Intuit app's registered redirect URI; must be this deployment's public API URL. |
| `QBO_ENVIRONMENT` | unset (`production` when set) | `sandbox` or `production` Intuit API host. |
| `QBO_WEBHOOK_VERIFIER_TOKEN` | unset | Without it, `POST /api/v1/webhooks/quickbooks` answers 503 and payment pull-back relies solely on the 15-minute sweep. |

Also changed (not a new app-facing var, but a default-safety fix worth flagging for self-hosters): the M365 executor JWK secret file default moved from `/dev/null` to a tracked empty file `./docker/secrets/.empty-jwk` (#2991) — bind-mounting `/dev/null` read-only broke `docker compose up` on some kernel/storage-driver combos.

---

### Group 1 — Billing, Quotes, Contracts & Accounting (11 rows)

| # | PR(s) | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 1 | Device-set billing #3205 (W01–W07): #4585, #4694, #4745, #4808, #4854, #4731, #4838, #4901, #4706 (docs) | web | Settings → Organizations → an org → **Contracts** tab (`/settings/organizations/[id]#contracts`) → open/add a contract line billed by **device role** or **device group**; then Billing → Contracts → open a contract → generate an invoice. | Contract line editor shows a device-role/device-group picker and included-quantity/overage fields; toast **"Line updated"** on save. Generating an invoice with uncovered devices shows the toast **"Invoice generated, but N device(s) are not billed by any line on this contract: …"**; the contract detail page renders a coverage warning (`DeviceCoverageNotice`, testid `contract-coverage-warning`) with deep links into Devices filtered by role. Quote editor (Billing → Quotes) shows device-set lines with a **"Refresh counts"** button and a drift toast ("N line(s) no longer match the live device count"). | None — seeded org with devices + a contract works. | TODO |
| 2 | QuickBooks Phase C + D: #4492 (Phase C invoice push), #4531 (Phase D payment pull-back), #4800, #4795, #4827, #4537 (docs), #4509 (docs) | web | Settings → **Integrations** (`/integrations`) → Accounting tab → **quickbooks** sub-tab (`QuickbooksIntegration.tsx`); also Billing → Invoices → select unsent invoices → bulk action **"Push to QuickBooks"**. | Integrations page shows the QuickBooks connect/reconcile card with a **"Payment sync"** control (`quickbooksIntegration.pullPayments`); bulk-pushing invoices shows the toast **"1 queued for QuickBooks"** (or the bulk-limit warning **"Select up to N at a time."**); invoice detail shows **"Invoice pushed to QuickBooks."** on success. | **BLOCKED without live QuickBooks OAuth app** (`QBO_CLIENT_ID`/`QBO_CLIENT_SECRET`/`QBO_REDIRECT_URI` unset ⇒ integration reports "not configured"). Reconcile-skip-reason and voided-invoice-block logic can be checked without a live connection by inspecting the invoice detail UI copy. | TODO |
| 3 | #4802 | web | Billing → Quotes or Invoices → open a **draft** quote/invoice → currency selector/dialog. | Currency-change dialog opens (`copy.title`); on confirm, toast **"Quote currency changed to {currency}."** / **"Invoice currency changed to {currency}."** | Draft-status quote/invoice only (change-currency UI is gated off non-draft docs). | TODO |
| 4 | #4819 | web | Billing → Quotes → open a quote → reload mid-edit (force a re-render, e.g. resize/hot-reload). | Draft line values are reseeded during React render, not from a passive `useEffect` — no stale/duplicated line values after a re-render. No new visible text; regression-only. | None | TODO |
| 5 | #4816 | web | Billing → Quotes → open a superseded quote (`GET` after acceptance); Tickets → open a ticket → Resolve dialog note field. | No visible UI change — deflakes two existing tests (`QuoteWorkspace` superseded-GET race, `TicketWorkbench` resolve-note prefill race). Verify: opening a superseded quote still shows correct read-only state; ticket resolve-note field still prefills. | None | TODO |
| 6 | #4698 | web | Billing → Quotes → open a quote with several lines at browser width **1280px** with the right-hand sidebar open. | Quote editor's line grid no longer clips/scrolls sideways at 1280px with the sidebar open — full grid visible without horizontal scroll. | Resize viewport to exactly 1280px wide. | TODO |
| 7 | #4397 | web | Billing → Quotes → open a quote → add a new line while a background quiet-refetch is in flight, then force that refetch to fail (e.g. toggle network offline briefly). | Instead of losing the newly-added line, shows the warning toast **"Line added, but the list could not refresh. Reload the page to see it."**; a successful catalog save shows **"Saved to catalog"**. | None | TODO |
| 8 | #4408 | web | Billing → Quotes → line item image upload → pick a `.webp` file. | Upload is rejected client-side (and server-side) with a validation error; no WebP image reaches the quote line. | A `.webp` test image. | TODO |
| 9 | #4639 | web | Settings → **Catalog** (`/settings/catalog`) → catalog item → image upload → pick a `.webp` file. | Same WebP rejection as quote images, applied to catalog item images at the upload boundary. | A `.webp` test image. | TODO |
| 10 | #4634 | web | Billing → Quotes/Invoices → with a partner-level currency set, open a quote/invoice, then trigger a partner currency reset (Settings → org's Billing currency) while the doc is open. | The mounted approximate-total line refetches and reflects the new currency instead of showing a stale total. | Two browser contexts or a quick org-currency change while the quote tab stays open. | TODO |
| 11 | #4636 | web | Settings (`/settings`) → left sidebar. | **"Billing"** now appears as a section in the Settings sidebar (previously missing/orphaned link). | None | TODO |

### Group 2 — Devices, Patching & Reboot (9 rows)

| # | PR(s) | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 1 | Reboot deferral #3207 (W1–W5): #4686, #4699, #4728, #4733, #4737, #4666 (docs) | web (+agent) | Configuration Policies (`/configuration-policies/[id]`) → **Patch** tab → toggle **"Let users postpone the restart"**; then Devices → a device with a pending reboot → device console. | Toggling on reveals **"Maximum postponements"** and **"Postpone by (minutes)"** number fields. On a device with a scheduled restart, the console renders a `RebootScheduledBadge` showing deferrals-used/max and the scheduled time. | **BLOCKED for the end-user prompt itself** — the actual Windows/macOS/Linux reboot-prompt-with-deferral UI (W2–W4) requires a live enrolled agent to receive and act on the reboot command; only the policy-settings UI and the heartbeat-driven console badge (W1, W5) are checkable from a seeded stack alone. | TODO |
| 2 | Unrated patch severity handling: #4721 (web badge), #4787 (api evaluator guards) | web | Fleet → **Patches** (`/patches`) → severity filter dropdown; a patch with no CVSS rating. | Severity filter offers an **"Unrated"** option; a null-severity patch row shows an **"Unrated"** badge instead of being coerced to "Low," with a note that it will not auto-approve. Sorts last after Low. | A patch with `severity: null` in the seed data. | TODO |
| 3 | Windows-installer hardening: #4730 (force TLS 1.2), #4688 (Windows 10/Server 2016 OS floor) | web | Devices → **Add Device** / enrollment flow → copy the generated Windows PowerShell install one-liner. | The generated `Invoke-WebRequest` one-liner now pins `[Net.ServicePointManager]::SecurityProtocol = Tls12` and declares Windows 10 / Server 2016 as the supported floor in the accompanying copy. | None (just inspect the generated command text) | TODO |
| 4 | #4842 | web | Devices → a device → **Activities** tab. | Activity/audit list is capped at 10,000 rows instead of unbounded — on a device with a very large audit history, the tab loads instead of timing out. | A device with 10k+ audit rows to see the cap trigger; otherwise just confirm the tab loads normally. | TODO |
| 5 | #4832 | web | Devices → a device with an existing **live remote session** → click **"Connect Desktop"** to start a *second* session. | Starting a new Connect Desktop session no longer silently revokes every other live remote session for that device — only the superseded session (if any) is affected. | Two remote sessions open concurrently (or two browser tabs) against the same device. | TODO |
| 6 | #4783 | web | Devices (`/devices`) → **Advanced filter** → author a filter, then force the preview call to fail (e.g. malformed filter or backend hiccup). | Instead of silently showing a stale/wrong device list, shows the toast **"Advanced filter failed to load — showing no devices. Edit or clear the filter to try again."**, and the list fails closed (empty) rather than open. | None | TODO |
| 7 | #4701 | web | Devices → a device → **Patches** tab. | Tab shows a **"Manage patches"** link to `/patches`, and — when the device has an assigned patch policy — a second link with the policy's own name (e.g. "Standard Patch Ring") to `/configuration-policies/{policyId}`. Falls back to the raw policy id as the link label if the name is missing; no crash if the effective-config fetch fails. | Device with (and without) an assigned patch policy, to see both branches. | TODO |
| 8 | #4695 | web | Devices → a device detail page → breadcrumb showing the org name. | Org name in the device breadcrumb is now a link to `/settings/organizations/{orgId}` instead of static text. | None | TODO |
| 9 | #4394 | web | Devices → a device → row action **"Permanent Delete"** → confirm, on a device the agent can't currently reach. | Toast surfaces the agent-unreachable warning explicitly instead of a generic failure. | A device whose agent is offline/unreachable. | TODO |

### Group 3 — Tickets, AI Agents & Workspace (12 rows)

| # | PR(s) | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 1 | AI agents phase 2 — graduation & impact #4187: #4490, #4533, #4565, #4486, #4504, #4581 (docs), #4595 (fix) | web | Settings → **AI Agents** (`/settings/ai-agents`) → edit an agent → **Graduation** panel; then AI Agents → **Impact** (`/ai-agents/impact`). | Graduation panel section titled **"Graduation"** lists per-operation evidence with a **"Promote"** button (enabled once `state === 'eligible'`) and a **"Revoke"** button on promoted rows (requires a reason). Impact page shows tiles: "Alerts judged," "Flagged as noise," "Tickets triaged," "Drafts sent," "Fixes executed," "Estimated time saved," "LLM spend," plus a **"Promotion-eligible operations"** tile and a PDF export. | manage_ai_agents permission; feature is fail-closed if `policyDecideEnabled` is off (Promote button hidden, "Pre-authorized execution is turned off for this deployment; these figures are informational" note shown instead). Four-eyes promote requires a second approver account. | TODO |
| 2 | AI budget alerts #4388 (W01–W04): #4487, #4496, #4517, #4529, #4805 (render-seed fix) | web | Settings → **AI Usage** (`/settings/ai-usage`) → **Budget alert thresholds** section; also visible on `/ai/usage`. | Threshold-ladder inputs render pre-seeded from server data on first paint (no flash of defaults); usage page shows **"Breeze AI credits remaining"** label and partner-level threshold fields with placeholder **"Not set, orgs configure individually"** when unset. | Partner-level AI usage/budget feature must be enabled for the org/partner. | TODO |
| 3 | AI agents UI critique passes: #4667, #4729, #4822 | web | AI Agents → **Impact** page and **Runs** list (`/ai-agents/runs`). | Impact page: refresh control aria-label toggles between **"Refresh"** and **"Rebuilding…"**; a **"More actions"** menu; six effective weights shown in an accessible always-reachable disclosure (not a hover-only `title=` tooltip) — toast **"The impact report has been rebuilt."** on refresh. Runs list: disable-agent confirm dialog titled **"Disable {name}?"**; a banner when **"All agents are disabled"**; re-enabling shows toast **"Agent re-enabled"**. Mobile runs list and policy-key labels also corrected (verify visually only; no separate route). `c715d99c20` is test-scoping only — no product change. | None | TODO |
| 4 | #4704 | web | Platform admin → `/admin/ai-kill-switch`. | Page titled **"AI Kill Switch"**; flipping it shows toast **"AI actions killed platform-wide."** / **"AI actions restored platform-wide."**; a confirm dialog requires a reason (**"Describe the incident or reason for this change…"**); non-platform-admins see **"Platform-admin access required"** instead of the control. | **Platform-admin role required.** | TODO |
| 5 | #4691 | web | Tickets → AI triage batch view → a ticket the AI could not draft for. | Skip reasons are now surfaced on the card instead of the ticket silently disappearing from the batch; drifted cards auto-deselect from the batch action. | An org with AI ticket-triage enabled and at least one triage-ineligible ticket. | TODO |
| 6 | #4683 | web | Tickets → a ticket with an AI-drafted reply → click the draft card's action (approve/discard) rapidly / from two tabs. | The action lock is now scoped to that draft's own id — acting on one draft no longer locks/blocks the action button on a different draft card. | Two AI draft cards on different tickets (or two tabs on the same ticket). | TODO |
| 7 | #4702 | web | AI **Workspace** (`/workspace`) → chat tab. | Double-clicking a chat tab lets you rename it (aria-label **"Rename chat"**, hint **"Double-click to rename"**). | None | TODO |
| 8 | #4687 | web | AI Agents → Shadow mode view; Approvals inbox. | Shadow-mode partner-key rows show a ceiling-hint tooltip; the approvals inbox collapses multiple links pointing at the same intent into one; a client-side guard caps batch-approve size (`BATCH_MAX`). | Partner-level AI agent key configured in Shadow mode. | TODO |
| 9 | #4705 | web | Alerts (`/alerts`) → an alert with existing operator feedback (thumbs up/down). | Alert verdict badge now shows **who** gave the feedback (`feedbackByName`) directly on the badge instead of requiring a hover; badge omits the feedback-by label if the name can't be resolved (e.g., deleted user). | An alert that already has feedback recorded. | TODO |
| 10 | #4868 | web | Approvals inbox (wherever AI-agent approvals surface, e.g. Workspace sidebar). | Refactor only — batch-grouping rule and inbox helpers moved to `@breeze/shared`. No visible UI change; verify the approvals inbox still groups/renders identically to before. | None | TODO |
| 11 | #4875 | web | Anywhere `SweepProposalReason` renders (alerts/AI sweep proposal copy). | Refactor only — type promoted to `@breeze/shared`. No visible UI change; spot-check that sweep-proposal reason text still renders correctly wherever it appears. | None | TODO |
| 12 | iOS papercuts #4916 (defect #5 of 7 in the PR is the only browser-testable piece; defects 1-4/6/7 are mobile-only, see "Not browser-verifiable") | web | AI **Workspace** (`/workspace`) chat → send a message that triggers an approval-required tool (e.g. `execute_command`) → approve/deny the pending request from a second session (mobile app or a direct API call) while the web chat tab stays open. | `processStreamEvent` now clears `pendingApproval` when the `tool_result` for the awaited tool arrives over the open SSE stream — the web chat's pending-approval card disappears once the OTHER session decides, instead of staying stuck showing a decision that already happened. | **BLOCKED for full verification without a second session/device to approve from** — needs an approval-required AI action plus a way to resolve it out-of-band. | TODO |

### Group 4 — Org, Contacts, Settings, Audit & Trust (12 rows)

| # | PR(s) | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 1 | Org contacts #3258 (W02–W04 + follow-ups): #4530, #4557, #4546, #4590, #4593, #4594 | web | Settings → Organizations → an org → **Contacts** tab (`/settings/organizations/[id]#contacts`). | Tab titled **"Contacts"** with buttons **"Add contact"** and **"Import CSV"**; imported/added contacts list with **"Edit"**/**"Delete"** row actions, save button **"Save contact"**. Sending a portal invite to an email matching multiple contacts shows the warning toast (ambiguous-contact copy); a clean match shows toast **"Invite sent"**. | None | TODO |
| 2 | Audit log retention #4633/#4824: #4825, #4900 | web | Settings → Organizations → an org → **Audit Retention** tab (`/settings/organizations/[id]#audit-retention`). | New nav entry **"Audit Retention"** ("How long audit logs are kept"); saving settings shows toast **"Audit retention policy saved."**; a newly-created org already has a seeded retention policy (no need to configure it before it takes effect). | None | TODO |
| 3 | Partner trust probation #4549 (W02–W07): #4567, #4588, #4599, #4603, #4602, #4604, #4558 (docs) | web | Platform admin → `/admin/trust-queue` (**"Partner Trust Queue"**); a probation email link → `/admin/trust/act?token=…` (**"Partner trust action"**). | Trust queue lists partners in probation with identity/billing evidence tables ("User name," "Cardholder name," "Names match," "Billing region," etc.); acting on a queued partner shows toast **"Partner suspended"** or generic **"Trust action completed"**, and the standalone action page renders **"Trust action completed"** with a partner summary (name, slug, plan, trust state, signup IP class). | **Platform-admin role required** for the queue; the tokenized `/admin/trust/act` page is reachable without login (magic-link style) but needs a real probation record seeded. | TODO |
| 4 | Portal visibility #4562 (W01–W10 + QA/critique fixes): #4626, #4627, #4647, #4740, #4744, #4749, #4750, #4734, #4804, #4815, #4598 (docs), #4836 (QA fix), #4850 (critique) | portal | Log into the customer **Portal** (`apps/portal`, separate origin from `apps/web`) → `/dashboard`, `/security`, `/backups`, `/devices`, `/tickets`, `/reports`. | Dashboard (`PortalLayout title="Dashboard"`) shows tiles: "Security score," "Devices protected," "Patches applied this month," "Last backup verified," "Support," "Action items," "Awaiting you," with lede "Current as of …". Security page (`h1: "Security"`) lists per-device AV/firewall/encryption columns. Backups page lists "Last restore point," "Last test restore," "Open breaches," "Readiness" per device. Devices page adds "Last patch," "Protection," "Encryption," "Last backup," "Warranty ends" columns + CSV export. Tickets show an SLA badge (testid `portal-ticket-sla-badge`) and a "Support usage" panel with "Billed"/"To be billed"/"Covered by contract"/"Pending review." Reports tab (`title="Reports"`) lists/generates/downloads PDF/CSV with an empty state **"No reports yet."** All of it must be **fail-closed per-toggle**: MSP disables a visibility toggle in Settings → Organizations → org → **Customer Portal** tab (`#portal`, `OrgPortalSettingsEditor`) and the corresponding portal page/nav item must disappear for that org's portal users, not just hide client-side. | A seeded portal user account + org with backups/security/ticket data. MSP-side visibility toggles must be exercised from `/settings/organizations/[id]#portal` in the main web app to confirm fail-closed behavior. | TODO |
| 5 | Auth rejection error-code hardening: #4739, #4651, #4820, #4782 | web | Login/Account pages: change-password form with wrong current password; account-deletion confirm with wrong password; MFA challenge with a bad code; repeated wrong-password guesses. | Wrong current password on change-password / account-deletion now returns **400 with a stable error code** (not a 401 that could be mistaken for a dead session); a rejected MFA proof is 400 + stable code, not 401; repeated guesses are rate-limited rather than just repeatedly 401ing. | None | TODO |
| 6 | #4860 | web | `/login` at a partner that enforces SSO. | Login page collapses the password form behind the SSO button when the partner's `enforceSSO` is true, with an explicit way to reveal the password form if needed. | An organization/partner configured with `enforceSSO: true` in seed data. | TODO |
| 7 | #4853 | web | `/login` → trigger an SSO failure (e.g. cancel the IdP flow) right as a token refresh is in flight. | The SSO failure notice now survives (isn't clobbered by) a concurrent refresh-token eviction — the error message stays visible instead of silently disappearing. | Ability to force an SSO failure + concurrent refresh (timing-sensitive; may need to simulate). | TODO |
| 8 | #4869 | web | Settings → Organizations list (`/settings/organizations`) → an org mid-archive/offboarding. | Archiving org stays visible in the list for its entire drain window with an **"Archiving…"** badge, then **"Archived"** once complete — instead of disappearing early. | An org actively mid-archive (may need to trigger org offboarding first). | TODO |
| 9 | #4877 | web | Security → **PAM** rules tab → a PAM rule pinned to a risk tier no longer offered by the current tier set. | Rule row shows a **"Stale"** badge with a tooltip explaining the pinned tier no longer matches anything selectable. | A PAM rule seeded against a risk tier that's since been removed/renamed. | TODO |
| 10 | #4870 | web | Settings → **Extensions** → an extension detail page (`/extensions/[name]`) that ships a dynamically-imported asset. | Extension asset URLs are signed so `import()` can load them; previously-broken dynamic imports now load without a console/network error. | An installed extension with a dynamic-import asset. | TODO |
| 11 | #4646 | web | Account → Security → MFA → rotate recovery codes, in a second browser tab holding the same session. | Rotating recovery codes re-issues the caller's own session so the tab that rotated them isn't logged out. | Two tabs signed into the same account. | TODO |
| 12 | #4405 | web | Settings → Organizations → org → **Audit Retention**/Activity log view → look at an `agent.command.*` audit row right after dispatch (before completion). | Audit row for a just-dispatched agent command no longer shows "success" prematurely at dispatch time — status reflects actual completion. | A command dispatched to a live/simulated agent, checked immediately after dispatch. | TODO |

### Group 5 — Scripts, Custom Fields & Remote Tools (8 rows)

| # | PR(s) | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 1 | Script custom-field write-back: #4917 (api validate), #4692 (api ingest), #4722 (web toggle + panel), #4703 (help tooltip) | web | Scripts → a script → **Parameter Binding** section (bind a parameter to "From a device custom field"); also Settings → **Custom Fields** (`/settings/custom-fields`) → a field's **Field Key** input. | A hover-independent help affordance (`HelpTooltip`, role="tooltip") next to the binding/Field Key explains: "Read: … delivered to the script like a normal parameter… Write: … call the device's custom-fields API with a PATCH request and an API key." Run-detail panel shows the write-back toggle. | None | TODO |
| 2 | Script cancellation #3525 (W01-W02): #4788, #4799 | web | Scripts → **Executions** → start/observe a running execution → cancel action; execution history list/detail status badges. | Status badges/maps now cover every execution and run status including **Cancelling** (amber, spinner icon) and **Cancelled** (muted, Ban icon) — previously some statuses fell through to a default. A `script_cancel` command is now registered so cancelling actually asks the agent to stop the run, not just marks it cancelled server-side. | **BLOCKED for the actual stop-in-progress behavior** — confirming the running script process is killed requires a live connected agent; the status-badge/map UI (W01) is checkable without one. | TODO |
| 3 | #4906 | web | Scripts → run a script → run picker. | Run-context controls show labels **"Run as"** and **"Target session"** wherever a run context is selectable (previously inconsistent/hidden in places). | None | TODO |
| 4 | #4897 | web | Scripts (`/scripts`) → a script row → row action. | Row action titled **"Duplicate script"** clones the script; failure shows toast **"Failed to duplicate script"**. | None | TODO |
| 5 | #4895 | web | Scripts → **Executions** → a past execution → click **"Run again"**. | Re-opens the execute flow pre-filled with the same device(s) and parameters from that past execution; on submit, navigates to the new execution and shows a success toast. | A past script execution to re-run from. | TODO |
| 6 | #4893 | web | Remote → **Terminal** on a cold/first connection where the xterm stylesheet preload fails (e.g. throttled network). | Terminal still mounts and is usable (console warns "Terminal stylesheet failed to load; continuing with an unstyled terminal") instead of crashing the pane. | Throttle network in devtools to force the stylesheet preload to fail/timeout. | TODO |
| 7 | #4641 | web | Remote → **Tools** (`/remote/tools`) → switch between sub-tabs (Terminal/Files/etc.), then reload the page. | The active Remote Tools sub-tab persists in the URL hash across a reload; the terminal pane fills the available viewport instead of leaving dead space. | A connected/simulated remote session. | TODO |
| 8 | #4791 | web | Configuration Policies → a **partner-wide** custom-field policy tab; a dropdown-type custom field's options editor. | Partner-wide custom-field UI is now properly gated (visible/editable only where appropriate); dropdown option entries save with `{label, value}` pairs correctly instead of a malformed contract. | A partner-wide (not org-scoped) configuration policy. | TODO |

---

### Not browser-verifiable (~190 commits)

api-only, agent-only, mobile-only, and docs/chore commits cannot be exercised through a Playwright browser sweep of `apps/web`/`apps/portal`. Clustered by theme below with sub-PRs and how each IS actually verified.

#### API — tenancy / org-move / org-erasure FK integrity sweep (14 PRs)
#4908, #4898, #4857, #4863, #4879, #4833, #4831, #4821, #4823, #4741, #4736, #4786, #4726, #4642

A long-running sweep making every FK that references a ticket/device/org row behave correctly across org-move, org-merge and org-erasure (sever, tombstone, re-stamp, or cascade, per column). **Verified via:** `apps/api` FK-ON-DELETE contract tests (#4519 "contract-test FK ON DELETE actions against org erasure", #3880 "end-to-end coverage for cascadeDeleteOrg breadth and failure semantics") and the migration-replay guard added in this same sweep (#4896, "preserve current functions after migration replay").

#### API — site/partner-scope RLS & config-policy enforcement (8 PRs)
#4846, #4847, #4848, #4855, #4835, #4803, #4690, #4493

Closes several site-scope and partner-wide RLS escalation gaps (config-policy assignment reads, DR plan mutations, maintenance windows, remote-access launcher partner read, device-activity-feed RLS-promotable predicate). **Verified via:** `apps/api` RLS integration suite (site-scope-coverage.integration.test.ts and friends).

#### API — AI agent session lifecycle & notification correctness (9 PRs)
#4892, #4801, #4796, #4785, #4742, #4697, #4640, #4684, #4406

Session eviction/staleness races (Anthropic + OpenAI providers), MCP tool-name allowlist enforcement, dedup of intent-outcome notifications, auto-demote notify-on-no-run, alert baseline fan-out suppression. **Verified via:** `apps/api/src/services/ai` unit suite (session eviction/staleness tests referenced in each PR's own description).

#### API — patch/backup/DR dispatch & audit-chain reliability (7 PRs)
#4843, #4845, #4849, #4852, #4548, #4400, #4407

Stranded-patch flipping, silent-failure paths in patch dispatch, backup-dispatch retry duplication, bounded nightly audit-chain verification, and the new outbox/anomaly-incident retention/prune job (#4210, backing the `TICKET_OUTBOX_RETENTION_*` / `INTENT_OUTBOX_RETENTION_*` / `METRIC_ANOMALY_INCIDENT_RETENTION_*` env vars above). **Verified via:** patch-dispatch and audit-chain integration tests; the retention job has its own worker-role test suite.

#### API — QuickBooks/accounting internals (1 PR)
#4858 — accounting `captureException` tags dropped by the Sentry allowlist. **Verified via:** Sentry-tag unit test on the accounting error path.

#### API — remote-access / extensions / desktop protocol (3 PRs)
#4403, #4401, #4398 — split remote-access launcher availability from issuance, extension `activeVersion` recorded only after built-in activation succeeds, `desktopWs` input schema gap dropping paste-as-keystrokes on the WS fallback. **Verified via:** `desktopWs` schema tests, remote-access launcher unit tests.

#### API — ops/CI/migration guards (7 PRs)
#4866, #4896, #4878, #4689, #4591, #4409, #4478 — migration guard hardening, non-UTC TZ CI coverage extension, worker `/metrics` wired into Prometheus scrape config, migration-order pre-push guard, `docker-compose.monitoring.yml` composability, guided-setup.sh end-to-end CI run. **Verified via:** the CI jobs these PRs themselves add/fix (self-referential — check the named workflow is green on main).

#### API — miscellaneous correctness & hardening (39 PRs)
#4856, #4899, #4841, #4844, #4861, #4865, #4874, #4890, #4839, #4720, #4829, #4826, #4818, #4799, #4789, #4784, #4790, #4794, #4735, #4725, #4723, #4724, #4649, #4638, #4700, #4637, #4563, #4538, #4515, #4516, #4419, #4411, #4410, #4402, #4395, #4506, #4507, #4738, #2826

Grab-bag of one-off API fixes: mail-transport transaction ordering (#3905), PAM cleanup edge case, config-policy RLS branches, alert-rule template categories, script-dispatch actor-probe escaping, enrollment-key TTL defaults, the v0.109.0 sweep doc itself (#4507), AI-field-provenance stamping (#4738), and the new Partner API `POST /partner-api/v1/enrollment-keys` endpoint (#2826, gated by the `PARTNER_API_ENROLLMENT_KEY_*` env vars above). **Verified via:** each PR's own `apps/api` unit/integration test (named in the commit body); the Partner API endpoint has its own route test suite (not a browser surface — it's a service-to-service API).

#### Mobile app (iOS/Android) — not verifiable without a device/TestFlight build (19 PRs)
#4851 (api-side, mobile pagination), #4780 (Android FCM registration), #4779, #4727, #4696, #4589, #4579, #4568, #4566, #4560, #4541, #4536, #4508, #4534, #4396, #4393, #4494, #4488, #4793 (W02 challenge/verify mobile registration protocol, part of the platform-bound-passkey L4 work gated by `BREEZE_AUTHENTICATOR_ATTESTATION_ENFORCED` above)

Also the mobile-only defects (1–4, 6, 7) inside the **iOS papercuts (#4916)** row already listed in Group 3 above — approval-takeover overlay, duplicate-approval replay guard, per-request toast keying, keyboard dismiss/scroll on ticket screens, and camera-upload error-mapping — need a TestFlight build against a real device, not a browser. **Verified via:** `apps/mobile` vitest suite (each PR names its new test file) plus the next TestFlight build for anything gated on real device behavior (camera upload, Face ID, push notifications).

#### Go agent + native remote-desktop apps (viewer/helper Tauri) — needs a live enrolled agent (16 PRs)
#4914 (Homebrew cleanup), #4876 (#3109 scrub agent logs / helper session ids), #4859 (confirm delivery before advancing upload state), #4862 (macOS code-signing identity churn), #4864 (Caps Lock sync over sync instead of forwarding as a key), #4891 (script parameters on runAs=user helper path), #4840 (secrets.yaml rename retry), #4592 (PAM cleanup for never-provisioned elevation accounts), #4817 (go-m1cpu bump for Apple Silicon), #4781 (extract custom-field markers from script stdout), plus 6 Tauri dependency bumps in `apps/viewer`/`apps/helper` (#4814, #4813, #4812, #4811, #4810, #4809).

**Verified via:** the Go agent's own test suite (`go test ./...`) plus manual exercise against a live enrolled agent for anything touching the update/install/PAM lifecycle; the Tauri dependency bumps are covered by the viewer/helper build + existing e2e smoke tests, not new behavior.

#### Docs / CI / dependency / self-host chores — no application code path (14 PRs)
#4915, #4894, #4889, #4532, #4443, #4635, #4631, #4625, #4632, #4580, #4564, #4539, #4479, plus the portal dev-mode-only infra pair **#4540 + #4563** ("serve the dev module graph under /portal so islands hydrate" / "non-blocking portal dev-mode e2e job") — this last pair only affects `astro dev` hot-reload behavior and does not apply to a docker-compose/production-built stack, so it is not checkable in a seeded release-candidate environment regardless of surface.

**Verified via:** the CI checks each of these touches directly (Dependabot version bumps go green on their own PR checks; the self-host guided-setup and CADDY_LOCAL_CERTS backfill are covered by `ci(selfhost): run guided-setup.sh end to end on a runner` (#4478)); doc-only commits have no test to run — spot-check the rendered doc/CLAUDE.md page instead.

---

### How to use this document

Work top-to-bottom through Groups 1–5 with a seeded local stack (see `worktree-stack` skill) logged in as an org admin (web) and a portal user (portal) plus one platform-admin account for the rows marked as requiring it. Flip each Status to PASS/FAIL/BLOCKED as you go, and file a GitHub issue per FAIL with the row number and PR(s) it maps to. Rows marked BLOCKED in the Prereqs column need either a live agent connection, a mobile TestFlight build, or a real third-party OAuth app (QuickBooks) and should be explicitly called out as un-testable in this pass rather than silently skipped.


## Sweep log (append as you go)

## UI/UX paper cuts

| # | Where | Observation | Severity | Disposition (fixed <sha> / issue # / noted) |
|---|---|---|---|---|

## Fixes applied

| Commit | PR/area | What | Test |
|---|---|---|---|

## Issues filed

| Issue | Title | From row |
|---|---|---|

## Summary

| Group | PASS | PARTIAL | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|

**Top findings:**

**Oldest PR reached:** #…

**Before the release cut:** flags to enable, BLOCKED rows needing a live agent, open issues.
