# Pre-release sweep (follow-up) — `eee22184a` → main `c07c0a37f` (2026-09-15)

| | |
|---|---|
| Range | `eee22184a` (base of `2026-09-14b-6dddaad-to-main.md`, 2026-09-15 00:06Z) → `origin/main` `c07c0a37f` (#5932, 2026-09-15) — 33 commits |
| Gate | 31 open non-draft PRs against main (deps run #5844–#5858, AI sweeps W05 #5937, install W03 #5931, guardrails #5934, fence W06 #5922, tunnels #5907, installer #5900/#5901, security #5938, docs #5893/#5909/#5935, i18n #5886); none waited on, none merged |
| Stack | `pnpm wt-stack up` on `qa/sweep-post-v0.113.0-c` @ `c07c0a37f`, baseUrl `http://localhost:32813` (project `breeze-wt-qa-sweep-post-v0-113-0-c`) |
| Flags enabled | `BREEZE_AI_AGENTS_ENABLED`, `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED`, `BREEZE_AI_AGENTS_SWEEP_ACT_ENABLED` (new, #5913 — needed the compose mapping fix `ed47f0ce8` to reach the container), `BREEZE_AI_SCRIPT_AUTHORING_ENABLED`, `LLM_PROVIDER_CATALOG_ENABLED`, `MCP_BOOTSTRAP_ENABLED`, `MCP_OAUTH_ENABLED`, `WEBAUTHN_RP_ID=localhost`. Not enabled: `BREEZE_AI_WORKSPACE_ENABLED` (hosted-only, no sandbox backend locally) |
| New env vars in range | `BREEZE_AI_AGENTS_SWEEP_ACT_ENABLED` (default false), `SOFTWARE_INSTALL_REMEDIATION_MAX_PER_PASS` (50), `SOFTWARE_INSTALL_REMEDIATION_MAX_ATTEMPTS` (3) |
| Driver | skill `pre-release-sweep`; inventory: sonnet Explore; browser groups: opus, sequential; api-only rows: opus, SQL/curl |

## Change inventory

Status ∈ `TODO | PASS | PARTIAL | FAIL | BLOCKED | N/A`. Every row must leave `TODO`.

| PR | Merged | Surface | Title | Route / click-path | Expected visible outcome | Prereq | Status |
|---|---|---|---|---|---|---|---|
| #5932 | c07c0a37f | ci-test-chore | extend partner-export lock guard to configuration-material trigger family | n/a — vitest guard | suite green | — | N/A |
| #5933 | 2149fb909 | api-only | project install_remediation_* on GET /software-policies/violations | `GET /api/v1/software-policies/violations?policyId=`; `software_compliance_status.install_remediation_status/last_install_remediation_attempt/install_remediation_attempts` | response carries `installRemediationStatus`, `lastInstallRemediationAttempt`, `installRemediationAttempts` | seeded compliance row | PASS |
| #5921 | 6c4e58d88 | web | ticket-checklist wiring on deliverables (W03 #5811) | `/settings/deliverable-templates` item form `data-testid="deliverable-template-item-checklist-template"` / `-instructions`; `OccurrenceDrawer.tsx` | template item accepts checklist template + instructions; portal never renders `instructions` | seeded deliverable template; nightly sweep for ticket creation | PARTIAL |
| #5919 | d3862e99e | web | execution plane W05 — chat integration, artifact surfaces | `RunDetailPage.tsx` artifact link; `POST /reports/runs`; `ticket_attachments`/`reports` reference cols | artifact attaches by reference | `BREEZE_AI_WORKSPACE_ENABLED` + hosted sandbox | PARTIAL |
| #5920 | 9aacc2c1c | web | AI patch agent W04 — reactive routing & reboot planning | `/ai-agents/runs/[id]` reboot-plan item (window, redundancy group, refusal copy) | patch-classified alert routes to patch agent; run trace shows reboot plan | patch agent + alert + maintenance window | PASS |
| #5913 | 299055c07 | web | AI sweeps act mode W04 — the act gate | `/settings/ai-agents` → `AiAgentSchedulesSection.tsx` `data-testid="ai-agent-schedule-act-mode"` / `-act-mode-disable` / `-act-mode-disabled-hint` | Act mode toggle on a sweep schedule; disabled hint when off | `BREEZE_AI_AGENTS_SWEEP_ACT_ENABLED` + `POLICY_DECIDE` | PASS |
| #5915 | d654135c7 | agent-only | durable desktop start fence + helper IPC generation + resync (SEC-038 W05) | RD session lease renew over agent WS (`remoteRevocationLease.ts`, `agentWs.ts`) | lease renews / self-stops | live agent + RD session | TODO |
| #5917 | 15feb733f | api-only | compliance worker install-remediation branch, per-pass cap, give-up counter | `jobs/softwareComplianceWorker.ts`; migration `191500` | `install_remediation_status` transitions, attempts ≤ cap | armed autoInstall policy + missing violations | PASS |
| #5914 | fe331c77b | web | allow unapproving an approved patch | `/patches` row action `data-testid="patch-row-<id>-unapprove"` → `PatchApprovalModal` decline tab; "Clear all ring approvals" checkbox; `POST /patches/:id/decline {allRings:true}` | status flips to declined, ring approvals cleared, toast | seeded approved patch | PASS |
| #5916 | 87059eba3 | web | ticket checklist template library, partner-wide ownership, apply-to-ticket | `/settings/ticket-checklist-templates` (`checklist-template-add`, `checklist-template-owner`); ticket `TicketChecklistCard` "Apply template"; `POST /tickets/:id/checklist/apply-template` | create partner-wide template with steps; apply → items populate | partner scope | PASS |
| #5910 | 8b22ad438 | api-only | bind event-triggered automation runs to the triggering device | `jobs/automationWorker.ts processTriggerEvent`; `automation_runs`/`automation_run_targets` | run bound to triggering device only; out-of-scope logged `event_device_outside_automation_scope` | seeded event automation | PASS |
| #5904 | 5192b6ec6 | web | expiring_certs sweep kind — typed TLS observation on network_monitors | `/monitoring` monitor detail; `network_monitors.tls_*` (migration `191100`) | http_check populates tls cols; sweep kind selectable | live agent running HTTPS check | TODO |
| #5887 | aac5e39c6 | web | agreements area — IA split (W03) | sidebar "Agreements" → `/agreements/templates`, `/agreements/templates/[id]`, `/agreements/signed?linked=&orgId=`; `/contracts#tab=templates` redirects | nav item, pages render, redirect works, Contracts tabs drop Templates/Documents | — | PARTIAL |
| #5778 | e1b22f4c3 | web | monitoring Test connection performs a real provider check | `/settings/integrations/monitoring` per-provider "Test connection"; `POST /integrations/monitoring/test` | real pass/fail per provider (bad URL → failure toast, not success) | provider reachable for pass | PASS |
| #5903 | a458da878 | web | AI patch agent W03 — chase, retry, escalate | `/ai-agents/runs/[id]` `ai-agent-run-patch-item-<i>-class` / `-attempts`, escalation line w/o approve | failure class + attempt count; escalation without approve control | seeded failing patch_job_results | PASS |
| #5902 | b10fd571d | api-only | execution plane W04 — workspace tools, analysis profile, admission | `runService.ts admitAnalysisRun`; `organizations.ai_external_processing`, `ai_agent_runs.staged_inputs` | analysis run admitted only with org switch + hosted flag | hosted + `BREEZE_AI_WORKSPACE_ENABLED` | PARTIAL |
| #5897 | 49542b5e1 | agent-only | hold the recovery console PID alive after poweroff | `agent/internal/recoveryconsole/console.go` | one `media_booted` per boot | QEMU e2e | TODO |
| #5889 | 2b12608e0 | api-only | sweep-condition fix watches for schedule-triggered intents | `jobs/intentReleaseWorker.ts watchReleasedIntent`; `ai_agent_fix_watches` (migration `190300`) | fix-watch row opened on release of schedule-triggered intent | seeded sweep-minted intent | PARTIAL |
| #5888 | 3f0f89437 | web | AI patch agent W02 — Tier-3 install cards | `/approvals` device-scoped `manage_patches:install` card; run trace `ai-agent-run-patch-item-<i>-intent` | one card per device; suppressed updates listed w/ reasons | patch agent + eligible patch | PASS |
| #5885 | 47b7af9a3 | web | measured impact band renders censored p50/p90, 90-day omission label | `/ai-agents/impact` `measured-percentiles-<arm>`, `measured-censored-note` | p50/p90 line per arm; 90-day empty → "insufficient data" not "try longer window" | cohorts ≥ MEASURED_MIN_COHORT_N | PASS |
| #5884 | 674b9b70c | portal | lifecycle rows not linked when self-service off | portal `/reports/lifecycle` `lifecycle-plan-row-link-<id>` | plain text when `enable_self_service=false`; link when on | lifecycle report enabled | TODO |
| #5883 | 11b1c2cc4 | portal | stabilize DeviceList relative-time cells across hydration | portal `/devices` Last online / Last patch / Last backup | no hydration flash, relative label after mount | — | TODO |
| #5872 | 4855b6a16 | web | Tier-2 time-entry proposal from AI-assisted ticket work | ticket detail AI draft send / resolve; `/approvals` `manage_tickets:log_time_entry`; `time_entries.source='ai_suggested'`; `ticket_categories.default_time_entry_minutes` | pending approval card; approve → time entry row | AI drafting + category default minutes | FAIL |
| #5835 | 41bb8912d | web | agreements permission (W02) | `/settings/roles` editor "Agreements" read/write; sidebar RBAC | Agreements row separate from Contracts; role w/o read hides nav | role editing | PASS |
| #5792 | 81968091c | web | AI patch agent W01 — the patch lane end to end | `/settings/ai-agents` create Patching agent; run "Run now"; `/ai-agents/runs/[id]` patch-profile run | patch run per org with evidence-based plan; zero intents | AI agents enabled | FAIL |
| #5780 | 544e84908 | web | remediation trigger provenance on intents / script executions / automation results | `DeviceActivityFeed.tsx` trigger chip; migration `182900` cols | chip renders; cols non-null for new rows | seeded AI-triggered execution | PASS |
| #5882 | b6c7c7549 | web | prev-sweep fixes: narrative delivery summary, QuoteDetail blocks, ContractEditor refetch, agreement copy | `/ai-agents/runs/[id]` `narrative-delivery-summary`; `/quotes/[id]` Detail tab; `/contracts` editor | already verified 09-14; re-check quote Detail tab + no refetch loop | — | PASS |
| #5871 | e5c7cc0f0 | web | prev-sweep fixes: Fleet Design auth, device purchaseDate, monitor deploy/save feedback, ticket stats orgId | `/monitoring` `monitor-editor-deploy` / `monitor-editor-save`; device purchaseDate | already verified 09-14; re-check monitor save toast | — | PASS |
| #5911 / #5896 / #5895 / #5894 / #5873 | — | ci-test-chore | test/CI-only | n/a | — | — | N/A |

## Sweep log (append as you go)

## UI/UX paper cuts

| # | Where | Observation | Severity | Disposition (fixed <sha> / issue # / noted) |
|---|---|---|---|---|
| G1-1 | `/agreements/templates` for a user without `agreements:read` | Generic "Failed to load agreement templates" instead of a permission-denied state, and "New agreement template" is still offered (dialog opens; create would 403) | low | noted |
| G1-2 | `/settings/roles` create/edit role form | No `data-testid` anywhere on the role form or the permission matrix — every assertion has to go through placeholders/labels | low | noted |
| G1-3 | Ticket checklist card | Item delete has no confirmation and no toast; the row vanishing is the only feedback | low | noted |
| G1-4 | `/alerts/monitors/[id]` deployed card | Attach/detach give no toast, and `handleDetach` (MonitorEditor.tsx:411) bypasses `runAction` — a failed detach would be silent | medium | noted |
| G1-5 | `PatchApprovalModal` | "Clear all ring approvals" checkbox has no `data-testid` | low | noted |
| G1-6 | `/integrations#monitoring` | PagerDuty/OpsGenie "Test connection" is disabled while the provider toggle is off, with no tooltip explaining why | low | noted |
| G1-7 | `/settings/roles` create role | A role saved with only devices/contracts/tickets `read` ticked also came back carrying `agent_rollback:create`; the UI does not cascade, so it is added server-side | medium | issue #5962 |
| G-API-1 | API — software policies | A software policy with `enforceMode`+`autoInstall` armed evaluates **zero** devices until someone also creates a configuration policy, links `software_policy` to it and assigns it. Nothing in the policy create response, the `/check` response (`"Compliance check scheduled"`, 200) or the violations list says the policy reaches no devices — it just silently does nothing. | low | noted |
| G-API-2 | API — `/tickets/:id/attachments` | `POST` exists, `GET` (list) does not — a bare `GET /tickets/:id/attachments` returns the generic 404 `{"error":"Not Found","path":…}` rather than 405/route-not-found, which reads like the ticket is missing. | low | noted |
| G-API-3 | DB — `ticket_attachments_backend_chk` | The `artifact` arm requires `data IS NULL AND storage_key IS NULL` but does **not** require `artifact_id IS NOT NULL`, so a `storage_backend='artifact'` row with a NULL `artifact_id` is insertable — an attachment that points at nothing. The other two arms are fully pinned. | low | issue #5955 |
| G2-1 | `/settings/ai-agents` → schedule editor, Act mode | The web has no knowledge of `BREEZE_AI_AGENTS_SWEEP_ACT_ENABLED` (grep: zero references in `apps/web`). On a DEFAULT deployment (flag `false`) the switch is still armable, `POST /ai/agents/schedules` accepts `actMode:true` and persists it, and the amber scope note promises unattended service restarts — while `sweepActMode.ts:55` returns `false` on its first line and nothing ever runs. Armed-and-inert, with no indication anywhere. | medium | issue #5960 |
| G2-2 | `/ai-agents/impact` main empty state | With the 90-day (widest) window selected, `ai-impact-empty` still advises "Widen the reporting window above" — the exact defect class #5885 fixed in the measured band directly below it, which correctly says "Not enough data". | medium | noted |
| G2-3 | `/ai-agents/runs/[id]` patch item vs Linked approvals | After the linked intent is rejected, `run-detail-intents` shows "Rejected" while `ai-agent-run-patch-item-0-intent` on the same page still reads "Awaiting approval — open card" (it renders off the run's frozen disposition, never the intent's live status), and its link lands on an inbox where the card is gone. | medium | issue #5961 |
| G2-4 | `/approvals` deep link | The run trace links `/approvals#intent-<uuid>`, but `ApprovalsInbox.tsx` has no `location.hash` handling and no element carries `id="intent-…"` — the anchor resolves to nothing (no scroll, no highlight, no filter). | medium | noted |
| G2-5 | Device activity feed trigger chip | The #5780 trigger chip renders beside the pre-existing `initiatedBy` chip with no dedupe, and the two vocabularies overlap on `manual`/`schedule`/`policy`/`automation` — a row carrying both rendered "SCHEDULE SCHEDULE". (*Inferred*: no production writer was found that sets the pair; the readers just don't guard against it.) The chip also has no `data-testid`. | low | noted |
| G2-6 | Run detail → "Attach to a ticket" | The control asks for a raw ticket UUID typed by hand — no picker, no search, and the human-facing ticket number (`5GEQIUGS3O`) is rejected. | low | noted |
| G2-7 | `/ai-agents/runs` and `/approvals` filters | Filter state lives in React state only; `location.hash` stays empty, so a filtered view can't be shared and resets on reload or Back — against the repo's own hash URL-state convention that `/ai-agents/impact#90` and `/agreements/signed#unlinked=1` follow. | low | noted |
| G2-8 | `/settings/ai-agents` schedules section on a PATCH agent | Still headed "SCHEDULED SWEEPS — Cron schedules that run this agent's fleet sweep" with the empty state "No sweep schedules yet", although the only schedule kind it offers there is "Patch plan". | low | noted |
| G2-9 | `/settings/ai-agents` list | "No partner-wide baseline exists yet for this kind. An organization-only agent only overrides a baseline, so it has no effect until one is created." renders above a list containing two partner-wide ("All orgs") agents. | low | noted |
| G2-10 | `/approvals` card | Expiry reads "Expires in 10079 min" for a 7-day window instead of rolling up to days/hours. | low | noted |
| G2-11 | `/ai-agents/runs/[id]` patch rollup | `ai-agent-run-patch-counts` prints zero clauses ("0 already proposed") and never mentions `refusedCount`, so a refused reboot plan is invisible in the summary line. | low | noted |

## Fixes applied

| Commit | PR/area | What | Test |
|---|---|---|---|
| `ed47f0ce8` | #5913 config | `BREEZE_AI_AGENTS_SWEEP_ACT_ENABLED` read by `config/env.ts` but never mapped in `docker-compose.yml` / `deploy/docker-compose.prod.yml` / `.env.example` — un-enableable in any compose deployment. Mapped + documented. | manual: `docker exec api env` shows the var after recreate |
| `47ac7de24` | #5887 contracts | `/contracts#tab=templates|documents` deep links: Astro `navigate()` threw on null `history.state` and swallowed it; now `window.location.replace` | `ContractsTabs.redirect.test.tsx` (4 tests) |
| `6432c8416` | #5921 deliverables | org-owned template set + partner-wide checklist template 404'd: owner check received the set's always-null `partner_id`; now resolves the org's partner | `deliverableTemplateService.test.ts` (+1, 1 corrected) |
| `2c6d40141` | #5792 patch plan | `patchPlanRoutes` never applied `authMiddleware` (bare mount in index.ts) → 401 → web logged the tech out | `patchPlan.test.ts` bare-mount "is behind authMiddleware" |

## Issues filed

| Issue | Title | From row |
|---|---|---|
| #5954 | ticket_categories.default_time_entry_minutes has no UI/API surface | #5872 (FAIL) |
| #5955 | ticket_attachments_backend_chk artifact arm does not require artifact_id | G-API-3 |
| #5960 | Act mode armable in the web when BREEZE_AI_AGENTS_SWEEP_ACT_ENABLED is off | G2-1 |
| #5961 | Patch item still "Awaiting approval" after linked intent rejected | G2-3 |
| #5962 | Read-only role gains agent_rollback:create server-side | G1-7 |

## Summary

| Group | PASS | PARTIAL | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|

**Top findings:**

**Oldest PR reached:** #…

**Before the release cut:**

### G-API — api-only rows (curl + `docker exec psql` + vitest; no browser)

Session setup: `POST /api/v1/auth/login` returns **428 `auth_binding_rotation_required`** on the first call with a fresh cookie jar and **200** on the immediate repeat with the same jar (known noise). The login limiter (`login:ip:*` / `login:socket:*` in Redis) is shared with the browser group and produced a run of 429s; cleared those two keys once to get a session. A `POST /auth/logout` from the other group invalidated my access token mid-run — re-logged in. Seeded, in `Default Organization` (`2bd066eb…`): software policy `e3c8f8d1…`, config policy `ff7750ee…` + `software_policy` feature link + org-level assignment, `software_catalog` row `432b2ab0…` (7-Zip), automation `4bb4f121…`, ticket `32b8be67…` + one PNG attachment.

**#5933 — `GET /software-policies/violations` projects the install-remediation triad — PASS**
- ✅ Seeded `software_compliance_status` (device `42fc7de0…`, policy `e3c8f8d1…`, `install_remediation_status='queued'`, `attempts=2`, `last_install_remediation_attempt=now()`).
- ✅ `GET /api/v1/software-policies/violations?policyId=e3c8f8d1…&limit=10` → **200**, body `…"remediationStatus":"none","installRemediationStatus":"queued","lastInstallRemediationAttempt":"2026-09-15T19:40:14.594Z","installRemediationAttempts":2…`, `total:1`. All three new fields present and equal to the seeded values.
- ✅ `GET /api/v1/software-policies/violations` (no `policyId`) → **200**, same row, unchanged shape.

**#5917 — compliance worker install-remediation branch — PASS (drove the real BullMQ worker end to end)**
- ✅ Migration `191500`: `install_remediation_status varchar(20) DEFAULT 'none'`, `last_install_remediation_attempt timestamp`, `install_remediation_attempts integer NOT NULL DEFAULT 0`. **No CHECK constraint, by design** — the migration header states the allowed values are a TypeScript union, mirroring the existing `remediation_status` column.
- ✅ Knobs: `SOFTWARE_INSTALL_REMEDIATION_MAX_ATTEMPTS` default 3, `…MAX_PER_PASS` default 50, both read per-pass (`services/softwareInstallRemediationKnobs.ts`).
- ✅ Trigger path: `POST /software-policies/:id/check` (and the PATCH route) → `scheduleSoftwareComplianceCheck` → BullMQ `software-compliance`. First run returned `devicesEvaluated:0` because the policy had **no config-policy assignment** — devices resolve only via `resolveDeviceIdsForSoftwarePolicy` (config-policy link + assignment), not from the policy row. After creating the config policy, the `software_policy` feature link and an organization-level assignment: `{"devicesEvaluated":2,"violations":2,…}`.
- ✅ Arming: PATCH `enforceMode:true` + `remediationOptions.autoInstall:true` + a rule carrying `catalogId`. Job result `{"devicesEvaluated":2,"violations":2,"remediationQueued":0,"installRemediationQueued":2}`; both rows moved to `install_remediation_status='pending'`, `install_remediation_attempts=1`, `last_install_remediation_attempt` stamped. Audit row `install_queued {"maxPerPass":50,"maxAttempts":3,"queuedCount":2,"targetCount":2,"deferredCount":0}`.
- ✅ Give-up: set `install_remediation_attempts=3` by SQL and re-ran scoped to one device → that row flipped to **`gave_up`** (the untouched sibling stayed `pending`), with a single audit row `install_gave_up {"attempts":3,"policyName":…,"maxAttempts":3}`. The once-per-transition audit rule holds (one row, not one per pass).
- ⚠️ No live agent, so the install command itself never completes — `pending`→`completed` was not observed. Expected.
- ⚠️ Note for re-testers: `/check` dedupes by `jobId` over `ON_DEMAND_DEDUPE_WINDOW_MS` and the id includes a hash of `deviceIds`, so two back-to-back identical `/check` calls return the *same* jobId and the second never runs. Pass `deviceIds` to force a fresh job. Intentional, not a defect.
- ✅ `npx vitest run src/jobs/softwareComplianceWorker src/services/softwareInstallRemediationKnobs …` — 13 files / 294 tests green.

**#5910 — event-triggered automation runs bound to the triggering device — PASS**
- ✅ Created automation `4bb4f121…` (org-scoped, `trigger:{type:'event',eventType:'alert.triggered'}`, no conditions, one `execute_command` action) via `POST /automations` → 201.
- ✅ Drove the real dispatch path by calling `queueEventTriggers()` inside the API container (`npx tsx` on a throwaway file under the container-only `node_modules` volume — nothing written to the repo) with a synthetic `alert.triggered` carrying `deviceId=42fc7de0…`. (A plain `publishEvent` from a one-off process does **not** work: durable subscribers are registered at API boot, so the event never reaches `event-dispatch`.)
- ✅ Result: exactly **one** `automation_runs` row, `triggered_by='event:alert.triggered'`, **`devices_targeted = 1`**, and the only `automation_run_device_results` row is device `42fc7de0…`. The org has **2** online devices, so the pre-fix fleet-wide fan-out would have produced 2 — the binding is proven, not assumed.
- ⚠️ `automation_run_targets` does not exist; the per-device table is `automation_run_device_results` (inventory row wording is off).
- ✅ `npx vitest run src/jobs/automationWorker` green (in the 294-test batch above).

**#5889 — sweep-condition fix watches — PARTIAL**
- ✅ Migration `190300` on `ai_agent_fix_watches`: `subject_kind text`, `subject_key varchar(200)`, `ai_agent_fix_watches_subject_kind_chk`, `ai_agent_fix_watches_subject_shape_chk`, partial index `ai_agent_fix_watches_subject_idx (org_id, device_id, subject_kind, subject_key) WHERE subject_kind IS NOT NULL`.
- ✅ Forged all three constraints as `breeze` with `breeze.scope='system'`: half-record (`subject_kind` without `subject_key`) → `23514 …subject_shape_chk`; `subject_kind='bogus'` → `23514 …subject_kind_chk`; `source_kind='intent'` with `intent_id NULL` → `23514 …intent_shape_chk`.
- ✅ Live `subject_kind_chk` carries **7** kinds — the 6 from `190300` plus `expiring_certs`, added later by `191100` (#5904). The migration-parsing guard `aiAgentFixWatches.subjectKinds.test.ts` is green against that.
- ✅ `npx vitest run fixWatch` + `intentReleaseWorker` — 5 files / 157 tests green.
- ⚠️ Not driven: no schedule-triggered `action_intents` row was minted and released, so `watchReleasedIntent` opening a subject-anchored watch was verified by unit test only.

**#5902 — execution plane W04 — PARTIAL (sandbox-dependent half BLOCKED, as scoped)**
- ✅ Migration `190500`: `organizations.ai_external_processing boolean DEFAULT false`; `ai_agent_runs.staged_inputs jsonb`; `ai_agent_runs_profile_chk` = `{full, verdict, sweep, narrative, triage, design, patch, **analysis**}`.
- ✅ Org consent flag round-trips: `GET /api/v1/orgs/organizations/:id` → `aiExternalProcessing:false`; `PATCH … {"aiExternalProcessing":true}` → 200, `GET` → `true`, DB `ai_external_processing = t`; `PATCH … false` → back to `false`. (Route is `/api/v1/orgs/organizations/:id`, not `/api/v1/organizations/:id`.)
- ✅ Container env has `BREEZE_AI_WORKSPACE_ENABLED=false` and `BREEZE_WORKSPACE_ENABLED=false`; `GET /api/v1/config` exposes no workspace flag at all (it only carries `billing`/`support`/`aiOperatorTasks`/`cfAccessLogin`/`registration`/`softwarePackages`) — the lane is gated in-process by `aiWorkspaceEnabled()` in `workspaceLaunchTool.ts` / `toolResultCapture.ts`, not advertised to clients.
- ✅ `npx vitest run src/services/workspace/ src/services/aiWorkspaceMetrics src/services/artifacts src/routes/aiAgents.test.ts` — 16 files / 439 tests green.
- 🚫 `admitAnalysisRun` with a real sandbox is BLOCKED (hosted-only, no Vercel sandbox backend locally).

**#5780 — remediation trigger provenance — PARTIAL (DB contract PASS; UI chip belongs to the browser group)**
- ✅ Migration `182900` added `trigger_kind text`, `trigger_ref_id uuid`, `trigger_key varchar` to **all three** of `action_intents`, `script_executions`, `automation_action_results`, each with a `_trigger_kind_chk` (11 kinds: manual, schedule, sweep_finding, alert, monitor, fleet_finding, policy, automation, ticket, anomaly, api) and a `_trigger_shape_chk` = `trigger_kind IS NOT NULL OR (trigger_ref_id IS NULL AND trigger_key IS NULL)`.
- ✅ Forged per table: invalid kind → `23514 <table>_trigger_kind_chk` (×3); `trigger_ref_id`/`trigger_key` set with `trigger_kind NULL` → `23514 <table>_trigger_shape_chk` (×3); a fully-populated valid triple inserts (×3). Note the shape check permits `trigger_kind` **without** a ref/key — that direction is deliberately allowed.
- ✅ `npx vitest run src/services/remediationIdentity src/services/automationActionResults.trigger src/services/automationRuntime.trigger src/services/auditService.trigger src/services/tenantExportPolicyRegistry.trigger src/db/schema/actionIntents` — 6 files / 42 tests green.
- ⚠️ `DeviceActivityFeed.tsx` chip not checked (browser group owns it).

**#5919 — artifact attachments (migration `192900`) — PARTIAL**
- ✅ Schema: `ticket_attachments.artifact_id uuid` FK → `ai_run_artifacts(id) ON DELETE SET NULL` + partial index; `ticket_attachments_backend_chk` now three-armed (`s3`/`db`/`artifact`); `ticket_attachments_size_chk` = 128 MiB for `artifact`, 10 MiB otherwise. The column is on **`report_runs`**, not `reports` (with `report_runs_artifact_idx`) — inventory row wording is off.
- ✅ Ordinary attachments unaffected: created ticket `32b8be67…`, `POST /tickets/:id/attachments` with a PNG → **201** (`byteSize:70`, `contentType:"image/png"`), `GET /tickets/:id/attachments/:attachmentId/content` → **200**, 70 bytes; DB row `storage_backend='db'`, `artifact_id NULL`. A `text/plain` upload is correctly refused with **415 `UNSUPPORTED_ATTACHMENT_TYPE`**.
- ✅ `POST /tickets/:id/attachments/from-artifact` fails closed: malformed handle → **400 `Invalid GUID`**; well-formed unknown handle → **404 `ARTIFACT_NOT_FOUND` "No such artifact is available to this organization"**.
- ⚠️ There is no `GET /tickets/:id/attachments` list route (404) — attachments come back on the ticket detail payload; `POST /:id/attachments`, `POST /:id/attachments/from-artifact`, `GET /:id/attachments/:attachmentId/content` and `DELETE` are the surface.
- 🚫 Attaching a real artifact by reference is BLOCKED (needs `BREEZE_AI_WORKSPACE_ENABLED` + sandbox).
- ✅ `npx vitest run src/routes/tickets/attachments src/routes/reports/runs.fromArtifact src/db/schema/ticketAttachments.artifact src/routes/orgs.test.ts src/services/ticketAttachmentStorage src/db/schema/aiAgentFixWatches` — 8 files / 411 tests green.

**Migrations sanity — PASS**
- ✅ All **50** `2026-10-16-*.sql` files on disk are in `breeze_migrations`, byte-identical list (`diff` clean), including all 13 of the `2026-10-16-19*` files from this range (`190000`…`192900`).
- ✅ Drift: Postgres is not published to the host in this stack (`5432/tcp → null`), so the check ran inside the API container: `npx tsx scripts/check-drift.ts` → *"No drift detected — all 800 migration files match the breeze_migrations ledger."* `npx drizzle-kit check` → *"Everything's fine"*.

### G1 — Agreements / Tickets & checklists / Patches / Monitoring integrations (browser, opus)

Seeded for this group (all via the app's own create flows unless noted): agreement template `QA Sweep MSA` (draft body + published v1), ticket checklist template `QA Onboarding Checklist` (partner-wide, 3 steps), ticket `T-2026-0001`, deliverable template sets `QA Managed Tier` (org-owned) and `QA Partner-wide Tier` (partner-wide, 1 item), update ring `QA Ring`, monitor `QA CPU monitor`, quote `QA sweep quote`, role `QA No Agreements` + user `qa-noagree@breeze.local` (SQL: row cloned from admin's password hash, `partner_users` row with the custom role). One ring-scoped `patch_approvals` row was inserted by SQL to give the "clear all rings" path something to clear.

**#5887 agreements IA split — PARTIAL**
- ✅ Sidebar "Agreements" → `/agreements/templates` present under Billing, separate from Contracts.
- ✅ `/agreements/templates`: empty state, create dialog (`contract-template-owner` partner/org selector), create → toast "Template created" → redirect to `/agreements/templates/<id>`; body editor + `Save draft` → "Draft version saved" → v1 Draft; `Publish` → "Version published" → v1 Published.
- ✅ `/agreements/signed`: tab renders, empty state; "Unlinked only" chip drives `GET /contracts/contract-documents?linked=all|unlinked&orgId=…` (both 200). Filter state is in the hash (`#unlinked=1`), per CLAUDE.md — not a query param.
- ✅ `/contracts` no longer renders Templates/Documents tabs.
- ❌ **`/contracts#tab=templates` (and `#tab=documents`) redirects the CONTENT but not the URL.** Deep-linking those legacy hashes swaps in the Agreements page body while the address bar and `<title>` stay `/contracts#tab=templates` / "Contracts". Console shows `TypeError: Cannot read properties of null (reading 'index')` at `moveToLocation` in Astro's transitions router ×4 — `history.state` is `null` on a fresh load, so `navigate(..., {history:'replace'})` throws inside Astro's async `updateDOM`, which swallows it, so `navigateTo`'s `window.location.replace` fallback never fires. Reproduced on both legacy hashes, twice each.

**#5835 agreements permission (W02) — PASS**
- ✅ Role editor matrix has an "Agreements" resource row with Read/Write, distinct from "Contracts" (Read/Write/Manage). `permissions` table carries `agreements:read` / `agreements:write`.
- ✅ Created custom role `QA No Agreements` (devices/contracts/tickets read only) via the UI → toast `Role "QA No Agreements" created`.
- ✅ Logged in as a user carrying it: sidebar has Contracts, no Agreements item; `/agreements/templates` → `GET /contracts/contract-templates` **403**.
- ⚠️ Paper cut: the denied page renders a generic "Failed to load agreement templates" and still offers "New agreement template" (the dialog opens; the create would 403).
- ⚠️ Observation: the role saved with a 4th permission I never ticked — `agent_rollback:create` alongside the three reads. The UI does not cascade (re-checked: ticking Devices→Read leaves every other row off), so it is added server-side. Worth a look — it is a `create` action implied by read selections.

**#5916 ticket checklist templates — PASS**
- ✅ `/settings/ticket-checklist-templates`: empty state → `checklist-template-add` → name/description/`checklist-template-instructions` + `checklist-template-owner` (All orgs / This organization only) → created partner-wide (`ticket_checklist_templates.org_id IS NULL`, `partner_id` set) with the "All orgs" badge; 3 steps added.
- ✅ Ticket `T-2026-0001` (created through `/tickets` → Create ticket, toast "Ticket T-2026-0001 created"): checklist card → "Apply template" → picker lists "QA Onboarding Checklist — All orgs", append/replace modes → Apply → 3 items, progress 0/3.
- ✅ Tick (0/3 → 1/3), edit a label, reorder (▲), delete → 1/2; all survive a reload.
- ⚠️ Paper cut: item delete has no confirm step and no toast (list update is the only feedback). Create/apply are also toast-less by design (`runClientAction` with `errorFallback` only) — outcome is visible, so not a silent mutation.

**#5921 deliverable checklist wiring (W03) — PARTIAL**
- ✅ `/settings/deliverable-templates` item form exposes `deliverable-template-item-checklist-template` and `-instructions` ("Internal — never shown to the customer").
- ✅ On the **partner-wide** set: picked the partner-wide checklist template + instructions → saved; DB row `deliverable_template_items` carries `checklist_template_id` + `instructions`; reopening the item form re-populates both.
- ❌ **On an ORG-OWNED set the same save is a hard 404.** `POST /deliverable-templates/<set>/items` → `400/404 {"error":"Checklist template not found","code":"NOT_FOUND"}`, UI shows a bare "Not found". Cause (read from source): `deliverableTemplateService.ts:253/282` call `assertChecklistTemplateUsableByTemplateItemOwner(id, { orgId: set.orgId, partnerId: set.partnerId })`; the set's `one_owner` XOR means `partner_id IS NULL` whenever `org_id` is set, so `assertChecklistTemplateUsableByOrg(..., partnerId=null)` always throws for a partner-wide template — contradicting that module's own documented rule ("an ORG-scoped row may reference its own org's template, or its partner's partner-wide one"). `serviceDeliverableService.ts:204` does it correctly by passing the ORGANIZATION's `partnerId`. The unit test at `checklistTemplateReference.test.ts:111` uses `{ orgId:'o-1', partnerId:'p-1' }` — a combination the XOR makes impossible in the DB, which is why CI is green. The picker still offers partner-wide templates for org-owned sets, so this is reachable from the happy path.
- ✅ Portal no-leak: zero occurrences of `instructions` anywhere in `apps/portal/src`; `services/portal/serviceReadModel.ts` uses column-explicit selects that never include `instructions` or `checklist_template_id`.
- ⚠️ BLOCKED: nightly-sweep ticket creation (out of scope per the row). There is no manual "run sweep" route — `grep` finds only `jobs/deliverableWorker.ts`, no route trigger.

**#5872 AI time-entry proposal (settings half) — FAIL / AI half BLOCKED**
- ❌ **`default_time_entry_minutes` is not exposed anywhere in the product.** The column exists (`db/schema/tickets.ts:33`, migration `2026-10-16-190100`) and is read by `services/aiTimeEntryProposal.ts`, but `grep defaultTimeEntryMinutes apps/web packages` returns **nothing**, and `routes/ticketCategories.ts` never reads or writes it. Confirmed in the browser: `/settings/partner#ticketing` → Categories tab, category edit form has name/color/parent/priority/response SLA/resolution SLA/billable/hourly rate and no minutes field; the Time Tracking tab only carries the remote-session suggestion settings. The per-category default can only be set with SQL.
- ⚠️ BLOCKED: the approval-card half (AI drafting → `/approvals` `manage_tickets:log_time_entry` → `time_entries.source='ai_suggested'`) — no LLM on this stack.

**#5914 unapprove an approved patch — PASS**
- ✅ Approved "Cumulative Update for Windows 11 (E2E synthetic)" through Review → Approve → confirm dialog ("Approve patch on 1 device in Default Organization?") → toast "Patch approved", row → Approved with Deploy/Unapprove actions.
- ✅ Created update ring `QA Ring` (toast "Update ring created") and seeded a ring-scoped `patch_approvals` row so both a global and a ring approval existed.
- ✅ `patch-row-<id>-unapprove` → modal opens preselected on Decline with "Clear all ring approvals" + its explanatory copy → submit → toast **"Patch declined for all update rings"**, row → Declined.
- ✅ SQL: BOTH `patch_approvals` rows (ring_id NULL and ring_id=QA Ring) went `approved → rejected` with `approved_by`/`approved_at` cleared.
- ⚠️ Paper cut: the "Clear all ring approvals" checkbox has no `data-testid`.

**#5778 monitoring Test connection — PASS (real provider checks, not stubs)**
- ✅ Prometheus, `http://127.0.0.1:9/` → **Failed**, `POST /integrations/monitoring/test` → `400 {"success":false,"error":"127.0.0.1:9 is not an allowed destination: URL points to blocked address: 127.0.0.1"}` (SSRF guard).
- ✅ Prometheus + Grafana, `http://breeze-unreachable.invalid/` → **Failed**, "Could not reach breeze-unreachable.invalid: getaddrinfo ENOTFOUND …" — real DNS resolution, so a real request.
- ✅ OpsGenie with a junk API key → **Failed**, "Opsgenie returned HTTP 422" (real upstream call).
- ✅ PagerDuty → **Failed**, "PagerDuty integration key must be the 32-character Events API v2 key" (format check only, as designed).
- ✅ Webhooks: `https://example.com/` → **Failed**, "returned HTTP 405"; `https://httpbin.org/post` → **Active**, "Webhook Datadog relay accepted the test event (HTTP 200)". Success and failure both reachable.
- ⚠️ Paper cut: PagerDuty/OpsGenie "Test connection" is `disabled` while the provider toggle is off, with no tooltip saying why. Settings were NOT saved (test-only edits discarded on reload).

**#5871 re-check (monitor save/deploy feedback) — PASS**
- ✅ `/alerts/monitors/new` → name → `monitor-editor-save` → toast **"Monitor saved"**, `POST /monitor-definitions` 201, redirect to the monitor id.
- ✅ `monitor-editor-deploy` opens the Deploy dialog (it is not itself a mutation); Attach is correctly disabled until a policy is chosen; choosing one attaches and the "Deployed to" card updates (policy link + Devices (2)). Detach removes the row.
- ⚠️ Paper cut: attach and detach show no toast, and `handleDetach` (MonitorEditor.tsx:411) calls `fetchWithAuth` directly rather than `runAction` — a failed detach would be silent.

**#5882 re-check (quote Detail tab, contract editor refetch) — PASS**
- ✅ `/billing/quotes` → New quote → toast "Draft quote created". Added a `table` block and a `contract` block (needed a PUBLISHED agreement version — the Add button stays disabled until `contractVersion` resolves, which is correct). Detail tab renders both: the table block content and the agreement ("QA Sweep MSA — v1", body text, auto variables) — no empty pricing table.
- ✅ `/contracts/new` editor watched for 16 s across 6 re-renders: exactly ONE `GET /catalog/distributors/<id>/status` per distributor (td-synnex-ec, pax8). No refetch loop.

### G2 — AI agents / patch agent / sweeps / impact (browser, opus)

Seeded for this group, through the app's own create flows unless noted: partner-wide **triage** agent `QA Triage Agent` (`2c16d9d4…`) + one partner-wide **sweep** schedule (`09f79321…`, `0 3 * * *`, act mode ARMED) + one org override (`7b2f8352…`, act mode disabled-for-org); partner-wide **patch** agent `QA Patch Agent` (`99141c29…`, enabled) + one partner-wide **patch** schedule (`46ef19fe…`, `0 2 * * *`). By SQL: a `patch`-profile run `0aa11111…` (completed) whose `outcome.patchPlan` carries four items (chase / escalation / recorded reboot_plan / refused reboot_plan) with matching `dispositions`, a linked agent-origin `action_intents` row `0bb11111…` (`manage_patches:install`, device-scoped, four_eyes) and its `approval_requests` row `0cc11111…`; an `analysis`-profile run `0dd11111…` + one `ai_run_artifacts` row `0ee11111…`; 60 synthetic `alerts` on rule `High CPU usage` (30 with an `ai_alert_verdicts` exposure row) for the impact cohorts; two `audit_logs` rows on `e2e-macos.local` (`script.execute`, one with `details.triggerKind='schedule'`, one without).

**Unplanned bonus: the LLM lane is NOT dead on this stack.** The patch schedule I created fired on its own and produced a REAL, model-driven run `96d0fe36…` (`turn_count 5`, 55 s, evidence-based plan, zero intents) — so #5792/#5903/#5920's renderers were checked against genuine output as well as the seeded fixture.

**#5913 — AI sweeps act mode W04, the act gate — PASS**
- ✅ Created the triage agent + a sweep schedule through the UI. Editor exposes `ai-agent-schedule-act-mode` (baseline branch): `disabled=false`, `aria-checked` flips false→true, and arming reveals `ai-agent-schedule-act-mode-scope-note` — *"Unattended fixes are limited to restarting a stopped service, and only on devices whose organization has already been granted manage_services:restart."*
- ✅ Save captured: `POST /ai/agents/schedules?orgId=…` → **201**, body `…"actMode":true…`; the immediate `GET` re-read and the DB both show `ai_agent_schedules.act_mode = t` on the partner row.
- ✅ "Override for this org" opens the override branch, which offers ONLY `ai-agent-schedule-act-mode-disable` (no arm switch, per design). Toggling it hides the scope note and saves `POST … {"ownerScope":"organization","baselineScheduleId":"09f79321…","actMode":false}` → **201**; DB override row `act_mode = f`. Toast "Schedule saved" both times.
- ✅ `ai-agent-schedule-act-mode-disabled-hint` correctly **absent**: it is gated on `canManageBaselines && canManagePartnerWide` (AiAgentSchedulesSection.tsx:379/991), **not** on the toggle's own state — the inventory row's wording ("only shows when the toggle is off") does not match the implementation, and the implementation is the right one. Platform admin holds both, so no hint; that branch could not be exercised without a demoted partner user.
- ⚠️ **The flag is not exposed to any client.** `grep sweepActEnabled apps/api/src` finds it only in `config/env.ts` and server-side call sites; `GET /api/v1/config` returns `{"features":{"billing","support","aiOperatorTasks"},"cfAccessLogin","registration","softwarePackages"}` and no AI flag at all. Container env confirmed `BREEZE_AI_AGENTS_SWEEP_ACT_ENABLED=true` (after `ed47f0ce8`). So the "report the flag true" half of the row is **N/A — no endpoint publishes it**, by design (same posture G-API found for the workspace flag).
- ⚠️ Consequence, and the one thing worth a decision before release (see paper cut G2-1): because the web knows nothing about the flag, on a DEFAULT deployment (`false`) the Act-mode switch is fully armable, persists `act_mode=true`, and shows the amber "unattended fixes…" promise — while `resolveEffectiveScheduleActMode()` returns false on the first line and nothing ever executes. `scheduleService.ts` does not refuse `actMode:true` when the flag is off either.

**#5792 — AI patch agent W01, the patch lane end to end — FAIL (the "Run now" route is unauthenticated-by-omission and 401s for everyone)**
- ✅ Created a partner-wide Patching agent through the 4-step create flow (toast "Agent saved"); DB `ai_agents` row `kind='patch'`, `partner_id` set, `org_id NULL`.
- ✅ Nightly patch schedule: `ai-agent-schedule-add` on a patch agent offers schedule kind **"Patch plan"** pre-filled with `PATCH_DEFAULT_CRON` = `0 2 * * *` and no act-mode switch (correct — act mode is sweep-only). Saved: `POST /ai/agents/schedules {"kind":"patch","cron":"0 2 * * *"}` → **201**; row shows "Next run Sep 16, 2026, 2:00 AM".
- ✅ "Run now" is offered — `ai-agent-run-now-<agentId>` appears once the agent is enabled (it is correctly hidden for a disabled agent and disabled with a tooltip in the all-orgs view).
- ❌ **`POST /ai/patch-plan/runs` answers `401 {"error":"Not authenticated"}` for every caller.** `apps/api/src/routes/patchPlan.ts` never applies `authMiddleware` — `grep authMiddleware apps/api/src/routes/patchPlan.ts` returns **nothing** across all 90 lines — while the module it is a self-described "near-copy" of does (`fleetDesign.ts:59` `fleetDesignRoutes.use('*', authMiddleware)`). `index.ts` applies no global auth (each route module owns it), so `c.get('auth')` is undefined and `requireScope` throws 401 before anything else runs.
  - Browser: pressing Run now → `POST /api/v1/ai/patch-plan/runs?orgId=2bd066eb…` **401** ×2, no toast, and because `runAction` treats 401 as session-expired the technician is **redirected to `/login`**. Reproduced twice, the second time seconds after a fresh login in which `POST /ai/agents/schedules` and `PATCH /ai/agents/:id` both succeeded.
  - curl, same Bearer token, back to back: `POST /ai/patch-plan/runs` → **401**; `POST /ai/fleet-design/runs` → **404** (route reached, auth fine, no designer agent); `GET /ai/agents` → **200**.
  - Why CI is green: `patchPlan.test.ts:55` builds its own Hono app and injects `c.set('auth', …)` in a test-local middleware, so the module's missing `authMiddleware` is invisible to the unit suite. Identical shape to #5866 (Fleet Design W03-W05 dead on main) from the 09-14 sweep.
- ✅ The lane itself is NOT broken — only its HTTP trigger. The schedule created above fired and produced run `96d0fe36…`: `profile='patch'`, `trigger_kind='schedule'`, `status='completed'`, `turn_count=5`, a real evidence-based plan (approval_advisory on the QA Ring, two install items, one escalation for the missing maintenance window), **zero intents minted** exactly as W01 promises, and `droppedPatchIds` reasons rendered ("The device has no update ring"). So the fix is one line in `patchPlan.ts`, not a design problem.
- ✅ `ai_agent_runs` rows with `profile='patch'` confirmed by SQL (the real one plus the seeded fixture).

**#5903 / #5920 — patch run trace surfaces (chase, retry, escalate; reactive routing & reboot planning) — PASS**
- ✅ Against the seeded fixture at `/ai-agents/runs/0aa11111…`, every W03/W04 testid renders: `ai-agent-run-patch-item-0-class` ("Failure class: Transient"), `-0-attempts` ("1 failed attempt"), `-0-intent` (link to `/approvals#intent-…`), `-1-class`/"Permanent", `-1-attempts`/"3 failed attempts".
- ✅ **The escalation line carries NO approve control**: `ai-agent-run-patch-item-1-escalation` renders "Needs a human — nothing will retry this automatically." and `item-1.querySelectorAll('button,a,input')` returns **`[]`**.
- ✅ Reboot plan: `-2-window` "Window: 9/16/2026, 8:00:00 PM – 9/17/2026, 12:00:00 AM", `-2-redundancy` "Redundancy group: role:file-server"; the colliding sibling shows `-3-reason` refusal copy "Not recorded: Another device in the same redundancy group is already planned for this window".
- ✅ Suppressed/dropped updates listed with reasons: `-0-dropped` → "2 updates were left out of the card / Superseded by a newer update / Awaiting manual approval", raw patch ids only in the `title` attribute, never as visible text (the documented rule holds).
- ✅ Re-checked against the REAL run `96d0fe36…`: `-1-reason` ("No update is currently eligible for this device"), `-1-dropped`, `-2-escalation`, `-3-reason`, `-3-dropped` all render off model output.
- ❌ **Same page, two contradictory readings of one intent.** After denying the linked approval, `run-detail-intents` correctly shows **"manage_patches:install — Rejected"** while the item's own line still reads **"Awaiting approval — open card"**: the item renders off the run's frozen `disposition: 'intent_created'` and never re-reads the intent's live status. The stale link then lands on an inbox where the card is gone. See G2-3.

**#5888 — Tier-3 install cards on /approvals — PASS**
- ✅ `/approvals` renders one device-scoped card per intent: `approval-row-0cc11111…` with `approval-target-device-…` = "Target device: e2e-windows.local", `approval-agent-badge-…` "Proposed by QA Patch Agent (AI agent)", tool `manage_patches:install`, high risk, Deny/Approve.
- ✅ Decide path driven for real: Deny → inline "Confirm denial" → `POST /mobile/approvals/:id/deny` **200**, toast "Action denied"; SQL confirms `approval_requests.status='denied'` AND `action_intents.status='rejected'` with `decided_by_user_id` stamped — both sides landed in one decision.
- ✅ Empty state after the decision: "No approvals waiting / New requests will appear here when your decision is needed."
- ❌ **The run-trace link `/approvals#intent-<uuid>` is a dead anchor** — `ApprovalsInbox.tsx` contains no reference to `location.hash` and no element carries `id="intent-…"`; `document.getElementById('intent-0bb11111…')` → `false`. Harmless with one card; with a full inbox the "open card" affordance drops you at an unfiltered top of list. See G2-4.

**#5885 — measured impact band — PASS (cohorts seeded, percentiles and censored note both rendered)**
- ✅ Empty state first: with no cohorts, all three signals read **"Not enough data: no group of comparable work has at least 20 items in each arm"** on the **90-day** window — the #5885 contract (an "insufficient data" reason, never "try a longer window") holds. `measured-alert-omitted` / `measured-ticket-omitted` / `measured-technician-minutes-omitted`.
- ✅ Seeded 60 alerts on one rule, 30 carrying an `ai_alert_verdicts` exposure 5 min after trigger (inside `ALERT_EXPOSURE_AGE_MINUTES=15`), 6 left open to force censoring. Result on 7 / 30 / 90 (all three pills): `measured-cohort-975a58a1…` "High CPU usage" with **`measured-percentiles-ai-touched`** "90% · p50 53 min · p90 92 min · 30 in cohort" vs **`measured-percentiles-untouched`** "90% · p50 275 min · p90 340 min · 30 in cohort", and **`measured-censored-note`** "Any percentile shown is right-censored: still-open items count as unfinished rather than being dropped."
- ✅ Ticket + technician-minutes signals stay on their omission rows (no ticket cohorts seeded) — the band degrades per-signal, not wholesale.
- ⚠️ The MAIN impact empty state above the band still says "Widen the reporting window above" while the 90-day (widest) window is selected — the same defect class #5885 fixed in the measured band, unfixed in the band above it. See G2-2.

**#5780 — trigger provenance chip on the device activity feed — PASS**
- ✅ Seeded two `audit_logs` rows on `e2e-macos.local` (`script.execute`), one with `details.triggerKind='schedule'` + `triggerRefId` + `triggerKey`, one with none. `GET /devices/:id/events` returns both with `details.triggerKind` intact through `redactAiProvenance`.
- ✅ Feed renders the chip on the provenance row with the **translated** label (`deviceActivityFeed.trigger.schedule` → "Schedule", uppercased by CSS) and the `triggerKey` in its `title`; the row without provenance renders **no chip**. Both halves of the row confirmed.
- ⚠️ *Inferred, not observed in production data*: the chip is rendered independently of the pre-existing `initiatedBy` chip with no dedupe, and the two vocabularies overlap on `manual`/`schedule`/`policy`/`automation` — my `initiated_by='schedule'` + `triggerKind='schedule'` row rendered **"SCHEDULE SCHEDULE"**. Every writer I read (`scriptDispatch.ts:879`, `sweepFindings.ts:411`) pairs a *different* `initiatedBy` with the trigger, so I could not prove a real path produces the pair. See G2-5.
- ⚠️ No `data-testid` on the trigger chip (or on any feed row element) — assertions have to go through text.

**#5919 — execution plane W05, chat + artifact surfaces — PARTIAL (more than expected got proven; the workspace lane itself stays BLOCKED)**
- ✅ AI chat page loads clean: the sidebar's "AI Assistant" is **`/workspace`** (not `/ai` or `/ai-assistant`, both 404) — "AI Workspace / Run multiple AI conversations simultaneously / New Conversation / Up to 5 concurrent conversations", no errors.
- ✅ `RunArtifactsSection` proven end to end by seeding an `analysis`-profile run + one `ai_run_artifacts` row: `ai-agent-run-artifacts` renders `run-artifact-row-…`, `run-artifact-download-…` (`/api/v1/ai/artifacts/<id>`), `run-artifact-preview-toggle-…` and `attach-artifact-open-…`. Note the section is gated on `profile === 'analysis'` (aiAgents.ts:1627), not on a flag — which is why it was reachable without the sandbox.
- ✅ **The by-reference ticket attachment G-API had to leave BLOCKED now has a green path**: Attach to a ticket → `POST /tickets/32b8be67…/attachments/from-artifact {"handle":"0ee11111…"}` → **201**, toast "Attached to the ticket"; DB row `storage_backend='artifact'`, `artifact_id` set, `data IS NULL`, `byte_size=2048` — no bytes copied, exactly the reference semantics the design claims.
- 🚫 Still BLOCKED: a real workspace/sandbox run producing the artifact (`BREEZE_AI_WORKSPACE_ENABLED=false`, hosted-only).
- ⚠️ `RunDetailPage` has **no** back-link from a run to a ticket the artifact was attached to — the relationship is one-way (artifact → ticket). The inventory row's "run detail page renders an attached-artifact link" is only true in the artifact→attach direction.
- ⚠️ The attach control takes a **raw ticket UUID typed by hand** — no picker, and a ticket NUMBER (`5GEQIUGS3O`) will not work. See G2-6.

**Everyday pass — /ai-agents, /ai-agents/runs, /approvals**
- ✅ `/settings/ai-agents`: create flow (4 steps, recommended-capability apply, review summary), agent list with kind/all-orgs/mode/last-run/next-occurrence, edit drawer, enable toggle, Runs link — all work; every mutation toasts.
- ✅ `/ai-agents/runs`: agent + status filters both drive the server read, filtered-empty state with "Clear filters" works, run links open, browser Back returns to the list without error.
- ✅ `/approvals`: org filter with per-org pending counts, search, `approvals-filter-summary` ("Showing 0 of 1 loaded"), filtered-empty + `approvals-filtered-empty-clear`, true empty state after the deny.
- ⚠️ Runs-list and approvals filter state is held in React state only — `location.hash` stays empty and a filtered view resets on reload/back-nav, against the repo's own `window.location.hash` URL-state convention (which `/ai-agents/impact#90` and `/agreements/signed#unlinked=1` do follow). See G2-7.
- ⚠️ Cosmetic, noted once: on a PATCH agent the schedules section is still headed "SCHEDULED SWEEPS … runs this agent's fleet sweep" / "No sweep schedules yet", though the only kind it offers is "Patch plan". And the AI-agents list shows "No partner-wide baseline exists yet for this kind" above two partner-wide agents. See G2-8, G2-9.

**G2 seed left on the stack (heads-up for other groups / re-testers):** 60 synthetic `alerts` on rule `High CPU usage` against `e2e-macos.local` (54 resolved, 6 active, titled "QA sweep synthetic CPU alert #N", ids `00000000-0000-4000-8000-…`) plus 30 `ai_alert_verdicts`, seeded for the #5885 cohorts — they will show up on `/alerts` and in any alert-count assertion. Also two synthetic `audit_logs` rows on `e2e-macos.local` (`0ff11111-…`), a denied `manage_patches:install` approval, an `artifact`-backed `ticket_attachments` row on ticket `32b8be67…`, and two enabled AI agents whose schedules (`0 3 * * *` sweep, `0 2 * * *` patch) WILL keep firing real LLM runs for as long as the stack is up.
