# Pre-release sweep — v0.110.0 → main (2026-09-08)

| | |
|---|---|
| Range | `v0.110.0` (`8b989054fb`, 2026-09-05) → `origin/main` `4290c59536` (193 first-parent commits) |
| Gate | merge queue drained 2026-09-08 ~23:00Z; last landed #5120 #5118 #5305; #5163 (iOS attestation W05, mobile-only) still open → follow-up row if it lands; no noise checks dismissed |
| Stack | `pnpm wt-stack up` on `qa/sweep-post-v0.110.0` @ `4290c59536`, subnet 192.168.250.0/24, baseUrl `http://localhost:33910` (portal `/portal`), seed admin promoted to platform admin |
| Flags enabled | `BREEZE_AI_AGENTS_ENABLED=true`, `AI_OPERATOR_TASKS_ENABLED=true`, `AI_OPERATOR_RECIPE_SERVICE_RECOVERY_ENABLED=true`, `DEVICE_COMMAND_OFFLINE_QUEUE_ENABLED=true` (default-on this release), `MFA_FORCE_FOR_PARTNER_ADMIN=false` (relief valve, see R1) |
| Driver | skill `pre-release-sweep` (release-prep session, Fable); inventory: sonnet Explore; sweep agents: opus, one per group |

## Release-cut findings surfaced during setup

**R1 — Forced MFA for every existing Partner Admin on upgrade (#4491, migration `2026-10-11-170000-partner-admin-force-mfa-reconcile.sql`).** Intended: the migration flips `roles.force_mfa` to true on the global system Partner Admin template AND every tenant copy, bumping `permissions_epoch` so each member without an enrolled factor gets `428 mfa_enrollment_required` and is sent to `/auth/mfa/setup?forced=1` on their next request. Observed on this stack: the seeded `admin@breeze.local` (Partner Admin) could not reach the app after login until enrolling. Relief valve documented in the migration header: `MFA_FORCE_FOR_PARTNER_ADMIN=false` (suppresses only the role-force component). **Release cut must:** (a) put this in the release notes as a breaking behaviour change for hosted AND self-hosted, (b) decide whether EU/US deploy with the valve set for a grace period, (c) note that the valve env var needs to be mapped through compose on the droplets if used. Sweep stack runs with the valve set so the seeded admin can log in; Group 4 verifies the forced flow explicitly with a fresh Partner Admin user. **Disposition (Todd, 2026-09-08):** do not enforce by default this release → PR #5307 flips the env default to OFF (opt-in via `MFA_FORCE_FOR_PARTNER_ADMIN=true`); notification-period rollout planned for next release as #5306. Release-day check: droplet `.env` must not pin the var to true.

## Change inventory

Status ∈ `TODO | PASS | PARTIAL | FAIL | BLOCKED | N/A`. Every row must leave `TODO`.

**Counts by surface (193 first-parent commits):** web 53 · portal 2 · mobile 11 (out of scope, verify via app) · agent/viewer/helper-only 15 · api-only 66 (~30 browser-visible, folded into rows below) · docs/CI/deps/tests-only 46. Oldest PR in range: #4967 (2026-09-05 16:07 -0600). Newest: #5305.

**New/changed env vars:** `AI_OPERATOR_TASKS_ENABLED` (new, default false) · `AI_OPERATOR_RECIPE_SERVICE_RECOVERY_ENABLED` (new, default false) · `DEVICE_COMMAND_OFFLINE_QUEUE_ENABLED` (new, **default ON**; compose ships it blank). `BREEZE_AI_AGENTS_ENABLED` unchanged (pre-existing, default false).

### Group 1 — Devices, Remote Tools & Discovery
| # | PR | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 1 | #5058,#5066 | web | `/devices` → filters → "Show removed" toggle; select 5 removed devices | Toggle adds removed rows; Compare bar stays visible past 4 selections | none | PASS |
| 2 | #5034,#5035 | web | Device row menu → **Remove** modal | Modal defaults to uninstall agent; removed list shows an Agent uninstall status badge | live/decommissioned agent for full state | PASS |
| 3 | #5053,#5042,#5047 | web | Removed devices → bulk select → **Restore** / **Permanently delete**; `/configuration-policies/[id]` → Device Lifecycle tab | Bulk actions succeed async; policy tab sets "purge removed devices after N days" | platform-admin for policy edit | PASS |
| 4 | #5255,#5253,#5261,#5260,#5258,#5239,#5271 | web | Devices → "+" split button → **Add asset manually…** (3rd device class + website/URL); Monitoring → **Add network asset** | Modal creates a manual/network asset; list refreshes and stays mounted post-create | none | PASS |
| 5 | #5090 | web | `/devices` (unified list default) → open a network row → `/devices/network/[id]` | Network device detail page renders; unified list is on by default | none | PASS |
| 6 | #4630,#5015,#5039 | web | `/devices/groups` → edit a dynamic group's criteria | Membership re-evaluates and updates without manual re-run | a device matching new criteria | PASS |
| 7 | #5294 | web | Device detail → Remote Tools tab / Overview card | desktopAccess change updates the tab live, no reload | live agent | PARTIAL |
| 8 | #4992 | web | Device detail → Diagnose tab | Loads without 500 on devices with bigint metric columns | none | BLOCKED |
| 9 | #4996 | web | Device detail → Processes tab, agent offline | Shows "couldn't load" error, not "0 processes / No data" | agent offline/503 | PASS |
| 10 | #5007,#4921 | web | Device detail → **Enter maintenance mode** | Toast text pluralizes correctly; lease persists across reload | none | PARTIAL |
| 11 | #5095 | web | Device detail → Services tab, a service with a space in its name | Start/stop succeeds; failed service commands surface an error | live agent, Windows service with space | FAIL |
| 12 | #5071,#5070 | web | Devices → Security tab summaries; `/security` pages | Summaries scoped to caller's site; Alerts list blocked without `alerts:read` | site-scoped user account | BLOCKED |
| 13 | #5195,#5191,#5096 | web | Discovery / network devices from UniFi | UniFi devices dedupe by canonical MAC; no duplicate rows on pagination | live UniFi integration | BLOCKED (integration) |
| 14 | #5060,#5061,#5055,#4991,#5028,#5044 | web | Devices → "+" menu → **Import from another RMM…** wizard | Definitions step then Values step import custom fields correctly | CSV export from another RMM | PASS |

### Group 2 — AI Agents, AI Operator, Automations & Scripts
| # | PR | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 15 | #5054,#5057,#5062,#5064,#5067 | web | Settings → AI Agents → **New agent** | Four-step guided create flow; capability picker replaces tool-allowlist textarea; edit drawer reuses same steps | AI flag (ON) | PASS |
| 16 | #5089 | web | AI Agent form → script picker | "Allowed to run unattended" picker enforces ceiling-aware validation | AI flag (ON) | PASS |
| 17 | #5167 | web | Scripts → script detail → Security tab | "Strict" security pattern requires explicit acknowledgment before save | script matching a Strict pattern | PASS |
| 18 | #5033,#4990,#4967,#4971,#5030 | web | Scripts → Execution History row → **Stop**; Automations → Run History → **Cancel run** | Running script/automation halts; UI reflects cancelling→cancelled | live agent for real process kill | FAIL |
| 19 | #5178,#5119,#5121 | web | AI chat → approve a pending action | Approval dialog headline matches command type; a worker-approved result no longer shows red "Failed" | AI flag (ON) | FAIL |
| 20 | #5031,#5014 | web | Settings → AI Agents list | Inert-agent badge has its own aria-label; org-only agent with no partner baseline shows a warning banner | AI flag (ON) | PASS |
| 21 | #4994 | web | `/admin/ai-kill-switch` | Kill-switch activity log shows admin's name, not raw ID | platform-admin | PASS |
| 22 | #5145,#5164,#5174,#5243,#5248 | web | Automation editor action step → "when offline" option; Device page action | Choosing "queue" vs "skip" persists; device page shows a "Queued (offline)" badge | offline-queue flag (ON) | FAIL |
| 23 | #5244,#5245,#5259,#5262,#5263,#5254,#5272 | web | Alert detail page → **Delegate to Operator** button → `/operator/tasks/[taskId]` | Task admits, runs a recipe, task detail page shows device feed & result | operator flags (ON), live agent | PARTIAL |
| 24 | #5248 | web | AI chat, ask an offline-only tool on an offline device | Error text now says "use the Run Script / deployment tools instead" | AI flag (ON), offline device | PASS |
| 25 | #5004,#5297 | web | Automations list, trigger the same metric anomaly twice quickly | No duplicate/piled-up runs against one rollup | none | N/A |
| 26 | #4902,#4903 | web | Ticket moved between orgs by an AI automation → ticket Activity tab | Activity log attributes the move to the AI agent; ticket's AI intent stays anchored to its org | AI flag (ON) + org-move automation | BLOCKED |

### Group 3 — Tickets, Organization Record & Service Desk
| # | PR | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 27 | #5086,#5109,#5110,#5100 | web | `/organizations/[id]` | Overview/Contacts/Sites/Devices/Activity/Tickets/Contracts & Billing tabs all render; URL-pinned org persists regardless of OrgSwitcher | none | PASS |
| 27a | everyday flow | web | Org record → Tickets → New ticket; reply; status; assignee; log time; Create invoice; new contact; new site | Each mutation lands with a visible confirmation | none | PASS |
| 28 | #5108 | web | Sidebar, org in Service Management mode | New "Service Desk" section appears; Timesheets relocates into it | org flagged for Service Management mode | PASS |
| 29 | #5118 | web | Any list with an advanced-filter drawer | Closing the drawer doesn't leave a stale filter-action scope open | none | PASS |
| 30 | #5006,#5013 | web | Quotes → editor, multi-run themed quote | Title no longer collapses; illegal reprice offer blocked; PDF cell alignment correct with theme fonts | none | PASS |
| 31 | #5041,#5001 | web | Settings → Profile → change language | `<html lang>` updates on next load via cookie; alert-verdict badge text is corrected | non-English locale | PASS |
| 32 | #5068 | web | Collapse the left sidebar rail | Icons render centered, not left-shifted | none | PASS |

### Group 4 — Auth, MFA, Sessions & Admin Roles
| # | PR | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 33 | #5120 | web | Settings/Security policy → activate an MFA policy that would lock out active sessions | Activation is blocked with an explanatory error | platform-admin | TODO |
| 34 | #4920 | web | Admin → Users → reset a user's MFA | User's passkeys are revoked in the same transaction as the MFA reset | platform-admin, target user with a passkey | TODO |
| 35 | #5008,#5037 | web | Account → Security → disable MFA / remove a factor | Session persists after disabling; removing a factor is explicit; rotation requires a code | active MFA enrolled | TODO |
| 36 | #5194 | web | Account → Security → Passkeys → register/delete | Session persists (no forced re-login) | WebAuthn-capable browser | TODO |
| 37 | #5203 | web | Account → Security → Phone number → replace | Session persists after replacing phone | SMS/phone MFA configured | TODO |
| 38 | #4491 | web | Settings → Roles → create a system Partner Admin role | New role stores `force_mfa=true` on every creation path | platform-admin | TODO |

### Group 5 — Billing, Accounting/QuickBooks & Portal
| # | PR | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 39 | #4624 | web | Settings → Billing → Integrations → Accounting; Invoice → **Sync to QuickBooks** | Payment pushes Breeze → QuickBooks | live QBO connection | BLOCKED (QBO) |
| 40 | #5094,#5072,#5092 | web | Settings → Billing → Integrations → Accounting tab, QuickBooks mapping workbench | Auto-syncs mappings after a decision; hides `quick_support` org; nested hash stays on Accounting sub-tab | live QBO connection | BLOCKED (QBO) |
| 41 | #5093 | web | Billing → Invoice card → **Issue** | Card shows a live QuickBooks sync state after issue | live QBO connection | BLOCKED (QBO) |
| 42 | #5187 | web | Billing → Invoice with applied payments → **Void** | Void is refused with an error | invoice with a recorded payment | TODO |
| 43 | #5029 | portal | Portal `/tickets` as a disabled user | Redirects to portal home instead of a 403 error page | disabled portal user | TODO |
| 44 | #4995 | portal | Portal, a module disabled for the org | Redirects instead of erroring | module disabled for org | TODO |
| 45 | #5097 | web | Security → DNS Security tab | Activity data loads from the v2 Umbrella endpoint | live Umbrella integration | BLOCKED (integration) |

### Group 6 — Admin, Settings & Configuration Policy
| # | PR | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 46 | #5099,#5122,#5125,#5111 | web | `/configuration-policies/[id]` on a child policy | Inheritance from parent policy displays; effective-links resolve consistently across every consuming feature tab | none | TODO |
| 47 | #5200 | web | Scripts → Categories → delete a parent category | Children re-parent cleanly instead of erroring | a category with children | TODO |
| 48 | #5197,#5012,#5010,#5000,#4999,#4998 | web | As Partner Admin, switch org context to "All organizations", open Custom Fields / AI Agents / Automations / Notifications / Software-Security / Identity-contract lists | Records from every child org appear (not just the primary org) | 2+ orgs with data (agent creates sibling org) | TODO |
| 49 | #5184 | web | Devices → Fleet Findings, request a malformed finding id | API returns 400, not 500 | none | TODO |
| 50 | #5192,#5201 | web | Settings → Integrations → Accounting, trigger a sync error | Error toast text; Sentry tags allowlisted (no direct UI symptom) | none | TODO |
| 51 | #5196 | web | Settings → Agent download link (self-hosted GitHub mode) | Serves the pinned agent version | self-hosted GitHub release mode | BLOCKED (env) |
| 52 | #5182 | web | Settings → API Keys, a delegated key scoped to a site | Device list via that key excludes devices outside its site | delegated key with site restriction (agent creates) | TODO |
| 53 | #5223,#5215 | docs-chore | Tool catalog / AI-authored flows design doc only | No UI yet this range | — | N/A |

### No browser surface (N/A for this sweep; verified elsewhere)
- #5296 #5302 (Linux/macOS/Windows service install + watchdog STANDBY) — agent binary; gate = SemoTech Linux canary + Windows VM.
- #5295 #5303 (Winlogon GDI GetDIBits, mid-session stop reason) — agent; gate = meckyp Winlogon retest.
- #5293 (viewer FPS counter) — viewer binary; macOS Tauri check.
- #5185 macOS uninstall cleanup · #5019 #4993 #4997 Windows PAM/reboot dialogs · #4969 process-tree cancel internals — agent.
- #5297 anomaly job dedup · #5027/#4007 admission gate + offline sweep · #4927 extensions boot sweep · #3918 server-side i18n resolver · #5011 PSA access-axis · #5021 #5018 #5009 indexes · #5004 cronDue memo — api internals.
- 11 mobile PRs (#5188/89 #5140/44 #5139/43 #5117/42 #5115/38 #5105/14 #5104/13 #5103/12 #5106/19 #4489/939 #5171/72/75/76/77) — TestFlight round.
- #5301 #5305 #5280 #5100 + 46 deps/CI/test-only PRs — no product change.


## Sweep log (append as you go)

### [Group — area] — agent, timestamp

#### [#PR / area] — PASS | PARTIAL | FAIL | BLOCKED
- ✅ what was checked, concretely
- ❌ BUG: symptom → API actual (status + body) vs UI actual
- ⚠️ UI/UX: paper cut (also copy to the list below)
- BLOCKED: prerequisite

## UI/UX paper cuts

| # | Where | Observation | Severity | Disposition (fixed <sha> / issue # / noted) |
|---|---|---|---|---|
| G1-1 | Remote Tools → Services / Registry / Event Log / Scheduled Tasks (offline device) | A 503 `device_offline` renders as `0 of 0 services` + "No Services Available" — the #4935 misread, still unfixed on the tabs #4996 did not touch | High | noted (row 11 FAIL, needs an issue) |
| G1-2 | Remote Tools → Services panel (all devices) | Raw i18n placeholders ship as visible copy: the panel heading renders literally **"Title"** and the empty state **"Refresh Hint"** (`en/remote.json: servicesManager.title="Title"`, `refreshHint="Refresh Hint"`, `adjustHint`, `agentRestartConfirm`, …). Pre-existing (#4321), not in this range | Medium | noted |
| G1-3 | Add network asset → "Add an HTTP check" follow-up | Monitor **Name** is not prefilled from the asset label, and the required-name validation only appears after a Create click that fires no request | Low | noted |
| G1-4 | Create Network Monitor (all entry points) | A 201 gives **no confirmation at all** — `CreateMonitorForm` (`:116-126`) has no `showToast`, so both modals just close | Medium | noted |
| G1-5 | Config policy → Device Lifecycle tab → Save | No success feedback; the summary line follows the local toggle, not the saved link, so a successful save looks identical to a no-op (only `PatchTab` toasts) | Medium | noted |
| G1-6 | Devices list → bulk purge result toast | Skip reasons print as raw enum codes (`No device was deleted: 2 UNINSTALL_PENDING`) | Low | noted |
| G1-7 | Devices list toasts | Plural-by-parenthesis copy: "2 device(s) removed", "Permanently deleting 2 device(s)…", "Permanently delete 2 device(s)?" | Low | noted |
| G1-8 | Unified device list → network/manual rows | The device name is a `<span>`, not a link — the row navigates on click but middle-click / open-in-new-tab / copy-link do not work as they do for agent rows | Low | noted |
| G1-9 | Import from another RMM → step 2 | No header auto-guessing (a column named `hostname` still defaults to "Ignore"), and "Custom field…" makes you retype the field key as free text instead of picking from the fields the wizard just created; after commit the button reads "Import 0 rows" | Medium | noted |
| G1-10 | Remote Tools → Processes on an unreachable device | 30 s of bare spinner before "Request timed out after 30s"; nothing says the device has not checked in | Low | noted |
| G1-11 | Device page (any device with no reliability snapshot) | `GET /api/v1/reliability/<id>` 404s into the browser console on every load | Low | noted |
| G2-1 | Automations list → **Run** (row action) | A `POST /automations/:id/trigger` **200** produces **no toast at all** — `AutomationsPage.handleRun` (`:263-280`) is a bare `fetchWithAuth` that only `setError`s on failure; the sole success signal is the Last Run cell flipping to "Just now". Not a `runAction` caller and not in `runActionAllowlist.ts` | Medium | noted |
| G2-2 | Automation editor → **Create Automation** / **Save** | A 201/200 gives no success toast either — `AutomationEditPage` (`:369-382`) is also a bare `fetchWithAuth` with inline `setError`; success is only the redirect to the list | Medium | noted |
| G2-3 | `/automations/new` and `/automations/[id]` | React **hydration mismatch error in the console on every load**: the SSR HTML has the old `<div class="grid gap-6 md:grid-cols-2">` where the client renders the `<fieldset data-testid="automation-owner">` SCOPE block, so React discards and regenerates the form subtree. `showOwnerScope` is derived from auth, which SSR does not have. Pre-existing (#2133), not in this range | Low | noted |
| G2-4 | Scripts → Run → **Confirm Execution** copy | Reads "Scripts are queued and begin executing immediately." even when every selected target is offline and the dispatch will in fact queue for up to 7 days. The post-submit toast ("Runs when the device is online") is correct — only the confirm step is wrong | Medium | noted |
| G2-5 | Device page → Scripts tab → Execution History table | The STATUS column prints the **raw lowercase enum** (`pending`) while the detail panel beside it prints "Pending". `DeviceScriptHistory.statusConfig` also has **no `queued` entry** and `en/devices.json > deviceScriptHistory.status` has no `queued` key, so the new offline-queue state has no rendering on this surface at all | Medium | noted (row 22) |
| G2-6 | Device page → Scripts tab → Execution Details panel | Offers only **Run again / Close** — no Stop, for any status. `DeviceScriptHistory.tsx` has no cancel affordance at all, while `ExecutionHistory.tsx`/`ExecutionDetails.tsx` on `/scripts/[id]/executions` offer Stop for `pending`/`queued`/`running`. A tech who reaches a queued run from the device (which is where the app itself navigates after Run) cannot stop it | Medium | noted |
| G2-7 | Settings → AI Agents list | The `sr-only` inert-badge description ("No partner-wide baseline exists yet for this kind…") is emitted once per list unconditionally, referenced by `aria-describedby` only from rows that actually carry the Inactive badge. A screen reader reading the list linearly hears it even when every agent is partner-wide, where it is the opposite of true | Low | noted |
| G2-8 | Alert detail → **Delegate to Operator** | Nothing on the alert records that a task already exists — the button stays enabled with unchanged copy, and a second click mints a **second Operator task** for the same alert (`replayed: false`, two rows in `ai_operator_tasks`). The idempotency key is deliberately per-opened-dialog (`DelegateToOperatorButton.tsx:54-58`), so this is by design for retries, but nothing stops a tech from queuing N tasks racing on the same service. Mitigated at execution: the duplicate never ran a second LLM turn — it went `handed_off` / `document` with outcome `unresolved` and detail **"run admission skipped: cooldown"**. The cost is a bogus "unresolved" task in the operator queue, not duplicate work on the device | Low | noted (row 23) |
| G2-9 | `/operator/tasks/[taskId]` → Linked runs | The linked agent run is listed as a **bare UUID** (`c5c0be0e-2d69-4048-aa98-537a6f22ab42`) with only `running` beside it — no step name, no timestamp, nothing a tech can act on | Low | noted |
| G2-10 | AI chat → approval card headline | The card renders `Execute "restart_service" command on device e65460f3...` — a **truncated device UUID** — while the `approval_requests.action_label` the server persisted for the same request already reads `… command on e2e-windows.local`. Two headline builders disagree: the live card comes from `aiGuardrails.buildApprovalDescription` (`:2141`, `deviceId.slice(0, 8)`), the stored label resolves the hostname | Medium | noted (row 19) |
| G2-11 | Ticket detail → **Move to another org…** | With only one organization the dialog opens with a picker whose sole entry is the placeholder "Select organization…" and no empty-state copy — nothing says why there is nowhere to move to | Low | noted |
| G3-1 | Organization record → Overview `Devices` tile + Sites table device counts | Both **include removed (decommissioned) devices** that the record's own Devices tab correctly excludes: Overview says `Devices 6 · 1 of 6 online` and Default Site says `6`, while the Devices tab says `4 of 4` (DB: 6 rows, 2 `decommissioned`). Three surfaces on one page give a tech three different fleet sizes for the same customer | Medium | noted |
| G3-2 | Organization record → header summary chips | The header's `1 site` / `No primary contact` chips do not refetch after a mutation made in the tabs below them: after creating a primary contact and a second site the header still read `1 site` + `No primary contact` while the tables underneath read `2 of 2 sites` and showed the contact badged `Primary (organization)`. Correct after a reload | Low | noted |
| G3-3 | Organization record → Activity tab (and Overview's Recent activity) | Every mutation writes **two** audit rows: a semantic one (`Created site` / `Contact create`, with the object name) and a generic route-derived one (`Api post orgs sites`, details `path: /api/v1/orgs/sites, method: POST, fallback: true, status code: 201`). The `fallback: true` rows double the feed and read as developer output — "Api patch ai agents :id", "Api post alerts :id create-ticket" | Medium | noted |
| G3-4 | `/organizations` (no id) | Hard **404 "Page not found"** — the record page lives at `/organizations/[id]` but the collection URL above it has no page; the actual list is `/settings/organizations`. A tech who edits the URL, or follows a stale link, gets a 404 instead of the list | Low | noted |
| G3-5 | Ticket → TIME & BILLING → **Log time** quick-add | The quick-add has Minutes / Description / Billable and **no rate field**, so the very next step — **Create invoice** — is guaranteed to fail `409 ALL_MISSING_RATE`. Nothing at log time says a billable entry needs a rate, and the card's `Time amount` just shows `—` | Medium | noted |
| G3-6 | Quote editor → Table block → **Save** | Saving a table cell warns about formatting the user never applied: **"Saved, but some unsupported formatting was removed: p. Rich text supports headings, bold, italic, underline, lists and links."** The cell editor is a ProseMirror instance whose own serialization is `<p>…</p>`, and the server's inline-only sanitizer strips it — so the normal happy path always shows a scary partial-success warning. Content itself persists correctly | Medium | noted |
| G3-7 | Quote editor → Table block → cell Bold/Italic/Underline/Link | Mark toggles `selectAll()` before applying (`InlineRichTextEditor.tsx:154-179`, deliberate: "Marks apply to the whole cell in one click"), so a **mixed-format cell cannot be authored at all** — verified with a real double-click on one word followed by a real click on Bold: the whole cell bolded. Consequence: the multi-run centred/right-aligned cell that #5013's `drawRuns` rewrite exists to render is unreachable from the web UI (only wrapped single-run cells are), so that path is only exercised by API/imported content | Low | noted (row 30) |
| G3-8 | `/tickets/new` with Service Management **off** | The route is not gated — it renders the full create form, org picker and all, and only fails on submit with `409 service_management_off`. A dead-end form for a module the partner has turned off | Low | noted (row 28) |
| G3-9 | Devices → Bulk Actions → **Wake Selected** result toast | Reads **"Bulk Wake All Failed"** — title-cased and enum-ish — and discards the 202 body's per-device reason (`code: NO_MACS`, "Target has no recorded MAC address. The agent m…"), so the tech is told it failed but not why | Low | noted |
| G3-10 | Alerts → AI VERDICT column, no verdict | The em-dash cell carries the explanation as `title` only (`title="No AI verdict yet"` / fr-FR `"Aucun verdict de l'IA pour le moment"`) with **no `aria-label`**, so a screen reader hears a bare "—" and touch users get nothing | Low | noted |
| G3-11 | Collapsed sidebar rail on a machine with classic (space-taking) scrollbars | Post-#5068 every icon, highlight and divider shares one centre line (verified), but because the fix drops `scrollbar-gutter: stable` in icon mode the whole column sits **8 px (half the scrollbar) left of the rail's geometric centre** once the rail overflows. Internally consistent — nothing looks broken — but the icon strip is not on the rail's visual midline. Not reproducible in headless Chromium, which has overlay scrollbars | Low | noted (row 32) |

## Fixes applied

| Commit | PR/area | What | Test |
|---|---|---|---|

## Issues filed

| Issue | Title | From row |
|---|---|---|

## Summary

| Group | PASS | PARTIAL | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|

**Top findings:**

**Oldest PR reached:** #…

**Before the release cut:** flags to enable, BLOCKED rows needing a live agent, open issues.

### Group 1 — Devices, Remote Tools & Discovery — opus, 2026-09-08 23:55Z

Fixtures created through the UI for this group: manual assets `QA Spare Laptop G1`, `QA Asset G1-2/3/4`; network asset `QA Website Asset G1` (type Website, `https://qa.example.com`) + linked HTTP monitor `QA Website HTTP check`. Four synthetic `qa-agent-N.local` agent rows were inserted directly in Postgres (with `breeze.scope=system`) purely to reach 5 selected agent devices for row 1 — agent enrolment needs real agents.

#### [#5058 #5066 / devices list — show removed + Compare cap] — PASS
- ✅ Removing `e2e-windows.local` left the list at `3 of 3 devices  1 removed hidden — show`; clicking **show** ADDED the removed row (`All 4 / Agent 2`) and flipped the hint to `1 removed shown — hide` (`data-testid="decommissioned-shown-hint"`) — the #5023 paper cut (swapping the view to a removed-only filter) is gone.
- ✅ With 5 agent devices selected the selection bar stayed up (`5 selected`) and the Bulk Actions menu still carries the Compare item, present-but-disabled and labelled **"Compare supports up to 4 devices"** instead of vanishing. At 2–4 selected the bar also prints a class breakdown (`3 selected · 1 agent, 0 network, 2 manual`).
- ✅ Compare is gated on `selectedAgentCount >= 2` (post-#5090), so a mixed agent+manual selection correctly offers no Compare item.

#### [#5034 #5035 / Remove modal + agent-uninstall state] — PASS
- ✅ Row menu → **Remove** opens "Remove e2e-windows.local?" with the radio **"Uninstall the Breeze agent"** pre-selected (`data-testid="remove-choice-uninstall"`, `checked`) and "Leave the agent installed" as the opt-out; sub-copy "Queued now — an online agent collects it within moments."
- ✅ Confirm → toast `Removing` with an **Undo** affordance; the removal commits after the undo window (device row disappears, `devices.status = 'decommissioned'`, `device_commands` row `type='self_uninstall' status='pending'` created).
- ✅ Device page for the removed device shows `Removed` + **"Agent uninstall queued — expires in 2 days"** (#5035's `UninstallStateBadge`). Detail-payload-only, as documented — the list row shows just `Removed`.
- ⚠️ UI/UX: `GET /api/v1/reliability/<id>` 404s in the console on a device with no reliability score yet (both agent devices) — no visible symptom, but it is a console error on every device page load.

#### [#5255 #5253 #5261 #5260 #5258 #5239 #5271 / manual + network assets] — PASS
- ✅ `/devices` "+" menu offers `Install agent…` / `Add asset manually…` / `Add network asset…`. Manual create: `POST /devices/manual` → **201**, toast "Manual asset created.", list refreshed in place (`All 3 / Agent 2 / Network 0 / Manual 1`) and a **Class** column appeared — third device class is live.
- ✅ Network asset create with type **Website** and `URL https://qa.example.com`: `POST /devices/network` → **201**, toast "Network asset created". The modal offers IP / hostname / URL with "Provide at least one of…" — #5253's website/URL target.
- ✅ #5271 verified directly: after the 201 the modal **stays mounted** across the list refresh and advances to a follow-up step ("Monitor this asset with an HTTP check?" → *Done* / *Add an HTTP check*). Choosing the HTTP check opens Create Network Monitor pre-set to **HTTP Check** with the URL prefilled; submit → `POST /monitors` **201**, row lands in `network_monitors` with `asset_id` set.
- ✅ Network row opens `/devices/network/<id>` (row 5).
- ⚠️ UI/UX: the follow-up monitor form does **not** prefill Name from the asset label, and its "Name is required" validation only appears after a click that looks like a no-op (first Create Monitor click fired no request at all).
- ⚠️ UI/UX: monitor creation has **no success confirmation** — `CreateMonitorForm` (`apps/web/src/components/monitors/CreateMonitorForm.tsx:116-126`) calls `fetchWithAuth` and `onCreated()` with no `showToast`, so a 201 just closes both modals and drops the tech back on `/devices` with nothing said. Not a `runAction` caller and not in `runActionAllowlist.ts`.

#### [#5090 / unified device list + network device detail] — PASS
- ✅ Unified list is on by default: the class segment (`All / Agent / Network / Manual`) renders on first load of `/devices` with no flag flip, and the Class badge column appears once a non-agent row exists.
- ✅ `/devices/network/<id>` renders: header (name, `Website`, `Approved`, `Offline`, site), stat tiles (Status/Ping/Open ports/Linked device), tabs **Overview / Monitoring / Identity**, editable Type with "Reset to auto-detected", SNMP empty-state copy, open-ports empty state. Monitoring tab shows `Network monitoring: Enabled` for the asset that has the HTTP check.
- ⚠️ UI/UX: a network row's name is plain text, not a link — the row is click-navigable but there is no href, so middle-click / "open in new tab" / copy-link do not work the way they do for agent rows.

#### [#4996 / Processes tab on an unreachable agent] — PASS
- ✅ Device page → **Remote Tools** → Processes (`/remote/tools?deviceId=…`): while the command is outstanding the tiles render `-` (not `0`), and after the agent timeout the table body shows **"Request timed out after 30s"** with a **Retry** link. No "0 processes / No Data" misread — #4935 is fixed.
- ⚠️ UI/UX: it takes the full 30 s agent timeout to say anything, and the three tiles read `-` with a bare spinner for that whole time; nothing says "this device has not checked in".

#### [#5053 #5042 #5047 #5058 / bulk restore, async purge, Device Lifecycle policy] — PASS
- ✅ Bulk **Remove Selected** on 2 devices: confirm dialog reads "Remove 2 devices?" with a liveness line ("2 online, 0 not currently online.") and the same uninstall/leave radio; two `DELETE /devices/:id` → **200**, toast "2 device(s) removed".
- ✅ A removed-only selection swaps the bulk menu to **Restore Selected / Compare Selected / Delete Permanently…**. Restore: `POST /devices/bulk/restore` → **200** `{succeeded:[…3 ids]}`; all three came back as `offline` (liveness-truthful, not a blind `online`), and `/devices` reloaded with **no stale removed-only filter** and no leftover hint — the #5058 stale-filter paper cut is fixed.
- ✅ Purge confirm parity (#5058): "Permanently delete 2 device(s)?" lists the hostnames, warns that history dies with them, and requires typing the count. `confirm-bulk-purge` carries `aria-disabled="true"` until the count matches — a click before typing is refused (verified: Playwright could not click it), and no request fires.
- ✅ Async purge (#5042): `POST /devices/bulk/permanent-delete` → **202** `{jobId, accepted:2, rejected:[]}`, then the page polls `GET /devices/bulk/purge-runs/:jobId` and reports the real outcome. Both targets carried a never-collectable `self_uninstall` (no live agent), so the worker returned `{purged:[], skipped:[{code:'UNINSTALL_PENDING'}×2]}` and the UI raised the warning toast **"No device was deleted: 2 UNINSTALL_PENDING"** — the honest zero-purge path, not a false success.
- ✅ Device Lifecycle policy tab (#5053): `/configuration-policies/<id>#device_lifecycle` → toggle **Permanently delete removed devices** reveals `Permanently delete removed devices after [30] days`; set 45 → `POST /configuration-policies/:id/features` **201** with `inlineSettings {"purgeRemovedAfterDays":45}`, persists across reload, summary reads "Removed devices are permanently deleted 45 days after removal." Copy is honest about irreversibility and about `UNINSTALL_PENDING` devices being skipped.
- ⚠️ UI/UX: skip reasons surface as raw enum codes (`2 UNINSTALL_PENDING`) rather than a sentence a tech can act on.
- ⚠️ UI/UX: the Device Lifecycle tab's **Save** gives no success feedback at all — `DeviceLifecycleTab.handleSave` has no toast, and the summary line follows the local toggle rather than the saved link, so a successful save is indistinguishable from a no-op. (Only `PatchTab` among the feature tabs toasts.)

#### [#4630 #5015 #5039 / dynamic group re-evaluation] — PASS
- ✅ Created a dynamic group (`Hostname contains macos`) — the builder previews matches live via `POST /filters/preview` (1 device) before saving; `POST /device-groups` **201**, card reads `Dynamic · 1 device · Matches 1 device`.
- ✅ Edited the criteria to `Hostname contains windows` → `PATCH /device-groups/:id` **200** and membership re-evaluated **on save, with no manual re-run**: `group_membership_log` records, at one timestamp, `e65460f3… added / filter_match` and `42fc7de0… removed / filter_unmatch`. The card's rule text updated in place.

#### [#5007 #4921 / per-device maintenance mode] — PARTIAL
- ✅ #5007's entry point exists: device page → **Power** → `device-power-action-maintenance` ("Enter Maintenance"), plus the overflow menu item.
- ✅ The dialog is honest per #4921 — "Records a maintenance window on e2e-macos.local and notes who started it. **Alerts, monitoring, patching, scripts and reboots continue as usual.**" Reason (3–500 chars) + duration presets, with "The window always runs from now — extending replaces the current window instead of adding to it."
- ✅ Actuation is gated: submit → `POST /devices/:id/maintenance` **403** `{"error":"Step-up required","code":"STEP_UP_REQUIRED"}`, and the dialog replaces its controls with "Add an authenticator app or passkey to your account before putting devices into maintenance mode." — a correct, non-silent failure.
- BLOCKED: the toast pluralisation and lease-persists-across-reload halves need an MFA factor enrolled on the acting user. Not enrolled deliberately — Group 4 owns MFA rows 33–38 on this same admin, and enrolling here would disturb them.

#### [#4992 / device diagnose] — BLOCKED
- BLOCKED: `POST /devices/:id/diagnose` requires a live agent socket — it short-circuits before the metric read the #4992 fix touches: **500** `{"error":"Device is not currently reachable over the live connection…","code":"agent_execution_failed"}`. The bigint serialisation path (screenshot completes → `device_metrics` rows projected → `c.json`) is unreachable without a live agent. There is also no web caller for this route (grep: zero `"/diagnose"` in `apps/web/src`), so "Device detail → Diagnose tab" does not exist as a browser surface this release; the consumers are mobile/AI.
- ⚠️ UI/UX (pre-existing, not in range): unreachable-device is reported as HTTP **500**, not 409/503.

#### [#5294 / Remote Tools + Overview pick up desktopAccess live] — PARTIAL
- ✅ The web half's **visibilitychange backstop** is live-verified without an agent. With Remote Tools open for `e2e-macos.local`, flipping `devices.desktop_access` in Postgres to `{"mode":"unavailable","reason":"helper_not_running",…}` and firing a hidden→visible `visibilitychange` produced a single `GET /devices/:id` and the header button changed **in place** from an enabled "Connect Desktop" to a disabled **"Desktop Unavailable"** (`title="Desktop is unavailable on this device"`) — no reload, no remount. Flipping the row back to `mode:"available"` and repeating restored the enabled button, so it is live in both directions (the v0.110.0 complaint was the recovery direction).
- ✅ Remote Tools now opens an event-stream subscription on mount (`POST /events/ws-ticket` fires on page load), consistent with the new `useEventStream` subscription.
- BLOCKED: the `device.updated { fields:['desktopAccess'] }` push itself originates in `POST /agents/heartbeat`, which needs a real agent token (the e2e seed sets no `agent_token_hash`), so the socket path was not exercised end-to-end.

#### [#5095 / Windows service names with spaces + failed service commands] — FAIL (sibling defect, not the fixed path)
- ❌ **BUG — the Services tab reads a 503 as "no services", the exact #4935 misread that #4996 fixed one tab over.** Remote Tools → **Services** on the offline `e2e-windows.local`: `GET /api/v1/system-tools/devices/e65460f3-…/services?limit=500` → **503** `{"error":"The device is offline.","code":"device_offline"}`, and the UI renders `0 of 0 services on E2E Windows Test Device` with the empty state **"No Services Available"**. No error, no mention of offline; the operator cannot tell an idle box from an unreachable one. Root cause confirmed in source: `RemoteToolsPage.fetchServices` (`apps/web/src/components/remote/RemoteToolsPage.tsx:649-665`) swallows the throw into `console.error` and `ServicesManager` is rendered with **no `loadError` prop** (`:1043-1054`), while the sibling `ProcessManager` receives `loadError={processError}` (`:1026`). Same for the Registry / Event Log / Scheduled Tasks tabs, which share the pattern. Suggested fix is the #4996 one: hold a `serviceError` state and pass it down.
- ✅ #5095's own claim (a failed service command surfaces an error) is wired correctly by inspection — `handleStartService/StopService/RestartService` all go through `runAction`.
- BLOCKED: interior-space service names and an actually-failing start/stop need a live Windows agent with such a service.

#### [#5071 #5070 / site-scoped security + alerts summaries] — BLOCKED
- BLOCKED on creating a restricted principal, and the blocker is structural on this seed: (a) the seed has exactly **one site**, so a site-restricted total is indistinguishable from the org total; (b) **Settings → Users → Invite user only offers partner-scoped roles** (Partner Billing/Viewer/Security Approver/Admin/Technician/Viewer), so the org-scoped custom role needed for a restricted reader cannot be assigned through the partner UI. I did create the role — `QA G1 No Alerts` (devices/users/organizations/sites/reports only, **no `alerts:read`**, verified in `role_permissions`) — so the assign-and-log-in half is all that is missing; it needs a second site + a device in it, and an org-scoped invite path.
- ✅ No regression on the unrestricted path: `/security` renders the score, vulnerability and AV-coverage cards ("4 devices tracked"), and `/alerts` lists both seeded alerts with summary counts; zero 4xx/5xx on either page.

#### [#5195 #5191 #5096 / UniFi dedupe] — BLOCKED (integration)
- BLOCKED: needs a live UniFi controller. `/discovery` itself renders (Assets/Profiles/Jobs/Topology/Changes) and correctly lists the manual website asset as `Approved · Monitored`, so the discovered-assets surface those PRs touch is up.

#### [#5060 #5061 #5055 #4991 #5028 #5044 / Import from another RMM] — PASS
- ✅ Step 1 (definitions): source picker (Datto RMM / NinjaOne / ConnectWise Automate / N-central / Generic CSV). Dropped a 2-row CSV (`key,label`) as Generic CSV → both slots parsed, **Name prefilled from the label column**, per-row type selector, owner-scope radio (All organizations / This organization only). `POST /custom-fields/import/preview` **200** annotates each row `create`; commit → `POST /custom-fields/import` **200** with both `definitionId`s, and the wizard advances to step 2 with a "Back to field definitions" affordance.
- ✅ Step 2 (values): dropped `hostname,QA Asset Owner,QA Warranty Months` with the two seeded hostnames. Per-column target select (identifier / custom field / warranty / ignore), "When a value is already set" skip-vs-overwrite, date-format selector, warranty-override checkbox. `POST /devices/custom-fields/import/preview` **200** → both rows `matched` by `method:"hostname"`, every value `applied`; commit → `POST /devices/custom-fields/import` **200** `{appliedValues:4, skippedValues:0, failedValues:0}` and the modal prints "Applied 4, skipped 0, failed 0 values".
- ✅ Landed correctly in `device_custom_field_values` with `source='import'`, and **#5028's coercion works against a real DB**: the quoted thousands-grouped cell `"1,236"` stored as `value_number = 1236` on a `number` field, while text went to `value_text`. Both values render on the device page under **Custom Fields** ("QA Asset Owner: Alice Ops", "QA Warranty Months: 24").
- ⚠️ UI/UX: step 2 auto-guesses **nothing** — even a column literally named `hostname` defaults to "Ignore this column", though step 1 guessed its own headers.
- ⚠️ UI/UX: mapping a column to "Custom field…" requires **typing the field key by hand** (`cf-val-fieldkey-<header>`, free text) — the keys the wizard itself created one step earlier are not offered as a picker, so a typo silently maps nothing.
- ⚠️ UI/UX: after a successful import the commit button re-labels to "Import 0 rows" while the preview table still shows the imported rows.

**Group 1 fixtures left on the stack** (for whoever sweeps after me): manual assets `QA Spare Laptop G1`, `QA Asset G1-2/3/4`; network asset `QA Website Asset G1` + monitor `QA Website HTTP check`; 4 synthetic agent rows `qa-agent-1…4.local` (1–2 restored/offline, 3–4 removed with a never-collectable `self_uninstall`, so they cannot be purged); config policy `QA G1 Lifecycle` (device_lifecycle, 45 days); dynamic group `QA G1 Dynamic macOS` (`Hostname contains windows`); custom-role `QA G1 No Alerts` (unassigned); custom fields `qa_asset_owner` / `qa_warranty_months` with values on both e2e devices. `e2e-windows.local` was removed and restored (now `offline`); `e2e-macos.local`'s `desktop_access` was set to `mode:"available"` while testing row 7.

### Group 2 — AI Agents, AI Operator, Automations & Scripts — opus, 2026-09-09 00:38Z

Fixtures created through the UI: AI agents `QA G2 Alert Triage` (partner-wide, triage, shadow, 2 unattended scripts authorized) and `QA G2 Org Patching Orphan` (org-only, patching — deliberately baseline-less, for the Inactive badge); script `QA G2 Strict HKLM Script` (PowerShell, HKLM write, Strict pattern acknowledged); automation `QA G2 Offline Queue Automation` (manual trigger, run_script, `whenOffline`). The AI kill switch was flipped and restored (epoch 0 → 2, currently **AI actions active** — restored, do not re-flip).

#### [#5054 #5057 #5062 #5064 #5067 / AI agent guided create flow] — PASS
- ✅ Four-step stepper is live: **1 Purpose and posture** (mode / kind / ownership / name) → **2 What it does** (triggers + capabilities) → **3 Safety and oversight** (protected resources, limits, approvers) → **4 Review and create**. `/settings/ai-agents` empty state → "Create your first agent" opens it full-width in place of the list, not in a drawer.
- ✅ #5057 verified by absence: there is **no tool-allowlist textarea anywhere in the flow or the edit drawer**. The only textareas are the three protected-resource lists and Instructions. Step 2 is the `capability-picker` — 12 capability groups, per-operation checkboxes each labelled `Approval request` or `Logged proposal`, an "always on / 60 read-only tools" note, `Show tool names`, and op-level search (`disk cleanup` filtered the tree to the one matching operation). "Use recommended" applied 6 operations across 4 capabilities and the live summary sentence moved from "may propose 0 operations across 0 capabilities" to "6 operations across 4 capabilities (4 approval requests and 2 logged proposals); executes nothing unattended."
- ✅ Step 4 is a server-evaluated plain-language review with a per-section **Edit** link (Runs when / Can read / May propose / Executes unattended / Never touches / Limits / Asks for approval from) and a "Start enabled" toggle that defaults off ("Off is the safe default"). It is honest about mode overriding the script authorization: with 2 scripts ticked but shadow mode selected, "Executes unattended" reads "Nothing. Shadow mode observes and proposes."
- ✅ Selecting **Act** on step 1 reveals a five-line consequence list and a required acknowledgement ("I understand act mode executes unattended, verification can fail, and there is no rollback") — `agent-create-flow-next` stays `disabled` until it is ticked.
- ✅ Create: `POST /ai/agents` **201**, toast "Agent saved", row lands in the list. Partner ownership persisted correctly (`ai_agents.org_id IS NULL`, `partner_id` set) for "All organizations" and org-owned for "This organization only".
- ✅ #5067: **Edit** opens `ai-agent-editor-drawer` which renders the create flow's own step components — `agent-step-does` and `agent-step-safety` are both present inside the drawer, carrying the same `capability-picker` and `ai-agent-scripts` picker, pre-populated from the saved row ("Applied" on the recommended block, "2 of 7 operations"), plus the drawer-only Enabled switch and Graduation panel.

#### [#5089 / scripts allowed to run unattended — ceiling-aware validation] — PASS
- ✅ Step 3 carries **SCRIPTS ALLOWED TO RUN UNATTENDED** with `ai-agent-scripts-count` ("2 scripts authorized") and, on a partner draft only, the ceiling hint "Partner-level scripts are a ceiling: an organization's agent can only pick from this list." Saved to `ai_agents.act_assets.scriptIds`.
- ✅ **The ceiling is exact.** With the partner triage baseline authorizing 2 of the 24 system scripts, an org-scoped act-mode triage draft's picker enabled **exactly those 2** and rendered the other **22 disabled** with the inline label "Not in partner baseline" — verified per-checkbox (`disabled: true` on 22, `false` on 2), not just by copy.
- ✅ The picker is correctly gated: `SafetyStep.tsx:203` renders it only for `ownerScope === 'partner' || mode === 'act'`, so an org-scoped shadow draft (where unattended scripts cannot apply) does not show it at all. Deliberate, matches the section's own copy.

#### [#5167 / Strict security pattern acknowledgement] — PASS
- ✅ `ScriptSecurityReview` renders nothing until the content matches a Strict pattern, then appears live as you type: for `Set-ItemProperty -Path 'HKLM:\…'` it showed "This script matches 1 pattern the agent refuses to run without an acknowledgement", the pattern name **PowerShell HKLM modification**, its explanation, and the per-pattern checkbox — plus the warning "Not acknowledged — the agent will refuse to run this script." No "acknowledge all" affordance, as designed.
- ✅ Ticking it flipped the heading to "Every matched pattern has been acknowledged"; `PUT /scripts/:id` **200**, toast "Script saved", and `scripts.acknowledged_security_patterns = {"PowerShell HKLM modification"}` with `security_acknowledged_by` and `security_acknowledged_at` both set.
- ✅ **Enforcement is at dispatch, and it is real.** Before acknowledging, the queued `device_commands.payload` carried **no `acknowledgedSecurityPatterns` key at all** (so the agent's validator refuses). After acknowledging, the next dispatch's payload carried `"acknowledgedSecurityPatterns": ["PowerShell HKLM modification"]`. Verified by diffing the two command rows in Postgres.
- Note on the inventory wording: save is **not** blocked on acknowledgement (a 201 is returned with an unacknowledged Strict pattern), and by design — the acknowledgement gates *execution*, and the UI says so. The row's "before save" phrasing overstates it.

#### [#5033 #4990 #4967 #4971 #5030 / Stop script · Cancel automation run] — FAIL
- ✅ Script **Stop** works end to end. `/scripts/[id]/executions` row → `cancel-execution-<id>` (icon-only, `title="Stop"`) → confirm dialog "Stop this execution? … Force stop skips the grace period and kills the process immediately." → `POST /scripts/executions/:id/cancel` **200** `{"success":true,"execution":{"status":"cancelled","cancelState":"confirmed"}}`. Row flipped to **Cancelled** in place; `script_executions` row `status=cancelled cancel_state=confirmed` with the actor recorded, and the **offline-queued `device_commands` row was retracted to `cancelled` too**, so it will not fire on a later check-in. The status filter offers the new `Queued` and `Stopping…` values.
- ✅ Automation **Cancel run** works at the API and data layer. Run history → `cancel-run` → "Cancel this run? Breeze will stop every execution this run is still waiting on." → `POST /automations/runs/:id/cancel` **200** `{"success":true,"run":{"status":"cancelled"},"executionsStopped":3,"executions":{"retracted":3,…}}`; the card flipped **Running → Cancelled** and 3 more `script_executions` + their `device_commands` went to `cancelled`.
- ❌ **BUG — the automation cancel-run success toast renders a raw i18n key: `automationRunHistory.actions.cancelRunSuccess`.** Root cause is a field-name mismatch, confirmed in both files: the API returns **`executionsStopped`** (`apps/api/src/routes/automations.ts:865,879`; `automationRunCancellation.ts:339`), while the web handler reads **`executionsCancelled`** (`apps/web/src/components/automations/AutomationRunHistory.tsx:375,380`) and passes it as i18next's `count`. `en/scripts.json` defines only `cancelRunSuccess_one` / `cancelRunSuccess_other`, so with `count: undefined` no plural form resolves, there is no bare key to fall back to, and the key itself is displayed. The stale comment at `:370-373` documents the wrong "Real shape" of the response. **Why CI is green:** `AutomationRunHistory.cancel.test.tsx:115,133,147` stubs the response as `executionsCancelled: 3/4` — the test asserts against a field the API never sends. Fix is one word in the web component plus the test fixtures and the comment.
- BLOCKED: the `cancelling` → `cancelled` intermediate state was never observable here. Both cancels short-circuited straight to `cancelled` with `cancelState: 'confirmed'`, which is correct for work the server can retract before a device sees it; reaching `cancelling` needs an execution actually running on a live agent.

#### [#5031 #5014 / inert-agent badge aria-label · org-only agent warning] — PASS
- ✅ #5031: every list chip carries its own `aria-label` — `Kind: Alert triage`, `Ownership: All orgs`, `Mode: Shadow`, `Status: Not running`, and on the inert row `Partner baseline: Inactive` (`data-testid="ai-agent-inert-badge-<id>"`), each with a distinct prefix so a screen reader can tell four same-shaped pills apart.
- ✅ #5014: the org-only Patching agent — created while no partner-wide Patching baseline exists — renders the warning **Inactive** badge, `aria-describedby` a description that reads "No partner-wide baseline exists yet for this kind. An organization-only agent only overrides a baseline, so it has no effect until one is created." The partner-wide triage agent correctly does **not** get the badge. Condition in source is `!agent.allOrgs && agent.hasPartnerBaseline === false`.
- ⚠️ UI/UX: that description node is emitted once per list regardless of whether any row uses it (G2-7).

#### [#4994 / AI kill-switch actor name] — PASS
- ✅ `/admin/ai-kill-switch` → **Kill AI actions platform-wide** → confirm modal requiring a reason ("Reason (required, for the audit log)") → `POST /admin/ai-kill-state` **200**. **Last changed by** rendered **"Breeze Admin"** — the user's display name, not the raw UUID (#4994's fix). Epoch 0 → 1, **Last reason** echoed the text typed, status badge flipped to "AI actions killed", CTA became "Restore AI actions", toast "AI actions killed platform-wide."
- ✅ Restore round-trips the same way (epoch 1 → 2, name still resolved). Left in the **AI actions active** state.

#### [#5145 #5164 #5174 #5243 #5248 / automation "when offline" · device "Queued (offline)" badge] — FAIL
- ✅ The automation-editor half is correct. An action step of type Run Script exposes `action-0-when-offline-select` — **"If the device is offline: Queue (default) / Skip"** — with the honest explainer "Queued work waits for the device and runs on its next check-in, or expires after 7 days. Skip fails the step immediately." Chose **Skip** → `POST /automations` **201** with `actions:[{…,"whenOffline":"skip"}]`; reload of `/automations/[id]` re-selected `skip`. Flipped to **Queue** → `PUT` **200** with `"whenOffline":"queue"`; reload re-selected `queue`. Persists both directions.
- ✅ The offline queue itself works and is honestly reported at the point of dispatch: `POST /scripts/:id/execute` on the offline `e2e-windows.local` → **201** `{"status":"queued","targets":[{"admission":"admitted","delivery":"queued_offline",…}]}` with the toast **"Runs when the device is online"**. The automation run against 6 devices queued 3 Windows targets and failed the 3 incompatible ones, keeping the run `Running` at "3 of 6 devices finished".
- ❌ **BUG — the "Queued — device offline" execution badge is unreachable: no server path ever writes `script_executions.status = 'queued'`.** The offline-queued run rendered as a plain **`pending` / "Script is waiting to be executed"**, indistinguishable from a run waiting on an online agent, and `script_executions.status` was `pending` in Postgres. The web side is fully built — `apps/web/src/components/scripts/executionStatus.ts:23,34` maps `queued` → `status.queuedOffline`, `en/scripts.json` ships "Queued — device offline" in all 8 locales, `executionStatus.test.tsx:63-65` asserts the mapping, and the execution-history filter offers a **Queued** option — but the only `status: 'queued'` write in the API is to `scriptExecutionBatches` (`scriptExecution.ts:353`), never to `scriptExecutions`; `grep -rn "status: 'queued'" apps/api/src` finds no execution-row writer. `scriptExecution.ts:342-347` says so out loud: "`delivered: false` covers every queued outcome … all of which land the row in `pending` awaiting the next heartbeat." So the label, the eight translations, the filter option and the unit test all describe a state the product cannot produce.
- ❌ Compounding it on the surface the row actually names: the device page's own Scripts tab renders the raw lowercase enum in its STATUS column, `DeviceScriptHistory.statusConfig` has **no `queued` entry**, and `en/devices.json > deviceScriptHistory.status` has no `queued` key — so even if the server were fixed, the device page would still not show the badge (G2-5).

#### [#5178 #5119 #5121 / AI chat approval dialog] — FAIL
- ✅ The approval surface itself works. A real chat turn against `e2e-windows.local` raised an inline **"Your Approval Is Required"** card with a live countdown (`4:30` → `3:02`), the target device chip with liveness (`E2E Windows Test Device · offline 2h`), a **Show parameters** disclosure, and **Verify & Approve** / **Deny**. Deny → `POST /mobile/approvals/:id/deny` **200**, the tool result came back `{"error":"Tool execution was rejected, cancelled, or expired"}` and the model reported it — no silent hang.
- ✅ Non-`execute_command` headlines are call-specific: the `network_discovery` request rendered **"Network discovery scan on 8.8.8.8"** (`action_tool_name: network_discovery`, `action_label` matching).
- ❌ **BUG — #5178's `execute_command` headline still falls back to the generic wording it was written to remove.** Asked the model to `execute_command` / `restart_service` the Spooler service, the card rendered:
  `Execute "restart_service" command on device e65460f3...`
  which is verbatim the pre-#5178 string the PR's own comment quotes as the defect (`aiGuardrails.ts:2074`). Root cause, from the persisted request: `approval_requests.action_arguments` = `{"payload":{"serviceName":"Spooler"},"deviceId":"e65460f3-…","commandType":"restart_service"}`. #5178's builder reads **`payload.name`** (`EXECUTE_COMMAND_HEADLINE_BUILDERS.restart_service`), the model sent **`payload.serviceName`**, so the builder returned `null` and `buildApprovalDescription` fell through. The model is not guessing badly on purpose: `execute_command`'s own schema declares `payload: { type: 'object', description: 'Command-specific parameters' }` (`aiToolsScripts.ts:243`) with **no key names at all**, while the sibling `manage_service` tool's parameter *is* called `serviceName` (`:659`) and is mapped to `payload.name` internally (`:683`). So the one field name the headline depends on is undocumented to the caller and contradicted by the neighbouring tool. Fix is either to accept `payload.name ?? payload.serviceName` in the builders, or to document the payload keys in the tool description. #5178's unit tests pass because they construct `payload: { name }` directly.
- BLOCKED: the #5121 half (a worker-approved result must not render as a red "Failed") could not be reached. **Verify & Approve** replaced itself with "Approving your own request requires Touch ID / Windows Hello. Register this device", and `POST /mobile/approvals/:id/assertion-challenge` returned `{"options":{"rpId":"2breeze.app",…}}` — the RP ID is the production domain while the origin is `localhost:33910`, so no authenticator (virtual included) can satisfy it on this stack. Enrolling a factor on this admin would disturb Group 4's rows 33–38, so it was not done. Needs a second approver account, or a stack whose `PUBLIC_APP_URL` matches its origin.

#### [#5248 / offline-only AI tool hint] — PASS
- ✅ Verified through the real chat path, verbatim in the transcript. `execute_command` / `list_processes` (a read-only type that skips the durable approval step) against the offline `e2e-windows.local` returned:
  `{"error":"Device e2e-windows.local is not online (status: offline). This tool needs a live connection; to run when the device reconnects use the Run Script / deployment tools instead."}`
  — exactly #5248's new sentence, on a real tool call, not by inspection.
- Note: the guard sits **after** the approval gate, so for an approval-gated tool (`network_discovery`, `restart_service`) the tech sees the approval card first and this hint only after approving. On the offline path that means the hint is reachable only for the no-approval command types unless someone approves work that cannot run.

#### [#5244 #5245 #5259 #5262 #5263 #5254 #5272 / Delegate to Operator → task admission → task detail] — PARTIAL
- ✅ **Delegate to Operator** is on both the alert detail page (`data-testid="delegate-to-operator"`) and the device header. The dialog reads "The Operator will investigate and attempt to resolve this using the workflow below." with the DEVICE, a **Service name** input, and **Workflow: Service recovery** named explicitly.
- ✅ Admission is honestly gated: with both QA agents created disabled, `POST /ai/operator/tasks` returned **422** `{"error":"No enabled AI agent is available for this organization. Enable an agent in Settings → AI Agents before delegating a task.","code":"OPERATOR_NO_AGENT"}` and toasted that same sentence — an actionable refusal naming the exact screen to fix it, not a generic 500.
- ✅ With the partner triage agent enabled: **202** `{"taskId":"d5160921-…","replayed":false}`, toast "Task delegated to the Operator.", and the browser navigated to `/operator/tasks/d5160921-…`, which rendered the objective ("Restore the Spooler service on e2e-windows.local"), **Handled by QA G2 Alert Triage**, TARGET, ORIGIN `Alert`, STATE `Queued`/Investigate, NEXT ACTION "Queued for admission", DEADLINE, and empty Operations / Linked runs sections.
- ✅ **The coordinator really admits it, with no live agent.** Polling the same page a minute later showed the task advanced on its own: STATE **Waiting**/Investigate, **WAITING ON Information**, NEXT ACTION "Answer a question", and a **Linked run** appeared. Confirmed in Postgres: `ai_operator_tasks.state='waiting' wait_reason='information'` with `next_wake_at` ~30 min out, and an `ai_agent_runs` row `status='running' resolved_model='claude-sonnet-4-6'`. It parks in `waiting`, it does not fail.
- ✅ The device page's **Operator Tasks** tab lists both delegated tasks with `Queued` badges and timestamps.
- BLOCKED: the "runs a recipe, task detail shows device feed & result" half needs a live agent — the service_recovery recipe's first step has to reach the device, which is why the task sits in `waiting/information`.
- ⚠️ UI/UX: a second delegation of the same alert silently mints a second task (G2-8); Linked runs shows a bare UUID (G2-9).

#### [#5004 #5297 / metric-anomaly job dedup] — N/A
- No browser surface can exercise it on this stack, for two independent reasons: **`device_metrics` is empty (0 rows)** so no metric series exists to be anomalous, and the device page's **Anomalies** tab reports "Anomaly detection is disabled for this organization." (a clean, honest empty state — no error, no fake zero). `metric_anomalies`, `metric_anomaly_candidates` and `metric_anomaly_incidents` are all 0, so "trigger the same anomaly twice" cannot be staged without fabricating a metric history and enabling detection.
- Per the row's own note, the dedup contract is covered by #5297's integration tests (`metric_anomalies` job dedup) and #5004's `cronDue` memo — both api-internal and already listed under "No browser surface".

#### [#4902 #4903 / AI-attributed ticket org move] — BLOCKED
- BLOCKED on the fixture, twice over. (a) **The seed has exactly one organization**, so a ticket org move is impossible by any actor: the ticket's **Move to another org…** dialog opens with a picker whose only entry is the placeholder "Select organization…" (G2-11). (b) The attribution #4902 fixes is set only on the *AI* path (`aiToolsTicketing` → `ticketService.moveTicketOrg` with `actor: {kind:'ai_agent',…}`), and a tenancy-class AI tool is approval-gated — the same self-approval WebAuthn wall that blocked row 19.
- ✅ What is testable renders correctly. Created ticket **T-2026-0001** from the alert (`POST /alerts/:id/create-ticket` **201**, toast "Ticket T-2026-0001 created"; the toast's number comes from `internalNumber`, correctly, not from the portal-facing `ticket_number` `L6R6VVBY74`). The ticket page shows the org, device, `Source: Alert`, `Requester: Breeze Admin`, the linked alert, and a timestamped thread entry — actor labels resolve to display names, no raw ids.
- By inspection of `a2df6af8b1`: the AI branch writes the destination comment with `authorType: 'ai_agent'` / `originPrincipalKind: 'ai_agent'` and both audit rows with `actorType: 'ai_agent', initiatedBy: 'ai'`, deliberately nulling `agentRunId` so a destination-org comment never links back to a source-org run. Note the row's "Activity tab" is the ticket **thread**; there is no separate Activity tab on the ticket page.

#### Addendum — runtime evidence collected after the rows above (bears on 15, 16, 23)
- ✅ **The capability allowlist is enforced at execution, not just stored.** The operator-driven agent run (`ai_agent_runs.c5c0be0e`) completed with 8 turns / 15c and an `outcome` recording `deniedActions: [{"tool":"set_device_context","reason":"Tool \"set_device_context\" is not in the agent's allowlist"}]` alongside 6 `executedActions`. So an operation the step-2 picker did not grant is refused at runtime with a named reason — the picker is a real policy, not decoration.
- ✅ The run reached `runVerdict: "no_action"` with `proposedActions: []` on an offline device, and the one device-touching tool in it (`analyze_disk_usage`) is recorded `result: "failed"` rather than silently omitted.
- ✅ **The duplicate operator task did not double-execute.** `ai_operator_tasks.e3c927a4` ended `handed_off` / phase `document`, `outcome = 'unresolved'`, `outcome_detail = 'run admission skipped: cooldown'` — the per-device cooldown from the agent's Limits blocked the second run. (Downgraded G2-8 to Low on this evidence.)
- **LLM provider note for whoever sweeps next:** contrary to the group brief, this stack **does** have a working `ANTHROPIC_API_KEY`; real chat turns and agent runs succeed (`$0.38 this month / 10 msgs` on the workspace header, `resolved_model = claude-sonnet-4-6`). The LLM-dependent blocker on this stack is **not** the provider — it is the self-approval WebAuthn wall plus `rpId: "2breeze.app"` on a `localhost` origin (see row 19).

**Group 2 fixtures left on the stack:** AI agents `QA G2 Alert Triage` (partner-wide, triage, shadow, **left disabled** — it was enabled only long enough to admit the operator tasks, then switched back off so it does not spend Anthropic tokens on alerts other groups create; `act_assets.scriptIds` holds 2 authorized scripts, which is the ceiling any org-scoped triage agent inherits) and `QA G2 Org Patching Orphan` (org-only, patching, disabled, carries the Inactive badge deliberately — do not create a partner-wide Patching agent or it disappears). Script `QA G2 Strict HKLM Script` (partner-wide, PowerShell, `acknowledged_security_patterns = {"PowerShell HKLM modification"}`) with 5 executions: 4 `cancelled`, 1 left `pending` as the live offline-queue fixture (`device_commands.f3a80117…`, `deliver_by` set). Automation `QA G2 Offline Queue Automation` (org-owned, manual trigger, `whenOffline: 'queue'`) with one `cancelled` run. Two Operator tasks on `e2e-windows.local` (`d5160921` waiting/information, `e3c927a4` handed_off/unresolved) plus their `ai_agent_runs` row. Ticket **T-2026-0001** from the `high CPU` alert on `e2e-macos.local`. The **AI kill switch was flipped and restored** — it is at epoch 2, **AI actions active**; two entries now name "Breeze Admin" as the last actor. The `high CPU` alert now has a linked ticket; the `disk full` alert has two delegated operator tasks.

### Group 3 — Tickets, Organization Record & Service Desk — opus, 2026-09-09 01:25Z

Fixtures created through the UI for this group (reuse, don't delete): organization **QA G3 Second Org** (`c93030d7-abe4-468a-90dd-d6d3b5749020`, customer/active) with site **QA G3 Org2 HQ** — created because the seed had exactly one org and rows 27/G2-11 both need two. On **Default Organization**: contact **QA G3 Contact** (`qa-g3-contact@example.com`, Technical, primary for the organization scope) and site **QA G3 Branch Office** (Denver, UTC, 0 devices).

#### [#5086 #5109 #5110 #5100 / organization record page — all tabs + URL-pinned org] — PASS
- ✅ All **seven** tabs render with real data on `/organizations/050f1818-…`, each pinned to `window.location.hash` (`#contacts`, `#sites`, `#devices`, `#tickets`, `#billing`, `#activity`) per the repo convention — no query params:
  - **Overview** — 7 KPI tiles (`org-overview-tile-{devices,alerts,tickets,contracts,invoices,contacts,portalUsers}`), an Open-critical-alerts card resolving `E2E fixture: disk full` on `e2e-windows.local`, and a Recent-activity feed.
  - **Contacts** — was empty, so a contact was created (below); site + role filter selects, Import CSV, Add contact.
  - **Sites** — `2 of 2 sites` table with per-row device counts, Edit/Delete.
  - **Devices** — `4 of 4 devices`, status + site filters (the new `QA G3 Branch Office` shows up in the site filter immediately), Columns picker, pagination.
  - **Tickets** — Open/All/Closed segmented filter, `New ticket`, and Group 2's `T-2026-0001`.
  - **Contracts & Billing** — three stacked sections (Contracts, Invoices, Quotes) each with its own status filter, empty state and create button.
  - **Activity** — the org-scoped audit trail with Filters + Export Logs; the contact and site creations appeared in it immediately with resolved actor names.
- ✅ **The URL-pinned org is genuinely authoritative over the OrgSwitcher.** With the switcher set to `QA G3 Second Org`, opening `/organizations/050f1818-…` (Default Organization) rendered *Default Organization's* data — `2 sites`, `Primary contact: QA G3 Contact`, `Devices 6 · 1 of 6 online`, `Contacts 1` — and the header added an explicit divergence line **"Workspace: QA G3 Second Org"** so the tech can see the record's subject is not their active workspace. No silent substitution, no 404.
- ✅ Switching the OrgSwitcher **while on** the record page deliberately redirects up to `/settings/organizations` rather than reloading the old org's record — `getOrgSwitchRedirect` (`apps/web/src/lib/orgSwitch.ts:47-52`) does this for `^/organizations/:id$` exactly as it does for `/devices/:id`, with the reason in the comment ("reloading it after a switch would leave the user on the customer they just switched away from (#5075)"). The switch is confirmed by a **"Switched to QA G3 Second Org"** toast that survives the reload via `sessionStorage` (`breeze.orgSwitch.toast`, verified consumed). Note the row's wording ("URL-pinned org persists regardless of OrgSwitcher") describes the *rendering* contract, which holds; the *navigation* contract is the opposite by design.
- ⚠️ UI/UX: the Overview `Devices 6` tile and the Sites table's per-row device count both **include the 2 removed devices** that the record's own Devices tab (`4 of 4`) correctly excludes — three surfaces on one page disagree (G3-1). Header summary chips don't refetch after an in-tab mutation (G3-2). Every mutation writes a second, route-derived audit row (G3-3). `/organizations` with no id is a hard 404 (G3-4).

#### [27a / everyday ticket workflow from the org record] — PASS
Every mutation below was driven through the UI with a `MutationObserver` on `[data-testid="toast"]` and a `window.fetch` wrapper capturing method/status/body in the same evaluate.
- ✅ **Create from the org record.** Org record → Tickets tab → **New ticket** is an `<a href="/tickets/new#orgId=050f1818-…">`, and the create form arrived with **Organization already preselected to Default Organization** from that hash (not from the OrgSwitcher). `POST /api/v1/tickets` **201**, toast **"Ticket T-2026-0002 created"**, redirected to `/tickets#T-2026-0002` with the workbench open on the new ticket. The toast quotes the internal number `T-2026-0002`, not the portal-facing `ticketNumber` `HVMC-EUGOF` — same correct choice Group 2 saw.
- ✅ **Reply** — `POST /tickets/:id/comments` **201**; the reply lands in the thread immediately with author, timestamp and Edit/Delete, and the SLA rail flipped **First response → Met**. No toast, but the posted message is the confirmation (chat convention).
- ✅ **Status** New → Open — `POST /tickets/:id/status` **200**, toast **"Status updated"**, select reflects `Open`, and a system entry "Breeze Admin changed status: New to Open" appears in the thread.
- ✅ **Assignee** Unassigned → Breeze Admin — `POST /tickets/:id/assign` **200**, toast **"Assigned"**, thread entry "Breeze Admin assigned this ticket".
- ✅ **Time entry** — Log time → 45 min, billable, description — `POST /api/v1/time-entries` **201**, toast **"Time logged"**, the TIME & BILLING card recomputed to `Total time 45m / Billable 45m` and listed the entry; thread entry "Breeze Admin logged 45m (billable)".
- ✅ **Convert to billing** — **Create invoice** correctly *refused*: `POST /tickets/:id/invoice` **409** `{"code":"ALL_MISSING_RATE","error":"1 billable time entry has no hourly rate in USD — set a rate on it and assemble again","details":{"missingRate":[{"timeEntryId":"bbdc9587-…"}]}}`, surfaced verbatim as a toast. An actionable refusal naming the fix, not a silent no-op — but see G3-5: the quick-add that created that entry has no rate field, so this 409 is the guaranteed outcome of the happy path.
- Note: tickets have **Create invoice**, not "convert to quote" — quotes are created from `/billing/quotes` or the org record's Contracts & Billing tab. There is no ticket→quote link affordance.

#### [#5006 #5013 / quote editor + themed multi-run PDF] — PASS
Fixture: quote **Q-2026-0001** (`fcfe7e29-9173-43de-8cd3-5652cace4efc`) on Default Organization — a pricing-table block with **three recurrence runs** (one-time $1,250.00 · monthly $948.00/mo · annual $9,600.00/yr) plus a **table block with a right-aligned and a centre-aligned column**. Partner `documentTheme` was switched **classic → condensed** (Settings → Billing, `PATCH /partner/billing-settings` **200**, toast "Billing settings saved") so the PDF renders on a non-default theme.
- ✅ **#5006 title collapse.** The `min-w-52` (208 px) floor is on both the title cluster and the left group (measured `getComputedStyle`). At **1280 px** the title input measures **256 px**; at 1024 px, 347 px; at **700 px** it is still **295 px** and the actions (`quote-send`) have wrapped to their own line (`top` 167 vs the title's 82) while the status pill stays inline. The title never starves.
- ✅ **#5006 illegal reprice blocked.** With manual lines present, Details → **Change currency** renders `quote-currency-mode-reprice` with `disabled: true` and the reason string **"Not available — at least one line is manual or a bundle, so the price book cannot reprice it. Remove all lines instead."**, while `quote-currency-mode-clear` stays enabled with "Required when any line is manual or a bundle." Client predicate mirrors the server's 409 `CURRENCY_LOCKED` — no silently-failing option.
- ✅ **#5006 triple label** — `en/billing.json > quotes.editor.deviceSet` now carries three distinct strings: `allowanceGroupLabel: "Quantity allowance"` (legend), `allowanceToggle: "Include a fixed quantity, then handle extras"` (toggle), `includedLabel: "Included quantity"` (qty input). Verified by string inspection; the surface needs a device-set-priced line, which this seed has no fixture for.
- ✅ **#5013 verified from the rendered PDF bytes.** `quote-download-pdf` → `GET /api/v1/quotes/:id/pdf` **200**, `content-type: application/pdf`, 19,532 bytes, `%PDF-1.3`, ends `%%EOF`, 1 page. Decompressed the content stream and read every text op's font + `Tm` position:
  - **Embedded fonts are exactly `BarlowCondensed-SemiBold`, `DMSans9pt-Regular`, `DMSans9pt-Bold` — zero Helvetica anywhere in the file.** Line-table column headers (y 579.10) draw in BarlowCondensed (heading), every row and the whole recurring summary (y 380.54 → 297.54) in DMSans Regular/Bold (body) — precisely #5013's "headers + section label → heading, row/summary text → body" mapping, on a themed doc.
  - **Multi-line centred cell places each wrapped line individually.** The centred column's cell wraps to three lines at y `459.60 / 446.58 / 433.56` (13.02 pt apart, no overlap) with x `329.39 / 332.24 / 369.24` for 44 / 42 / 25 glyphs — x moves with each line's own width, i.e. each line is centred against the box rather than every run re-anchoring on one origin (the pre-#5013 overlap bug).
  - Right-aligned cells are individually right-anchored: `$4,800.00` at x 251.69, and the line table's price/total columns land at x 420.42/432.40/416.41 and 517.70/504.33/500.50 — each varying with its own string width.
- ⚠️ UI/UX: saving any table cell warns about formatting the user never applied (G3-6); a mixed-format cell is unauthorable because mark toggles `selectAll` by design (G3-7).

#### [#5108 / Service Management mode → Service Desk section + Timesheets relocation] — PASS
**The row's premise is wrong in one respect worth carrying into the release notes: Service Management is a PARTNER-level flag, not an org flag.** It lives on `partners.service_management_mode` (`text`, enum `native | external | off`, default **`native`**) and is toggled in the UI at **Settings → Partner → Modules** (`PartnerModulesCard`, `partner-modules-card`). No org-level column or control exists. Because the default is `native`, the seeded partner already had the module on — nothing had to be enabled to see the new section.
- ✅ **Service Desk section exists and Timesheets has moved into it.** The rail reads `… BACKUP · **SERVICE DESK** (Tickets, Timesheets) · BILLING (Quotes, Invoices, Contracts, Product Catalog) · REPORTING …`. `/timesheet` is under Service Desk and no longer stranded under Billing, exactly as `Sidebar.tsx:295-308` intends.
- ✅ **The module gate really withdraws both sections, live.** Clicking `partner-modules-mode-off` → `PATCH /api/v1/orgs/partners/me` **200**, toast **"Modules updated"**, and **SERVICE DESK and BILLING both vanished from the rail with no reload** (`serviceDesk:false`, `hasQuotes:false`, `hasTimesheets:false`, `hasTickets:false`).
- ✅ **The org record follows the same gate.** With the module off, `/organizations/[id]` dropped to **Overview · Contacts · Sites · Devices · Activity** (Tickets and Contracts & Billing gone) and the Overview tiles reduced to `devices, alerts, contacts, portalUsers` — the ticket/contract/invoice tiles disappear too. The card's promise ("Hides the Service Desk and Billing sections and the customer record's Tickets and Billing tabs") is literally true.
- ✅ **The gate is enforced server-side, not just in the nav.** `/tickets/new` is still reachable by direct URL with the module off, but submitting returns **409** `{"error":"Service Management is turned off for this partner","code":"service_management_off"}`, toasted verbatim. Existing tickets stay readable, as the copy says.
- Restored to `native` afterwards (toast "Modules updated", both sections back).
- Note for the release notes: the enum has a third value **`external`** (with `partners.service_management_psa_connection_id` alongside it) but the Modules card only offers **native** and **off** — there is no UI to select external-PSA mode yet.
- ⚠️ UI/UX: `/tickets/new` renders a full, submittable form that is guaranteed to 409 while the module is off (G3-8).

#### [#5118 / advanced-filter drawer leaves no stale action scope] — PASS
Exercised on **both** surfaces #5118 touches (Devices and Alerts), watching `window.fetch` bodies so the claim rests on the ids actually sent.
- ✅ **Devices.** Advanced → condition `Hostname contains qa-agent` → Apply (`POST /api/v1/filters/preview` **200**) narrowed the list to `2 of 9 devices`. Selecting all gave `bulk-selection-summary` = **"2 selected"**. **Editing the filter value to `e2e` removed the selection summary from the DOM immediately** — before Apply, on the change itself — so no scope survives a filter edit. After Apply the rows were the two `e2e-*` devices and the class facets recomputed to `All 2 / Agent 2 / Network 0 / Manual 0` (no stale counts).
- ✅ **Closing the drawer keeps the filter, drops the scope, and stays legible.** `filter-advanced-toggle` closed the builder; no `bulk-selection-summary`; the applied filter persisted as a readable chip **"Hostname contains e2e"** with a **Clear** action in the toolbar — no hidden filter, no orphan scope.
- ✅ **The bulk action fires against the current scope only.** Re-selecting and running **Wake Selected** sent `POST /api/v1/devices/bulk/commands` **202** with `{"deviceIds":["42fc7de0-…","e65460f3-…"],"type":"wake"}` — exactly the two currently-filtered devices, none of the earlier `qa-agent` ids. (The wake itself correctly failed per-device with `NO_MACS` "Target has no recorded MAC address" — seeded devices carry no MAC.)
- ✅ **Alerts.** Advanced Filter → `contains windows` narrowed to `1 of 1`; selecting gave "1 selected"; changing the value to `macos` **dropped the selection bar and left zero checked boxes** while swapping the row to the high-CPU alert — the previously-selected disk-full alert did not stay selected into a filter it no longer matches.
- ⚠️ UI/UX: the bulk-wake toast reads "Bulk Wake All Failed" and drops the 202 body's per-device reason (G3-9).

#### [#5041 #5001 / locale cookie + `<html lang>` + alert-verdict copy] — PASS
- ✅ **#5041 verified against the real SSR output, not just the client.** Settings → Profile → Theming → Language → **Français (France)** set `localStorage['breeze.locale']` **and** the `breeze.locale=fr-FR` cookie, and `document.documentElement.lang` became `fr-FR` at once. Then, fetching two routes fresh with that cookie returned **`<html lang="fr-FR">`** from the server for both `/alerts` and `/devices` — the middleware → `Astro.locals.locale` → layout chain works end to end.
- ✅ The console localised broadly: nav (`Tableau de bord · Dispositifs · Alertes · Accès à distance · Correctifs · Orchestration de la flotte`), page titles (`Alertes | Breeze RMM`, `Paramètres du profil | Breeze RMM`), the alerts table (`DISPOSITIF · TITRE · GRAVITÉ · STATUT · VERDICT DE L'IA · DÉCLENCHÉ`), row actions (`Acquitter · Résoudre · Muet · Ignorer`), severities and the whole Filters panel.
- ✅ **#5001's reviewed fr-FR verdict copy renders live.** `alertVerdict.none` = **"Aucun verdict de l'IA pour le moment"** is the `title` on each em-dash verdict cell, and `alertVerdict.hideNoise` = **"Masquer le bruit signalé par l'IA"** is the Filters-panel toggle. Structurally the fr-FR `alertVerdict` block has all 11 keys with matching `{{pct}}`/`{{name}}` placeholders and formal (V-form) address throughout (`Merci pour votre retour`, `Avis donné par {{name}}`).
- Not reachable on this stack: the verdict **label** and **confidence** strings (`Action requise`, `{{pct}} % de confiance`) need an alert carrying an AI verdict; every seeded alert has `ai_verdict` null, so the badge only ever renders the `none` state.
- Expected-by-design, worth a release note: **the first server paint is still English** — the fr-FR title only appears after hydration, because #5041 ships only `<html lang>`; SSR shell strings are Tasks 2–4 of the same plan and are not in this range.
- Switched back to `en` at the end (cookie `breeze.locale=en`, `lang="en"`, nav English).
- ⚠️ UI/UX: the verdict cell's explanation is `title`-only, no accessible name (G3-10).

#### [#5068 / collapsed sidebar rail icon centering] — PASS
Measured with one `getBoundingClientRect` sweep over every `nav svg` and `nav a`, plus a scrollbar-width probe.
- **This headless Chromium has OVERLAY scrollbars** (a `overflow:scroll` probe measures `offsetWidth - clientWidth = 0`), so the reported condition — classic, space-taking scrollbars — does not occur natively here. Both cases were measured.
- ✅ **Overlay (native) case:** rail 63 px, `scrollbar-gutter: auto` (not `stable` — this is #5068's first change), every one of the **19 icons shares a single centre x = 31.5**, which is exactly the rail's centre, and every link is 47 px wide (full row minus the 8 px padding) with the same 31.5 centre. `maxOffsetFromRailCenter = 0`.
- ✅ **Emulated classic-scrollbar case** (injected `nav::-webkit-scrollbar{width:16px}` at a 500 px viewport so the rail actually overflows — `scrollHeight 817 > clientHeight 436`): the gutter took 16 px, leaving a 47 px client box, and **every icon and every link still shared one centre (23.5) with one identical width (31 px)** — zero misalignment *between* elements, which is precisely the defect #5068 fixed (active highlight + section icons at −8 px while inactive icons sat at 0). Because `scrollbar-gutter` is now `auto`, the whole icon column sits 8 px (half the scrollbar) left of the rail's geometric centre in that case, but consistently — highlight, icon and dividers move together, so nothing reads as off-centre. Noted as G3-11, not a regression.

**Group 3 fixtures left on the stack (reuse, don't delete):**
- **Organization `QA G3 Second Org`** (`c93030d7-abe4-468a-90dd-d6d3b5749020`, customer/active) with site **`QA G3 Org2 HQ`**, 0 devices. Created because the seed had exactly one org. **This unblocks G2-11 and Group 2 row 26** — a ticket org move now has somewhere to move to, and the OrgSwitcher has a real second entry (`org-option-c93030d7-…`).
- On **Default Organization**: contact **`QA G3 Contact`** (`qa-g3-contact@example.com`, Technical role, **primary for the organization scope** — the record header's "No primary contact" is now "Primary contact: QA G3 Contact"), and site **`QA G3 Branch Office`** (Denver, UTC, 0 devices — it appears in every site picker/filter now, including the org record's Devices tab and the quote/ticket forms).
- Ticket **`T-2026-0002`** (`90a4c43c-507a-4844-b64d-fcf6da6878d0`, portal number `HVMC-EUGOF`) — High, **status Open, assigned to Breeze Admin**, device `e2e-windows.local`, one customer reply, one 45-minute billable time entry (`time_entries.bbdc9587-…`, **no hourly rate**, which is why Create invoice 409s), first-response SLA met. Group 2's `T-2026-0001` untouched.
- Quote **`Q-2026-0001`** (`fcfe7e29-9173-43de-8cd3-5652cace4efc`) on Default Organization — Draft, three-recurrence pricing table (one-time $1,250 / monthly $948 / annual $9,600) plus a table block with a right-aligned and a centre-aligned column. Safe to reuse for any quote/PDF work.
- **Partner `documentTheme` was changed `classic` → `condensed`** and **left as `condensed`** so the themed-PDF evidence stays reproducible. Any later group rendering an invoice/quote/contract PDF will get Barlow Condensed + DM Sans, not the classic theme — flip it back at Settings → Billing if a row needs `classic`.
- **`partners.service_management_mode` was toggled `native → off → native`** and is back at **`native`** (verified: Service Desk + Billing sections present in the rail).
- One throwaway artifact: a `qa-g3-second-org` slug and the `QA G3 module-off gate probe` ticket was **never created** (the 409 refused it) — no cleanup needed. Sidebar mode and locale were both restored (`sidebar-mode=open`, `breeze.locale=en`).

### Group 4 — Auth, MFA, Sessions & Admin Roles — opus, 2026-09-09 02:12Z

**TOTP factor enrolled on `admin@breeze.local`** to unblock rows 33/35 and the deferred halves of row 10. Secret, recovery codes and the exact click-path are in the Group 4 fixtures block at the end of this log. Codes computed with a 20-line RFC 6238 python3 helper (`/tmp/g4/totp.py`) — neither `pyotp` nor `oathtool` is installed on this machine.

#### [#4491 / system Partner Admin `force_mfa` on every creation path] — PASS
- ✅ **Seed path (live DB):** `select name, scope, is_system, force_mfa from roles` — the global system template **`Partner Admin | partner | is_system=t | force_mfa=t`**, and every one of the other nine system roles is `force_mfa=f` (`Org Admin`, `Org Technician`, `Org Viewer`, `Partner Billing`, `Partner Billing Viewer`, `Partner Security Approver`, `Partner Technician`, `Partner Viewer`, `Security Approver`). That is exactly the design (`SYSTEM_ROLES[].forceMfa` — only Partner Admin forced, the rest per-deployment opt-ins per the `2026-05-25-f` header) and proves the fix's core claim: the flag survives a fresh-database seed, where the old `2026-05-25-f` migration ran before `seed()` created the row.
- ✅ **No UI creation path can mint a Partner Admin equivalent** — checked all three the Roles page offers:
  - **Clone** on the Partner Admin row → `POST /roles/448d68b1…/clone` **403** `{"error":"Wildcard permissions cannot be assigned to custom roles"}`, toasted verbatim. The clone dialog also omits Partner Admin from its own parent picker.
  - **Create Role** → `roleData` is hard-coded `isSystem: false` (`routes/roles.ts:684`) with no `forceMfa` field, so a custom role is created `force_mfa=f`. Per design ("custom roles stay opt-in").
  - **Create Role → Inherit From = Partner Admin (System)** → `POST /roles` **400** `{"error":"Parent role must be in the same scope"}` (the page posts `?orgId=…`, i.e. organization scope). So the escalation-by-inheritance route is closed too.
- Verified by code read, not reachable at runtime: the **`partnerCreate()`** path (`services/partnerCreate.ts` inserts the tenant Partner Admin copy with a literal `forceMfa: true`) and the **`2026-10-11-170000` reconcile migration**. `POST /auth/register-partner` **200** but the account is only created when the emailed verification link is clicked, and the raw token never leaves `createPendingRegistration` (only its hash is stored) — the local Resend key rejects `@example.com`, so step 2 is unreachable. This stack also has exactly one partner and **zero tenant role copies** (`roles.partner_id` is null on all 11 rows), so the migration's tenant-copy arm had nothing to reconcile here either.
- ⚠️ UI/UX: `force_mfa` is **not exposed anywhere in the Roles UI** — no toggle, no column, no badge (G4-1).
- ⚠️ UI/UX: Create Role's parent picker offers **Partner Admin (System)** even though selecting it always 400s on the org-scoped page (G4-2).

#### [#5007 #4921 / maintenance-mode toast plural + lease persistence] — PASS (Group 1 row 10, deferred halves)
Both halves needed an enrolled factor; done here.
- ✅ **Singular:** device page → Power → Enter Maintenance → step-up (`POST /auth/mfa/step-up` **200** `{"stepUpGrantId":"a5a8d8f1-…"}`) → `POST /devices/42fc7de0…/maintenance` **200** `{"action":"enable","maintenance":{"until":"2026-09-09T06:05:40.180Z","startedAt":"…02:05:40.180Z","reason":"QA G4 row10 single-device maintenance probe"}}`. Toast: **"e2e-macos.local put into maintenance mode"** — names the device, no `(s)`.
- ✅ **Plural (2):** devices list → 2 selected → Bulk Actions → Enable Maintenance. Dialog title pluralizes too: **"Enter maintenance mode on 2 devices"**; `POST /devices/bulk/maintenance` **200** with two `succeeded` entries. Toast: **"2 devices put into maintenance mode"**.
- ✅ **Plural (3) on the way back out:** Disable Maintenance on 3 selected → toast **"3 devices taken out of maintenance mode"**. All three plural forms are real plurals, not `device(s)` — unlike the removal/purge toasts G1-7 flagged.
- ✅ **#4921's lease persists across a hard reload.** After `page.reload()` the header status chip reads **Maintenance** (`title="Device is in maintenance mode"`), the Activity feed carries "Maintenance mode enabled — e2e-macos.local · Breeze Admin · Just now", and the DB holds the full lease: `status=maintenance`, `maintenance_until=2026-09-09 06:05:40.18+00`, `maintenance_reason='QA G4 row10 single-device maintenance probe'`, `maintenance_started_at`, `maintenance_started_by`.
- ⚠️ UI/UX: the reloaded page surfaces **none of the persisted lease detail** — no end time, no reason, no who-started-it, just a bare "Maintenance" chip (G4-3).
- ⚠️ UI/UX: once in maintenance the **Power button is `disabled`**; Exit Maintenance moves to the unlabelled `device-actions-menu` overflow, so you enter one way and leave another (G4-4).
- Note (not a defect): exiting maintenance needs **no step-up** and fires N individual `POST /devices/:id/maintenance` calls rather than the bulk endpoint — asymmetric with entering, but exiting only restores alerting.

#### [#5008 #5037 / recovery-code rotation keeps the caller's session] — PASS (row 35, rotation half)
- ✅ **Enrollment itself keeps the session** (#5008's shared primitive): `POST /auth/mfa/setup` **200** → `POST /auth/mfa/enable` **200** returning 10 recovery codes **and** a replacement `accessToken` whose claims carry `"mfa":true, "mep":2` — no redirect to `/login`, page stayed on `/settings/profile`.
- ✅ **Rotation is an explicit, confirmed action.** `Regenerate codes` is `disabled` until a password is typed, then opens a confirm dialog (`confirm-regenerate-recovery-codes`) whose copy is accurate: *"Regenerating immediately invalidates every recovery code you have saved, including printed copies. The replacements are shown once, right after they are generated. Your other signed-in devices are signed out; this one stays signed in."*
- ✅ **Rotation requires the password**, and a wrong one is refused without touching the stored set: `POST /auth/mfa/recovery-codes` **400** `{"error":"Invalid credentials","code":"invalid_credentials"}`, inline "Invalid credentials", the ten original codes still on screen.
- ✅ **Correct password → 200 with a non-empty replacement pair** (#5037's red-first case — a rotation that omitted its code pair used to silently write `[]`): `{"success":true,"recoveryCodes":[10 codes],"message":"Recovery codes generated successfully","tokens":{"accessToken":…}}`, panel repaints the new ten, inline "New recovery codes generated".
- ✅ **Session survives, proven the hard way:** a full `page.goto('/devices')` after the rotation returned `GET /devices` **200**, `GET /devices/network` **200**, `GET /devices/manual` **200** with no login redirect — so the re-minted refresh family is durable, not just the in-memory token.

#### [R1 #4491 / forced-MFA relief valve with a fresh Partner Admin] — VERIFIED (valve OFF path proven end-to-end)
The forced path itself (`428 mfa_enrollment_required` → `/auth/mfa/setup?forced=1`, copy "Your role requires multi-factor authentication") was observed by the orchestrator at stack setup on the seeded admin, before the valve was set — recorded as **VERIFIED-AT-SETUP** in the R1 finding above. What Group 4 adds is the half that matters for Todd's disposition (#5307): **with `MFA_FORCE_FOR_PARTNER_ADMIN=false`, a brand-new Partner Admin is genuinely not forced.**
- ✅ Invited `qa-g4-pa@example.com` with the **system Partner Admin** role (`POST /users/invite` **201**, `roleId: 448d68b1-…` — the exact row that carries `force_mfa=t`), accepted in a **fresh browser context** (`POST /auth/invite/preview` 200 → `POST /auth/accept-invite` **200**, `"mfaEnabled": false`).
- ✅ Landed on **`/` (Dashboard)** — not `/auth/mfa/setup?forced=1` — and then made ~30 authenticated API calls (`/users/me`, `/orgs/partners/me`, `/devices`, `/alerts`, `/audit-logs/logs`, `/security/dashboard`, `/patches/compliance`, `/vulnerabilities/stats`, `/orgs/organizations`, `/events/ws-ticket`, `/auth/refresh`, …) — **every one 200, not a single 428**. Navigating on to `/devices` was equally clean.
- So the valve suppresses only the role-force component while `roles.force_mfa` stays `true` in the DB — exactly the behaviour #5307 makes the default, and exactly what a droplet needs if `.env` ever pins the var to `true`.
- The valve is **not** discoverable from the UI: nothing on Settings → Roles, Settings → Users or the user's own Security panel says the role would otherwise force MFA (see G4-1).

#### [#5120 / unsafe MFA policy activation is blocked] — PASS
Verified on **both** axes against real Postgres, with a discriminating control on each.
- ✅ **Organization axis, through the real UI** (Settings → Organizations → Default Organization → **Security** → ALLOWED METHODS → uncheck **Authenticator app (TOTP)** → **Save security**): `PATCH /orgs/organizations/050f1818…` **409** `{"code":"mfa_policy_would_lock_out_users","error":"Enroll an allowed MFA method for affected users before changing this policy.","count":1,"countCapped":false}`. The **error toast renders the server message verbatim** and the unsaved checkbox state is **retained** (TOTP stays unchecked so the tech can fix the inventory and retry) — both claims from the PR body hold.
- ✅ **Discriminating control on the same surface:** the immediately preceding save on that panel, which left TOTP *allowed*, returned **200** + "Settings saved". So the 409 is the guard firing on the unsafe delta, not a broken endpoint.
- ✅ **Partner axis** (`PATCH /orgs/partners/me`, the endpoint the Partner Settings page already saves through, driven with the app's own bearer token): `{security:{allowedMethods:{totp:false,sms:false}}}` → **409** with the same body and `count: 1`; `{totp:true,sms:false}` → **200** and persisted. **The legacy alias arm is covered too** — `{security:{allowedMfaMethods:{totp:false,sms:false}}}` also **409**, i.e. `foldAllowedMfaMethodsAlias` runs before the preflight and the alias cannot smuggle an unsafe policy past it.
- ✅ **The count is inventory-accurate, not a headcount.** Two Partner Admins exist; only `admin@breeze.local` is enrolled (`mfa_enabled=t`, TOTP secret, `0` live passkeys) and `qa-g4-pa@example.com` is not (`mfa_enabled=f`) — the response said `count: 1`, not 2. An unenrolled user is correctly *not* "stranded"; they merely have to enrol.
- Fixture note: the organization axis needed an `organization_users` member (this stack seeds **zero** — the seeded admin is a `partner_users` row), so the admin was temporarily given an Org Admin membership by SQL for this one check. **Both the membership and the settings written during the test were reverted** — `organization_users` is empty again, `organizations.settings` is back to `{}` and `partners.settings` back to `{"ticketing":{"inbound":{"enabled":false}}}` (verified by re-select).
- Worth knowing for a real deployment: once a partner sets `security.allowedMethods`, the **org** panel can no longer touch it — the org PATCH returns **403** "The following fields are locked by the partner and cannot be changed: security.allowedMethods" (observed mid-test). So the org-axis guard only ever fires on orgs whose partner has left the field unset.
- ⚠️ UI/UX: the 409 toast drops the `count` the server sent — "affected users" with no number and no name (G4-5).
